# v0.34.0 剑意机制设计

**目标：** 实现剑修核心机制——剑意叠加与消耗；替换剑修起始牌组为 GDD 定义的 13 张牌组；养剑葫遗物生效（战斗开始获得 3 层剑意）；战斗界面显示剑意计数器。

**架构：** 剑意状态存在 Combatant（sword_intent / sword_intent_cap / sword_intent_damage_bonus），EffectResolver 在 resolve() 中处理招式加成与终结技消耗，CombatEngine.end_turn() 执行回合结束折半，RelicEngine 的 SWORD_INTENT 分支改为真实效果。

---

## 数据层

### CardData.gd

新增三个导出字段：

```gdscript
@export var card_type: String = ""             # "招式" / "身法" / "功法"
@export var is_finisher: bool = false           # 终结技：消耗全部剑意打出爆发
@export var sword_intent_consume_bonus: int = 0 # 终结技每层剑意额外伤害倍率
```

### Combatant.gd

新增三个字段，存储剑意运行时状态：

```gdscript
var sword_intent: int = 0
var sword_intent_cap: int = 10
var sword_intent_damage_bonus: int = 1
```

### CardEffectData.gd（无需修改）

养剑术使用现有 type/value 结构，新增两种 effect type 字符串：
- `"sword_intent_cap"` — value = 5（上限 +5）
- `"sword_intent_bonus"` — value = 1（每层伤害加成 +1）

### 新建卡牌文件

| 文件 | card_type | is_finisher | cost | effects | sword_intent_consume_bonus |
|------|-----------|-------------|------|---------|---------------------------|
| `data/cards/zhan_tie.tres` | 招式 | false | 1 | damage=6 | 0 |
| `data/cards/po_feng.tres` | 招式 | false | 2 | damage=10 | 0 |
| `data/cards/ce_bu.tres` | 身法 | false | 1 | block=5 | 0 |
| `data/cards/lian_xi.tres` | 身法 | false | 0 | block=3 | 0 |
| `data/cards/chuan_xin.tres` | 招式 | true | 2 | damage=8 | 3 |
| `data/cards/yang_jian_shu.tres` | 功法 | false | 0 | sword_intent_cap+5, sword_intent_bonus+1 | 0 |

> 文件名使用拼音，避免文件系统中文兼容性问题。

---

## 逻辑层

### EffectResolver.gd

`resolve()` 内修改 "damage" 效果分支，末尾追加剑意增减逻辑：

```gdscript
static func resolve(card: CardData, attacker: Combatant, defender: Combatant) -> void:
    for effect: CardEffectData in card.effects:
        if effect.type == "damage":
            if defender != null:
                var bonus: int = 0
                if card.is_finisher:
                    bonus = attacker.sword_intent * card.sword_intent_consume_bonus
                elif card.card_type == "招式":
                    bonus = attacker.sword_intent * attacker.sword_intent_damage_bonus
                apply_damage(attacker, defender, effect.value + bonus)
        elif effect.type == "block":
            attacker.add_block(effect.value)
        elif effect.type == "weak":
            if defender != null:
                defender.add_weak(effect.value)
        elif effect.type == "vulnerable":
            if defender != null:
                defender.add_vulnerable(effect.value)
    # 剑意增减（在所有效果之后）
    if card.is_finisher:
        attacker.sword_intent = 0
    elif card.card_type == "招式":
        attacker.sword_intent = mini(attacker.sword_intent + 1, attacker.sword_intent_cap)
```

### CombatEngine.gd

**`end_turn()` 末尾**追加剑意折半（整除）：

```gdscript
player.sword_intent = player.sword_intent / 2
```

**`_apply_engine_effects()` 新增两个 elif 分支**：

```gdscript
elif effect.type == "sword_intent_cap":
    player.sword_intent_cap += effect.value
elif effect.type == "sword_intent_bonus":
    player.sword_intent_damage_bonus += effect.value
```

### RelicEngine.gd

`SWORD_INTENT` 分支从 push_warning 改为真实效果：

```gdscript
RelicData.EffectType.SWORD_INTENT:
    engine.player.sword_intent = mini(
        engine.player.sword_intent + relic.value,
        engine.player.sword_intent_cap
    )
```

### GameManager.gd

`start_new_run()` 中替换起始牌组，剑修起始 13 张：

```gdscript
var zhan_tie: CardData    = preload("res://data/cards/zhan_tie.tres")
var po_feng: CardData     = preload("res://data/cards/po_feng.tres")
var ce_bu: CardData       = preload("res://data/cards/ce_bu.tres")
var lian_xi: CardData     = preload("res://data/cards/lian_xi.tres")
var chuan_xin: CardData   = preload("res://data/cards/chuan_xin.tres")
var yang_jian_shu: CardData = preload("res://data/cards/yang_jian_shu.tres")
for i in 4:
    player_state.deck.append(zhan_tie.duplicate())
for i in 2:
    player_state.deck.append(po_feng.duplicate())
for i in 4:
    player_state.deck.append(ce_bu.duplicate())
player_state.deck.append(lian_xi.duplicate())
player_state.deck.append(chuan_xin.duplicate())
player_state.deck.append(yang_jian_shu.duplicate())
```

旧的 strike/defend/bash/slash/whirlwind 起始牌组全部移除。

---

## UI 层

### CombatScreen.tscn

PlayerPanel 下新增 Label 节点（LblPlayerBlock 之后）：

```
[node name="LblSwordIntent" type="Label" parent="VBoxContainer/PlayerPanel"]
layout_mode = 2
text = "剑意：0"
```

### CombatScreen.gd

新增 @onready：

```gdscript
@onready var _lbl_sword_intent: Label = $VBoxContainer/PlayerPanel/LblSwordIntent
```

`_refresh_ui()` 中追加：

```gdscript
_lbl_sword_intent.text = "剑意：%d / %d" % [
    _engine.player.sword_intent,
    _engine.player.sword_intent_cap
]
```

---

## 游戏流程

1. 选择剑修开始游戏
2. 起始牌组：斩铁×4、破风×2、侧步×4、敛息×1、穿心×1、养剑术×1
3. 养剑葫生效：战斗开始 sword_intent = min(0+3, 10) = 3
4. 打出斩铁：造成 6 + 3×1 = 9 伤害，剑意变为 4
5. 打出穿心：造成 8 + 4×3 = 20 伤害，剑意归零
6. 回合结束：剑意 0 → 折半 → 0（无变化）
7. 打出养剑术：sword_intent_cap = 15，sword_intent_damage_bonus = 2
8. 战斗界面始终显示"剑意：X / Y"

---

## 版本号

`menu/MainMenu.tscn` 改为 `v0.34.0`

---

## 不在本版范围内

- 剑修以外职业的机制（法修五行、体修气血）
- 凡品/灵品/仙品剑修牌（战斗掉落/商店/精英奖励）
- 剑意相关遗物（v0.35.0+）
- 养剑术"剑意上限+5"对上限显示的动态更新（上限固定从 Combatant 读，养剑术打出后 cap 自动变为 15，UI 同步刷新）
