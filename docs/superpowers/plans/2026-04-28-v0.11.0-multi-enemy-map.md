# v0.11.0 多敌人地图 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 地图扩展为 3 个节点，普通敌人（颚虫 / 烈焰蜥蜴）随机排列，Boss 固定压轴，地图只显示"关卡 X"不透露敌人信息。

**Architecture:** `start_new_run()` 时从 NORMAL_POOL 随机打乱取 2 个再追加 BOSS，生成 `enemy_sequence: Array[String]` 写入 `PlayerState`；`get_current_enemy_data()` 读序列；`MapScreen` 动态读 `enemy_sequence.size()` 生成按钮，标签只显示"关卡 X"。

**Tech Stack:** Godot 4.6, GDScript 严格类型模式（`class_name` / `@export` / `Array[T]`）

---

## 文件变更一览

| 操作 | 文件 | 变更摘要 |
|---|---|---|
| 新增 | `data/enemies/fire_lizard.tres` | 烈焰蜥蜴：hp=38，蓄力(block 8) → 爆发(attack 18) |
| 修改 | `PlayerState.gd` | 新增 `enemy_sequence: Array[String]` 字段 |
| 修改 | `GameManager.gd` | 删除 STAGE_ENEMIES；新增 NORMAL_POOL/BOSS_ENEMY；序列生成；读序列 |
| 修改 | `map/MapScreen.gd` | 删除 NODE_LABELS；动态读 enemy_sequence.size() 生成按钮 |

---

## Task 1：新增烈焰蜥蜴敌人数据文件

**Files:**
- Create: `e:/MyWork/AIGameTest2/data/enemies/fire_lizard.tres`

> 烈焰蜥蜴是蓄力爆发型：第 1 回合蓄力（block 8），第 2 回合爆发（attack 18），循环。两个 UID 从已有敌人文件中获取（EnemyData.gd = `uid://bp7tc2xnj6g4e`，EnemyActionData.gd = `uid://dkqvfnm3r8w5y`）。

- [ ] **Step 1：创建 fire_lizard.tres**

创建文件 `e:/MyWork/AIGameTest2/data/enemies/fire_lizard.tres`，内容如下（注意：actions 数组顺序决定行动循环，第一项 = 第 1 回合）：

```
[gd_resource type="Resource" script_class="EnemyData" load_steps=5 format=3]

[ext_resource type="Script" uid="uid://bp7tc2xnj6g4e" path="res://EnemyData.gd" id="1_EnemyData"]
[ext_resource type="Script" uid="uid://dkqvfnm3r8w5y" path="res://EnemyActionData.gd" id="2_EnemyActionData"]

[sub_resource type="Resource" id="Action_block"]
script = ExtResource("2_EnemyActionData")
type = "block"
value = 8

[sub_resource type="Resource" id="Action_attack"]
script = ExtResource("2_EnemyActionData")
type = "attack"
value = 18

[resource]
script = ExtResource("1_EnemyData")
display_name = "烈焰蜥蜴"
hp = 38
actions = [SubResource("Action_block"), SubResource("Action_attack")]
```

- [ ] **Step 2：提交**

```bash
git add data/enemies/fire_lizard.tres
git commit -m "feat: add fire_lizard enemy data (block 8 / attack 18 cycle)"
```

---

## Task 2：更新 PlayerState.gd

**Files:**
- Modify: `e:/MyWork/AIGameTest2/PlayerState.gd`

> 新增 `enemy_sequence` 字段存储本局随机生成的敌人路径序列。空数组为默认值，向后兼容（GameManager.start_new_run 调用后才会填充）。

- [ ] **Step 1：修改 PlayerState.gd**

将 `e:/MyWork/AIGameTest2/PlayerState.gd` 完整内容替换为：

```gdscript
class_name PlayerState
extends RefCounted

var deck: Array[CardData] = []
var current_node: int = 0
var hp: int = 70
var enemy_sequence: Array[String] = []
```

注意缩进使用 Tab。

- [ ] **Step 2：提交**

```bash
git add PlayerState.gd
git commit -m "feat: add enemy_sequence field to PlayerState"
```

---

## Task 3：更新 GameManager.gd 和 MapScreen.gd

**Files:**
- Modify: `e:/MyWork/AIGameTest2/GameManager.gd`
- Modify: `e:/MyWork/AIGameTest2/map/MapScreen.gd`

> 这两个文件必须在同一 Task 内同步修改：GameManager 将 enemy_sequence 扩展为 3 个元素，MapScreen 必须同步读 enemy_sequence.size() 来生成 3 个节点按钮。若分开提交，中间状态地图只有 2 个按钮但实际有 3 个节点，会导致 Boss 节点永远无法到达。

- [ ] **Step 1：修改 GameManager.gd**

将 `e:/MyWork/AIGameTest2/GameManager.gd` 完整内容替换为：

```gdscript
extends Node

enum Phase { MENU, MAP, COMBAT, REWARD, WIN }

const NORMAL_POOL: Array[String] = [
	"res://data/enemies/jaw_worm.tres",
	"res://data/enemies/fire_lizard.tres",
]
const BOSS_ENEMY: String = "res://data/enemies/boss.tres"

var current_phase: Phase = Phase.MENU
var player_state: PlayerState

func start_new_run() -> void:
	player_state = PlayerState.new()
	var strike: CardData = preload("res://data/cards/strike.tres")
	var defend: CardData = preload("res://data/cards/defend.tres")
	var bash: CardData   = preload("res://data/cards/bash.tres")
	var slash: CardData  = preload("res://data/cards/slash.tres")
	for i in 4:
		player_state.deck.append(strike.duplicate())
	for i in 4:
		player_state.deck.append(defend.duplicate())
	player_state.deck.append(bash.duplicate())
	player_state.deck.append(slash.duplicate())
	var pool: Array[String] = NORMAL_POOL.duplicate()
	pool.shuffle()
	player_state.enemy_sequence.assign(pool)
	player_state.enemy_sequence.append(BOSS_ENEMY)
	current_phase = Phase.MAP
	get_tree().change_scene_to_file("res://map/MapScreen.tscn")

func go_to_combat() -> void:
	current_phase = Phase.COMBAT
	get_tree().change_scene_to_file("res://combat/CombatScreen.tscn")

func go_to_reward() -> void:
	current_phase = Phase.REWARD
	get_tree().change_scene_to_file("res://reward/RewardScreen.tscn")

func go_to_map() -> void:
	player_state.current_node += 1
	current_phase = Phase.MAP
	get_tree().change_scene_to_file("res://map/MapScreen.tscn")

func go_to_win() -> void:
	current_phase = Phase.WIN
	get_tree().change_scene_to_file("res://win/WinScreen.tscn")

func go_to_menu() -> void:
	player_state = null
	current_phase = Phase.MENU
	get_tree().change_scene_to_file("res://menu/MainMenu.tscn")

func get_current_enemy_data() -> EnemyData:
	return load(player_state.enemy_sequence[player_state.current_node]) as EnemyData

func is_final_node() -> bool:
	return player_state.current_node == player_state.enemy_sequence.size() - 1
```

注意缩进使用 Tab。

- [ ] **Step 2：修改 MapScreen.gd**

将 `e:/MyWork/AIGameTest2/map/MapScreen.gd` 完整内容替换为：

```gdscript
extends Control

@onready var _node_container: VBoxContainer = $VBoxContainer/NodeContainer

func _ready() -> void:
	var current: int = GameManager.player_state.current_node
	var total: int = GameManager.player_state.enemy_sequence.size()
	for i: int in total:
		var btn: Button = Button.new()
		var label: String = "关卡 %d" % (i + 1)
		if i == total - 1:
			label = "关卡 %d（Boss）" % (i + 1)
		if i < current:
			btn.text = label + "（已完成）"
			btn.disabled = true
		elif i == current:
			btn.text = label
			btn.pressed.connect(GameManager.go_to_combat)
		else:
			btn.text = label + "（未解锁）"
			btn.disabled = true
		_node_container.add_child(btn)
```

注意缩进使用 Tab。

- [ ] **Step 3：运行项目验证无脚本错误**

使用 `mcp__godot__run_project`（projectPath="e:/MyWork/AIGameTest2"）运行项目，等待约 4 秒后调用 `mcp__godot__get_debug_output`。

确认：
- 无 `SCRIPT ERROR:` 或 `Parser Error:`
- 游戏启动进入主菜单

- [ ] **Step 4：停止项目**

使用 `mcp__godot__stop_project` 停止项目。

- [ ] **Step 5：提交**

```bash
git add GameManager.gd map/MapScreen.gd
git commit -m "feat: randomize enemy sequence at run start, expand map to 3 nodes"
```

---

## Task 4：完整验收测试 + 打标签

**Files:** 无代码变更

> 通过完整游戏流程验证所有验收标准，然后打 v0.11.0 标签。

- [ ] **Step 1：运行项目**

使用 `mcp__godot__run_project`（projectPath="e:/MyWork/AIGameTest2"）启动游戏。

- [ ] **Step 2：检查调试输出无错误**

调用 `mcp__godot__get_debug_output`，确认无 `SCRIPT ERROR:` 或 `Parser Error:`。

- [ ] **Step 3：验收清单**

在游戏中手动验证以下各项：

**地图显示：**
- [ ] 开始新游戏后，地图显示 3 个节点
- [ ] 节点标签为"关卡 1"、"关卡 2"、"关卡 3（Boss）"，不显示任何敌人名称
- [ ] 只有"关卡 1"按钮可点击，其余显示"（未解锁）"

**随机性验证（多局）：**
- [ ] 开始第二局，进入地图，第 1 个节点有约 50% 概率和第一局不同（颚虫 vs 烈焰蜥蜴顺序互换）

**烈焰蜥蜴战斗节奏：**
- [ ] 当节点对应烈焰蜥蜴时，第 1 回合敌人行动显示"格挡 8"（蓄力）
- [ ] 第 2 回合显示"攻击 18"（爆发）
- [ ] 第 3 回合重新显示"格挡 8"（循环）

**颚虫行为不变：**
- [ ] 颚虫仍然交替攻击 11 / 格挡 6

**完整流程：**
- [ ] 能完成 3 个节点后进入胜利界面
- [ ] 第 3 节点（Boss）始终是精英守卫（hp=80，攻 15/挡 8）

**v0.10.0 回归：**
- [ ] 洞察/迅刺/蓄力/冲刺卡牌效果正常
- [ ] 奖励界面正常出现并可选牌
- [ ] HP 在关卡间正确保留

- [ ] **Step 4：停止项目**

使用 `mcp__godot__stop_project` 停止项目。

- [ ] **Step 5：打标签并确认**

```bash
git tag v0.11.0
git log --oneline -5
```

期望输出（commit hash 不同，内容类似）：
```
xxxxxxx feat: randomize enemy sequence at run start, expand map to 3 nodes
xxxxxxx feat: add enemy_sequence field to PlayerState
xxxxxxx feat: add fire_lizard enemy data (block 8 / attack 18 cycle)
xxxxxxx docs: add v0.11.0 multi-enemy map design spec
xxxxxxx fix: take_damage reduces block correctly when damage < block
```

---

## 验收总结

完成所有 Task 后，以下标准应全部满足：

- [ ] 地图有 3 个节点，标签为"关卡 1 / 关卡 2 / 关卡 3（Boss）"
- [ ] 前 2 个节点随机排列颚虫与烈焰蜥蜴
- [ ] 第 3 节点始终是精英守卫
- [ ] 烈焰蜥蜴：第 1 回合 block 8，第 2 回合 attack 18，循环
- [ ] 地图不透露任何敌人信息
- [ ] 全部 v0.10.0 验收标准继续通过
