# v0.15.0 NodeData 重构 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 `PlayerState.enemy_sequence: Array[String]` 升级为 `node_sequence: Array[NodeData]`，消除魔法字符串，使节点类型显式且类型安全（纯重构，零行为变化）。

**Architecture:** 新建 `NodeData.gd`（Resource 子类，含 `Type` enum 和 `enemy_data: EnemyData`）；`PlayerState` 字段改名改类型；`GameManager` 在运行时构造 `NodeData` 对象并移除 `REST_NODE` 常量；`MapScreen` 改用 `node.type == NodeData.Type.REST` 判断。无新 `.tres` 文件，EnemyData 资源文件不变。

**Tech Stack:** Godot 4.6, GDScript 严格类型模式

---

## 文件变更一览

| 操作 | 文件 | 变更摘要 |
|---|---|---|
| 新增 | `NodeData.gd` | Type enum + enemy_data 字段 |
| 修改 | `PlayerState.gd` | `enemy_sequence: Array[String]` → `node_sequence: Array[NodeData]` |
| 修改 | `GameManager.gd` | 构造 NodeData；移除 REST_NODE；更新 get_current_enemy_data() 和 is_final_node() |
| 修改 | `map/MapScreen.gd` | sequence 类型改为 Array[NodeData]；类型判断改为 node.type |

---

## Task 1：新建 NodeData.gd + 更新 PlayerState.gd

**Files:**
- Create: `e:/MyWork/AIGameTest2/NodeData.gd`
- Modify: `e:/MyWork/AIGameTest2/PlayerState.gd`

> `NodeData.gd` 是后续所有改动的基础类型，必须先建好。`PlayerState.gd` 引用了 `Array[NodeData]`，两个文件需要同步提交以避免中间编译错误。

- [ ] **Step 1：新建 NodeData.gd**

新建文件 `e:/MyWork/AIGameTest2/NodeData.gd`，内容为（缩进使用 Tab）：

```gdscript
class_name NodeData
extends Resource

enum Type { COMBAT, REST }

var type: Type = Type.COMBAT
var enemy_data: EnemyData
```

- [ ] **Step 2：更新 PlayerState.gd**

将 `e:/MyWork/AIGameTest2/PlayerState.gd` 完整内容替换为（缩进使用 Tab）：

```gdscript
class_name PlayerState
extends RefCounted

var deck: Array[CardData] = []
var current_node: int = 0
var hp: int = 70
var max_hp: int = 70
var node_sequence: Array[NodeData] = []
```

- [ ] **Step 3：提交**

```bash
git -C "e:/MyWork/AIGameTest2" add NodeData.gd PlayerState.gd
git -C "e:/MyWork/AIGameTest2" commit -m "refactor: add NodeData resource, replace enemy_sequence with node_sequence in PlayerState"
```

---

## Task 2：更新 GameManager.gd

**Files:**
- Modify: `e:/MyWork/AIGameTest2/GameManager.gd`

> 移除 `REST_NODE` 常量，`start_new_run()` 改为构造 `NodeData` 对象，`get_current_enemy_data()` 和 `is_final_node()` 改读 `node_sequence`。`NORMAL_POOL` 和 `BOSS_ENEMY` 字符串常量保留（仍用于 `load` EnemyData 资源）。

- [ ] **Step 1：更新 GameManager.gd**

将 `e:/MyWork/AIGameTest2/GameManager.gd` 完整内容替换为（缩进使用 Tab）：

```gdscript
extends Node

enum Phase { MENU, MAP, COMBAT, REWARD, REST, WIN }

const NORMAL_POOL: Array[String] = [
	"res://data/enemies/jaw_worm.tres",
	"res://data/enemies/fire_lizard.tres",
]
const BOSS_ENEMY: String = "res://data/enemies/boss.tres"

var current_phase: Phase = Phase.MENU
var player_state: PlayerState
var last_rest_heal: int = 0

func start_new_run() -> void:
	player_state = PlayerState.new()
	var strike: CardData = preload("res://data/cards/strike.tres")
	var defend: CardData = preload("res://data/cards/defend.tres")
	var bash: CardData   = preload("res://data/cards/bash.tres")
	var slash: CardData  = preload("res://data/cards/slash.tres")
	for i in 4:
		player_state.deck.append(strike.duplicate())
	for i in 4:
		player_state.deck.append(defend.duplicate())
	player_state.deck.append(bash.duplicate())
	player_state.deck.append(slash.duplicate())
	var pool: Array[NodeData] = []
	for path: String in NORMAL_POOL:
		var nd: NodeData = NodeData.new()
		nd.type = NodeData.Type.COMBAT
		nd.enemy_data = load(path) as EnemyData
		pool.append(nd)
	pool.shuffle()
	player_state.node_sequence.assign(pool)
	var rest_nd: NodeData = NodeData.new()
	rest_nd.type = NodeData.Type.REST
	player_state.node_sequence.append(rest_nd)
	var boss_nd: NodeData = NodeData.new()
	boss_nd.type = NodeData.Type.COMBAT
	boss_nd.enemy_data = load(BOSS_ENEMY) as EnemyData
	player_state.node_sequence.append(boss_nd)
	current_phase = Phase.MAP
	get_tree().change_scene_to_file("res://map/MapScreen.tscn")

func go_to_combat() -> void:
	current_phase = Phase.COMBAT
	get_tree().change_scene_to_file("res://combat/CombatScreen.tscn")

func go_to_reward() -> void:
	current_phase = Phase.REWARD
	get_tree().change_scene_to_file("res://reward/RewardScreen.tscn")

func go_to_rest() -> void:
	last_rest_heal = int(player_state.max_hp * 0.3)
	player_state.hp = mini(player_state.hp + last_rest_heal, player_state.max_hp)
	current_phase = Phase.REST
	get_tree().change_scene_to_file("res://rest/RestScreen.tscn")

func go_to_map() -> void:
	player_state.current_node += 1
	current_phase = Phase.MAP
	get_tree().change_scene_to_file("res://map/MapScreen.tscn")

func go_to_win() -> void:
	current_phase = Phase.WIN
	get_tree().change_scene_to_file("res://win/WinScreen.tscn")

func go_to_menu() -> void:
	player_state = null
	current_phase = Phase.MENU
	get_tree().change_scene_to_file("res://menu/MainMenu.tscn")

func get_current_enemy_data() -> EnemyData:
	return player_state.node_sequence[player_state.current_node].enemy_data

func is_final_node() -> bool:
	return player_state.current_node == player_state.node_sequence.size() - 1
```

- [ ] **Step 2：提交**

```bash
git -C "e:/MyWork/AIGameTest2" add GameManager.gd
git -C "e:/MyWork/AIGameTest2" commit -m "refactor: update GameManager to construct NodeData objects, remove REST_NODE constant"
```

---

## Task 3：更新 map/MapScreen.gd

**Files:**
- Modify: `e:/MyWork/AIGameTest2/map/MapScreen.gd`

> `sequence` 类型从 `Array[String]` 改为 `Array[NodeData]`；节点类型判断从 `sequence[i] == GameManager.REST_NODE` 改为 `sequence[i].type == NodeData.Type.REST`；`_get_node_label()` 参数类型同步更新。

- [ ] **Step 1：更新 map/MapScreen.gd**

将 `e:/MyWork/AIGameTest2/map/MapScreen.gd` 完整内容替换为（缩进使用 Tab）：

```gdscript
extends Control

@onready var _node_container: VBoxContainer = $VBoxContainer/NodeContainer

func _ready() -> void:
	var current: int = GameManager.player_state.current_node
	var sequence: Array[NodeData] = GameManager.player_state.node_sequence
	var total: int = sequence.size()
	for i: int in total:
		var btn: Button = Button.new()
		var label: String = _get_node_label(i, sequence, total)
		if i < current:
			btn.text = label + "（已完成）"
			btn.disabled = true
		elif i == current:
			btn.text = label
			if sequence[i].type == NodeData.Type.REST:
				btn.pressed.connect(GameManager.go_to_rest)
			else:
				btn.pressed.connect(GameManager.go_to_combat)
		else:
			btn.text = label + "（未解锁）"
			btn.disabled = true
		_node_container.add_child(btn)

func _get_node_label(i: int, sequence: Array[NodeData], total: int) -> String:
	if sequence[i].type == NodeData.Type.REST:
		return "休息站"
	elif i == total - 1:
		return "关卡 %d（Boss）" % (i + 1)
	else:
		return "关卡 %d" % (i + 1)
```

- [ ] **Step 2：提交**

```bash
git -C "e:/MyWork/AIGameTest2" add map/MapScreen.gd
git -C "e:/MyWork/AIGameTest2" commit -m "refactor: update MapScreen to use NodeData.Type enum instead of string comparison"
```

---

## Task 4：验收 + 打标签

**Files:** 无代码变更

> Godot MCP 工具在 subagent 环境中不可用，运行时验收由用户手动完成。本 Task 只做收尾和打标签。

- [ ] **Step 1：确认 git log 无遗漏**

```bash
git -C "e:/MyWork/AIGameTest2" log --oneline -5
```

期望输出（hash 不同，内容如下）：
```
xxxxxxx refactor: update MapScreen to use NodeData.Type enum instead of string comparison
xxxxxxx refactor: update GameManager to construct NodeData objects, remove REST_NODE constant
xxxxxxx refactor: add NodeData resource, replace enemy_sequence with node_sequence in PlayerState
xxxxxxx docs: add v0.15.0 NodeData refactor design spec
xxxxxxx refactor: remove max_hp hardcode in CombatEngine, read from PlayerState via setup()
```

- [ ] **Step 2：打标签**

```bash
git -C "e:/MyWork/AIGameTest2" tag v0.15.0
```

---

## 验收清单（用户手动验证）

- [ ] 游戏启动正常，主菜单可进入
- [ ] 开始新游戏后地图显示 4 个节点，第 3 个标为"休息站"
- [ ] 已完成节点标为"（已完成）"，未解锁标为"（未解锁）"
- [ ] 点击当前战斗节点可进入战斗，正常结算
- [ ] 点击休息站可进入休息场景，显示回血并可继续
- [ ] Boss 节点可正常进入战斗
- [ ] v0.14.0 全部功能正常（虚弱效果、查看牌组等）
- [ ] 代码中不再出现字符串 `"rest"` 用于节点类型判断
