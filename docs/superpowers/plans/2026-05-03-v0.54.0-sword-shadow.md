# v0.54.0 剑气残影 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增剑气残影怪物，配套引入加权随机行动选择、蓄力状态机制和多段攻击三个新机制。

**Architecture:** 分三层扩展：数据类新增字段（weight/count/random_actions/is_charging/charge_value）→ 引擎层新增 `_pending_actions` 缓存+加权随机+蓄力分支 → 纯数据文件+注册。`_pending_actions` 在每个玩家回合开始时统一解算，确保意图显示和行动执行使用同一次随机结果。

**Tech Stack:** GDScript 4，Godot 4.4 .tres resource 文件

---

## File Structure

**Modified:**
- `EnemyActionData.gd` — 新增 `weight: int = 1` 和 `count: int = 1`
- `EnemyData.gd` — 新增 `random_actions: bool = false`
- `Combatant.gd` — 新增 `is_charging: bool = false` 和 `charge_value: int = 0`
- `CombatEngine.gd` — 新增 `_pending_actions`、`_resolve_enemy_actions()`、`_weighted_random_action()`，修改 `get_enemy_action()` 和 `_do_enemy_turn()`
- `combat/CombatScreen.gd` — 扩展 `_intent_text()` 支持 `multi_attack` 和 `charge`

**Created:**
- `data/enemies/sword_shadow.tres` — 剑气残影数据
- `data/enemy_groups/single_sword_shadow.tres` — 单体组

---

### Task 1: 扩展数据类字段

三个数据类各加新字段，均带默认值，现有 .tres 文件无需修改。

**Files:**
- Modify: `EnemyActionData.gd`
- Modify: `EnemyData.gd`
- Modify: `Combatant.gd`

- [ ] **Step 1: 修改 `EnemyActionData.gd`**

当前文件全文：
```gdscript
class_name EnemyActionData
extends Resource

@export var type: String
@export var value: int
```

替换为：
```gdscript
class_name EnemyActionData
extends Resource

@export var type: String
@export var value: int
@export var weight: int = 1
@export var count: int = 1
```

- [ ] **Step 2: 修改 `EnemyData.gd`**

当前文件全文：
```gdscript
class_name EnemyData
extends Resource

@export var display_name: String
@export var hp: int
@export var actions: Array[EnemyActionData]
```

替换为：
```gdscript
class_name EnemyData
extends Resource

@export var display_name: String
@export var hp: int
@export var actions: Array[EnemyActionData]
@export var random_actions: bool = false
```

- [ ] **Step 3: 修改 `Combatant.gd`**

在 `var current_intent: String = ""` 这一行之后追加两行：

```gdscript
var is_charging: bool = false
var charge_value: int = 0
```

完成后 `Combatant.gd` 末尾应为：
```gdscript
var current_intent: String = ""
var is_charging: bool = false
var charge_value: int = 0

func take_damage(amount: int) -> void:
```

- [ ] **Step 4: Commit**

```bash
git add EnemyActionData.gd EnemyData.gd Combatant.gd
git commit -m "feat: add weight/count to EnemyActionData, random_actions to EnemyData, charge state to Combatant"
```

---

### Task 2: CombatEngine + CombatScreen 逻辑扩展

引入 `_pending_actions` 缓存机制：每个玩家回合开始时统一解算所有敌人的本轮行动（加权随机或顺序循环），`get_enemy_action(i)` 直接返回缓存，保证意图显示和执行一致。

**Files:**
- Modify: `CombatEngine.gd`
- Modify: `combat/CombatScreen.gd`

- [ ] **Step 1: 在 `CombatEngine.gd` 中添加 `_pending_actions` 成员变量**

在现有成员变量区（`var _relics: Array[RelicData] = []` 之后，`func setup(...)` 之前）追加：

```gdscript
var _pending_actions: Array[EnemyActionData] = []
```

- [ ] **Step 2: 替换 `get_enemy_action(i)` 方法**

当前实现（第 50-52 行）：
```gdscript
func get_enemy_action(i: int) -> EnemyActionData:
	var actions: Array = _enemy_data_list[i].actions
	return actions[(turn_number - 1) % actions.size()]
```

替换为：
```gdscript
func get_enemy_action(i: int) -> EnemyActionData:
	return _pending_actions[i]
```

- [ ] **Step 3: 在 `CombatEngine.gd` 中添加 `_resolve_enemy_actions()` 和 `_weighted_random_action()` 两个方法**

在 `get_enemy_action(i)` 方法之后（`func get_draw_pile()` 之前）插入：

```gdscript
func _resolve_enemy_actions() -> void:
	_pending_actions.resize(enemies.size())
	for i in enemies.size():
		if enemies[i].hp <= 0:
			continue
		if enemies[i].is_charging:
			var a := EnemyActionData.new()
			a.type = "charge_attack"
			a.value = enemies[i].charge_value
			_pending_actions[i] = a
		elif _enemy_data_list[i].random_actions:
			_pending_actions[i] = _weighted_random_action(_enemy_data_list[i].actions)
		else:
			_pending_actions[i] = _enemy_data_list[i].actions[(turn_number - 1) % _enemy_data_list[i].actions.size()]

func _weighted_random_action(actions: Array[EnemyActionData]) -> EnemyActionData:
	var total: int = 0
	for a: EnemyActionData in actions:
		total += a.weight
	var roll: int = randi() % total
	var cumulative: int = 0
	for a: EnemyActionData in actions:
		cumulative += a.weight
		if roll < cumulative:
			return a
	return actions[-1]
```

- [ ] **Step 4: 修改 `_start_player_turn()` — 在意图更新循环前调用 `_resolve_enemy_actions()`**

找到 `_start_player_turn()` 中的 `for i in enemies.size():` 循环（当前在第 139 行附近），在该循环**之前**插入 `_resolve_enemy_actions()` 调用：

```gdscript
	# 原有内容（draw per turn 处理）之后，for 循环之前插入：
	_resolve_enemy_actions()
	for i in enemies.size():
		if enemies[i].hp > 0:
			enemies[i].current_intent = get_enemy_action(i).type
```

- [ ] **Step 5: 修改 `_do_enemy_turn()` — 添加 multi_attack、charge 分支，处理蓄力状态清除**

找到 `_do_enemy_turn()` 中的：
```gdscript
		var action: EnemyActionData = get_enemy_action(i)
		match action.type:
```

在这两行之间插入蓄力状态清除，并扩展 match 块。完整替换 `_do_enemy_turn()` 中从 `var action: EnemyActionData` 到 `_:` 块结尾的部分：

```gdscript
		var action: EnemyActionData = get_enemy_action(i)
		enemies[i].is_charging = false
		enemies[i].charge_value = 0
		match action.type:
			"attack", "charge_attack":
				var hp_before: int = player.hp
				EffectResolver.apply_damage(enemies[i], player, action.value)
				var dmg: int = hp_before - player.hp
				if dmg > 0:
					player_damaged.emit(dmg)
				enemies[i].weak = max(0, enemies[i].weak - 1)
			"attack_weak":
				var hp_before: int = player.hp
				EffectResolver.apply_damage(enemies[i], player, action.value)
				var dmg: int = hp_before - player.hp
				if dmg > 0:
					player_damaged.emit(dmg)
				player.add_weak(1)
				enemies[i].weak = max(0, enemies[i].weak - 1)
			"attack_vulnerable":
				var hp_before: int = player.hp
				EffectResolver.apply_damage(enemies[i], player, action.value)
				var dmg: int = hp_before - player.hp
				if dmg > 0:
					player_damaged.emit(dmg)
				player.add_vulnerable(1)
				enemies[i].weak = max(0, enemies[i].weak - 1)
			"multi_attack":
				var hp_before: int = player.hp
				for _hit in action.count:
					EffectResolver.apply_damage(enemies[i], player, action.value)
				var dmg: int = hp_before - player.hp
				if dmg > 0:
					player_damaged.emit(dmg)
				enemies[i].weak = max(0, enemies[i].weak - 1)
			"charge":
				enemies[i].is_charging = true
				enemies[i].charge_value = action.value
			"poison":
				for _j in action.value:
					_draw_pile.append(VENOM_CARD.duplicate())
				_draw_pile.shuffle()
			"discard_curse":
				for _j in action.value:
					_discard_pile.append(VENOM_CARD.duplicate())
			_:
				enemies[i].add_block(action.value)
```

注意：`enemies[i].is_charging = false` 在 match 之前，确保每轮清除旧的蓄力状态；若本轮 `"charge"` 分支触发，会重新置为 true。

- [ ] **Step 6: 修改 `combat/CombatScreen.gd` — 扩展 `_intent_text()`**

当前 `_intent_text()` 方法（第 131-147 行），将整个方法替换为：

```gdscript
func _intent_text(action: EnemyActionData, e: Combatant) -> String:
	var val: int = int(action.value * 0.75) if e.weak > 0 else action.value
	match action.type:
		"attack":
			return "意图：攻击 %d" % val
		"charge_attack":
			return "意图：蓄力攻击 %d" % val
		"attack_weak":
			return "意图：攻击 %d + 虚弱" % val
		"attack_vulnerable":
			return "意图：攻击 %d + 易伤" % val
		"multi_attack":
			return "意图：连斩 %d ×%d" % [val, action.count]
		"charge":
			return "意图：蓄力"
		"poison":
			return "意图：投毒 %d" % action.value
		"discard_curse":
			return "意图：心魔 ×%d" % action.value
		_:
			return "意图：格挡 %d" % action.value
```

- [ ] **Step 7: Commit**

```bash
git add CombatEngine.gd combat/CombatScreen.gd
git commit -m "feat: add weighted random actions, charge state, multi_attack action type"
```

---

### Task 3: 剑气残影数据文件

HP = 46（范围 42-50 中值），random_actions = true，3 个加权行动：攻击9（weight=6）/ 连斩5×2（weight=3）/ 蓄力16（weight=1）。

**Files:**
- Create: `data/enemies/sword_shadow.tres`
- Create: `data/enemy_groups/single_sword_shadow.tres`

- [ ] **Step 1: 创建 `data/enemies/sword_shadow.tres`**

```
[gd_resource type="Resource" script_class="EnemyData" load_steps=6 format=3]

[ext_resource type="Script" path="res://EnemyData.gd" id="1_EnemyData"]
[ext_resource type="Script" path="res://EnemyActionData.gd" id="2_EnemyActionData"]

[sub_resource type="Resource" id="Action_attack"]
script = ExtResource("2_EnemyActionData")
type = "attack"
value = 9
weight = 6
count = 1

[sub_resource type="Resource" id="Action_multi"]
script = ExtResource("2_EnemyActionData")
type = "multi_attack"
value = 5
weight = 3
count = 2

[sub_resource type="Resource" id="Action_charge"]
script = ExtResource("2_EnemyActionData")
type = "charge"
value = 16
weight = 1
count = 1

[resource]
script = ExtResource("1_EnemyData")
display_name = "剑气残影"
hp = 46
random_actions = true
actions = Array[ExtResource("2_EnemyActionData")]([SubResource("Action_attack"), SubResource("Action_multi"), SubResource("Action_charge")])
```

- [ ] **Step 2: 创建 `data/enemy_groups/single_sword_shadow.tres`**

```
[gd_resource type="Resource" script_class="EnemyGroupData" load_steps=4 format=3]

[ext_resource type="Script" path="res://EnemyGroupData.gd" id="1_GroupData"]
[ext_resource type="Script" path="res://EnemyData.gd" id="2_EnemyData"]
[ext_resource type="Resource" path="res://data/enemies/sword_shadow.tres" id="3_sword_shadow"]

[resource]
script = ExtResource("1_GroupData")
enemies = Array[ExtResource("2_EnemyData")]([ExtResource("3_sword_shadow")])
```

- [ ] **Step 3: Commit**

```bash
git add data/enemies/sword_shadow.tres data/enemy_groups/single_sword_shadow.tres
git commit -m "feat: add sword shadow enemy data"
```

---

### Task 4: MapGenerator 注册 + Version bump

**Files:**
- Modify: `MapGenerator.gd`
- Modify: `menu/MainMenu.tscn`

- [ ] **Step 1: 修改 `MapGenerator.gd` — 在 EARLY_GROUP_PATHS 末尾追加剑气残影**

当前 EARLY_GROUP_PATHS：
```gdscript
const EARLY_GROUP_PATHS: Array[String] = [
	"res://data/enemy_groups/single_stone_demon.tres",
	"res://data/enemy_groups/pair_fire_bat.tres",
	"res://data/enemy_groups/triple_fire_bat.tres",
	"res://data/enemy_groups/single_bronze_corpse.tres",
]
```

替换为：
```gdscript
const EARLY_GROUP_PATHS: Array[String] = [
	"res://data/enemy_groups/single_stone_demon.tres",
	"res://data/enemy_groups/pair_fire_bat.tres",
	"res://data/enemy_groups/triple_fire_bat.tres",
	"res://data/enemy_groups/single_bronze_corpse.tres",
	"res://data/enemy_groups/single_sword_shadow.tres",
]
```

- [ ] **Step 2: 修改版本号**

将 `menu/MainMenu.tscn` 中：
```
text = "v0.53.0"
```
改为：
```
text = "v0.54.0"
```

- [ ] **Step 3: Commit**

```bash
git add MapGenerator.gd menu/MainMenu.tscn
git commit -m "feat: register sword shadow in MapGenerator, bump version to v0.54.0"
```
