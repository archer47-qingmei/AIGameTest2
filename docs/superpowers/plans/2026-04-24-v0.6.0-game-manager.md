# v0.6.0 GameManager 与主菜单实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 引入 GameManager Autoload 单例，新建主菜单场景，建立完整游戏循环（主菜单 → 战斗 → 主菜单）。

**Architecture:** GameManager 作为 Autoload Node 管理场景切换（唯一允许调用 SceneTree 的基础设施层）；MainMenu.gd 纯 UI 层，只转发按钮事件；CombatScreen.gd 通过 `GameManager.go_to_menu()` 返回主菜单，不持有路径字符串。

**Tech Stack:** Godot 4.6, GDScript strict 类型模式，Godot Autoload 系统，`get_tree().change_scene_to_file()`

---

## 文件变更一览

| 操作 | 文件 | 职责 |
|------|------|------|
| 新建 | `res://GameManager.gd` | Autoload 单例，管理游戏阶段与场景切换 |
| 新建 | `res://GameManager.gd.uid` | Godot 4.4+ UID sidecar |
| 新建 | `res://menu/MainMenu.gd` | 主菜单 UI 脚本，转发开始按钮到 GameManager |
| 新建 | `res://menu/MainMenu.tscn` | 主菜单场景（Control + BtnStart） |
| 修改 | `res://.godot/global_script_class_cache.cfg` | 注册 GameManager class_name |
| 修改 | `res://project.godot` | 注册 GameManager Autoload + 主场景改为 MainMenu.tscn |
| 修改 | `res://combat/CombatScreen.tscn` | 新增 BtnReturnMenu 节点（visible=false） |
| 修改 | `res://combat/CombatScreen.gd` | 连接 BtnReturnMenu，战斗结束时显示 |

---

## Task 1：GameManager Autoload

**Files:**
- Create: `e:/MyWork/AIGameTest2/GameManager.gd`
- Create: `e:/MyWork/AIGameTest2/GameManager.gd.uid`
- Modify: `e:/MyWork/AIGameTest2/.godot/global_script_class_cache.cfg`
- Modify: `e:/MyWork/AIGameTest2/project.godot`

> **背景：** GameManager 使用 `class_name GameManager extends Node`，需要 .uid sidecar 和 global_script_class_cache.cfg 条目。Autoload 在 project.godot 中以 `*res://` 前缀注册——星号表示允许 Godot 在脚本未作为场景节点时仍创建它。主场景暂保持 CombatScreen.tscn，Task 2 再切换。

- [ ] **Step 1：创建 GameManager.gd**

创建 `e:/MyWork/AIGameTest2/GameManager.gd`：

```gdscript
class_name GameManager
extends Node

enum Phase { MENU, COMBAT }

var current_phase: Phase = Phase.MENU

func go_to_combat() -> void:
	current_phase = Phase.COMBAT
	get_tree().change_scene_to_file("res://combat/CombatScreen.tscn")

func go_to_menu() -> void:
	current_phase = Phase.MENU
	get_tree().change_scene_to_file("res://menu/MainMenu.tscn")
```

- [ ] **Step 2：创建 GameManager.gd.uid**

创建 `e:/MyWork/AIGameTest2/GameManager.gd.uid`：

```
uid://dugntw3h4ym1x
```

- [ ] **Step 3：更新 global_script_class_cache.cfg**

将 `e:/MyWork/AIGameTest2/.godot/global_script_class_cache.cfg` 的 `list=[{` 到 `}]` 整体替换，在 EnemyData 条目之后追加 GameManager 条目：

```
list=[{
"base": &"Resource",
"class": &"CardData",
"icon": "",
"is_abstract": false,
"is_tool": false,
"language": &"GDScript",
"path": "res://CardData.gd"
}, {
"base": &"Resource",
"class": &"CardEffectData",
"icon": "",
"is_abstract": false,
"is_tool": false,
"language": &"GDScript",
"path": "res://CardEffectData.gd"
}, {
"base": &"RefCounted",
"class": &"CombatEngine",
"icon": "",
"is_abstract": false,
"is_tool": false,
"language": &"GDScript",
"path": "res://CombatEngine.gd"
}, {
"base": &"RefCounted",
"class": &"Combatant",
"icon": "",
"is_abstract": false,
"is_tool": false,
"language": &"GDScript",
"path": "res://Combatant.gd"
}, {
"base": &"RefCounted",
"class": &"EffectResolver",
"icon": "",
"is_abstract": false,
"is_tool": false,
"language": &"GDScript",
"path": "res://EffectResolver.gd"
}, {
"base": &"Resource",
"class": &"EnemyActionData",
"icon": "",
"is_abstract": false,
"is_tool": false,
"language": &"GDScript",
"path": "res://EnemyActionData.gd"
}, {
"base": &"Resource",
"class": &"EnemyData",
"icon": "",
"is_abstract": false,
"is_tool": false,
"language": &"GDScript",
"path": "res://EnemyData.gd"
}, {
"base": &"Node",
"class": &"GameManager",
"icon": "",
"is_abstract": false,
"is_tool": false,
"language": &"GDScript",
"path": "res://GameManager.gd"
}]
```

- [ ] **Step 4：更新 project.godot（注册 Autoload，暂保持主场景）**

将 `e:/MyWork/AIGameTest2/project.godot` 完整内容替换为（主场景暂保留 CombatScreen，Task 2 再切换）：

```
config_version=5

[application]

config/name="AIGameTest2"
run/main_scene="res://combat/CombatScreen.tscn"
config/features=PackedStringArray("4.6", "Forward Plus")
config/icon="res://icon.svg"

[autoload]

GameManager="*res://GameManager.gd"
```

- [ ] **Step 5：运行项目验证无报错**

```
mcp__godot__run_project(projectPath="e:/MyWork/AIGameTest2")
```

等待约 3 秒后调用 `mcp__godot__get_debug_output`，确认：
- 无 `SCRIPT ERROR:` 或 `Parser Error:`
- 游戏正常进入战斗场景（此时主场景仍是 CombatScreen）

- [ ] **Step 6：停止项目**

```
mcp__godot__stop_project()
```

- [ ] **Step 7：提交**

```bash
git add GameManager.gd GameManager.gd.uid .godot/global_script_class_cache.cfg project.godot
git commit -m "feat: add GameManager autoload singleton"
```

---

## Task 2：menu/ 主菜单场景 + 主场景切换

**Files:**
- Create: `e:/MyWork/AIGameTest2/menu/MainMenu.gd`
- Create: `e:/MyWork/AIGameTest2/menu/MainMenu.tscn`
- Modify: `e:/MyWork/AIGameTest2/project.godot`

> **背景：** MainMenu.gd 无 class_name，不需要 .uid sidecar 或 global_script_class_cache.cfg 条目（同 CombatScreen.gd 的处理方式一致）。MainMenu.tscn 只含一个 BtnStart 按钮，居中显示。project.godot 的 main_scene 改为 MainMenu.tscn。

- [ ] **Step 1：创建 menu/MainMenu.gd**

创建 `e:/MyWork/AIGameTest2/menu/MainMenu.gd`：

```gdscript
extends Control

@onready var _btn_start: Button = $BtnStart

func _ready() -> void:
	_btn_start.pressed.connect(_on_start_pressed)

func _on_start_pressed() -> void:
	GameManager.go_to_combat()
```

- [ ] **Step 2：创建 menu/MainMenu.tscn**

创建 `e:/MyWork/AIGameTest2/menu/MainMenu.tscn`：

```
[gd_scene load_steps=2 format=3 uid="uid://btpx8q5lnmk2v"]

[ext_resource type="Script" path="res://menu/MainMenu.gd" id="1_script"]

[node name="MainMenu" type="Control"]
layout_mode = 3
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
script = ExtResource("1_script")

[node name="BtnStart" type="Button" parent="."]
layout_mode = 1
anchors_preset = 8
anchor_left = 0.5
anchor_top = 0.5
anchor_right = 0.5
anchor_bottom = 0.5
offset_left = -60.0
offset_top = -15.0
offset_right = 60.0
offset_bottom = 15.0
text = "开始游戏"
```

- [ ] **Step 3：更新 project.godot（切换主场景）**

将 `e:/MyWork/AIGameTest2/project.godot` 中的 `run/main_scene` 行改为：

```
run/main_scene="res://menu/MainMenu.tscn"
```

改完后完整文件内容：

```
config_version=5

[application]

config/name="AIGameTest2"
run/main_scene="res://menu/MainMenu.tscn"
config/features=PackedStringArray("4.6", "Forward Plus")
config/icon="res://icon.svg"

[autoload]

GameManager="*res://GameManager.gd"
```

- [ ] **Step 4：运行项目验证主菜单启动**

```
mcp__godot__run_project(projectPath="e:/MyWork/AIGameTest2")
```

等待约 3 秒后调用 `mcp__godot__get_debug_output`，确认：
- 无 `SCRIPT ERROR:` 或 `Parser Error:`
- 游戏启动进入主菜单场景（不是直接进战斗）

- [ ] **Step 5：停止项目**

```
mcp__godot__stop_project()
```

- [ ] **Step 6：提交**

```bash
git add menu/MainMenu.gd menu/MainMenu.tscn project.godot
git commit -m "feat: add MainMenu scene and set as main scene"
```

---

## Task 3：CombatScreen 新增"返回主菜单"按钮

**Files:**
- Modify: `e:/MyWork/AIGameTest2/combat/CombatScreen.tscn`
- Modify: `e:/MyWork/AIGameTest2/combat/CombatScreen.gd`

> **背景：** CombatScreen.tscn 在根节点下新增 BtnReturnMenu（Button，visible=false，居中偏下）。CombatScreen.gd 新增 @onready 引用，在 `_ready()` 连接 `GameManager.go_to_menu`，在 `_on_combat_ended()` 将其设为 visible=true 并禁用手牌按钮。

- [ ] **Step 1：更新 CombatScreen.tscn（新增 BtnReturnMenu 节点）**

将 `e:/MyWork/AIGameTest2/combat/CombatScreen.tscn` 完整内容替换为（新增最后一个节点）：

```
[gd_scene load_steps=2 format=3 uid="uid://combat_screen"]

[ext_resource type="Script" path="res://combat/CombatScreen.gd" id="1_script"]

[node name="CombatScreen" type="Control"]
layout_mode = 3
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
script = ExtResource("1_script")

[node name="VBoxContainer" type="VBoxContainer" parent="."]
layout_mode = 1
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
theme_override_constants/separation = 10

[node name="EnemyPanel" type="VBoxContainer" parent="VBoxContainer"]
layout_mode = 2

[node name="LblEnemyName" type="Label" parent="VBoxContainer/EnemyPanel"]
layout_mode = 2
text = "Enemy"

[node name="LblEnemyHP" type="Label" parent="VBoxContainer/EnemyPanel"]
layout_mode = 2
text = "生命：?"

[node name="LblEnemyBlock" type="Label" parent="VBoxContainer/EnemyPanel"]
layout_mode = 2
text = "格挡：0"

[node name="LblEnemyIntent" type="Label" parent="VBoxContainer/EnemyPanel"]
layout_mode = 2
text = "意图：?"

[node name="PlayerPanel" type="VBoxContainer" parent="VBoxContainer"]
layout_mode = 2

[node name="LblPlayerHP" type="Label" parent="VBoxContainer/PlayerPanel"]
layout_mode = 2
text = "生命：?"

[node name="LblPlayerBlock" type="Label" parent="VBoxContainer/PlayerPanel"]
layout_mode = 2
text = "格挡：0"

[node name="LblEnergy" type="Label" parent="VBoxContainer/PlayerPanel"]
layout_mode = 2
text = "能量：3 / 3"

[node name="HandContainer" type="HBoxContainer" parent="VBoxContainer"]
layout_mode = 2

[node name="BtnEndTurn" type="Button" parent="VBoxContainer"]
layout_mode = 2
text = "结束回合"

[node name="LblResult" type="Label" parent="."]
layout_mode = 1
anchors_preset = 8
anchor_left = 0.5
anchor_top = 0.5
anchor_right = 0.5
anchor_bottom = 0.5
visible = false
text = ""

[node name="BtnReturnMenu" type="Button" parent="."]
layout_mode = 1
anchors_preset = 8
anchor_left = 0.5
anchor_top = 0.6
anchor_right = 0.5
anchor_bottom = 0.6
offset_left = -70.0
offset_top = -15.0
offset_right = 70.0
offset_bottom = 15.0
visible = false
text = "返回主菜单"
```

- [ ] **Step 2：更新 CombatScreen.gd**

将 `e:/MyWork/AIGameTest2/combat/CombatScreen.gd` 完整内容替换为：

```gdscript
extends Control

@onready var _lbl_enemy_name: Label    = $VBoxContainer/EnemyPanel/LblEnemyName
@onready var _lbl_enemy_hp: Label      = $VBoxContainer/EnemyPanel/LblEnemyHP
@onready var _lbl_enemy_block: Label   = $VBoxContainer/EnemyPanel/LblEnemyBlock
@onready var _lbl_enemy_intent: Label  = $VBoxContainer/EnemyPanel/LblEnemyIntent
@onready var _lbl_player_hp: Label     = $VBoxContainer/PlayerPanel/LblPlayerHP
@onready var _lbl_player_block: Label  = $VBoxContainer/PlayerPanel/LblPlayerBlock
@onready var _lbl_energy: Label        = $VBoxContainer/PlayerPanel/LblEnergy
@onready var _hand_container: HBoxContainer = $VBoxContainer/HandContainer
@onready var _btn_end_turn: Button     = $VBoxContainer/BtnEndTurn
@onready var _lbl_result: Label        = $LblResult
@onready var _btn_return_menu: Button  = $BtnReturnMenu

var _engine: CombatEngine
var _hand_buttons: Array[Button] = []

func _ready() -> void:
	_engine = CombatEngine.new()
	_engine.state_changed.connect(_refresh_ui)
	_engine.combat_ended.connect(_on_combat_ended)
	_btn_end_turn.pressed.connect(_engine.end_turn)
	_btn_return_menu.pressed.connect(GameManager.go_to_menu)
	_engine.setup()

func _on_card_pressed(card: CardData) -> void:
	_engine.play_card(card)

func _refresh_ui() -> void:
	_lbl_enemy_name.text = _engine.enemy.display_name
	_lbl_enemy_hp.text = "生命：%d / %d" % [_engine.enemy.hp, _engine.enemy.max_hp]
	_lbl_enemy_block.text = "格挡：%d" % _engine.enemy.block
	var action: EnemyActionData = _engine.get_current_enemy_action()
	var intent: String = "攻击 %d" % action.value if action.type == "attack" else "格挡 %d" % action.value
	_lbl_enemy_intent.text = "意图：" + intent
	_lbl_player_hp.text = "生命：%d / %d" % [_engine.player.hp, _engine.player.max_hp]
	_lbl_player_block.text = "格挡：%d" % _engine.player.block
	_lbl_energy.text = "能量：%d / 3" % _engine.energy
	_rebuild_hand()

func _rebuild_hand() -> void:
	for btn: Button in _hand_buttons:
		btn.queue_free()
	_hand_buttons.clear()
	for card: CardData in _engine.hand:
		var btn: Button = Button.new()
		var dmg: int = 0
		var blk: int = 0
		for effect: CardEffectData in card.effects:
			if effect.type == "damage":
				dmg = effect.value
			elif effect.type == "block":
				blk = effect.value
		btn.text = "%s\n费用:%d  攻:%d  挡:%d" % [card.card_name, card.cost, dmg, blk]
		btn.pressed.connect(_on_card_pressed.bind(card))
		_hand_container.add_child(btn)
		_hand_buttons.append(btn)

func _on_combat_ended(result: String) -> void:
	_lbl_result.text = "胜利！" if result == "victory" else "游戏结束"
	_lbl_result.visible = true
	_btn_end_turn.disabled = true
	_btn_return_menu.visible = true
	for btn: Button in _hand_buttons:
		btn.disabled = true
```

- [ ] **Step 3：运行项目验证完整循环**

```
mcp__godot__run_project(projectPath="e:/MyWork/AIGameTest2")
```

等待约 3 秒后调用 `mcp__godot__get_debug_output`，确认：
- 无 `SCRIPT ERROR:` 或 `Parser Error:`
- 游戏启动进入主菜单（能看到"开始游戏"按钮）

- [ ] **Step 4：停止项目**

```
mcp__godot__stop_project()
```

- [ ] **Step 5：提交并打标签**

```bash
git add combat/CombatScreen.tscn combat/CombatScreen.gd
git commit -m "feat: add BtnReturnMenu to CombatScreen, complete v0.6.0 game loop"
git tag v0.6.0
git log --oneline -5
```

---

## 验收总结

完成所有 Task 后，以下标准应全部满足：

- [ ] 游戏启动进入主菜单，显示"开始游戏"按钮
- [ ] 点击"开始游戏"进入战斗场景，游戏行为与 v0.5.0 完全一致
- [ ] 战斗胜利或失败后，出现"返回主菜单"按钮
- [ ] 点击"返回主菜单"回到主菜单场景
- [ ] 可多次循环（主菜单 → 战斗 → 主菜单 → 战斗...）
- [ ] 全部 v0.5.0 验收标准继续通过
