# 游戏元素详情面板 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 点击战斗界面中的遗物标签或 buff 标签，弹出居中详情面板显示名称和描述。

**Architecture:** 新建 `GameText.gd` 静态类存储 buff 文字；新建 `combat/InfoPanel.gd` 在 `_ready()` 中以代码构建所有子节点（无对应 .tscn），实现 `show_info` / `hide_info`；在 `CombatScreen.gd` 的 `_ready()` 动态创建 InfoPanel 实例，将遗物 Label 和虚弱/脆弱 Label 改为 flat Button，点击时调用 InfoPanel。

**Tech Stack:** GDScript 4, Godot 4.4, Control/ColorRect/PanelContainer/CenterContainer 节点

---

## 文件结构

| 操作 | 文件 | 职责 |
|---|---|---|
| 新建 | `GameText.gd` | 静态查表：buff key → {name, description} |
| 新建 | `combat/InfoPanel.gd` | 弹窗 Control：构建 UI，show_info/hide_info |
| 修改 | `combat/CombatScreen.gd` | 创建 InfoPanel，遗物/buff 改为可点击 Button |

不需要修改任何 .tscn 文件。

---

## 背景知识

### 遗物显示位置（CombatScreen.gd 约第 128–133 行）

```gdscript
for child in _relics_panel.get_children():
    child.queue_free()
for r: RelicData in GameManager.player_state.relics:
    var lbl: Label = Label.new()
    lbl.text = r.display_name
    _relics_panel.add_child(lbl)
```

`RelicData` 有 `display_name: String` 和 `description: String` 字段。

### Buff 显示位置（CombatScreen.gd 约第 446–458 行）

```gdscript
func _build_status_row(row: HBoxContainer, combatant: Combatant) -> void:
    for child in row.get_children():
        child.free()
    if combatant.weak > 0:
        var lbl := Label.new()
        lbl.text = "虚弱×%d" % combatant.weak
        lbl.add_theme_color_override("font_color", Color(1.0, 0.9, 0.2))
        row.add_child(lbl)
    if combatant.vulnerable > 0:
        var lbl := Label.new()
        lbl.text = "脆弱×%d" % combatant.vulnerable
        lbl.add_theme_color_override("font_color", Color(1.0, 0.55, 0.1))
        row.add_child(lbl)
```

### 运行时设锚点规则

在 GDScript 中运行时设置锚点必须用直接属性赋值：
```gdscript
node.anchor_right = 1.0
node.anchor_bottom = 1.0
```
不能用 `anchors_preset` 或 `set_anchors_and_offsets_preset()`。

---

## Task 1: GameText 静态类

**Files:**
- Create: `GameText.gd`

- [ ] **Step 1: 创建 GameText.gd**

路径：`E:\MyWork\AIGameTest2\GameText.gd`

```gdscript
class_name GameText
extends Object

static func get_buff_info(key: String) -> Dictionary:
	match key:
		"weak":
			return {"name": "虚弱", "description": "造成的伤害降低 25%，每回合结束减少 1 层。"}
		"vulnerable":
			return {"name": "脆弱", "description": "受到的伤害增加 25%，每回合结束减少 1 层。"}
		"strength":
			return {"name": "力量", "description": "每点力量使普通攻击伤害 +1。"}
		"sword_intent":
			return {"name": "剑意", "description": "积累剑意可提升下次攻击伤害，每层 +1 伤害，上限 10 层。"}
	return {"name": key, "description": "（暂无说明）"}
```

- [ ] **Step 2: 提交**

```bash
git add GameText.gd
git commit -m "feat: add GameText static class with buff descriptions"
```

---

## Task 2: InfoPanel

**Files:**
- Create: `combat/InfoPanel.gd`

InfoPanel 是一个 Control，在 `_ready()` 内以代码构建所有子节点。结构：

```
InfoPanel (Control, 全屏, z_index=10)
├── Overlay   (ColorRect, 全屏, 半透明, mouse_filter=STOP, 点击关闭)
└── Center    (CenterContainer, 全屏, mouse_filter=IGNORE)
    └── PanelContainer  (mouse_filter=STOP, 260px 最小宽度)
        └── MarginContainer (12px 四周边距)
            └── VBoxContainer
                ├── LblTitle    (Label, 字号 20)
                ├── HSeparator
                ├── LblDesc     (RichTextLabel, fit_content, 无滚动)
                └── BtnClose    (Button, 文字"关闭")
```

点击 PanelContainer 区域内：由 PanelContainer（STOP）拦截，不触发 Overlay 的 gui_input。  
点击 PanelContainer 区域外（遮罩）：PanelContainer 不在命中范围，Overlay（STOP）触发 gui_input → `hide_info()`。

- [ ] **Step 1: 创建 combat/InfoPanel.gd**

路径：`E:\MyWork\AIGameTest2\combat\InfoPanel.gd`

```gdscript
class_name InfoPanel
extends Control

var _overlay: ColorRect
var _lbl_title: Label
var _lbl_desc: RichTextLabel
var _btn_close: Button

func _ready() -> void:
	anchor_right = 1.0
	anchor_bottom = 1.0
	z_index = 10
	visible = false

	_overlay = ColorRect.new()
	_overlay.anchor_right = 1.0
	_overlay.anchor_bottom = 1.0
	_overlay.color = Color(0.0, 0.0, 0.0, 0.5)
	_overlay.mouse_filter = MOUSE_FILTER_STOP
	_overlay.gui_input.connect(_on_overlay_input)
	add_child(_overlay)

	var center := CenterContainer.new()
	center.anchor_right = 1.0
	center.anchor_bottom = 1.0
	center.mouse_filter = MOUSE_FILTER_IGNORE
	add_child(center)

	var panel := PanelContainer.new()
	panel.custom_minimum_size = Vector2(260.0, 0.0)
	center.add_child(panel)

	var margin := MarginContainer.new()
	margin.add_theme_constant_override("margin_left", 12)
	margin.add_theme_constant_override("margin_top", 12)
	margin.add_theme_constant_override("margin_right", 12)
	margin.add_theme_constant_override("margin_bottom", 12)
	panel.add_child(margin)

	var vbox := VBoxContainer.new()
	vbox.add_theme_constant_override("separation", 8)
	margin.add_child(vbox)

	_lbl_title = Label.new()
	_lbl_title.add_theme_font_size_override("font_size", 20)
	vbox.add_child(_lbl_title)

	vbox.add_child(HSeparator.new())

	_lbl_desc = RichTextLabel.new()
	_lbl_desc.bbcode_enabled = true
	_lbl_desc.fit_content = true
	_lbl_desc.scroll_active = false
	_lbl_desc.custom_minimum_size = Vector2(0.0, 40.0)
	vbox.add_child(_lbl_desc)

	_btn_close = Button.new()
	_btn_close.text = "关闭"
	_btn_close.pressed.connect(hide_info)
	vbox.add_child(_btn_close)

func show_info(title: String, description: String) -> void:
	_lbl_title.text = title
	_lbl_desc.text = description
	visible = true

func hide_info() -> void:
	visible = false

func _on_overlay_input(event: InputEvent) -> void:
	if event is InputEventMouseButton:
		var mb := event as InputEventMouseButton
		if mb.button_index == MOUSE_BUTTON_LEFT and mb.pressed:
			hide_info()
```

- [ ] **Step 2: 提交**

```bash
git add combat/InfoPanel.gd
git commit -m "feat: add InfoPanel overlay component"
```

---

## Task 3: CombatScreen 集成

**Files:**
- Modify: `combat/CombatScreen.gd`

本任务做三处修改：
1. 在 `_ready()` 末尾创建 InfoPanel 实例
2. 在 `_refresh_ui()` 中将遗物 Label 改为 flat Button
3. 在 `_build_status_row()` 中将虚弱/脆弱 Label 改为 flat Button

- [ ] **Step 1: 在 CombatScreen.gd 顶部声明 _info_panel 变量**

在现有 `var _engine: CombatEngine` 附近（约第 38 行），新增：

```gdscript
var _info_panel: InfoPanel
```

完整变量声明区域变为：

```gdscript
var _engine: CombatEngine
var _hand_buttons: Array[Button] = []
var _dragging_card_index: int = -1
var _info_panel: InfoPanel
```

- [ ] **Step 2: 在 _ready() 末尾创建 InfoPanel**

在 `_ready()` 函数末尾（`_refresh_ui()` 调用之后）添加：

```gdscript
	_info_panel = InfoPanel.new()
	add_child(_info_panel)
```

完整 `_ready()` 末尾变为：

```gdscript
	_drag_layer.card_played.connect(_on_drag_card_played)
	_drag_layer.drag_cancelled.connect(_on_drag_cancelled)
	_drag_layer.target_changed.connect(_on_drag_target_changed)
	_refresh_ui()
	_info_panel = InfoPanel.new()
	add_child(_info_panel)
```

- [ ] **Step 3: 将遗物 Label 改为 flat Button**

找到 `_refresh_ui()` 中的遗物循环（约第 128–133 行）：

旧代码：
```gdscript
	for child in _relics_panel.get_children():
		child.queue_free()
	for r: RelicData in GameManager.player_state.relics:
		var lbl: Label = Label.new()
		lbl.text = r.display_name
		_relics_panel.add_child(lbl)
```

新代码：
```gdscript
	for child in _relics_panel.get_children():
		child.queue_free()
	for r: RelicData in GameManager.player_state.relics:
		var btn := Button.new()
		btn.flat = true
		btn.text = r.display_name
		btn.pressed.connect(_info_panel.show_info.bind(r.display_name, r.description))
		_relics_panel.add_child(btn)
```

- [ ] **Step 4: 将虚弱/脆弱 Label 改为 flat Button**

找到 `_build_status_row()` 函数（约第 446–458 行）：

旧代码：
```gdscript
func _build_status_row(row: HBoxContainer, combatant: Combatant) -> void:
	for child in row.get_children():
		child.free()
	if combatant.weak > 0:
		var lbl := Label.new()
		lbl.text = "虚弱×%d" % combatant.weak
		lbl.add_theme_color_override("font_color", Color(1.0, 0.9, 0.2))
		row.add_child(lbl)
	if combatant.vulnerable > 0:
		var lbl := Label.new()
		lbl.text = "脆弱×%d" % combatant.vulnerable
		lbl.add_theme_color_override("font_color", Color(1.0, 0.55, 0.1))
		row.add_child(lbl)
```

新代码：
```gdscript
func _build_status_row(row: HBoxContainer, combatant: Combatant) -> void:
	for child in row.get_children():
		child.free()
	if combatant.weak > 0:
		var btn := Button.new()
		btn.flat = true
		btn.text = "虚弱×%d" % combatant.weak
		btn.add_theme_color_override("font_color", Color(1.0, 0.9, 0.2))
		var info := GameText.get_buff_info("weak")
		btn.pressed.connect(_info_panel.show_info.bind(info["name"], info["description"]))
		row.add_child(btn)
	if combatant.vulnerable > 0:
		var btn := Button.new()
		btn.flat = true
		btn.text = "脆弱×%d" % combatant.vulnerable
		btn.add_theme_color_override("font_color", Color(1.0, 0.55, 0.1))
		var info := GameText.get_buff_info("vulnerable")
		btn.pressed.connect(_info_panel.show_info.bind(info["name"], info["description"]))
		row.add_child(btn)
```

- [ ] **Step 5: 提交**

```bash
git add combat/CombatScreen.gd
git commit -m "feat: make relic and buff labels clickable with InfoPanel"
```

---

## 自测说明（无自动化测试）

启动项目后进入一场战斗：

1. 点击左下角某个遗物名称 → 应弹出居中面板，显示遗物名称和描述，背景半透明
2. 点击遮罩区域（面板外）→ 面板关闭
3. 点击「关闭」按钮 → 面板关闭
4. 如果当前战斗中玩家或敌人有虚弱/脆弱状态 → 点击对应标签 → 弹出对应说明面板
5. 无遗物时遗物区域为空，不崩溃
6. 面板显示时无法与其他 UI 交互（遮罩拦截事件）
