# v0.36.0 剑修完整卡牌池 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增 9 张剑修卡牌（掠影/碎星/横剑/裂空/无痕/破军/万剑归宗/剑域/心剑），扩展 effect 系统支持多段伤害和持续被动，将全部 15 张剑修牌纳入奖励池。

**Architecture:** `CardEffectData` 新增 `times` 字段支持多段；`Combatant` 新增三个持续被动字段；`EffectResolver` 加入 times 循环和格挡剑意加成；`CombatEngine` 新增三个 engine effect 分支及相应的回合触发逻辑；9 张新 `.tres` 数据文件；`CardPool.SWORD_REWARD_CARDS` 扩展至 15 条。破军的"消耗后抽牌"通过现有 `draw` effect type 实现（`_apply_engine_effects` 在 `EffectResolver.resolve` 之后运行，自然保证抽牌在剑意消耗后发生）。

**Tech Stack:** Godot 4 GDScript, Resource (.tres)

---

## File Map

| 文件 | 操作 | 说明 |
|------|------|------|
| `CardEffectData.gd` | 修改 | 新增 `times: int = 1` |
| `Combatant.gd` | 修改 | 新增三个持续被动字段 |
| `EffectResolver.gd` | 修改 | times 循环 + 格挡剑意加成 |
| `CombatEngine.gd` | 修改 | end_turn 条件折半 + start_turn 额外抽牌 + 三个新 engine effect |
| `CardData.gd` | 修改 | get_description() 支持新 effect type 显示 |
| `data/cards/lue_ying.tres` | 新建 | 掠影（凡品 招式） |
| `data/cards/sui_xing.tres` | 新建 | 碎星（凡品 招式） |
| `data/cards/heng_jian.tres` | 新建 | 横剑（凡品 身法） |
| `data/cards/lie_kong.tres` | 新建 | 裂空（灵品 终结技） |
| `data/cards/wu_hen.tres` | 新建 | 无痕（灵品 终结技） |
| `data/cards/po_jun.tres` | 新建 | 破军（灵品 终结技） |
| `data/cards/wan_jian_gui_zong.tres` | 新建 | 万剑归宗（仙品 功法） |
| `data/cards/jian_yu.tres` | 新建 | 剑域（仙品 功法） |
| `data/cards/xin_jian.tres` | 新建 | 心剑（仙品 功法） |
| `CardPool.gd` | 修改 | SWORD_REWARD_CARDS 扩展至 15 条 |
| `menu/MainMenu.tscn` | 修改 | 版本号改为 v0.36.0 |

---

### Task 1: CardEffectData.gd + Combatant.gd — 新增字段

**Files:**
- Modify: `E:\MyWork\AIGameTest2\CardEffectData.gd`
- Modify: `E:\MyWork\AIGameTest2\Combatant.gd`

- [ ] **Step 1: 修改 CardEffectData.gd — 新增 times 字段**

读取文件确认当前内容（应为 3 行 export）。将文件改为：

```gdscript
class_name CardEffectData
extends Resource

@export var type: String
@export var value: int
@export var upgrade_bonus: int = 0
@export var times: int = 1
```

- [ ] **Step 2: 修改 Combatant.gd — 新增三个持续被动字段**

读取文件确认当前末尾三行为：
```gdscript
var sword_intent: int = 0
var sword_intent_cap: int = 10
var sword_intent_damage_bonus: int = 1
```

在 `sword_intent_damage_bonus` 之后追加三行：

```gdscript
var draw_per_turn: int = 0
var sword_intent_retain: bool = false
var sword_intent_block_bonus: int = 0
```

完整 Combatant.gd 字段区域应为：

```gdscript
var display_name: String
var hp: int
var max_hp: int
var block: int
var weak: int = 0
var vulnerable: int = 0
var sword_intent: int = 0
var sword_intent_cap: int = 10
var sword_intent_damage_bonus: int = 1
var draw_per_turn: int = 0
var sword_intent_retain: bool = false
var sword_intent_block_bonus: int = 0
```

- [ ] **Step 3: 验证**

读取两个文件，确认：
1. `CardEffectData.gd` 有 `@export var times: int = 1`
2. `Combatant.gd` 末尾有三个新字段，顺序正确

- [ ] **Step 4: 提交**

```bash
git add CardEffectData.gd Combatant.gd
git commit -m "feat: add times field to CardEffectData; add draw_per_turn, sword_intent_retain, sword_intent_block_bonus to Combatant"
```

---

### Task 2: EffectResolver.gd — times 循环 + 格挡剑意加成

**Files:**
- Modify: `E:\MyWork\AIGameTest2\EffectResolver.gd`

- [ ] **Step 1: 完整替换 resolve() 方法**

找到现有的：

```gdscript
static func resolve(card: CardData, attacker: Combatant, defender: Combatant) -> void:
	for effect: CardEffectData in card.effects:
		if effect.type == "damage":
			if defender != null:
				var bonus: int = 0
				if card.is_finisher:
					bonus = attacker.sword_intent * card.sword_intent_consume_bonus
				elif card.card_type == "招式":
					bonus = attacker.sword_intent * attacker.sword_intent_damage_bonus
				apply_damage(attacker, defender, effect.value + bonus)
		elif effect.type == "block":
			attacker.add_block(effect.value)
		elif effect.type == "weak":
			if defender != null:
				defender.add_weak(effect.value)
		elif effect.type == "vulnerable":
			if defender != null:
				defender.add_vulnerable(effect.value)
	if card.is_finisher:
		attacker.sword_intent = 0
	elif card.card_type == "招式":
		attacker.sword_intent = mini(attacker.sword_intent + 1, attacker.sword_intent_cap)
```

整段替换为：

```gdscript
static func resolve(card: CardData, attacker: Combatant, defender: Combatant) -> void:
	for effect: CardEffectData in card.effects:
		for _i in effect.times:
			if effect.type == "damage":
				if defender != null:
					var bonus: int = 0
					if card.is_finisher:
						bonus = attacker.sword_intent * card.sword_intent_consume_bonus
					elif card.card_type == "招式":
						bonus = attacker.sword_intent * attacker.sword_intent_damage_bonus
					apply_damage(attacker, defender, effect.value + bonus)
			elif effect.type == "block":
				var blk_bonus: int = attacker.sword_intent * attacker.sword_intent_block_bonus
				attacker.add_block(effect.value + blk_bonus)
			elif effect.type == "weak":
				if defender != null:
					defender.add_weak(effect.value)
			elif effect.type == "vulnerable":
				if defender != null:
					defender.add_vulnerable(effect.value)
	if card.is_finisher:
		attacker.sword_intent = 0
	elif card.card_type == "招式":
		attacker.sword_intent = mini(attacker.sword_intent + 1, attacker.sword_intent_cap)
```

注意：`times` 循环只包裹 if/elif 分支，不包裹末尾的剑意 update（每张牌只+1次剑意，无论碎星打了几次）。

- [ ] **Step 2: 验证**

读取 `EffectResolver.gd`，确认：
1. `for _i in effect.times:` 在 `for effect` 循环体内，缩进正确
2. block 分支有 `blk_bonus` 加成
3. 剑意 update 在外层 `for effect` 循环之后（不受 times 影响）

- [ ] **Step 3: 提交**

```bash
git add EffectResolver.gd
git commit -m "feat: add times loop and sword_intent_block_bonus to EffectResolver"
```

---

### Task 3: CombatEngine.gd — 持续被动支持

**Files:**
- Modify: `E:\MyWork\AIGameTest2\CombatEngine.gd`

- [ ] **Step 1: 修改 end_turn() — 剑意折半加条件**

找到（位于 `hand.clear()` 之后，`_do_enemy_turn()` 之前）：

```gdscript
	player.sword_intent = player.sword_intent / 2
```

替换为：

```gdscript
	if not player.sword_intent_retain:
		player.sword_intent = player.sword_intent / 2
```

- [ ] **Step 2: 修改 _start_player_turn() — 额外抽牌**

找到：

```gdscript
	_draw_hand()
	if turn_number == 1:
```

在 `_draw_hand()` 和 `if turn_number == 1:` 之间插入：

```gdscript
	if player.draw_per_turn > 0:
		_draw_cards(player.draw_per_turn)
```

完整 `_start_player_turn()` 应为：

```gdscript
func _start_player_turn() -> void:
	player.weak = max(0, player.weak - 1)
	turn_number += 1
	energy = BASE_ENERGY
	player.block = 0
	_draw_hand()
	if player.draw_per_turn > 0:
		_draw_cards(player.draw_per_turn)
	if turn_number == 1:
		RelicEngine.apply_combat_start(_relics, self)
	RelicEngine.apply_turn_start(_relics, self)
	state_changed.emit()
```

- [ ] **Step 3: 修改 _apply_engine_effects() — 新增三个分支**

找到现有末尾：

```gdscript
		elif effect.type == "sword_intent_bonus":
			player.sword_intent_damage_bonus += effect.value
```

在其后追加：

```gdscript
		elif effect.type == "draw_per_turn":
			player.draw_per_turn += effect.value
		elif effect.type == "sword_intent_retain":
			player.sword_intent_retain = true
		elif effect.type == "sword_intent_block_bonus":
			player.sword_intent_block_bonus += effect.value
```

完整 `_apply_engine_effects()` 应为：

```gdscript
func _apply_engine_effects(card: CardData) -> void:
	for effect: CardEffectData in card.effects:
		if effect.type == "draw":
			_draw_cards(effect.value)
		elif effect.type == "energy":
			energy += effect.value
		elif effect.type == "sword_intent_cap":
			player.sword_intent_cap += effect.value
		elif effect.type == "sword_intent_bonus":
			player.sword_intent_damage_bonus += effect.value
		elif effect.type == "draw_per_turn":
			player.draw_per_turn += effect.value
		elif effect.type == "sword_intent_retain":
			player.sword_intent_retain = true
		elif effect.type == "sword_intent_block_bonus":
			player.sword_intent_block_bonus += effect.value
```

- [ ] **Step 4: 验证**

读取 `CombatEngine.gd`，确认：
1. `end_turn()` 中折半被 `if not player.sword_intent_retain:` 包裹
2. `_start_player_turn()` 中 `_draw_hand()` 之后有 `draw_per_turn` 额外抽牌
3. `_apply_engine_effects()` 末尾有三个新分支，缩进与现有分支一致

- [ ] **Step 5: 提交**

```bash
git add CombatEngine.gd
git commit -m "feat: support draw_per_turn, sword_intent_retain, sword_intent_block_bonus in CombatEngine"
```

---

### Task 4: CardData.gd — get_description() 支持新 effect type

**Files:**
- Modify: `E:\MyWork\AIGameTest2\CardData.gd`

- [ ] **Step 1: 完整替换 get_description() 方法**

找到现有：

```gdscript
func get_description() -> String:
	if is_venom:
		return "%s\n费用:%d  %s" % [card_name, cost, special_text]
	var dmg: int = 0
	var blk: int = 0
	var drw: int = 0
	var nrg: int = 0
	var wk: int = 0
	var vul: int = 0
	var si_cap: int = 0
	var si_bonus: int = 0
	for effect: CardEffectData in effects:
		if effect.type == "damage":               dmg = effect.value
		elif effect.type == "block":              blk = effect.value
		elif effect.type == "draw":               drw = effect.value
		elif effect.type == "energy":             nrg = effect.value
		elif effect.type == "weak":               wk = effect.value
		elif effect.type == "vulnerable":         vul = effect.value
		elif effect.type == "sword_intent_cap":   si_cap = effect.value
		elif effect.type == "sword_intent_bonus": si_bonus = effect.value
	var desc: String = "%s\n费用:%d" % [card_name, cost]
	if dmg > 0:
		desc += "  攻:%d" % dmg
		if sword_intent_consume_bonus > 0:
			desc += "(+剑意×%d)" % sword_intent_consume_bonus
	if blk > 0:  desc += "  挡:%d" % blk
	if drw > 0:  desc += "  抽:%d" % drw
	if nrg > 0:  desc += "  能:%d" % nrg
	if wk > 0:   desc += "  虚弱:%d" % wk
	if vul > 0:  desc += "  脆弱:%d" % vul
	if si_cap > 0:   desc += "  剑意上限+%d" % si_cap
	if si_bonus > 0: desc += "  剑意加成+%d" % si_bonus
	return desc
```

整段替换为：

```gdscript
func get_description() -> String:
	if is_venom:
		return "%s\n费用:%d  %s" % [card_name, cost, special_text]
	var dmg: int = 0
	var dmg_times: int = 1
	var blk: int = 0
	var drw: int = 0
	var nrg: int = 0
	var wk: int = 0
	var vul: int = 0
	var si_cap: int = 0
	var si_bonus: int = 0
	var dpt: int = 0
	var si_retain: bool = false
	var si_blk: int = 0
	for effect: CardEffectData in effects:
		if effect.type == "damage":
			dmg = effect.value
			dmg_times = effect.times
		elif effect.type == "block":              blk = effect.value
		elif effect.type == "draw":               drw = effect.value
		elif effect.type == "energy":             nrg = effect.value
		elif effect.type == "weak":               wk = effect.value
		elif effect.type == "vulnerable":         vul = effect.value
		elif effect.type == "sword_intent_cap":   si_cap = effect.value
		elif effect.type == "sword_intent_bonus": si_bonus = effect.value
		elif effect.type == "draw_per_turn":      dpt = effect.value
		elif effect.type == "sword_intent_retain": si_retain = true
		elif effect.type == "sword_intent_block_bonus": si_blk = effect.value
	var desc: String = "%s\n费用:%d" % [card_name, cost]
	if dmg > 0:
		if dmg_times > 1:
			desc += "  攻:%d×%d" % [dmg, dmg_times]
		else:
			desc += "  攻:%d" % dmg
		if sword_intent_consume_bonus > 0:
			desc += "(+剑意×%d)" % sword_intent_consume_bonus
	if blk > 0:       desc += "  挡:%d" % blk
	if drw > 0:       desc += "  抽:%d" % drw
	if nrg > 0:       desc += "  能:%d" % nrg
	if wk > 0:        desc += "  虚弱:%d" % wk
	if vul > 0:       desc += "  脆弱:%d" % vul
	if si_cap > 0:    desc += "  剑意上限+%d" % si_cap
	if si_bonus > 0:  desc += "  剑意加成+%d" % si_bonus
	if dpt > 0:       desc += "  多抽+%d张/回" % dpt
	if si_retain:     desc += "  剑意永续"
	if si_blk > 0:    desc += "  剑意挡加成+%d" % si_blk
	return desc
```

- [ ] **Step 2: 验证**

读取 `CardData.gd`，确认：
1. `dmg_times` 变量存在，damage 分支读取 `effect.times`
2. `dmg_times > 1` 时显示 `攻:N×M` 格式
3. 末尾四行分别显示 dpt / si_retain / si_blk

- [ ] **Step 3: 提交**

```bash
git add CardData.gd
git commit -m "feat: update get_description to display times, draw_per_turn, sword_intent_retain, sword_intent_block_bonus"
```

---

### Task 5: 凡品卡牌 .tres 文件（掠影 / 碎星 / 横剑）

**Files:**
- Create: `E:\MyWork\AIGameTest2\data\cards\lue_ying.tres`
- Create: `E:\MyWork\AIGameTest2\data\cards\sui_xing.tres`
- Create: `E:\MyWork\AIGameTest2\data\cards\heng_jian.tres`

> UIDs 来自现有文件（zhan_tie.tres 等已验证）：
> - CardData.gd: `uid://13mprie5g221`
> - CardEffectData.gd: `uid://cxv8nrm4tp7dk`

- [ ] **Step 1: 创建 lue_ying.tres（掠影：1费 招式 打4 抽1）**

```
[gd_resource type="Resource" script_class="CardData" load_steps=5 format=3]

[ext_resource type="Script" uid="uid://13mprie5g221" path="res://CardData.gd" id="1_CardData"]
[ext_resource type="Script" uid="uid://cxv8nrm4tp7dk" path="res://CardEffectData.gd" id="2_CardEffectData"]

[sub_resource type="Resource" id="Effect_damage"]
script = ExtResource("2_CardEffectData")
type = "damage"
value = 4
upgrade_bonus = 0

[sub_resource type="Resource" id="Effect_draw"]
script = ExtResource("2_CardEffectData")
type = "draw"
value = 1
upgrade_bonus = 0

[resource]
script = ExtResource("1_CardData")
card_name = "掠影"
cost = 1
price = 0
effects = Array[ExtResource("2_CardEffectData")]([SubResource("Effect_damage"), SubResource("Effect_draw")])
card_type = "招式"
```

- [ ] **Step 2: 创建 sui_xing.tres（碎星：2费 招式 打5×2）**

```
[gd_resource type="Resource" script_class="CardData" load_steps=4 format=3]

[ext_resource type="Script" uid="uid://13mprie5g221" path="res://CardData.gd" id="1_CardData"]
[ext_resource type="Script" uid="uid://cxv8nrm4tp7dk" path="res://CardEffectData.gd" id="2_CardEffectData"]

[sub_resource type="Resource" id="Effect_damage"]
script = ExtResource("2_CardEffectData")
type = "damage"
value = 5
upgrade_bonus = 0
times = 2

[resource]
script = ExtResource("1_CardData")
card_name = "碎星"
cost = 2
price = 0
effects = Array[ExtResource("2_CardEffectData")]([SubResource("Effect_damage")])
card_type = "招式"
```

- [ ] **Step 3: 创建 heng_jian.tres（横剑：2费 身法 格挡10）**

```
[gd_resource type="Resource" script_class="CardData" load_steps=4 format=3]

[ext_resource type="Script" uid="uid://13mprie5g221" path="res://CardData.gd" id="1_CardData"]
[ext_resource type="Script" uid="uid://cxv8nrm4tp7dk" path="res://CardEffectData.gd" id="2_CardEffectData"]

[sub_resource type="Resource" id="Effect_block"]
script = ExtResource("2_CardEffectData")
type = "block"
value = 10
upgrade_bonus = 0

[resource]
script = ExtResource("1_CardData")
card_name = "横剑"
cost = 2
price = 0
effects = Array[ExtResource("2_CardEffectData")]([SubResource("Effect_block")])
target_type = "none"
card_type = "身法"
```

- [ ] **Step 4: 验证**

读取三个文件，确认：
1. lue_ying.tres：effects 数组含 damage(4) 和 draw(1)，card_type="招式"
2. sui_xing.tres：Effect_damage 有 `times = 2`，value=5
3. heng_jian.tres：Effect_block value=10，target_type="none"，card_type="身法"

- [ ] **Step 5: 提交**

```bash
git add data/cards/lue_ying.tres data/cards/sui_xing.tres data/cards/heng_jian.tres
git commit -m "feat: add sword cultivator common cards — 掠影, 碎星, 横剑"
```

---

### Task 6: 灵品卡牌 .tres 文件（裂空 / 无痕 / 破军）

**Files:**
- Create: `E:\MyWork\AIGameTest2\data\cards\lie_kong.tres`
- Create: `E:\MyWork\AIGameTest2\data\cards\wu_hen.tres`
- Create: `E:\MyWork\AIGameTest2\data\cards\po_jun.tres`

- [ ] **Step 1: 创建 lie_kong.tres（裂空：2费 终结技 打10 消耗剑意×2）**

```
[gd_resource type="Resource" script_class="CardData" load_steps=4 format=3]

[ext_resource type="Script" uid="uid://13mprie5g221" path="res://CardData.gd" id="1_CardData"]
[ext_resource type="Script" uid="uid://cxv8nrm4tp7dk" path="res://CardEffectData.gd" id="2_CardEffectData"]

[sub_resource type="Resource" id="Effect_damage"]
script = ExtResource("2_CardEffectData")
type = "damage"
value = 10
upgrade_bonus = 0

[resource]
script = ExtResource("1_CardData")
card_name = "裂空"
cost = 2
price = 0
effects = Array[ExtResource("2_CardEffectData")]([SubResource("Effect_damage")])
card_type = "招式"
is_finisher = true
sword_intent_consume_bonus = 2
```

- [ ] **Step 2: 创建 wu_hen.tres（无痕：3费 终结技 打6 消耗剑意×4）**

```
[gd_resource type="Resource" script_class="CardData" load_steps=4 format=3]

[ext_resource type="Script" uid="uid://13mprie5g221" path="res://CardData.gd" id="1_CardData"]
[ext_resource type="Script" uid="uid://cxv8nrm4tp7dk" path="res://CardEffectData.gd" id="2_CardEffectData"]

[sub_resource type="Resource" id="Effect_damage"]
script = ExtResource("2_CardEffectData")
type = "damage"
value = 6
upgrade_bonus = 0

[resource]
script = ExtResource("1_CardData")
card_name = "无痕"
cost = 3
price = 0
effects = Array[ExtResource("2_CardEffectData")]([SubResource("Effect_damage")])
card_type = "招式"
is_finisher = true
sword_intent_consume_bonus = 4
```

- [ ] **Step 3: 创建 po_jun.tres（破军：1费 终结技 打3 消耗剑意×1 抽1张）**

破军通过在 effects 中同时包含 damage 和 draw 实现"消耗后抽牌"：`EffectResolver.resolve()` 先处理 damage + 消耗剑意，`_apply_engine_effects()` 随后处理 draw。

```
[gd_resource type="Resource" script_class="CardData" load_steps=5 format=3]

[ext_resource type="Script" uid="uid://13mprie5g221" path="res://CardData.gd" id="1_CardData"]
[ext_resource type="Script" uid="uid://cxv8nrm4tp7dk" path="res://CardEffectData.gd" id="2_CardEffectData"]

[sub_resource type="Resource" id="Effect_damage"]
script = ExtResource("2_CardEffectData")
type = "damage"
value = 3
upgrade_bonus = 0

[sub_resource type="Resource" id="Effect_draw"]
script = ExtResource("2_CardEffectData")
type = "draw"
value = 1
upgrade_bonus = 0

[resource]
script = ExtResource("1_CardData")
card_name = "破军"
cost = 1
price = 0
effects = Array[ExtResource("2_CardEffectData")]([SubResource("Effect_damage"), SubResource("Effect_draw")])
card_type = "招式"
is_finisher = true
sword_intent_consume_bonus = 1
```

- [ ] **Step 4: 验证**

读取三个文件，确认：
1. lie_kong.tres：is_finisher=true，damage=10，sword_intent_consume_bonus=2
2. wu_hen.tres：is_finisher=true，damage=6，sword_intent_consume_bonus=4
3. po_jun.tres：is_finisher=true，damage=3，sword_intent_consume_bonus=1，effects 含 draw(1)

- [ ] **Step 5: 提交**

```bash
git add data/cards/lie_kong.tres data/cards/wu_hen.tres data/cards/po_jun.tres
git commit -m "feat: add sword cultivator uncommon finisher cards — 裂空, 无痕, 破军"
```

---

### Task 7: 仙品卡牌 .tres 文件（万剑归宗 / 剑域 / 心剑）

**Files:**
- Create: `E:\MyWork\AIGameTest2\data\cards\wan_jian_gui_zong.tres`
- Create: `E:\MyWork\AIGameTest2\data\cards\jian_yu.tres`
- Create: `E:\MyWork\AIGameTest2\data\cards\xin_jian.tres`

- [ ] **Step 1: 创建 wan_jian_gui_zong.tres（万剑归宗：3费 功法 多抽+1/回 剑意上限+5）**

```
[gd_resource type="Resource" script_class="CardData" load_steps=5 format=3]

[ext_resource type="Script" uid="uid://13mprie5g221" path="res://CardData.gd" id="1_CardData"]
[ext_resource type="Script" uid="uid://cxv8nrm4tp7dk" path="res://CardEffectData.gd" id="2_CardEffectData"]

[sub_resource type="Resource" id="Effect_cap"]
script = ExtResource("2_CardEffectData")
type = "sword_intent_cap"
value = 5
upgrade_bonus = 0

[sub_resource type="Resource" id="Effect_dpt"]
script = ExtResource("2_CardEffectData")
type = "draw_per_turn"
value = 1
upgrade_bonus = 0

[resource]
script = ExtResource("1_CardData")
card_name = "万剑归宗"
cost = 3
price = 0
effects = Array[ExtResource("2_CardEffectData")]([SubResource("Effect_cap"), SubResource("Effect_dpt")])
target_type = "none"
card_type = "功法"
```

- [ ] **Step 2: 创建 jian_yu.tres（剑域：2费 功法 剑意永续）**

```
[gd_resource type="Resource" script_class="CardData" load_steps=4 format=3]

[ext_resource type="Script" uid="uid://13mprie5g221" path="res://CardData.gd" id="1_CardData"]
[ext_resource type="Script" uid="uid://cxv8nrm4tp7dk" path="res://CardEffectData.gd" id="2_CardEffectData"]

[sub_resource type="Resource" id="Effect_retain"]
script = ExtResource("2_CardEffectData")
type = "sword_intent_retain"
value = 1
upgrade_bonus = 0

[resource]
script = ExtResource("1_CardData")
card_name = "剑域"
cost = 2
price = 0
effects = Array[ExtResource("2_CardEffectData")]([SubResource("Effect_retain")])
target_type = "none"
card_type = "功法"
```

- [ ] **Step 3: 创建 xin_jian.tres（心剑：1费 功法 剑意格挡加成+1）**

```
[gd_resource type="Resource" script_class="CardData" load_steps=4 format=3]

[ext_resource type="Script" uid="uid://13mprie5g221" path="res://CardData.gd" id="1_CardData"]
[ext_resource type="Script" uid="uid://cxv8nrm4tp7dk" path="res://CardEffectData.gd" id="2_CardEffectData"]

[sub_resource type="Resource" id="Effect_blk_bonus"]
script = ExtResource("2_CardEffectData")
type = "sword_intent_block_bonus"
value = 1
upgrade_bonus = 0

[resource]
script = ExtResource("1_CardData")
card_name = "心剑"
cost = 1
price = 0
effects = Array[ExtResource("2_CardEffectData")]([SubResource("Effect_blk_bonus")])
target_type = "none"
card_type = "功法"
```

- [ ] **Step 4: 验证**

读取三个文件，确认：
1. wan_jian_gui_zong.tres：effects 含 sword_intent_cap(5) 和 draw_per_turn(1)，target_type="none"
2. jian_yu.tres：effect 类型为 "sword_intent_retain"，target_type="none"
3. xin_jian.tres：effect 类型为 "sword_intent_block_bonus"，value=1，target_type="none"

- [ ] **Step 5: 提交**

```bash
git add data/cards/wan_jian_gui_zong.tres data/cards/jian_yu.tres data/cards/xin_jian.tres
git commit -m "feat: add sword cultivator rare passive cards — 万剑归宗, 剑域, 心剑"
```

---

### Task 8: CardPool.gd + MainMenu.tscn + 手动验收

**Files:**
- Modify: `E:\MyWork\AIGameTest2\CardPool.gd`
- Modify: `E:\MyWork\AIGameTest2\menu\MainMenu.tscn`

- [ ] **Step 1: 修改 CardPool.gd — 扩展 SWORD_REWARD_CARDS**

找到现有：

```gdscript
const SWORD_REWARD_CARDS: Array[String] = [
	"res://data/cards/zhan_tie.tres",
	"res://data/cards/po_feng.tres",
	"res://data/cards/ce_bu.tres",
	"res://data/cards/lian_xi.tres",
	"res://data/cards/chuan_xin.tres",
	"res://data/cards/yang_jian_shu.tres",
]
```

替换为：

```gdscript
const SWORD_REWARD_CARDS: Array[String] = [
	"res://data/cards/zhan_tie.tres",
	"res://data/cards/po_feng.tres",
	"res://data/cards/ce_bu.tres",
	"res://data/cards/lian_xi.tres",
	"res://data/cards/chuan_xin.tres",
	"res://data/cards/yang_jian_shu.tres",
	"res://data/cards/lue_ying.tres",
	"res://data/cards/sui_xing.tres",
	"res://data/cards/heng_jian.tres",
	"res://data/cards/lie_kong.tres",
	"res://data/cards/wu_hen.tres",
	"res://data/cards/po_jun.tres",
	"res://data/cards/wan_jian_gui_zong.tres",
	"res://data/cards/jian_yu.tres",
	"res://data/cards/xin_jian.tres",
]
```

- [ ] **Step 2: 修改 menu/MainMenu.tscn — 版本号**

读取 `E:\MyWork\AIGameTest2\menu\MainMenu.tscn`，找到：

```
text = "v0.35.0"
```

改为：

```
text = "v0.36.0"
```

- [ ] **Step 3: 验证**

读取两个文件，确认：
1. `CardPool.gd` 中 `SWORD_REWARD_CARDS` 有 15 条路径，新增 9 条全部在列
2. `MainMenu.tscn` 中版本号为 `v0.36.0`

- [ ] **Step 4: 提交**

```bash
git add CardPool.gd menu/MainMenu.tscn
git commit -m "feat: expand SWORD_REWARD_CARDS to 15 cards; bump version to v0.36.0"
```

- [ ] **Step 5: 运行游戏手动验收**

运行项目（F5），依次验证：

1. **主菜单** 显示 `v0.36.0`
2. **卡牌描述** — 在奖励界面观察各新牌说明：
   - 掠影：`攻:4  抽:1`
   - 碎星：`攻:5×2`
   - 横剑：`挡:10`
   - 裂空：`攻:10(+剑意×2)`
   - 无痕：`攻:6(+剑意×4)`
   - 破军：`攻:3(+剑意×1)  抽:1`
   - 万剑归宗：`剑意上限+5  多抽+1张/回`
   - 剑域：`剑意永续`
   - 心剑：`剑意挡加成+1`
3. **掠影** — 打出后：造成 4+剑意×1 伤害，手牌+1，剑意+1
4. **碎星** — 打出后：两次 5+剑意×1 伤害，剑意+1
5. **横剑** — 打出后：格挡+10
6. **裂空** — 打出后（有剑意时）：造成 10+剑意×2 总伤，剑意归零
7. **破军** — 打出后（有剑意时）：造成 3+剑意×1 伤，剑意归零，手牌+1
8. **万剑归宗** — 打出后：下回合抽 6 张牌，剑意上限变 15
9. **剑域** — 打出后：回合结束剑意不折半（剑意保留全部）
10. **心剑** — 打出后（有剑意时）：打出侧步/横剑等身法，格挡值加上剑意层数