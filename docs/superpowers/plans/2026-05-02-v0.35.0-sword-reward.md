# v0.35.0 剑修奖励牌池 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将战斗奖励的选牌池替换为剑修专属卡牌，奖励池按职业区分，为后续职业扩展预留结构。

**Architecture:** `CardPool` 新增 `SWORD_REWARD_CARDS` 常量；`RewardEngine.get_options()` 接收 `character: String` 参数并按 match 选对应池；`RewardScreen` 调用时传入 `GameManager.player_state.character`。

**Tech Stack:** Godot 4 GDScript, Resource (.tres)

---

## File Map

| 文件 | 操作 | 说明 |
|------|------|------|
| `CardPool.gd` | 修改 | 新增 SWORD_REWARD_CARDS 常量 |
| `RewardEngine.gd` | 修改 | get_options() 接收 character 参数，按职业选池 |
| `reward/RewardScreen.gd` | 修改 | 传入 character 参数 |
| `menu/MainMenu.tscn` | 修改 | 版本号改为 v0.35.0 |

---

### Task 1: CardPool.gd + RewardEngine.gd

**Files:**
- Modify: `E:\MyWork\AIGameTest2\CardPool.gd`
- Modify: `E:\MyWork\AIGameTest2\RewardEngine.gd`

- [ ] **Step 1: 修改 CardPool.gd — 新增 SWORD_REWARD_CARDS 常量**

先读取文件确认当前内容，然后在 `SWORD_START_RELICS` 常量之后追加：

```gdscript
const SWORD_REWARD_CARDS: Array[String] = [
	"res://data/cards/zhan_tie.tres",
	"res://data/cards/po_feng.tres",
	"res://data/cards/ce_bu.tres",
	"res://data/cards/lian_xi.tres",
	"res://data/cards/chuan_xin.tres",
	"res://data/cards/yang_jian_shu.tres",
]
```

完整文件末尾应为：

```gdscript
const SWORD_START_RELICS: Array[String] = [
	"res://data/relics/sword_gourd.tres",
]

const SWORD_REWARD_CARDS: Array[String] = [
	"res://data/cards/zhan_tie.tres",
	"res://data/cards/po_feng.tres",
	"res://data/cards/ce_bu.tres",
	"res://data/cards/lian_xi.tres",
	"res://data/cards/chuan_xin.tres",
	"res://data/cards/yang_jian_shu.tres",
]
```

- [ ] **Step 2: 修改 RewardEngine.gd — 完整替换 get_options()**

找到现有的：

```gdscript
static func get_options() -> Array[CardData]:
	var pool: Array[CardData] = []
	for path: String in CardPool.CARDS:
		var card := load(path) as CardData
		if card != null:
			pool.append(card)
	pool.shuffle()
	return pool.slice(0, mini(3, pool.size()))
```

整段替换为：

```gdscript
static func get_options(character: String) -> Array[CardData]:
	var pool_paths: Array[String] = []
	match character:
		"sword":
			pool_paths.assign(CardPool.SWORD_REWARD_CARDS)
		_:
			pool_paths.assign(CardPool.CARDS)
	var pool: Array[CardData] = []
	for path: String in pool_paths:
		var card := load(path) as CardData
		if card != null:
			pool.append(card)
	pool.shuffle()
	return pool.slice(0, mini(3, pool.size()))
```

`get_gold_reward()` 方法不变。

- [ ] **Step 3: 验证**

读取两个文件，确认：
1. `CardPool.gd` 末尾有 `SWORD_REWARD_CARDS` 常量，包含 6 条路径
2. `RewardEngine.gd` 的 `get_options()` 签名含 `character: String` 参数，有 match 分支

- [ ] **Step 4: 提交**

```bash
git add CardPool.gd RewardEngine.gd
git commit -m "feat: add SWORD_REWARD_CARDS to CardPool; update RewardEngine.get_options to accept character"
```

---

### Task 2: RewardScreen.gd + 版本号

**Files:**
- Modify: `E:\MyWork\AIGameTest2\reward\RewardScreen.gd`
- Modify: `E:\MyWork\AIGameTest2\menu\MainMenu.tscn`

- [ ] **Step 1: 修改 RewardScreen.gd — 传入 character 参数**

先读取文件，找到 `_build_card_options()` 函数中以下这行：

```gdscript
	var options: Array[CardData] = RewardEngine.get_options()
```

改为：

```gdscript
	var options: Array[CardData] = RewardEngine.get_options(GameManager.player_state.character)
```

- [ ] **Step 2: 修改 menu/MainMenu.tscn — 版本号改为 v0.35.0**

读取 `E:\MyWork\AIGameTest2\menu\MainMenu.tscn`，找到：

```
text = "v0.34.0"
```

改为：

```
text = "v0.35.0"
```

- [ ] **Step 3: 验证**

读取两个文件确认：
1. `RewardScreen.gd` 第 96 行（或附近）的 `RewardEngine.get_options()` 调用已加上 `GameManager.player_state.character` 参数
2. `MainMenu.tscn` 版本号为 `v0.35.0`

- [ ] **Step 4: 运行游戏验证完整流程**

运行项目（F5），依次验证：
1. 主菜单显示 **v0.35.0**
2. 选剑修开始游戏，打赢一场战斗
3. 进入奖励界面，点击"卡牌"，显示 3 张卡牌选项
4. 确认 3 张卡均为剑修卡（斩铁 / 破风 / 侧步 / 敛息 / 穿心 / 养剑术 之一）
5. 选一张卡，确认加入牌组

- [ ] **Step 5: 提交**

```bash
git add reward/RewardScreen.gd menu/MainMenu.tscn
git commit -m "feat: pass character to RewardEngine in RewardScreen; bump version to v0.35.0"
```
