# v0.39.0 商店修复 + 6 个遗物 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复商店出售非剑修卡牌的 bug，并新增 6 个带代价的遗物（化凡、燃魂、碎丹、劫雷淬体、天道碎片、天劫余韵）。

**Architecture:** 扩展 `RelicData` 支持双效果字段（`trigger_b / effect_type_b / value_b / has_effect_b`）和商店限制标志 `blocks_relic_purchase`；重构 `RelicEngine` 为统一的 `_apply_for_trigger` + `apply_on_equip` 架构；在 `CombatEngine` 的 `end_turn()` 和 `_check_end()` 中新增触发点；所有遗物效果通过 `.tres` 数据文件驱动，逻辑代码不硬编码数值。

**Tech Stack:** Godot 4 GDScript，Resource `.tres` 文件，GDScript `enum`，现有 `RelicEngine` / `CombatEngine` / `GameManager` 体系。

---

## 文件结构

| 文件 | 动作 | 职责 |
|------|------|------|
| `RelicData.gd` | 修改 | 新增 Trigger/EffectType 枚举值 + 双效果字段 |
| `RelicEngine.gd` | 修改 | 重构为 `_apply_for_trigger`，新增 `apply_turn_end` / `apply_combat_end` / `apply_on_equip` |
| `CombatEngine.gd` | 修改 | `end_turn()` 加 TURN_END 触发，`_check_end()` 加 COMBAT_END 触发 |
| `GameManager.gd` | 修改 | `start_new_run` / `start_debug_run` / `collect_relic` 调用 `apply_on_equip` |
| `shop/ShopEngine.gd` | 修改 | 修复卡牌池 bug；`buy_relic` 调用 `apply_on_equip` |
| `shop/ShopScreen.gd` | 修改 | 持有天道碎片时禁用遗物购买按钮 |
| `CardPool.gd` | 修改 | 新增 6 个遗物路径到 `RELICS` |
| `data/relics/hua_fan.tres` | 新建 | 化凡数据 |
| `data/relics/ran_hun.tres` | 新建 | 燃魂数据 |
| `data/relics/sui_dan.tres` | 新建 | 碎丹数据 |
| `data/relics/jie_lei_cui_ti.tres` | 新建 | 劫雷淬体数据 |
| `data/relics/tian_dao_sui_pian.tres` | 新建 | 天道碎片数据 |
| `data/relics/tian_jie_yu_yun.tres` | 新建 | 天劫余韵数据 |

### 枚举索引（重要：.tres 文件用数字，必须与代码一致）

```
Trigger:    COMBAT_START=0  TURN_START=1  TURN_END=2  COMBAT_END=3  ON_EQUIP=4
EffectType: ENERGY=0  HEAL_HP=1  BLOCK=2  DRAW=3  SWORD_INTENT=4
            SELF_DAMAGE=5  SWORD_INTENT_CAP=6  MAX_HP_PERCENT=7
            FIRST_TURN_DRAW_PENALTY=8  BLOCK_DRAIN=9
```

---

## Task 1: 修复商店卡牌池 bug

**Files:**
- Modify: `shop/ShopEngine.gd`

- [ ] **Step 1: 将 CARDS 改为 SWORD_REWARD_CARDS**

打开 `shop/ShopEngine.gd`，将第 9 行：

```gdscript
	for path: String in CardPool.CARDS:
```

改为：

```gdscript
	for path: String in CardPool.SWORD_REWARD_CARDS:
```

- [ ] **Step 2: 手动验证**

运行游戏，进入商店，确认出售的是剑修卡牌（战铁、破风、侧步等）而非 strike/defend。

- [ ] **Step 3: 提交**

```bash
git add shop/ShopEngine.gd
git commit -m "fix: shop now sells sword reward cards instead of CARDS pool"
```

---

## Task 2: 扩展 RelicData 枚举与字段

**Files:**
- Modify: `RelicData.gd`

- [ ] **Step 1: 替换 RelicData.gd 全部内容**

```gdscript
class_name RelicData
extends Resource

enum Trigger { COMBAT_START, TURN_START, TURN_END, COMBAT_END, ON_EQUIP }
enum EffectType {
	ENERGY, HEAL_HP, BLOCK, DRAW, SWORD_INTENT,
	SELF_DAMAGE, SWORD_INTENT_CAP, MAX_HP_PERCENT,
	FIRST_TURN_DRAW_PENALTY, BLOCK_DRAIN
}
enum Source { SHOP, CHEST }

@export var display_name: String = ""
@export var description: String = ""
@export var trigger: Trigger = Trigger.COMBAT_START
@export var effect_type: EffectType = EffectType.ENERGY
@export var value: int = 0
@export var trigger_b: Trigger = Trigger.COMBAT_START
@export var effect_type_b: EffectType = EffectType.ENERGY
@export var value_b: int = 0
@export var has_effect_b: bool = false
@export var blocks_relic_purchase: bool = false
@export var price: int = 0
@export var source: Source = Source.SHOP
```

- [ ] **Step 2: 提交**

```bash
git add RelicData.gd
git commit -m "feat: extend RelicData with dual-effect fields and blocks_relic_purchase flag"
```

---

## Task 3: 重构并扩展 RelicEngine

**Files:**
- Modify: `RelicEngine.gd`

- [ ] **Step 1: 替换 RelicEngine.gd 全部内容**

```gdscript
class_name RelicEngine
extends RefCounted

static func apply_combat_start(relics: Array[RelicData], engine: CombatEngine) -> void:
	_apply_for_trigger(relics, RelicData.Trigger.COMBAT_START, engine)

static func apply_turn_start(relics: Array[RelicData], engine: CombatEngine) -> void:
	_apply_for_trigger(relics, RelicData.Trigger.TURN_START, engine)

static func apply_turn_end(relics: Array[RelicData], engine: CombatEngine) -> void:
	_apply_for_trigger(relics, RelicData.Trigger.TURN_END, engine)

static func apply_combat_end(relics: Array[RelicData], engine: CombatEngine) -> void:
	_apply_for_trigger(relics, RelicData.Trigger.COMBAT_END, engine)

static func apply_on_equip(relic: RelicData, player_state: PlayerState) -> void:
	if relic.trigger == RelicData.Trigger.ON_EQUIP:
		_apply_equip_effect(relic.effect_type, relic.value, player_state)
	if relic.has_effect_b and relic.trigger_b == RelicData.Trigger.ON_EQUIP:
		_apply_equip_effect(relic.effect_type_b, relic.value_b, player_state)

static func _apply_for_trigger(relics: Array[RelicData], trigger: RelicData.Trigger, engine: CombatEngine) -> void:
	for relic: RelicData in relics:
		if relic.trigger == trigger:
			_apply_effect(relic.effect_type, relic.value, engine)
		if relic.has_effect_b and relic.trigger_b == trigger:
			_apply_effect(relic.effect_type_b, relic.value_b, engine)

static func _apply_effect(effect_type: RelicData.EffectType, value: int, engine: CombatEngine) -> void:
	match effect_type:
		RelicData.EffectType.ENERGY:
			engine.energy += value
		RelicData.EffectType.HEAL_HP:
			engine.player.hp = mini(engine.player.hp + value, engine.player.max_hp)
		RelicData.EffectType.BLOCK:
			engine.player.add_block(value)
		RelicData.EffectType.DRAW:
			engine.draw_cards(value)
		RelicData.EffectType.SWORD_INTENT:
			engine.player.sword_intent = mini(
				engine.player.sword_intent + value, engine.player.sword_intent_cap
			)
		RelicData.EffectType.SELF_DAMAGE:
			engine.player.hp = max(0, engine.player.hp - value)
		RelicData.EffectType.SWORD_INTENT_CAP:
			engine.player.sword_intent_cap += value
		RelicData.EffectType.FIRST_TURN_DRAW_PENALTY:
			if engine.turn_number == 1:
				for i in value:
					if not engine.hand.is_empty():
						engine.hand.pop_back()
		RelicData.EffectType.BLOCK_DRAIN:
			if engine.player.block > 0:
				engine.player.block -= 1
			else:
				engine.player.hp = max(0, engine.player.hp - 1)

static func _apply_equip_effect(effect_type: RelicData.EffectType, value: int, player_state: PlayerState) -> void:
	match effect_type:
		RelicData.EffectType.MAX_HP_PERCENT:
			var reduction: int = max(1, int(player_state.max_hp * value / 100.0))
			player_state.max_hp = max(1, player_state.max_hp - reduction)
			player_state.hp = mini(player_state.hp, player_state.max_hp)
```

- [ ] **Step 2: 提交**

```bash
git add RelicEngine.gd
git commit -m "feat: refactor RelicEngine with unified _apply_for_trigger and add new trigger handlers"
```

---

## Task 4: CombatEngine 新增 TURN_END 和 COMBAT_END 触发点

**Files:**
- Modify: `CombatEngine.gd`

- [ ] **Step 1: 在 end_turn() 中添加 TURN_END 触发**

找到 `end_turn()` 中这段代码：

```gdscript
	if not player.sword_intent_retain:
		player.sword_intent = player.sword_intent / 2
	_do_enemy_turn()
```

改为：

```gdscript
	if not player.sword_intent_retain:
		player.sword_intent = player.sword_intent / 2
	RelicEngine.apply_turn_end(_relics, self)
	_do_enemy_turn()
```

- [ ] **Step 2: 在 _check_end() 中添加 COMBAT_END 触发**

找到 `_check_end()` 中这段代码：

```gdscript
	if _get_living_enemies().is_empty():
		combat_ended.emit("victory")
		return true
```

改为：

```gdscript
	if _get_living_enemies().is_empty():
		RelicEngine.apply_combat_end(_relics, self)
		combat_ended.emit("victory")
		return true
```

- [ ] **Step 3: 提交**

```bash
git add CombatEngine.gd
git commit -m "feat: add TURN_END and COMBAT_END relic trigger points in CombatEngine"
```

---

## Task 5: 在遗物装备时调用 apply_on_equip

**Files:**
- Modify: `GameManager.gd`
- Modify: `shop/ShopEngine.gd`

- [ ] **Step 1: 修改 GameManager.start_new_run 的遗物加载**

找到 `start_new_run` 中：

```gdscript
	for path in relic_paths:
		player_state.relics.append((load(path) as RelicData).duplicate())
```

改为：

```gdscript
	for path in relic_paths:
		var relic: RelicData = (load(path) as RelicData).duplicate()
		player_state.relics.append(relic)
		RelicEngine.apply_on_equip(relic, player_state)
```

- [ ] **Step 2: 修改 GameManager.start_debug_run 的遗物加载**

找到 `start_debug_run` 中：

```gdscript
	for path in CardPool.SWORD_START_RELICS:
		player_state.relics.append((load(path) as RelicData).duplicate())
```

改为：

```gdscript
	for path in CardPool.SWORD_START_RELICS:
		var relic: RelicData = (load(path) as RelicData).duplicate()
		player_state.relics.append(relic)
		RelicEngine.apply_on_equip(relic, player_state)
```

- [ ] **Step 3: 修改 GameManager.collect_relic**

找到：

```gdscript
func collect_relic() -> void:
	if pending_relic == null:
		return
	player_state.relics.append(pending_relic.duplicate())
	pending_relic = null
```

改为：

```gdscript
func collect_relic() -> void:
	if pending_relic == null:
		return
	var relic: RelicData = pending_relic.duplicate()
	player_state.relics.append(relic)
	RelicEngine.apply_on_equip(relic, player_state)
	pending_relic = null
```

- [ ] **Step 4: 修改 ShopEngine.buy_relic**

找到：

```gdscript
func buy_relic(relic: RelicData, player_state: PlayerState) -> bool:
	if player_state.gold < relic.price:
		return false
	player_state.gold -= relic.price
	player_state.relics.append(relic.duplicate())
	inventory_relics.erase(relic)
	return true
```

改为：

```gdscript
func buy_relic(relic: RelicData, player_state: PlayerState) -> bool:
	if player_state.gold < relic.price:
		return false
	player_state.gold -= relic.price
	var equipped: RelicData = relic.duplicate()
	player_state.relics.append(equipped)
	RelicEngine.apply_on_equip(equipped, player_state)
	inventory_relics.erase(relic)
	return true
```

- [ ] **Step 5: 提交**

```bash
git add GameManager.gd shop/ShopEngine.gd
git commit -m "feat: call apply_on_equip whenever a relic is equipped"
```

---

## Task 6: ShopScreen 天道碎片限制

**Files:**
- Modify: `shop/ShopScreen.gd`

- [ ] **Step 1: 修改 _rebuild_relic_list 添加购买限制**

找到整个 `_rebuild_relic_list` 函数：

```gdscript
func _rebuild_relic_list() -> void:
	for child in _relic_list.get_children():
		child.queue_free()
	for relic: RelicData in _engine.inventory_relics:
		var btn := Button.new()
		btn.text = "%s — %s  [%d金]" % [relic.display_name, relic.description, relic.price]
		btn.disabled = GameManager.player_state.gold < relic.price
		btn.pressed.connect(_on_buy_relic.bind(relic))
		_relic_list.add_child(btn)
```

改为：

```gdscript
func _rebuild_relic_list() -> void:
	var purchase_blocked: bool = GameManager.player_state.relics.any(
		func(r: RelicData) -> bool: return r.blocks_relic_purchase
	)
	for child in _relic_list.get_children():
		child.queue_free()
	for relic: RelicData in _engine.inventory_relics:
		var btn := Button.new()
		btn.text = "%s — %s  [%d金]" % [relic.display_name, relic.description, relic.price]
		btn.disabled = GameManager.player_state.gold < relic.price or purchase_blocked
		btn.pressed.connect(_on_buy_relic.bind(relic))
		_relic_list.add_child(btn)
```

- [ ] **Step 2: 提交**

```bash
git add shop/ShopScreen.gd
git commit -m "feat: disable relic purchase in shop when player holds blocks_relic_purchase relic"
```

---

## Task 7: 创建 6 个遗物数据文件

**Files:**
- Create: `data/relics/hua_fan.tres`
- Create: `data/relics/ran_hun.tres`
- Create: `data/relics/sui_dan.tres`
- Create: `data/relics/jie_lei_cui_ti.tres`
- Create: `data/relics/tian_dao_sui_pian.tres`
- Create: `data/relics/tian_jie_yu_yun.tres`

枚举索引速查：
```
Trigger:    COMBAT_START=0  TURN_START=1  TURN_END=2  COMBAT_END=3  ON_EQUIP=4
EffectType: ENERGY=0  HEAL_HP=1  BLOCK=2  DRAW=3  SWORD_INTENT=4
            SELF_DAMAGE=5  SWORD_INTENT_CAP=6  MAX_HP_PERCENT=7
            FIRST_TURN_DRAW_PENALTY=8  BLOCK_DRAIN=9
```

- [ ] **Step 1: 创建 hua_fan.tres（化凡）**

```
[gd_resource type="Resource" script_class="RelicData" format=3]

[ext_resource type="Script" path="res://RelicData.gd" id="1_RelicData"]

[resource]
script = ExtResource("1_RelicData")
display_name = "化凡"
description = "每场战斗开始获得 15 格挡。每回合结束失去 1 格挡（若没有格挡则扣 1 HP）。"
trigger = 0
effect_type = 2
value = 15
trigger_b = 2
effect_type_b = 9
value_b = 1
has_effect_b = true
price = 150
```

- [ ] **Step 2: 创建 ran_hun.tres（燃魂）**

```
[gd_resource type="Resource" script_class="RelicData" format=3]

[ext_resource type="Script" path="res://RelicData.gd" id="1_RelicData"]

[resource]
script = ExtResource("1_RelicData")
display_name = "燃魂"
description = "每回合获得 1 点额外真气。每场战斗结束后失去 3 血量。"
trigger = 1
effect_type = 0
value = 1
trigger_b = 3
effect_type_b = 5
value_b = 3
has_effect_b = true
price = 150
```

- [ ] **Step 3: 创建 sui_dan.tres（碎丹）**

```
[gd_resource type="Resource" script_class="RelicData" format=3]

[ext_resource type="Script" path="res://RelicData.gd" id="1_RelicData"]

[resource]
script = ExtResource("1_RelicData")
display_name = "碎丹"
description = "真气上限 +1。每场战斗开始时受到 6 点伤害。"
trigger = 0
effect_type = 6
value = 1
trigger_b = 0
effect_type_b = 5
value_b = 6
has_effect_b = true
price = 150
```

- [ ] **Step 4: 创建 jie_lei_cui_ti.tres（劫雷淬体）**

```
[gd_resource type="Resource" script_class="RelicData" format=3]

[ext_resource type="Script" path="res://RelicData.gd" id="1_RelicData"]

[resource]
script = ExtResource("1_RelicData")
display_name = "劫雷淬体"
description = "真气上限 +1。最大血量 -10%（立即生效）。"
trigger = 0
effect_type = 6
value = 1
trigger_b = 4
effect_type_b = 7
value_b = 10
has_effect_b = true
price = 150
```

- [ ] **Step 5: 创建 tian_dao_sui_pian.tres（天道碎片）**

```
[gd_resource type="Resource" script_class="RelicData" format=3]

[ext_resource type="Script" path="res://RelicData.gd" id="1_RelicData"]

[resource]
script = ExtResource("1_RelicData")
display_name = "天道碎片"
description = "每回合多抽 1 张牌。不能从坊市购买遗物。"
trigger = 1
effect_type = 3
value = 1
blocks_relic_purchase = true
price = 150
```

- [ ] **Step 6: 创建 tian_jie_yu_yun.tres（天劫余韵）**

```
[gd_resource type="Resource" script_class="RelicData" format=3]

[ext_resource type="Script" path="res://RelicData.gd" id="1_RelicData"]

[resource]
script = ExtResource("1_RelicData")
display_name = "天劫余韵"
description = "真气上限 +1。每场战斗首回合少抽 2 张牌。"
trigger = 0
effect_type = 6
value = 1
trigger_b = 1
effect_type_b = 8
value_b = 2
has_effect_b = true
price = 150
```

- [ ] **Step 7: 提交**

```bash
git add data/relics/hua_fan.tres data/relics/ran_hun.tres data/relics/sui_dan.tres
git add data/relics/jie_lei_cui_ti.tres data/relics/tian_dao_sui_pian.tres data/relics/tian_jie_yu_yun.tres
git commit -m "feat: add 6 new relic data files"
```

---

## Task 8: CardPool 注册新遗物 + 版本号

**Files:**
- Modify: `CardPool.gd`
- Modify: `menu/MainMenu.tscn`

- [ ] **Step 1: 将 6 个遗物加入 RELICS**

打开 `CardPool.gd`，将 `RELICS` 改为：

```gdscript
const RELICS: Array[String] = [
	"res://data/relics/burning_gem.tres",
	"res://data/relics/life_ring.tres",
	"res://data/relics/hua_fan.tres",
	"res://data/relics/ran_hun.tres",
	"res://data/relics/sui_dan.tres",
	"res://data/relics/jie_lei_cui_ti.tres",
	"res://data/relics/tian_dao_sui_pian.tres",
	"res://data/relics/tian_jie_yu_yun.tres",
]
```

- [ ] **Step 2: 提交 CardPool**

```bash
git add CardPool.gd
git commit -m "feat: register 6 new relics in CardPool.RELICS"
```

- [ ] **Step 3: 更新版本号**

打开 `menu/MainMenu.tscn`，找到 `LblVersion` 节点的 `text` 字段，将 `"v0.38.0"` 改为 `"v0.39.0"`。

- [ ] **Step 4: 提交版本号**

```bash
git add menu/MainMenu.tscn
git commit -m "feat: bump version to v0.39.0"
```

---

## 验收清单

运行游戏，按以下路径验收：

**商店 bug 修复**
- [ ] 进入商店，确认出售的是剑修卡牌（战铁、破风等），不再是 strike/defend

**化凡**（COMBAT_START block + TURN_END drain）
- [ ] 持有化凡进入战斗，第一回合有 15 格挡
- [ ] 结束回合后格挡减少 1；格挡为 0 时结束回合扣 1 HP

**燃魂**（TURN_START energy + COMBAT_END damage）
- [ ] 持有燃魂，每回合真气为 4（BASE_ENERGY 3 + 1）
- [ ] 战斗胜利后 HP 减少 3

**碎丹**（COMBAT_START sword_intent_cap + COMBAT_START self_damage）
- [ ] 持有碎丹，战斗开始剑意上限显示为 11（默认 10 + 1）
- [ ] 战斗开始 HP 减少 6

**劫雷淬体**（COMBAT_START sword_intent_cap + ON_EQUIP max_hp_percent）
- [ ] 购买后最大血量立即变为原来 90%（如 70 → 63）
- [ ] 当前 HP 同步 clamp 至新上限
- [ ] 每场战斗剑意上限 +1

**天道碎片**（TURN_START draw + blocks_relic_purchase）
- [ ] 持有天道碎片，每回合自动多抽 1 张牌
- [ ] 进入商店，遗物购买按钮全部禁用；卡牌购买正常

**天劫余韵**（COMBAT_START sword_intent_cap + TURN_START first_turn_draw_penalty）
- [ ] 持有天劫余韵，战斗第一回合只有 3 张手牌（5 - 2）
- [ ] 第二回合起正常 5 张
- [ ] 每场战斗剑意上限 +1
