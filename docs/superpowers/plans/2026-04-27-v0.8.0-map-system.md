# v0.8.0 地图系统实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 引入 2 节点线性地图，新增精英守卫 Boss，完整实现"主菜单 → 地图 → 战斗 → 奖励 → 地图 → Boss → 通关"游戏循环。

**Architecture:** GameManager 新增 `start_new_run()`（分离初始化与战斗路由）；PlayerState 追踪 `current_node`；CombatEngine 接受外部 EnemyData；MapScreen/WinScreen 纯 UI 层。

**Tech Stack:** Godot 4.6, GDScript strict 类型模式，Godot Resource .tres 格式

---

## 文件变更一览

| 操作 | 文件 | 职责 |
|------|------|------|
| 新建 | `res://data/enemies/boss.tres` | 精英守卫数据（HP 80，攻 15 / 挡 8） |
| 新建 | `res://map/MapScreen.gd` | 地图 UI 脚本 |
| 新建 | `res://map/MapScreen.tscn` | 地图场景 |
| 新建 | `res://win/WinScreen.gd` | 通关 UI 脚本 |
| 新建 | `res://win/WinScreen.tscn` | 通关场景 |
| 修改 | `res://PlayerState.gd` | 新增 `current_node: int = 0` |
| 修改 | `res://GameManager.gd` | 新增阶段/方法，分离 start_new_run() |
| 修改 | `res://CombatEngine.gd` | setup() 新增 enemy_data 参数 |
| 修改 | `res://combat/CombatScreen.tscn` | 新增 BtnWin 节点 |
| 修改 | `res://combat/CombatScreen.gd` | 传 enemy_data，按节点类型显示按钮 |
| 修改 | `res://reward/RewardScreen.gd` | go_to_menu() → go_to_map() |
| 修改 | `res://menu/MainMenu.gd` | go_to_combat() → start_new_run() |

---

## Task 1：boss.tres + PlayerState.current_node

**Files:**
- Create: `e:/MyWork/AIGameTest2/data/enemies/boss.tres`
- Modify: `e:/MyWork/AIGameTest2/PlayerState.gd`

> **背景：** boss.tres 结构与 jaw_worm.tres 完全一致，复用相同的 EnemyData.gd（uid://bp7tc2xnj6g4e）和 EnemyActionData.gd（uid://dkqvfnm3r8w5y）。PlayerState 新增 current_node 字段，其他不动。此 Task 不运行项目，仅创建/修改文件并提交。

- [ ] **Step 1：创建 boss.tres**

创建 `e:/MyWork/AIGameTest2/data/enemies/boss.tres`：

```
[gd_resource type="Resource" script_class="EnemyData" load_steps=5 format=3]

[ext_resource type="Script" uid="uid://bp7tc2xnj6g4e" path="res://EnemyData.gd" id="1_EnemyData"]
[ext_resource type="Script" uid="uid://dkqvfnm3r8w5y" path="res://EnemyActionData.gd" id="2_EnemyActionData"]

[sub_resource type="Resource" id="Action_attack"]
script = ExtResource("2_EnemyActionData")
type = "attack"
value = 15

[sub_resource type="Resource" id="Action_block"]
script = ExtResource("2_EnemyActionData")
type = "block"
value = 8

[resource]
script = ExtResource("1_EnemyData")
display_name = "精英守卫"
hp = 80
actions = [SubResource("Action_attack"), SubResource("Action_block")]
```

- [ ] **Step 2：更新 PlayerState.gd**

将 `e:/MyWork/AIGameTest2/PlayerState.gd` 完整内容替换为：

```gdscript
class_name PlayerState
extends RefCounted

var deck: Array[CardData] = []
var current_node: int = 0
```

- [ ] **Step 3：提交**

```bash
git add data/enemies/boss.tres PlayerState.gd
git commit -m "feat: add boss enemy and PlayerState.current_node"
```

---

## Task 2：GameManager 扩展 + CombatEngine 重构

**Files:**
- Modify: `e:/MyWork/AIGameTest2/GameManager.gd`
- Modify: `e:/MyWork/AIGameTest2/CombatEngine.gd`

> **背景：** GameManager 的 `go_to_combat()` 原本负责创建 PlayerState，现在分离为 `start_new_run()`（创建 PlayerState + 进地图）和 `go_to_combat()`（仅切战斗场景，PlayerState 已存在）。新增 `go_to_map()`（递增 current_node）、`go_to_win()`、`get_current_enemy_data()`、`is_final_node()`。CombatEngine.setup() 新增 `enemy_data: EnemyData` 参数，移除硬编码 preload。**此 Task 完成后项目暂无法正常运行**（CombatScreen 仍调用旧签名），Task 3 修复。

- [ ] **Step 1：更新 GameManager.gd**

将 `e:/MyWork/AIGameTest2/GameManager.gd` 完整内容替换为：

```gdscript
extends Node

enum Phase { MENU, MAP, COMBAT, REWARD, WIN }

const STAGE_ENEMIES: Array[String] = [
	"res://data/enemies/jaw_worm.tres",
	"res://data/enemies/boss.tres"
]

var current_phase: Phase = Phase.MENU
var player_state: PlayerState

func start_new_run() -> void:
	player_state = PlayerState.new()
	var strike: CardData = preload("res://data/cards/strike.tres")
	var defend: CardData = preload("res://data/cards/defend.tres")
	var bash: CardData   = preload("res://data/cards/bash.tres")
	var slash: CardData  = preload("res://data/cards/slash.tres")
	for i in 4:
		player_state.deck.append(strike.duplicate())
	for i in 4:
		player_state.deck.append(defend.duplicate())
	player_state.deck.append(bash.duplicate())
	player_state.deck.append(slash.duplicate())
	current_phase = Phase.MAP
	get_tree().change_scene_to_file("res://map/MapScreen.tscn")

func go_to_combat() -> void:
	current_phase = Phase.COMBAT
	get_tree().change_scene_to_file("res://combat/CombatScreen.tscn")

func go_to_reward() -> void:
	current_phase = Phase.REWARD
	get_tree().change_scene_to_file("res://reward/RewardScreen.tscn")

func go_to_map() -> void:
	player_state.current_node += 1
	current_phase = Phase.MAP
	get_tree().change_scene_to_file("res://map/MapScreen.tscn")

func go_to_win() -> void:
	current_phase = Phase.WIN
	get_tree().change_scene_to_file("res://win/WinScreen.tscn")

func go_to_menu() -> void:
	player_state = null
	current_phase = Phase.MENU
	get_tree().change_scene_to_file("res://menu/MainMenu.tscn")

func get_current_enemy_data() -> EnemyData:
	return load(STAGE_ENEMIES[player_state.current_node]) as EnemyData

func is_final_node() -> bool:
	return player_state.current_node == STAGE_ENEMIES.size() - 1
```

- [ ] **Step 2：更新 CombatEngine.gd 的 setup() 方法**

只修改 `setup()` 函数，其余方法完全不动。将 setup() 替换为：

```gdscript
func setup(initial_deck: Array[CardData], enemy_data: EnemyData) -> void:
	_enemy_data = enemy_data
	enemy = Combatant.new()
	enemy.display_name = _enemy_data.display_name
	enemy.hp = _enemy_data.hp
	enemy.max_hp = _enemy_data.hp
	enemy.block = 0

	player = Combatant.new()
	player.display_name = "玩家"
	player.hp = 70
	player.max_hp = 70
	player.block = 0

	for card: CardData in initial_deck:
		_draw_pile.append(card.duplicate())
	_draw_pile.shuffle()
	_start_player_turn()
```

- [ ] **Step 3：提交**

```bash
git add GameManager.gd CombatEngine.gd
git commit -m "refactor: add map/win routing to GameManager, CombatEngine.setup() accepts enemy_data"
```

---

## Task 3：CombatScreen + MainMenu + RewardScreen 更新

**Files:**
- Modify: `e:/MyWork/AIGameTest2/combat/CombatScreen.tscn`
- Modify: `e:/MyWork/AIGameTest2/combat/CombatScreen.gd`
- Modify: `e:/MyWork/AIGameTest2/menu/MainMenu.gd`
- Modify: `e:/MyWork/AIGameTest2/reward/RewardScreen.gd`

> **背景：** CombatScreen 新增 BtnWin 节点，_ready() 传入 enemy_data，_on_combat_ended() 按 is_final_node() 显示不同按钮。MainMenu 改调 start_new_run()。RewardScreen 改调 go_to_map()。**此 Task 完成后 map/ 和 win/ 场景还不存在**，go_to_map/go_to_win 调用会在运行时失败，Task 4 新建这两个场景后才能完整运行。

- [ ] **Step 1：更新 CombatScreen.tscn（新增 BtnWin）**

将 `e:/MyWork/AIGameTest2/combat/CombatScreen.tscn` 完整内容替换为：

```
[gd_scene load_steps=2 format=3 uid="uid://combat_screen"]

[ext_resource type="Script" path="res://combat/CombatScreen.gd" id="1_script"]

[node name="CombatScreen" type="Control"]
layout_mode = 3
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
script = ExtResource("1_script")

[node name="VBoxContainer" type="VBoxContainer" parent="."]
layout_mode = 1
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
theme_override_constants/separation = 10

[node name="EnemyPanel" type="VBoxContainer" parent="VBoxContainer"]
layout_mode = 2

[node name="LblEnemyName" type="Label" parent="VBoxContainer/EnemyPanel"]
layout_mode = 2
text = "Enemy"

[node name="LblEnemyHP" type="Label" parent="VBoxContainer/EnemyPanel"]
layout_mode = 2
text = "生命：?"

[node name="LblEnemyBlock" type="Label" parent="VBoxContainer/EnemyPanel"]
layout_mode = 2
text = "格挡：0"

[node name="LblEnemyIntent" type="Label" parent="VBoxContainer/EnemyPanel"]
layout_mode = 2
text = "意图：?"

[node name="PlayerPanel" type="VBoxContainer" parent="VBoxContainer"]
layout_mode = 2

[node name="LblPlayerHP" type="Label" parent="VBoxContainer/PlayerPanel"]
layout_mode = 2
text = "生命：?"

[node name="LblPlayerBlock" type="Label" parent="VBoxContainer/PlayerPanel"]
layout_mode = 2
text = "格挡：0"

[node name="LblEnergy" type="Label" parent="VBoxContainer/PlayerPanel"]
layout_mode = 2
text = "能量：3 / 3"

[node name="HandContainer" type="HBoxContainer" parent="VBoxContainer"]
layout_mode = 2

[node name="BtnEndTurn" type="Button" parent="VBoxContainer"]
layout_mode = 2
text = "结束回合"

[node name="LblResult" type="Label" parent="."]
layout_mode = 1
anchors_preset = 8
anchor_left = 0.5
anchor_top = 0.5
anchor_right = 0.5
anchor_bottom = 0.5
visible = false
text = ""

[node name="BtnReturnMenu" type="Button" parent="."]
layout_mode = 1
anchors_preset = 8
anchor_left = 0.5
anchor_top = 0.6
anchor_right = 0.5
anchor_bottom = 0.6
offset_left = -70.0
offset_top = -15.0
offset_right = 70.0
offset_bottom = 15.0
visible = false
text = "返回主菜单"

[node name="BtnGetReward" type="Button" parent="."]
layout_mode = 1
anchors_preset = 8
anchor_left = 0.5
anchor_top = 0.6
anchor_right = 0.5
anchor_bottom = 0.6
offset_left = -60.0
offset_top = -35.0
offset_right = 60.0
offset_bottom = -5.0
visible = false
text = "领取奖励"

[node name="BtnWin" type="Button" parent="."]
layout_mode = 1
anchors_preset = 8
anchor_left = 0.5
anchor_top = 0.6
anchor_right = 0.5
anchor_bottom = 0.6
offset_left = -50.0
offset_top = -55.0
offset_right = 50.0
offset_bottom = -25.0
visible = false
text = "通关！"
```

- [ ] **Step 2：更新 CombatScreen.gd**

将 `e:/MyWork/AIGameTest2/combat/CombatScreen.gd` 完整内容替换为：

```gdscript
extends Control

@onready var _lbl_enemy_name: Label    = $VBoxContainer/EnemyPanel/LblEnemyName
@onready var _lbl_enemy_hp: Label      = $VBoxContainer/EnemyPanel/LblEnemyHP
@onready var _lbl_enemy_block: Label   = $VBoxContainer/EnemyPanel/LblEnemyBlock
@onready var _lbl_enemy_intent: Label  = $VBoxContainer/EnemyPanel/LblEnemyIntent
@onready var _lbl_player_hp: Label     = $VBoxContainer/PlayerPanel/LblPlayerHP
@onready var _lbl_player_block: Label  = $VBoxContainer/PlayerPanel/LblPlayerBlock
@onready var _lbl_energy: Label        = $VBoxContainer/PlayerPanel/LblEnergy
@onready var _hand_container: HBoxContainer = $VBoxContainer/HandContainer
@onready var _btn_end_turn: Button     = $VBoxContainer/BtnEndTurn
@onready var _lbl_result: Label        = $LblResult
@onready var _btn_return_menu: Button  = $BtnReturnMenu
@onready var _btn_get_reward: Button   = $BtnGetReward
@onready var _btn_win: Button          = $BtnWin

var _engine: CombatEngine
var _hand_buttons: Array[Button] = []

func _ready() -> void:
	_engine = CombatEngine.new()
	_engine.state_changed.connect(_refresh_ui)
	_engine.combat_ended.connect(_on_combat_ended)
	_btn_end_turn.pressed.connect(_engine.end_turn)
	_btn_return_menu.pressed.connect(GameManager.go_to_menu)
	_btn_get_reward.pressed.connect(GameManager.go_to_reward)
	_btn_win.pressed.connect(GameManager.go_to_win)
	_engine.setup(GameManager.player_state.deck, GameManager.get_current_enemy_data())

func _on_card_pressed(card: CardData) -> void:
	_engine.play_card(card)

func _refresh_ui() -> void:
	_lbl_enemy_name.text = _engine.enemy.display_name
	_lbl_enemy_hp.text = "生命：%d / %d" % [_engine.enemy.hp, _engine.enemy.max_hp]
	_lbl_enemy_block.text = "格挡：%d" % _engine.enemy.block
	var action: EnemyActionData = _engine.get_current_enemy_action()
	var intent: String = "攻击 %d" % action.value if action.type == "attack" else "格挡 %d" % action.value
	_lbl_enemy_intent.text = "意图：" + intent
	_lbl_player_hp.text = "生命：%d / %d" % [_engine.player.hp, _engine.player.max_hp]
	_lbl_player_block.text = "格挡：%d" % _engine.player.block
	_lbl_energy.text = "能量：%d / 3" % _engine.energy
	_rebuild_hand()

func _rebuild_hand() -> void:
	for btn: Button in _hand_buttons:
		btn.queue_free()
	_hand_buttons.clear()
	for card: CardData in _engine.hand:
		var btn: Button = Button.new()
		btn.text = card.get_description()
		btn.pressed.connect(_on_card_pressed.bind(card))
		_hand_container.add_child(btn)
		_hand_buttons.append(btn)

func _on_combat_ended(result: String) -> void:
	_lbl_result.text = "胜利！" if result == "victory" else "游戏结束"
	_lbl_result.visible = true
	_btn_end_turn.disabled = true
	if result == "victory":
		if GameManager.is_final_node():
			_btn_win.visible = true
		else:
			_btn_get_reward.visible = true
	else:
		_btn_return_menu.visible = true
	for btn: Button in _hand_buttons:
		btn.disabled = true
```

- [ ] **Step 3：更新 MainMenu.gd**

将 `e:/MyWork/AIGameTest2/menu/MainMenu.gd` 完整内容替换为：

```gdscript
extends Control

@onready var _btn_start: Button = $BtnStart

func _ready() -> void:
	_btn_start.pressed.connect(_on_start_pressed)

func _on_start_pressed() -> void:
	GameManager.start_new_run()
```

- [ ] **Step 4：更新 RewardScreen.gd**

将 `e:/MyWork/AIGameTest2/reward/RewardScreen.gd` 完整内容替换为：

```gdscript
extends Control

@onready var _card_container: HBoxContainer = $VBoxContainer/CardContainer

func _ready() -> void:
	var options: Array[CardData] = RewardEngine.get_options()
	for card: CardData in options:
		var btn := Button.new()
		btn.text = card.get_description()
		btn.pressed.connect(_on_card_selected.bind(card))
		_card_container.add_child(btn)

func _on_card_selected(card: CardData) -> void:
	GameManager.player_state.deck.append(card.duplicate())
	GameManager.go_to_map()
```

- [ ] **Step 5：提交**

```bash
git add combat/CombatScreen.tscn combat/CombatScreen.gd menu/MainMenu.gd reward/RewardScreen.gd
git commit -m "feat: update CombatScreen/MainMenu/RewardScreen for map routing"
```

---

## Task 4：MapScreen + WinScreen 新建 + 验证

**Files:**
- Create: `e:/MyWork/AIGameTest2/map/MapScreen.gd`
- Create: `e:/MyWork/AIGameTest2/map/MapScreen.tscn`
- Create: `e:/MyWork/AIGameTest2/win/WinScreen.gd`
- Create: `e:/MyWork/AIGameTest2/win/WinScreen.tscn`

> **背景：** 此 Task 新建最后两个场景，完成所有场景路径。完成后运行项目做完整验证，打 v0.8.0 标签。MapScreen 动态读取 `GameManager.player_state.current_node` 渲染节点状态；WinScreen 只显示"通关！"文字和返回主菜单按钮。

- [ ] **Step 1：创建 map/MapScreen.gd**

创建 `e:/MyWork/AIGameTest2/map/MapScreen.gd`（如果 map/ 目录不存在先创建）：

```gdscript
extends Control

const NODE_LABELS: Array[String] = ["关卡一：颚虫", "关卡二：精英守卫"]

func _ready() -> void:
	var current: int = GameManager.player_state.current_node
	for i in NODE_LABELS.size():
		var btn := Button.new()
		if i < current:
			btn.text = NODE_LABELS[i] + "（已完成）"
			btn.disabled = true
		elif i == current:
			btn.text = NODE_LABELS[i]
			btn.pressed.connect(GameManager.go_to_combat)
		else:
			btn.text = NODE_LABELS[i] + "（未解锁）"
			btn.disabled = true
		$NodeContainer.add_child(btn)
```

- [ ] **Step 2：创建 map/MapScreen.tscn**

创建 `e:/MyWork/AIGameTest2/map/MapScreen.tscn`：

```
[gd_scene load_steps=2 format=3 uid="uid://dmap8x3qnc5vy"]

[ext_resource type="Script" path="res://map/MapScreen.gd" id="1_script"]

[node name="MapScreen" type="Control"]
layout_mode = 3
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
script = ExtResource("1_script")

[node name="VBoxContainer" type="VBoxContainer" parent="."]
layout_mode = 1
anchors_preset = 8
anchor_left = 0.5
anchor_top = 0.5
anchor_right = 0.5
anchor_bottom = 0.5
offset_left = -150.0
offset_top = -60.0
offset_right = 150.0
offset_bottom = 60.0
theme_override_constants/separation = 12

[node name="LblTitle" type="Label" parent="VBoxContainer"]
layout_mode = 2
text = "选择关卡"
horizontal_alignment = 1

[node name="NodeContainer" type="VBoxContainer" parent="VBoxContainer"]
layout_mode = 2
theme_override_constants/separation = 8
```

- [ ] **Step 3：创建 win/WinScreen.gd**

创建 `e:/MyWork/AIGameTest2/win/WinScreen.gd`（如果 win/ 目录不存在先创建）：

```gdscript
extends Control

func _ready() -> void:
	$BtnMenu.pressed.connect(GameManager.go_to_menu)
```

- [ ] **Step 4：创建 win/WinScreen.tscn**

创建 `e:/MyWork/AIGameTest2/win/WinScreen.tscn`：

```
[gd_scene load_steps=2 format=3 uid="uid://bwin9p4rke7mq"]

[ext_resource type="Script" path="res://win/WinScreen.gd" id="1_script"]

[node name="WinScreen" type="Control"]
layout_mode = 3
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
script = ExtResource("1_script")

[node name="VBoxContainer" type="VBoxContainer" parent="."]
layout_mode = 1
anchors_preset = 8
anchor_left = 0.5
anchor_top = 0.5
anchor_right = 0.5
anchor_bottom = 0.5
offset_left = -100.0
offset_top = -50.0
offset_right = 100.0
offset_bottom = 50.0
theme_override_constants/separation = 20

[node name="LblWin" type="Label" parent="VBoxContainer"]
layout_mode = 2
text = "通关！"
horizontal_alignment = 1

[node name="BtnMenu" type="Button" parent="VBoxContainer"]
layout_mode = 2
text = "返回主菜单"
```

- [ ] **Step 5：运行项目验证**

使用 `mcp__godot__run_project`（projectPath="e:/MyWork/AIGameTest2"）运行项目。
等待约 3 秒后调用 `mcp__godot__get_debug_output`，确认：
- 无 `SCRIPT ERROR:` 或 `Parser Error:`
- 游戏启动进入主菜单

- [ ] **Step 6：停止项目**

使用 `mcp__godot__stop_project` 停止项目。

- [ ] **Step 7：提交并打标签**

```bash
git add map/MapScreen.gd map/MapScreen.tscn win/WinScreen.gd win/WinScreen.tscn
git commit -m "feat: add MapScreen and WinScreen, complete v0.8.0 map system"
git tag v0.8.0
git log --oneline -6
```

---

## 验收总结

完成所有 Task 后，以下标准应全部满足：

- [ ] 游戏启动进入主菜单，点击"开始游戏"进入地图界面
- [ ] 地图界面显示 2 个节点，关卡一可点击，关卡二锁定
- [ ] 点击关卡一进入颚虫战斗，行为与 v0.7.0 一致
- [ ] 胜利后选牌，返回地图，关卡一显示"已完成"，关卡二变为可点击
- [ ] 点击关卡二进入精英守卫战斗（HP 80，攻 15 / 挡 8）
- [ ] 精英守卫战斗中能看到奖励选到的卡牌出现在手牌中
- [ ] 击败精英守卫后显示"通关！"按钮，点击进入通关界面
- [ ] 通关界面显示"通关！"，点击返回主菜单
- [ ] 任意关卡败北直接返回主菜单
- [ ] 全部 v0.7.0 验收标准继续通过
