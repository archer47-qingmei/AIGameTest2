# v0.37.0 剑修剩余卡牌 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增剑鞘/剑气盈体/惊鸿/剑步/人剑合一/意随心发 6 张剑修卡牌，扩展 effect 系统支持回合内状态追踪、下回合延迟触发、终结技被动响应和条件性剑意保留。

**Architecture:** 数据字段全部新增在 Combatant.gd（运行时状态）和 CardData.gd（蓝图标志位）；EffectResolver.gd 更新招式/终结技结算路径；CombatEngine.gd 在 `_start_player_turn()` 重置回合状态并应用延迟效果，在 `_apply_engine_effects()` 处理新 effect 类型；6 张新牌以 .tres 文件新增，奖励池扩至 21 张。

**Tech Stack:** Godot 4 GDScript，Resource (.tres)，无测试框架（GUT 未安装），用 `mcp__godot__run_project` + `mcp__godot__get_debug_output` 验证运行时无报错。

---

## 文件结构

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `Combatant.gd` | 修改 | +7 个运行时字段 |
| `CardData.gd` | 修改 | +1 个蓝图字段，get_description 更新 |
| `EffectResolver.gd` | 修改 | 招式剑意路径 + 终结技路径各改 |
| `CombatEngine.gd` | 修改 | _start_player_turn + _apply_engine_effects |
| `data/cards/jian_qiao.tres` | 新建 | 剑鞘 |
| `data/cards/jian_qi_ying_ti.tres` | 新建 | 剑气盈体 |
| `data/cards/jing_hong.tres` | 新建 | 惊鸿 |
| `data/cards/jian_bu.tres` | 新建 | 剑步 |
| `data/cards/ren_jian_he_yi.tres` | 新建 | 人剑合一 |
| `data/cards/yi_sui_xin_fa.tres` | 新建 | 意随心发 |
| `CardPool.gd` | 修改 | SWORD_REWARD_CARDS 15→21 |
| `menu/MainMenu.tscn` | 修改 | 版本号 v0.37.0 |

---

## Task 1: Combatant.gd + CardData.gd — 新增字段

**Files:**
- Modify: `E:\MyWork\AIGameTest2\Combatant.gd`
- Modify: `E:\MyWork\AIGameTest2\CardData.gd`

- [ ] **Step 1: 在 Combatant.gd 末尾现有字段后追加 7 个字段**

当前文件末尾（`sword_intent_block_bonus` 之后）：

```gdscript
var sword_intent_block_bonus: int = 0
```

改为：

```gdscript
var sword_intent_block_bonus: int = 0
var played_style_this_turn: bool = false
var gained_sword_intent_this_turn: bool = false
var first_si_block_bonus: int = 0
var next_turn_sword_intent: int = 0
var next_turn_draw: int = 0
var finisher_block_bonus: int = 0
var current_intent: String = ""
```

- [ ] **Step 2: 在 CardData.gd 的 `sword_intent_consume_bonus` 字段后追加新字段**

当前：

```gdscript
@export var sword_intent_consume_bonus: int = 0
var is_upgraded: bool = false
```

改为：

```gdscript
@export var sword_intent_consume_bonus: int = 0
@export var retain_si_if_target_attacks: bool = false
var is_upgraded: bool = false
```

- [ ] **Step 3: 启动项目验证无报错**

```
mcp__godot__run_project → 等待 3 秒 → mcp__godot__get_debug_output → mcp__godot__stop_project
```

预期：输出中无 `ERROR` 或 `SCRIPT ERROR`。

- [ ] **Step 4: 提交**

```bash
git add Combatant.gd CardData.gd
git commit -m "feat: add turn-state and delayed-effect fields to Combatant; add retain_si_if_target_attacks to CardData"
```

---

## Task 2: EffectResolver.gd — 招式和终结技路径更新

**Files:**
- Modify: `E:\MyWork\AIGameTest2\EffectResolver.gd`

当前文件末尾两行：

```gdscript
	if card.is_finisher:
		attacker.sword_intent = 0
	elif card.card_type == "招式":
		attacker.sword_intent = mini(attacker.sword_intent + 1, attacker.sword_intent_cap)
```

- [ ] **Step 1: 替换为新版终结技+招式路径**

```gdscript
	if card.is_finisher:
		attacker.add_block(attacker.finisher_block_bonus)
		var skip_consume: bool = card.retain_si_if_target_attacks \
			and defender != null and defender.current_intent == "attack"
		if not skip_consume:
			attacker.sword_intent = 0
	elif card.card_type == "招式":
		var will_gain: bool = attacker.sword_intent < attacker.sword_intent_cap
		if will_gain and not attacker.gained_sword_intent_this_turn:
			attacker.add_block(attacker.first_si_block_bonus)
			attacker.gained_sword_intent_this_turn = true
		attacker.sword_intent = mini(attacker.sword_intent + 1, attacker.sword_intent_cap)
		attacker.played_style_this_turn = true
```

完整替换后 `resolve()` 函数末尾应为：

```gdscript
static func resolve(card: CardData, attacker: Combatant, defender: Combatant) -> void:
	for effect: CardEffectData in card.effects:
		for _i in effect.times:
			if effect.type == "damage":
				if defender != null:
					var dmg_bonus: int = 0
					if card.is_finisher:
						dmg_bonus = attacker.sword_intent * card.sword_intent_consume_bonus
					elif card.card_type == "招式":
						dmg_bonus = attacker.sword_intent * attacker.sword_intent_damage_bonus
					apply_damage(attacker, defender, effect.value + dmg_bonus)
			elif effect.type == "block":
				var blk_bonus: int = attacker.sword_intent * attacker.sword_intent_block_bonus
				attacker.add_block(effect.value + blk_bonus)
			elif effect.type == "weak":
				if defender != null:
					defender.add_weak(effect.value)
			elif effect.type == "vulnerable":
				if defender != null:
					defender.add_vulnerable(effect.value)
	if card.is_finisher:
		attacker.add_block(attacker.finisher_block_bonus)
		var skip_consume: bool = card.retain_si_if_target_attacks \
			and defender != null and defender.current_intent == "attack"
		if not skip_consume:
			attacker.sword_intent = 0
	elif card.card_type == "招式":
		var will_gain: bool = attacker.sword_intent < attacker.sword_intent_cap
		if will_gain and not attacker.gained_sword_intent_this_turn:
			attacker.add_block(attacker.first_si_block_bonus)
			attacker.gained_sword_intent_this_turn = true
		attacker.sword_intent = mini(attacker.sword_intent + 1, attacker.sword_intent_cap)
		attacker.played_style_this_turn = true
```

- [ ] **Step 2: 启动项目验证无报错**

```
mcp__godot__run_project → 等待 3 秒 → mcp__godot__get_debug_output → mcp__godot__stop_project
```

预期：无 `ERROR` 或 `SCRIPT ERROR`。

- [ ] **Step 3: 提交**

```bash
git add EffectResolver.gd
git commit -m "feat: EffectResolver supports finisher_block_bonus, retain_si_if_target_attacks, first_si_block_bonus, played_style_this_turn"
```

---

## Task 3: CombatEngine.gd — _start_player_turn + _apply_engine_effects

**Files:**
- Modify: `E:\MyWork\AIGameTest2\CombatEngine.gd`

- [ ] **Step 1: 替换 `_start_player_turn()` 函数**

当前：

```gdscript
func _start_player_turn() -> void:
	player.weak = max(0, player.weak - 1)
	turn_number += 1
	energy = BASE_ENERGY
	player.block = 0
	_draw_hand()
	if player.draw_per_turn > 0:
		_draw_cards(player.draw_per_turn)
	if turn_number == 1:
		RelicEngine.apply_combat_start(_relics, self)
	RelicEngine.apply_turn_start(_relics, self)
	state_changed.emit()
```

替换为：

```gdscript
func _start_player_turn() -> void:
	player.played_style_this_turn = false
	player.gained_sword_intent_this_turn = false
	player.sword_intent = mini(
		player.sword_intent + player.next_turn_sword_intent,
		player.sword_intent_cap
	)
	player.next_turn_sword_intent = 0
	player.weak = max(0, player.weak - 1)
	turn_number += 1
	energy = BASE_ENERGY
	player.block = 0
	_draw_hand()
	if player.draw_per_turn > 0:
		_draw_cards(player.draw_per_turn)
	if player.next_turn_draw > 0:
		_draw_cards(player.next_turn_draw)
		player.next_turn_draw = 0
	for i in enemies.size():
		if enemies[i].hp > 0:
			enemies[i].current_intent = get_enemy_action(i).type
	if turn_number == 1:
		RelicEngine.apply_combat_start(_relics, self)
	RelicEngine.apply_turn_start(_relics, self)
	state_changed.emit()
```

- [ ] **Step 2: 在 `_apply_engine_effects()` 末尾追加 5 个新分支**

当前末尾：

```gdscript
		elif effect.type == "sword_intent_block_bonus":
			player.sword_intent_block_bonus += effect.value
```

改为：

```gdscript
		elif effect.type == "sword_intent_block_bonus":
			player.sword_intent_block_bonus += effect.value
		elif effect.type == "sword_intent_if_no_style":
			if not player.played_style_this_turn:
				player.sword_intent = mini(player.sword_intent + effect.value, player.sword_intent_cap)
		elif effect.type == "next_turn_si":
			player.next_turn_sword_intent += effect.value
		elif effect.type == "next_turn_draw":
			player.next_turn_draw += effect.value
		elif effect.type == "finisher_block":
			player.finisher_block_bonus += effect.value
		elif effect.type == "first_si_block":
			player.first_si_block_bonus += effect.value
```

- [ ] **Step 3: 启动项目验证无报错**

```
mcp__godot__run_project → 等待 3 秒 → mcp__godot__get_debug_output → mcp__godot__stop_project
```

预期：无 `ERROR` 或 `SCRIPT ERROR`。

- [ ] **Step 4: 提交**

```bash
git add CombatEngine.gd
git commit -m "feat: CombatEngine supports turn-state reset, next_turn_si/draw, enemy current_intent, and 5 new engine effect types"
```

---

## Task 4: CardData.get_description() — 新 effect 类型显示

**Files:**
- Modify: `E:\MyWork\AIGameTest2\CardData.gd`

- [ ] **Step 1: 在 get_description() 中追加新变量声明**

当前变量声明末尾（`var si_blk: int = 0` 之后）：

```gdscript
	var si_retain: bool = false
	var si_blk: int = 0
```

改为：

```gdscript
	var si_retain: bool = false
	var si_blk: int = 0
	var next_turn_si: int = 0
	var next_turn_drw: int = 0
	var finisher_blk: int = 0
	var first_si_blk: int = 0
	var si_if_no_style: int = 0
```

- [ ] **Step 2: 在 for effect 循环末尾追加新 elif 分支**

当前末尾：

```gdscript
		elif effect.type == "sword_intent_block_bonus": si_blk = effect.value
```

改为：

```gdscript
		elif effect.type == "sword_intent_block_bonus": si_blk = effect.value
		elif effect.type == "next_turn_si":              next_turn_si = effect.value
		elif effect.type == "next_turn_draw":            next_turn_drw = effect.value
		elif effect.type == "finisher_block":            finisher_blk = effect.value
		elif effect.type == "first_si_block":            first_si_blk = effect.value
		elif effect.type == "sword_intent_if_no_style":  si_if_no_style = effect.value
```

- [ ] **Step 3: 在描述行末尾（return desc 前）追加新显示行**

当前：

```gdscript
	if si_retain:     desc += "  剑意永续"
	if si_blk > 0:    desc += "  剑意挡加成+%d" % si_blk
	return desc
```

改为：

```gdscript
	if si_retain:          desc += "  剑意永续"
	if si_blk > 0:         desc += "  剑意挡加成+%d" % si_blk
	if next_turn_si > 0:   desc += "  下回合剑意+%d" % next_turn_si
	if next_turn_drw > 0:  desc += "  下回合抽+%d" % next_turn_drw
	if finisher_blk > 0:   desc += "  终结技触发挡+%d" % finisher_blk
	if first_si_blk > 0:   desc += "  首剑意+挡%d" % first_si_blk
	if si_if_no_style > 0: desc += "  无招式时+剑意%d" % si_if_no_style
	if retain_si_if_target_attacks: desc += "  攻击意图→剑意不消耗"
	return desc
```

- [ ] **Step 4: 启动项目验证无报错**

```
mcp__godot__run_project → 等待 3 秒 → mcp__godot__get_debug_output → mcp__godot__stop_project
```

- [ ] **Step 5: 提交**

```bash
git add CardData.gd
git commit -m "feat: CardData.get_description supports 6 new effect type displays"
```

---

## Task 5: 凡品 .tres 文件（剑鞘、剑气盈体、惊鸿）

**Files:**
- Create: `E:\MyWork\AIGameTest2\data\cards\jian_qiao.tres`
- Create: `E:\MyWork\AIGameTest2\data\cards\jian_qi_ying_ti.tres`
- Create: `E:\MyWork\AIGameTest2\data\cards\jing_hong.tres`

- [ ] **Step 1: 创建 jian_qiao.tres（剑鞘：身法 1费 挡7，无招式时+1剑意）**

```
[gd_resource type="Resource" script_class="CardData" load_steps=5 format=3]

[ext_resource type="Script" uid="uid://13mprie5g221" path="res://CardData.gd" id="1_CardData"]
[ext_resource type="Script" uid="uid://cxv8nrm4tp7dk" path="res://CardEffectData.gd" id="2_CardEffectData"]

[sub_resource type="Resource" id="Effect_block"]
script = ExtResource("2_CardEffectData")
type = "block"
value = 7
upgrade_bonus = 0

[sub_resource type="Resource" id="Effect_si_cond"]
script = ExtResource("2_CardEffectData")
type = "sword_intent_if_no_style"
value = 1
upgrade_bonus = 0

[resource]
script = ExtResource("1_CardData")
card_name = "剑鞘"
cost = 1
price = 0
effects = Array[ExtResource("2_CardEffectData")]([SubResource("Effect_block"), SubResource("Effect_si_cond")])
target_type = "none"
card_type = "身法"
```

- [ ] **Step 2: 创建 jian_qi_ying_ti.tres（剑气盈体：功法 1费 下回合剑意+2）**

```
[gd_resource type="Resource" script_class="CardData" load_steps=4 format=3]

[ext_resource type="Script" uid="uid://13mprie5g221" path="res://CardData.gd" id="1_CardData"]
[ext_resource type="Script" uid="uid://cxv8nrm4tp7dk" path="res://CardEffectData.gd" id="2_CardEffectData"]

[sub_resource type="Resource" id="Effect_next_si"]
script = ExtResource("2_CardEffectData")
type = "next_turn_si"
value = 2
upgrade_bonus = 0

[resource]
script = ExtResource("1_CardData")
card_name = "剑气盈体"
cost = 1
price = 0
effects = Array[ExtResource("2_CardEffectData")]([SubResource("Effect_next_si")])
target_type = "none"
card_type = "功法"
```

- [ ] **Step 3: 创建 jing_hong.tres（惊鸿：招式·终结技 2费 打7，攻击意图→剑意不消耗）**

```
[gd_resource type="Resource" script_class="CardData" load_steps=4 format=3]

[ext_resource type="Script" uid="uid://13mprie5g221" path="res://CardData.gd" id="1_CardData"]
[ext_resource type="Script" uid="uid://cxv8nrm4tp7dk" path="res://CardEffectData.gd" id="2_CardEffectData"]

[sub_resource type="Resource" id="Effect_damage"]
script = ExtResource("2_CardEffectData")
type = "damage"
value = 7
upgrade_bonus = 0

[resource]
script = ExtResource("1_CardData")
card_name = "惊鸿"
cost = 2
price = 0
effects = Array[ExtResource("2_CardEffectData")]([SubResource("Effect_damage")])
card_type = "招式"
is_finisher = true
retain_si_if_target_attacks = true
```

- [ ] **Step 4: 启动项目验证三张牌可加载**

```
mcp__godot__run_project → 等待 3 秒 → mcp__godot__get_debug_output → mcp__godot__stop_project
```

预期：无 `ERROR` 或 `Failed to load resource`。

- [ ] **Step 5: 提交**

```bash
git add data/cards/jian_qiao.tres data/cards/jian_qi_ying_ti.tres data/cards/jing_hong.tres
git commit -m "feat: add 剑鞘/剑气盈体/惊鸿 card .tres files"
```

---

## Task 6: 灵品 .tres 文件（剑步、人剑合一、意随心发）

**Files:**
- Create: `E:\MyWork\AIGameTest2\data\cards\jian_bu.tres`
- Create: `E:\MyWork\AIGameTest2\data\cards\ren_jian_he_yi.tres`
- Create: `E:\MyWork\AIGameTest2\data\cards\yi_sui_xin_fa.tres`

- [ ] **Step 1: 创建 jian_bu.tres（剑步：身法 1费 挡6，下回合抽+1）**

```
[gd_resource type="Resource" script_class="CardData" load_steps=5 format=3]

[ext_resource type="Script" uid="uid://13mprie5g221" path="res://CardData.gd" id="1_CardData"]
[ext_resource type="Script" uid="uid://cxv8nrm4tp7dk" path="res://CardEffectData.gd" id="2_CardEffectData"]

[sub_resource type="Resource" id="Effect_block"]
script = ExtResource("2_CardEffectData")
type = "block"
value = 6
upgrade_bonus = 0

[sub_resource type="Resource" id="Effect_next_draw"]
script = ExtResource("2_CardEffectData")
type = "next_turn_draw"
value = 1
upgrade_bonus = 0

[resource]
script = ExtResource("1_CardData")
card_name = "剑步"
cost = 1
price = 0
effects = Array[ExtResource("2_CardEffectData")]([SubResource("Effect_block"), SubResource("Effect_next_draw")])
target_type = "none"
card_type = "身法"
```

- [ ] **Step 2: 创建 ren_jian_he_yi.tres（人剑合一：功法 2费 终结技触发挡+3）**

```
[gd_resource type="Resource" script_class="CardData" load_steps=4 format=3]

[ext_resource type="Script" uid="uid://13mprie5g221" path="res://CardData.gd" id="1_CardData"]
[ext_resource type="Script" uid="uid://cxv8nrm4tp7dk" path="res://CardEffectData.gd" id="2_CardEffectData"]

[sub_resource type="Resource" id="Effect_finisher_blk"]
script = ExtResource("2_CardEffectData")
type = "finisher_block"
value = 3
upgrade_bonus = 0

[resource]
script = ExtResource("1_CardData")
card_name = "人剑合一"
cost = 2
price = 0
effects = Array[ExtResource("2_CardEffectData")]([SubResource("Effect_finisher_blk")])
target_type = "none"
card_type = "功法"
```

- [ ] **Step 3: 创建 yi_sui_xin_fa.tres（意随心发：功法 1费 首剑意+挡2）**

```
[gd_resource type="Resource" script_class="CardData" load_steps=4 format=3]

[ext_resource type="Script" uid="uid://13mprie5g221" path="res://CardData.gd" id="1_CardData"]
[ext_resource type="Script" uid="uid://cxv8nrm4tp7dk" path="res://CardEffectData.gd" id="2_CardEffectData"]

[sub_resource type="Resource" id="Effect_first_si_blk"]
script = ExtResource("2_CardEffectData")
type = "first_si_block"
value = 2
upgrade_bonus = 0

[resource]
script = ExtResource("1_CardData")
card_name = "意随心发"
cost = 1
price = 0
effects = Array[ExtResource("2_CardEffectData")]([SubResource("Effect_first_si_blk")])
target_type = "none"
card_type = "功法"
```

- [ ] **Step 4: 启动项目验证三张牌可加载**

```
mcp__godot__run_project → 等待 3 秒 → mcp__godot__get_debug_output → mcp__godot__stop_project
```

预期：无 `ERROR` 或 `Failed to load resource`。

- [ ] **Step 5: 提交**

```bash
git add data/cards/jian_bu.tres data/cards/ren_jian_he_yi.tres data/cards/yi_sui_xin_fa.tres
git commit -m "feat: add 剑步/人剑合一/意随心发 card .tres files"
```

---

## Task 7: CardPool.gd 扩展 + 版本号

**Files:**
- Modify: `E:\MyWork\AIGameTest2\CardPool.gd`
- Modify: `E:\MyWork\AIGameTest2\menu\MainMenu.tscn`

- [ ] **Step 1: 将 SWORD_REWARD_CARDS 从 15 条扩展至 21 条**

找到 `SWORD_REWARD_CARDS` 常量，在最后一条 `"res://data/cards/xin_jian.tres",` 后追加 6 行：

```gdscript
const SWORD_REWARD_CARDS: Array[String] = [
    "res://data/cards/zhan_tie.tres",
    "res://data/cards/po_feng.tres",
    "res://data/cards/ce_bu.tres",
    "res://data/cards/lian_xi.tres",
    "res://data/cards/chuan_xin.tres",
    "res://data/cards/yang_jian_shu.tres",
    "res://data/cards/lue_ying.tres",
    "res://data/cards/sui_xing.tres",
    "res://data/cards/heng_jian.tres",
    "res://data/cards/lie_kong.tres",
    "res://data/cards/wu_hen.tres",
    "res://data/cards/po_jun.tres",
    "res://data/cards/wan_jian_gui_zong.tres",
    "res://data/cards/jian_yu.tres",
    "res://data/cards/xin_jian.tres",
    "res://data/cards/jian_qiao.tres",
    "res://data/cards/jian_qi_ying_ti.tres",
    "res://data/cards/jing_hong.tres",
    "res://data/cards/jian_bu.tres",
    "res://data/cards/ren_jian_he_yi.tres",
    "res://data/cards/yi_sui_xin_fa.tres",
]
```

- [ ] **Step 2: 在 MainMenu.tscn 中将版本号改为 v0.37.0**

搜索文件中的 `v0.36.0`，替换为 `v0.37.0`。

- [ ] **Step 3: 启动项目，进入战斗，完成一场战役验证奖励界面出现 21 张牌**

```
mcp__godot__run_project → 等待 3 秒 → mcp__godot__get_debug_output → mcp__godot__stop_project
```

预期：无报错，主界面显示 `v0.37.0`。

- [ ] **Step 4: 提交**

```bash
git add CardPool.gd menu/MainMenu.tscn
git commit -m "feat: expand SWORD_REWARD_CARDS to 21 entries; bump version to v0.37.0"
```
