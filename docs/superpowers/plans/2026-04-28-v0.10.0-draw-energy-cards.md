# v0.10.0 抽牌与能量卡牌实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增 draw / energy 两种引擎级效果类型，并新增 4 张卡牌（洞察/迅刺/蓄力/冲刺）扩充奖励池，使奖励选择具备真正的策略差异化。

**Architecture:** `CombatEngine` 新增私有方法 `_apply_engine_effects()` 和 `_draw_cards()`，在 `play_card()` 调用 `EffectResolver.resolve()` 之后执行引擎层效果，保持 `EffectResolver` 接口完全不变。`CardData.get_description()` 改为只显示非零字段并支持 draw/energy 字段。4 张新卡均为 `.tres` 数据文件，`RewardEngine` 自动枚举 `data/cards/*.tres`，无需改动任何逻辑代码。

**Tech Stack:** Godot 4.6, GDScript 严格类型模式

---

## 文件变更一览

| 操作 | 文件 | 变更 |
|------|------|------|
| 修改 | `e:/MyWork/AIGameTest2/CombatEngine.gd` | `play_card()` 中新增 `_apply_engine_effects(card)` 调用；新增 `_apply_engine_effects()` 和 `_draw_cards()` 方法 |
| 修改 | `e:/MyWork/AIGameTest2/CardData.gd` | `get_description()` 支持 draw/energy，只显示非零字段 |
| 新增 | `e:/MyWork/AIGameTest2/data/cards/insight.tres` | 洞察：1费，抽2张 |
| 新增 | `e:/MyWork/AIGameTest2/data/cards/quick_strike.tres` | 迅刺：0费，攻4，抽1 |
| 新增 | `e:/MyWork/AIGameTest2/data/cards/energize.tres` | 蓄力：1费，获得3能量 |
| 新增 | `e:/MyWork/AIGameTest2/data/cards/dash.tres` | 冲刺：0费，攻4，获得1能量 |

---

## Task 1：修改 CombatEngine.gd 与 CardData.gd

**Files:**
- Modify: `e:/MyWork/AIGameTest2/CombatEngine.gd`
- Modify: `e:/MyWork/AIGameTest2/CardData.gd`

> **背景：** `CombatEngine.play_card()` 目前调用 `EffectResolver.resolve()` 处理 damage/block，但 EffectResolver 只接受 Combatant 参数，无法处理 draw/energy 这两种引擎层效果。此 Task 在 play_card() 中插入 `_apply_engine_effects(card)` 调用，并新增两个私有方法实现 draw/energy 逻辑。同时更新 CardData.get_description() 以支持这两个新字段。

- [ ] **Step 1：更新 CombatEngine.gd**

将 `e:/MyWork/AIGameTest2/CombatEngine.gd` 完整内容替换为：

```gdscript
class_name CombatEngine
extends RefCounted

signal state_changed
signal combat_ended(result: String)

var player: Combatant
var enemy: Combatant
var hand: Array[CardData] = []
var energy: int = 0
var turn_number: int = 0

var _draw_pile: Array[CardData] = []
var _discard_pile: Array[CardData] = []
var _enemy_data: EnemyData

func setup(initial_deck: Array[CardData], enemy_data: EnemyData, initial_hp: int) -> void:
	_enemy_data = enemy_data
	enemy = Combatant.new()
	enemy.display_name = _enemy_data.display_name
	enemy.hp = _enemy_data.hp
	enemy.max_hp = _enemy_data.hp
	enemy.block = 0

	player = Combatant.new()
	player.display_name = "玩家"
	player.hp = initial_hp
	player.max_hp = 70
	player.block = 0

	for card: CardData in initial_deck:
		_draw_pile.append(card.duplicate())
	_draw_pile.shuffle()
	_start_player_turn()

func get_current_enemy_action() -> EnemyActionData:
	return _enemy_data.actions[(turn_number - 1) % _enemy_data.actions.size()]

func play_card(card: CardData) -> void:
	if energy < card.cost:
		return
	energy -= card.cost
	EffectResolver.resolve(card, player, enemy)
	_apply_engine_effects(card)
	var idx: int = hand.find(card)
	if idx >= 0:
		hand.remove_at(idx)
	_discard_pile.append(card)
	state_changed.emit()
	_check_end()

func end_turn() -> void:
	for card: CardData in hand:
		_discard_pile.append(card)
	hand.clear()
	_do_enemy_turn()
	state_changed.emit()
	if not _check_end():
		_start_player_turn()

func _start_player_turn() -> void:
	turn_number += 1
	energy = 3
	player.block = 0
	_draw_hand()
	state_changed.emit()

func _draw_hand() -> void:
	hand.clear()
	for i in 5:
		if _draw_pile.is_empty():
			if _discard_pile.is_empty():
				break
			for card: CardData in _discard_pile:
				_draw_pile.append(card)
			_discard_pile.clear()
			_draw_pile.shuffle()
		if _draw_pile.is_empty():
			break
		var card: CardData = _draw_pile.pop_back()
		hand.append(card)

func _apply_engine_effects(card: CardData) -> void:
	for effect: CardEffectData in card.effects:
		if effect.type == "draw":
			_draw_cards(effect.value)
		elif effect.type == "energy":
			energy += effect.value

func _draw_cards(n: int) -> void:
	for i: int in n:
		if _draw_pile.is_empty():
			if _discard_pile.is_empty():
				break
			for card: CardData in _discard_pile:
				_draw_pile.append(card)
			_discard_pile.clear()
			_draw_pile.shuffle()
		if _draw_pile.is_empty():
			break
		var card: CardData = _draw_pile.pop_back()
		hand.append(card)

func _do_enemy_turn() -> void:
	enemy.block = 0
	var action: EnemyActionData = _enemy_data.actions[(turn_number - 1) % _enemy_data.actions.size()]
	if action.type == "attack":
		player.take_damage(action.value)
	else:
		enemy.add_block(action.value)

func _check_end() -> bool:
	if enemy.hp <= 0:
		combat_ended.emit("victory")
		return true
	if player.hp <= 0:
		combat_ended.emit("game_over")
		return true
	return false
```

注意缩进使用 Tab（不是空格）。

- [ ] **Step 2：更新 CardData.gd**

将 `e:/MyWork/AIGameTest2/CardData.gd` 完整内容替换为：

```gdscript
class_name CardData
extends Resource

@export var card_name: String
@export var cost: int
@export var effects: Array[CardEffectData]

func get_description() -> String:
	var dmg: int = 0
	var blk: int = 0
	var drw: int = 0
	var nrg: int = 0
	for effect: CardEffectData in effects:
		if effect.type == "damage":   dmg = effect.value
		elif effect.type == "block":  blk = effect.value
		elif effect.type == "draw":   drw = effect.value
		elif effect.type == "energy": nrg = effect.value
	var desc: String = "%s\n费用:%d" % [card_name, cost]
	if dmg > 0: desc += "  攻:%d" % dmg
	if blk > 0: desc += "  挡:%d" % blk
	if drw > 0: desc += "  抽:%d" % drw
	if nrg > 0: desc += "  能:%d" % nrg
	return desc
```

注意缩进使用 Tab（不是空格）。

- [ ] **Step 3：运行项目验证**

使用 `mcp__godot__run_project`（projectPath="e:/MyWork/AIGameTest2"）运行项目。
等待约 4 秒后调用 `mcp__godot__get_debug_output`，确认：
- 无 `SCRIPT ERROR:` 或 `Parser Error:`
- 游戏启动进入主菜单

- [ ] **Step 4：停止项目**

使用 `mcp__godot__stop_project` 停止项目。

- [ ] **Step 5：提交**

```bash
git -C "e:/MyWork/AIGameTest2" add CombatEngine.gd CardData.gd
git -C "e:/MyWork/AIGameTest2" commit -m "feat: add draw/energy engine effects to CombatEngine; update CardData.get_description"
```

---

## Task 2：新增 4 张卡牌数据文件

**Files:**
- Create: `e:/MyWork/AIGameTest2/data/cards/insight.tres`
- Create: `e:/MyWork/AIGameTest2/data/cards/quick_strike.tres`
- Create: `e:/MyWork/AIGameTest2/data/cards/energize.tres`
- Create: `e:/MyWork/AIGameTest2/data/cards/dash.tres`

> **背景：** `RewardEngine` 自动枚举 `data/cards/*.tres` 下的全部文件作为奖励池，无需任何代码改动。只需将 4 个 `.tres` 文件写入正确路径即可。所有文件使用已知 UID：CardData.gd = `uid://13mprie5g221`，CardEffectData.gd = `uid://cxv8nrm4tp7dk`。

- [ ] **Step 1：创建 data/cards/insight.tres（洞察：1费，抽2张）**

创建文件 `e:/MyWork/AIGameTest2/data/cards/insight.tres`，内容如下：

```
[gd_resource type="Resource" script_class="CardData" load_steps=4 format=3]

[ext_resource type="Script" uid="uid://13mprie5g221" path="res://CardData.gd" id="1_CardData"]
[ext_resource type="Script" uid="uid://cxv8nrm4tp7dk" path="res://CardEffectData.gd" id="2_CardEffectData"]

[sub_resource type="Resource" id="Effect_draw"]
script = ExtResource("2_CardEffectData")
type = "draw"
value = 2

[resource]
script = ExtResource("1_CardData")
card_name = "洞察"
cost = 1
effects = [SubResource("Effect_draw")]
```

- [ ] **Step 2：创建 data/cards/quick_strike.tres（迅刺：0费，攻4，抽1）**

创建文件 `e:/MyWork/AIGameTest2/data/cards/quick_strike.tres`，内容如下：

```
[gd_resource type="Resource" script_class="CardData" load_steps=5 format=3]

[ext_resource type="Script" uid="uid://13mprie5g221" path="res://CardData.gd" id="1_CardData"]
[ext_resource type="Script" uid="uid://cxv8nrm4tp7dk" path="res://CardEffectData.gd" id="2_CardEffectData"]

[sub_resource type="Resource" id="Effect_damage"]
script = ExtResource("2_CardEffectData")
type = "damage"
value = 4

[sub_resource type="Resource" id="Effect_draw"]
script = ExtResource("2_CardEffectData")
type = "draw"
value = 1

[resource]
script = ExtResource("1_CardData")
card_name = "迅刺"
cost = 0
effects = [SubResource("Effect_damage"), SubResource("Effect_draw")]
```

- [ ] **Step 3：创建 data/cards/energize.tres（蓄力：1费，获得3能量）**

创建文件 `e:/MyWork/AIGameTest2/data/cards/energize.tres`，内容如下：

```
[gd_resource type="Resource" script_class="CardData" load_steps=4 format=3]

[ext_resource type="Script" uid="uid://13mprie5g221" path="res://CardData.gd" id="1_CardData"]
[ext_resource type="Script" uid="uid://cxv8nrm4tp7dk" path="res://CardEffectData.gd" id="2_CardEffectData"]

[sub_resource type="Resource" id="Effect_energy"]
script = ExtResource("2_CardEffectData")
type = "energy"
value = 3

[resource]
script = ExtResource("1_CardData")
card_name = "蓄力"
cost = 1
effects = [SubResource("Effect_energy")]
```

- [ ] **Step 4：创建 data/cards/dash.tres（冲刺：0费，攻4，获得1能量）**

创建文件 `e:/MyWork/AIGameTest2/data/cards/dash.tres`，内容如下：

```
[gd_resource type="Resource" script_class="CardData" load_steps=5 format=3]

[ext_resource type="Script" uid="uid://13mprie5g221" path="res://CardData.gd" id="1_CardData"]
[ext_resource type="Script" uid="uid://cxv8nrm4tp7dk" path="res://CardEffectData.gd" id="2_CardEffectData"]

[sub_resource type="Resource" id="Effect_damage"]
script = ExtResource("2_CardEffectData")
type = "damage"
value = 4

[sub_resource type="Resource" id="Effect_energy"]
script = ExtResource("2_CardEffectData")
type = "energy"
value = 1

[resource]
script = ExtResource("1_CardData")
card_name = "冲刺"
cost = 0
effects = [SubResource("Effect_damage"), SubResource("Effect_energy")]
```

- [ ] **Step 5：运行项目验证**

使用 `mcp__godot__run_project`（projectPath="e:/MyWork/AIGameTest2"）运行项目。
等待约 4 秒后调用 `mcp__godot__get_debug_output`，确认：
- 无 `SCRIPT ERROR:` 或 `Parser Error:`
- 游戏可正常启动

- [ ] **Step 6：停止项目**

使用 `mcp__godot__stop_project` 停止项目。

- [ ] **Step 7：提交并打标签**

```bash
git -C "e:/MyWork/AIGameTest2" add data/cards/insight.tres data/cards/quick_strike.tres data/cards/energize.tres data/cards/dash.tres
git -C "e:/MyWork/AIGameTest2" commit -m "feat: add insight, quick_strike, energize, dash card data files"
git -C "e:/MyWork/AIGameTest2" tag v0.10.0
git -C "e:/MyWork/AIGameTest2" log --oneline -5
```

---

## 验收总结

完成所有 Task 后，以下标准应全部满足：

- [ ] 打出"洞察"：手牌正确增加 2 张（牌库不足时按剩余数量抽取）
- [ ] 打出"迅刺"：敌人受到 4 点伤害，手牌正确增加 1 张
- [ ] 打出"蓄力"：能量从 X 变为 X+3（净 +2，因为打牌消耗 1 费）
- [ ] 打出"冲刺"：敌人受到 4 点伤害，能量增加 1
- [ ] 奖励界面可能出现洞察/迅刺/蓄力/冲刺（新卡自动进入奖励池）
- [ ] 新卡牌按钮文字正确显示效果（"抽:2"、"能:3" 等）
- [ ] 原有卡牌（打击/防御/重击/斩击）只显示非零字段（不再显示"攻:0"或"挡:0"）
- [ ] 全部 v0.9.0 验收标准继续通过
