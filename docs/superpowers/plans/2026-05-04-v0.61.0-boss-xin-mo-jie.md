# 心魔劫 Boss 实现计划 (v0.61.0)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现心魔劫 Boss 及新诅咒牌走火入魔，完成筑基境全部敌人。

**Architecture:** 心魔劫的动态行为通过三处扩展实现：EnemyData 新增 copies_player_cards / periodic_interval / periodic_action 字段；CombatEngine 在 setup() 时扫描牌组填充复制卡池，在 _resolve_enemy_actions() 处理 mirror_attack 预解析和每3回合覆盖，在 _do_enemy_turn() 处理三个新动作类型和 HP<50% 被动；CardData 新增 is_zahuorumuo 标记，end_turn() 处理其弃牌效果。

**Tech Stack:** GDScript 4，Godot 4.4，Resource/.tres 数据文件

---

### Task 1: CardData 扩展 + 走火入魔数据文件

**Files:**
- Modify: `E:/MyWork/AIGameTest2/CardData.gd`
- Create: `E:/MyWork/AIGameTest2/data/cards/zao_huo_ru_mo.tres`

背景：走火入魔是一张诅咒牌，被摸到时留在手中（不像心魔那样自动弃牌），回合结束时随机弃掉另1张手牌。它不使用现有的 `is_curse` 标记（那意味着"摸到即弃"），而是新增一个 `is_zahuorumuo` 标记。

- [ ] **Step 1: 在 CardData.gd 中新增字段**

在 `is_curse: bool = false` 下方加一行：

```gdscript
@export var is_curse: bool = false
@export var is_zahuorumuo: bool = false
```

同时更新 `get_description()` 和 `get_upgrade_preview_bbcode()`，让走火入魔也走简单显示路径：

```gdscript
func get_description() -> String:
	if is_curse or is_zahuorumuo:
		return "%s\n%s" % [card_name, special_text]
	if is_venom:
		return "%s\n费用:%d  %s" % [card_name, cost, special_text]
	return _format_description(card_name, false)

func get_upgrade_preview_bbcode() -> String:
	if is_upgraded or is_curse or is_zahuorumuo:
		return get_description()
	if is_venom:
		return "%s+\n费用:%d  %s" % [card_name, cost, special_text]
	return _format_description(card_name + "+", true)
```

- [ ] **Step 2: 防止走火入魔被打出（CombatEngine.play_card）**

`E:/MyWork/AIGameTest2/CombatEngine.gd` 第 110 行附近，`play_card()` 开头的校验：

```gdscript
func play_card(card_index: int, target_index: int) -> bool:
	var card: CardData = hand[card_index]
	if card.is_curse or card.is_zahuorumuo:
		return false
	if card.cost > energy:
		return false
```

- [ ] **Step 3: 创建走火入魔数据文件**

创建 `E:/MyWork/AIGameTest2/data/cards/zao_huo_ru_mo.tres`：

```
[gd_resource type="Resource" script_class="CardData" load_steps=2 format=3]

[ext_resource type="Script" path="res://CardData.gd" id="1_CardData"]

[resource]
script = ExtResource("1_CardData")
card_name = "走火入魔"
cost = 0
is_zahuorumuo = true
special_text = "无法打出。回合结束：随机弃掉另1张手牌"
target_type = "none"
```

- [ ] **Step 4: 验证无解析错误**

```
"D:/steam/steamapps/common/Godot Engine/godot.windows.opt.tools.64.exe" --headless --path "E:/MyWork/AIGameTest2" --quit-after 1 2>&1
```

预期：无 `ERROR:` 或 `Parse Error` 行。

- [ ] **Step 5: 提交**

```bash
git add CardData.gd data/cards/zao_huo_ru_mo.tres
git commit -m "feat: add is_zahuorumuo to CardData; create 走火入魔 curse card"
```

---

### Task 2: EnemyData / EnemyActionData 字段扩展

**Files:**
- Modify: `E:/MyWork/AIGameTest2/EnemyData.gd`
- Modify: `E:/MyWork/AIGameTest2/EnemyActionData.gd`

背景：EnemyData 需要三个新字段支持心魔劫：copies_player_cards（触发复制逻辑）、periodic_interval（每N回合触发一次固定动作）、periodic_action（被周期触发的动作）。EnemyActionData 需要一个非导出的临时字段 display_label，用于 mirror_attack 在意图显示时存储复制的牌名。

- [ ] **Step 1: 更新 EnemyData.gd**

在文件末尾（`@export var phase2_actions` 之后）添加：

```gdscript
class_name EnemyData
extends Resource

@export var display_name: String
@export var hp: int
@export var actions: Array[EnemyActionData]
@export var random_actions: bool = false
@export var phase2_threshold: float = 0.0
@export var phase2_actions: Array[EnemyActionData] = []
@export var copies_player_cards: bool = false
@export var periodic_interval: int = 0
@export var periodic_action: EnemyActionData = null
```

- [ ] **Step 2: 更新 EnemyActionData.gd**

在现有字段末尾添加非导出字段（不加 @export，运行时临时填充）：

```gdscript
class_name EnemyActionData
extends Resource

@export var type: String
@export var value: int
@export var weight: int = 1
@export var count: int = 1
var display_label: String = ""
```

- [ ] **Step 3: 验证无解析错误**

```
"D:/steam/steamapps/common/Godot Engine/godot.windows.opt.tools.64.exe" --headless --path "E:/MyWork/AIGameTest2" --quit-after 1 2>&1
```

预期：无错误。

- [ ] **Step 4: 提交**

```bash
git add EnemyData.gd EnemyActionData.gd
git commit -m "feat: EnemyData adds copies_player_cards/periodic fields; EnemyActionData adds display_label"
```

---

### Task 3: CombatEngine — 复制卡池（setup）

**Files:**
- Modify: `E:/MyWork/AIGameTest2/CombatEngine.gd`

背景：CombatEngine 在 setup() 时，若任意敌人的 copies_player_cards == true，从玩家 draw pile 筛出纯伤害招式牌（card_type=="招式" 且 effects 全为 "damage" 类型），随机取最多5张存入 `_boss_copied_cards`。

- [ ] **Step 1: 添加 ZAHUORUMUO_CARD 常量和 _boss_copied_cards 字段**

在 CombatEngine.gd 顶部常量区：

```gdscript
const BASE_ENERGY: int = 3
const VENOM_CARD: CardData = preload("res://data/cards/venom.tres")
const CURSE_CARD: CardData = preload("res://data/cards/xin_mo.tres")
const ZAHUORUMUO_CARD: CardData = preload("res://data/cards/zao_huo_ru_mo.tres")
const ATTACK_TYPES_FOR_CHARGE: Array[String] = [
	"attack", "attack_weak", "attack_vulnerable", "multi_attack", "attack_curse", "vampiric_attack"
]
```

在变量区（_exhaust_pile 附近）添加：

```gdscript
var _boss_copied_cards: Array[CardData] = []
```

- [ ] **Step 2: 在 setup() 中填充 _boss_copied_cards**

在 `setup()` 函数中，`_draw_pile.shuffle()` 之后、`_start_player_turn()` 之前添加：

```gdscript
	for card: CardData in initial_deck:
		_draw_pile.append(card.duplicate())
	_draw_pile.shuffle()
	for data: EnemyData in enemy_group.enemies:
		if data.copies_player_cards:
			var style_cards: Array[CardData] = []
			for card: CardData in _draw_pile:
				if _is_pure_damage_style(card):
					style_cards.append(card)
			style_cards.shuffle()
			_boss_copied_cards = style_cards.slice(0, mini(5, style_cards.size()))
			break
	_start_player_turn()
```

- [ ] **Step 3: 添加 _is_pure_damage_style 辅助方法**

在 CombatEngine.gd 末尾（`_check_end()` 之后）添加：

```gdscript
func _is_pure_damage_style(card: CardData) -> bool:
	if card.card_type != "招式" or card.effects.is_empty():
		return false
	for effect: CardEffectData in card.effects:
		if effect.type != "damage":
			return false
	return true
```

- [ ] **Step 4: 验证无解析错误**

```
"D:/steam/steamapps/common/Godot Engine/godot.windows.opt.tools.64.exe" --headless --path "E:/MyWork/AIGameTest2" --quit-after 1 2>&1
```

预期：无错误。

- [ ] **Step 5: 提交**

```bash
git add CombatEngine.gd
git commit -m "feat: CombatEngine populates _boss_copied_cards on setup for copies_player_cards enemies"
```

---

### Task 4: CombatEngine — _resolve_enemy_actions 扩展

**Files:**
- Modify: `E:/MyWork/AIGameTest2/CombatEngine.gd`

背景：_resolve_enemy_actions() 需要两处扩展：（1）每N回合周期覆盖：若 periodic_interval > 0 且 turn_number % periodic_interval == 0，用 periodic_action 替换本回合动作；（2）mirror_attack 预解析：从 _boss_copied_cards 随机选一张牌，提取其伤害值和牌名，存入临时 EnemyActionData 的 value 和 display_label。

- [ ] **Step 1: 重写 _resolve_enemy_actions()**

用以下完整版本替换现有函数（注意：periodic override 在所有其他逻辑之前执行）：

```gdscript
func _resolve_enemy_actions() -> void:
	_pending_actions.resize(enemies.size())
	for i in enemies.size():
		if enemies[i].hp <= 0:
			_pending_actions[i] = null
			continue
		var data: EnemyData = _enemy_data_list[i]
		if data.periodic_interval > 0 and data.periodic_action != null \
				and turn_number % data.periodic_interval == 0:
			_pending_actions[i] = data.periodic_action
			continue
		var use_phase2: bool = (
			data.phase2_threshold > 0.0
			and not data.phase2_actions.is_empty()
			and float(enemies[i].hp) / float(data.hp) <= data.phase2_threshold
		)
		var action_list: Array[EnemyActionData] = data.phase2_actions if use_phase2 else data.actions
		var action: EnemyActionData
		if data.random_actions:
			action = _weighted_random_action(action_list)
		else:
			action = action_list[(turn_number - 1) % action_list.size()]
		if enemies[i].is_charging and action.type in ATTACK_TYPES_FOR_CHARGE:
			var doubled := EnemyActionData.new()
			doubled.type = action.type
			doubled.value = action.value * 2
			doubled.count = action.count
			_pending_actions[i] = doubled
		else:
			_pending_actions[i] = action
		if _pending_actions[i].type == "mirror_attack":
			var resolved := EnemyActionData.new()
			resolved.type = "mirror_attack"
			if _boss_copied_cards.is_empty():
				resolved.value = 8
				resolved.display_label = "攻击"
			else:
				var chosen: CardData = _boss_copied_cards[randi() % _boss_copied_cards.size()]
				resolved.value = chosen.effects[0].value
				resolved.display_label = chosen.card_name
			_pending_actions[i] = resolved
```

- [ ] **Step 2: 验证无解析错误**

```
"D:/steam/steamapps/common/Godot Engine/godot.windows.opt.tools.64.exe" --headless --path "E:/MyWork/AIGameTest2" --quit-after 1 2>&1
```

预期：无错误。

- [ ] **Step 3: 提交**

```bash
git add CombatEngine.gd
git commit -m "feat: _resolve_enemy_actions supports periodic override and mirror_attack pre-resolution"
```

---

### Task 5: CombatEngine — _do_enemy_turn 新动作类型 + 第二阶段被动

**Files:**
- Modify: `E:/MyWork/AIGameTest2/CombatEngine.gd`

背景：_do_enemy_turn() 需要：（1）三个新 match case：mirror_attack / xin_mo_fanshi / attack_zahuorumuo；（2）match 块之后追加第二阶段被动：若当前敌人的 copies_player_cards == true 且 HP < 50%，自失3HP + 获得3力量；（3）新辅助方法 _add_zahuorumuo_to_discard。

- [ ] **Step 1: 在 _do_enemy_turn 的 match 块中添加三个 case**

在 `"revive_minions":` case 之后、`_:` default 之前插入：

```gdscript
			"mirror_attack":
				_enemy_attack(enemies[i], action.value)
			"xin_mo_fanshi":
				var valid: Array[int] = []
				for idx in hand.size():
					if not hand[idx].is_curse and not hand[idx].is_zahuorumuo:
						valid.append(idx)
				if not valid.is_empty():
					hand[valid[randi() % valid.size()]] = CURSE_CARD.duplicate()
			"attack_zahuorumuo":
				_enemy_attack(enemies[i], action.value)
				_add_zahuorumuo_to_discard(action.count)
```

- [ ] **Step 2: 添加第二阶段被动（紧接 match 块之后，在 for 循环内）**

在 `_:` default case 和最终 for 循环的 `}` 之间，match 块闭合后添加：

```gdscript
		if _enemy_data_list[i].copies_player_cards \
				and float(enemies[i].hp) / float(enemies[i].max_hp) < 0.5 \
				and enemies[i].hp > 0:
			enemies[i].hp = max(0, enemies[i].hp - 3)
			enemies[i].strength += 3
```

完整的 `_do_enemy_turn` 循环体（for i in enemies.size()）结构如下：

```gdscript
func _do_enemy_turn() -> void:
	for i in enemies.size():
		if enemies[i].hp <= 0:
			continue
		enemies[i].block = 0
		enemies[i].vulnerable = max(0, enemies[i].vulnerable - 1)
		var action: EnemyActionData = get_enemy_action(i)
		enemies[i].is_charging = false
		match action.type:
			"attack":
				_enemy_attack(enemies[i], action.value)
			# ... 所有现有 case ...
			"mirror_attack":
				_enemy_attack(enemies[i], action.value)
			"xin_mo_fanshi":
				var valid: Array[int] = []
				for idx in hand.size():
					if not hand[idx].is_curse and not hand[idx].is_zahuorumuo:
						valid.append(idx)
				if not valid.is_empty():
					hand[valid[randi() % valid.size()]] = CURSE_CARD.duplicate()
			"attack_zahuorumuo":
				_enemy_attack(enemies[i], action.value)
				_add_zahuorumuo_to_discard(action.count)
			_:
				enemies[i].add_block(action.value)
		if _enemy_data_list[i].copies_player_cards \
				and float(enemies[i].hp) / float(enemies[i].max_hp) < 0.5 \
				and enemies[i].hp > 0:
			enemies[i].hp = max(0, enemies[i].hp - 3)
			enemies[i].strength += 3
```

- [ ] **Step 3: 添加 _add_zahuorumuo_to_discard 辅助方法**

在 `_add_venoms_to_draw` 之后添加：

```gdscript
func _add_zahuorumuo_to_discard(n: int) -> void:
	for _j in n:
		_discard_pile.append(ZAHUORUMUO_CARD.duplicate())
```

- [ ] **Step 4: 验证无解析错误**

```
"D:/steam/steamapps/common/Godot Engine/godot.windows.opt.tools.64.exe" --headless --path "E:/MyWork/AIGameTest2" --quit-after 1 2>&1
```

预期：无错误。

- [ ] **Step 5: 提交**

```bash
git add CombatEngine.gd
git commit -m "feat: _do_enemy_turn adds mirror_attack/xin_mo_fanshi/attack_zahuorumuo; phase2 self-drain passive"
```

---

### Task 6: CombatEngine — end_turn 走火入魔弃牌效果

**Files:**
- Modify: `E:/MyWork/AIGameTest2/CombatEngine.gd`

背景：回合结束时，若手中有走火入魔，每张走火入魔随机弃掉1张其他非诅咒手牌（is_zahuorumuo 和 is_curse 都排除在受害者之外）。多张走火入魔依次处理。处理完后走火入魔本身随正常手牌弃掉。

- [ ] **Step 1: 在 end_turn() 中插入走火入魔处理逻辑**

在毒液 HP 扣除块之后、`for card: CardData in hand: _discard_pile.append(card)` 之前插入：

```gdscript
func end_turn() -> void:
	var venom_count: int = 0
	for card: CardData in hand:
		if card.is_venom:
			venom_count += 1
	if venom_count > 0:
		player.hp = max(0, player.hp - venom_count)
		player_damaged.emit(venom_count)
		state_changed.emit()
		if _check_end():
			return
	var zao_count: int = 0
	for card: CardData in hand:
		if card.is_zahuorumuo:
			zao_count += 1
	for _z in zao_count:
		var victims: Array[int] = []
		for idx in hand.size():
			if not hand[idx].is_zahuorumuo and not hand[idx].is_curse:
				victims.append(idx)
		if not victims.is_empty():
			var victim_idx: int = victims[randi() % victims.size()]
			_discard_pile.append(hand[victim_idx])
			hand.remove_at(victim_idx)
	for card: CardData in hand:
		_discard_pile.append(card)
	hand.clear()
	if not player.sword_intent_retain:
		player.sword_intent = player.sword_intent / 2
	RelicEngine.apply_turn_end(_relics, self)
	_do_enemy_turn()
	state_changed.emit()
	if not _check_end():
		_start_player_turn()
```

- [ ] **Step 2: 验证无解析错误**

```
"D:/steam/steamapps/common/Godot Engine/godot.windows.opt.tools.64.exe" --headless --path "E:/MyWork/AIGameTest2" --quit-after 1 2>&1
```

预期：无错误。

- [ ] **Step 3: 提交**

```bash
git add CombatEngine.gd
git commit -m "feat: end_turn handles is_zahuorumuo random hand discard before clearing hand"
```

---

### Task 7: CombatScreen — 意图文字

**Files:**
- Modify: `E:/MyWork/AIGameTest2/combat/CombatScreen.gd`

背景：_intent_text() 需要三个新 case。mirror_attack 使用 action.display_label（牌名）和 val（已含力量/虚弱修正的伤害值）。

- [ ] **Step 1: 在 _intent_text() 的 match 块中添加三个 case**

在 `"revive_minions":` 之后、`_:` default 之前插入：

```gdscript
		"mirror_attack":
			return "意图：%s %d" % [action.display_label, val]
		"xin_mo_fanshi":
			return "意图：心魔反噬"
		"attack_zahuorumuo":
			return "意图：攻击 %d + 走火入魔 ×%d" % [val, action.count]
```

- [ ] **Step 2: 验证无解析错误**

```
"D:/steam/steamapps/common/Godot Engine/godot.windows.opt.tools.64.exe" --headless --path "E:/MyWork/AIGameTest2" --quit-after 1 2>&1
```

预期：无错误。

- [ ] **Step 3: 提交**

```bash
git add combat/CombatScreen.gd
git commit -m "feat: CombatScreen intent text for mirror_attack/xin_mo_fanshi/attack_zahuorumuo"
```

---

### Task 8: Boss 数据文件

**Files:**
- Create: `E:/MyWork/AIGameTest2/data/enemies/boss_xin_mo_jie.tres`
- Modify: `E:/MyWork/AIGameTest2/data/enemy_groups/single_boss.tres`

背景：心魔劫 EnemyData 文件包含三个 sub_resource（mirror_attack 动作、xin_mo_fanshi 周期动作、attack_zahuorumuo 第三阶段动作）。single_boss.tres 更新引用。

- [ ] **Step 1: 创建 boss_xin_mo_jie.tres**

```
[gd_resource type="Resource" script_class="EnemyData" load_steps=5 format=3]

[ext_resource type="Script" path="res://EnemyData.gd" id="1_EnemyData"]
[ext_resource type="Script" path="res://EnemyActionData.gd" id="2_EnemyActionData"]

[sub_resource type="Resource" id="Action_mirror"]
script = ExtResource("2_EnemyActionData")
type = "mirror_attack"

[sub_resource type="Resource" id="Action_periodic"]
script = ExtResource("2_EnemyActionData")
type = "xin_mo_fanshi"

[sub_resource type="Resource" id="Action_p3"]
script = ExtResource("2_EnemyActionData")
type = "attack_zahuorumuo"
value = 12
count = 1

[resource]
script = ExtResource("1_EnemyData")
display_name = "心魔劫"
hp = 270
copies_player_cards = true
periodic_interval = 3
periodic_action = SubResource("Action_periodic")
actions = Array[ExtResource("2_EnemyActionData")]([SubResource("Action_mirror")])
phase2_threshold = 0.3
phase2_actions = Array[ExtResource("2_EnemyActionData")]([SubResource("Action_p3")])
```

- [ ] **Step 2: 更新 single_boss.tres**

用以下内容完全替换（将引用从 boss.tres 改为 boss_xin_mo_jie.tres）：

```
[gd_resource type="Resource" script_class="EnemyGroupData" load_steps=4 format=3]

[ext_resource type="Script" path="res://EnemyGroupData.gd" id="1_GroupData"]
[ext_resource type="Script" path="res://EnemyData.gd" id="2_EnemyData"]
[ext_resource type="Resource" path="res://data/enemies/boss_xin_mo_jie.tres" id="3_boss"]

[resource]
script = ExtResource("1_GroupData")
enemies = Array[ExtResource("2_EnemyData")]([ExtResource("3_boss")])
```

- [ ] **Step 3: 验证无解析错误**

```
"D:/steam/steamapps/common/Godot Engine/godot.windows.opt.tools.64.exe" --headless --path "E:/MyWork/AIGameTest2" --quit-after 1 2>&1
```

预期：无错误。

- [ ] **Step 4: 功能验收——打开 Debug 模式**

启动游戏，进入"测试战斗"，选择剑修牌组（含有穿心/裂空等招式牌），进入 Boss 战。验证：
1. 第1回合意图显示具体招式名和数值，例如"意图：裂空 10"
2. 第3回合意图显示"意图：心魔反噬"
3. 心魔反噬执行后手牌中有1张变为心魔（占格、无法打出）
4. Boss HP 降至 50% 以下后开始每回合增加力量（意图伤害数值逐渐增大）
5. Boss HP 降至 30% 以下后意图变为"意图：攻击 12 + 走火入魔 ×1"
6. 走火入魔出现在弃牌堆，抽到后占手牌格
7. 手持走火入魔结束回合时，另一张手牌被弃掉

- [ ] **Step 5: 提交**

```bash
git add data/enemies/boss_xin_mo_jie.tres data/enemy_groups/single_boss.tres
git commit -m "feat: add boss_xin_mo_jie.tres and update single_boss group"
```

---

### Task 9: 版本号 + 开发日志

**Files:**
- Modify: `E:/MyWork/AIGameTest2/menu/MainMenu.tscn`
- Create: `E:/MyWork/Devlog/AIGameTest2_v0.61.0_2026-05-04.md`

- [ ] **Step 1: 更新版本号**

在 `E:/MyWork/AIGameTest2/menu/MainMenu.tscn` 中将：

```
text = "v0.60.0"
```

改为：

```
text = "v0.61.0"
```

- [ ] **Step 2: 写开发日志**

创建 `E:/MyWork/Devlog/AIGameTest2_v0.61.0_2026-05-04.md`，内容：

```markdown
# AIGameTest2 v0.61.0 开发日志

**日期：** 2026-05-04
**版本：** v0.61.0

## 本版本内容

### 新增：心魔劫 Boss
- HP=270，单体 Boss，筑基境最终关卡
- 核心机制：战斗开始时复制玩家牌组中最多5张纯伤害招式牌，每回合随机选1张使用，意图显示原牌名和伤害值
- 心魔反噬：每3回合将玩家手中随机1张非诅咒牌变为心魔，全程触发
- 自我崩解：HP < 50% 后每回合自失3HP + 获得3力量
- 第三阶段（HP < 30%）：改为每回合攻击12 + 塞1张走火入魔进弃牌堆

### 新增：走火入魔诅咒牌
- 无法打出，摸到时留在手中（占用手牌格）
- 回合结束：随机弃掉手中另1张手牌；若无其他牌则跳过
- 走火入魔本身于回合结束后进入弃牌堆

### 引擎扩展
- EnemyData：新增 copies_player_cards / periodic_interval / periodic_action
- EnemyActionData：新增运行时字段 display_label（用于意图显示）
- CardData：新增 is_zahuorumuo
- CombatEngine：_resolve_enemy_actions 支持 periodic 周期覆盖；mirror_attack 预解析；end_turn 处理走火入魔弃牌；第二阶段被动
```

- [ ] **Step 3: 提交**

```bash
git add menu/MainMenu.tscn
git commit -m "feat: bump version to v0.61.0"
```

---
