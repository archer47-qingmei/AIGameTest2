# v0.4.0 CardEffectData 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 `CardData` 的 `damage`/`block` 硬编码字段替换为 `effects: Array[CardEffectData]`，游戏行为与 v0.3.0 完全一致。

**Architecture:** 新建 `CardEffectData` Resource 子类（type/value），`CardData` 改为持有效果数组，`EffectResolver.resolve()` 改为遍历数组，三张卡牌 .tres 文件改用 sub_resource 格式，`CombatScreen._rebuild_hand()` 从 effects 提取值用于显示。Task 1 独立新增 CardEffectData，Task 2 原子替换所有耦合文件（CardData、EffectResolver、CombatScreen、.tres），避免中间状态破坏解析器。

**Tech Stack:** Godot 4.6, GDScript strict 类型模式，Godot Resource 系统（.tres）

---

## 文件变更一览

| 操作 | 文件 | 职责 |
|------|------|------|
| 新建 | `res://CardEffectData.gd` | 单条卡牌效果的数据蓝图（type/value） |
| 新建 | `res://CardEffectData.gd.uid` | Godot 4.4+ 资源 UID 文件 |
| 修改 | `res://CardData.gd` | 移除 damage/block，新增 effects: Array[CardEffectData] |
| 修改 | `res://EffectResolver.gd` | resolve() 改为遍历 card.effects |
| 修改 | `res://combat/CombatScreen.gd` | _rebuild_hand() 从 effects 提取 damage/block 显示值 |
| 修改 | `res://data/cards/strike.tres` | 改用 sub_resource 格式（damage:6） |
| 修改 | `res://data/cards/defend.tres` | 改用 sub_resource 格式（block:5） |
| 修改 | `res://data/cards/bash.tres` | 改用 sub_resource 格式（damage:8） |
| 修改 | `.godot/global_script_class_cache.cfg` | 注册 CardEffectData |
| 不动 | `Combatant.gd`、`CombatEngine.gd`、`EnemyActionData.gd`、`EnemyData.gd`、`CombatScreen.tscn`、`data/enemies/jaw_worm.tres` | — |

---

## Task 1：新建 CardEffectData.gd（含注册）

**Files:**
- Create: `e:/MyWork/AIGameTest2/CardEffectData.gd`
- Create: `e:/MyWork/AIGameTest2/CardEffectData.gd.uid`
- Modify: `e:/MyWork/AIGameTest2/.godot/global_script_class_cache.cfg`

- [ ] **Step 1：创建 CardEffectData.gd**

创建 `e:/MyWork/AIGameTest2/CardEffectData.gd`：

```gdscript
class_name CardEffectData
extends Resource

@export var type: String
@export var value: int
```

- [ ] **Step 2：创建 CardEffectData.gd.uid**

创建 `e:/MyWork/AIGameTest2/CardEffectData.gd.uid`，内容为一行 UID：

```
uid://cxv8nrm4tp7dk
```

- [ ] **Step 3：更新 global_script_class_cache.cfg**

将 `e:/MyWork/AIGameTest2/.godot/global_script_class_cache.cfg` 完整替换为以下内容（在现有 6 条记录末尾追加 CardEffectData，并按 class 名字母顺序插入 CardData 之后）：

```
list=[{
"base": &"Resource",
"class": &"CardData",
"icon": "",
"is_abstract": false,
"is_tool": false,
"language": &"GDScript",
"path": "res://CardData.gd"
}, {
"base": &"Resource",
"class": &"CardEffectData",
"icon": "",
"is_abstract": false,
"is_tool": false,
"language": &"GDScript",
"path": "res://CardEffectData.gd"
}, {
"base": &"RefCounted",
"class": &"CombatEngine",
"icon": "",
"is_abstract": false,
"is_tool": false,
"language": &"GDScript",
"path": "res://CombatEngine.gd"
}, {
"base": &"RefCounted",
"class": &"Combatant",
"icon": "",
"is_abstract": false,
"is_tool": false,
"language": &"GDScript",
"path": "res://Combatant.gd"
}, {
"base": &"RefCounted",
"class": &"EffectResolver",
"icon": "",
"is_abstract": false,
"is_tool": false,
"language": &"GDScript",
"path": "res://EffectResolver.gd"
}, {
"base": &"Resource",
"class": &"EnemyActionData",
"icon": "",
"is_abstract": false,
"is_tool": false,
"language": &"GDScript",
"path": "res://EnemyActionData.gd"
}, {
"base": &"Resource",
"class": &"EnemyData",
"icon": "",
"is_abstract": false,
"is_tool": false,
"language": &"GDScript",
"path": "res://EnemyData.gd"
}]
```

- [ ] **Step 4：运行项目验证无报错**

```
mcp__godot__run_project(projectPath="e:/MyWork/AIGameTest2")
```

等待约 3 秒后调用 `mcp__godot__get_debug_output`，确认无 `SCRIPT ERROR:` 或 `Parser Error:`。

- [ ] **Step 5：停止项目**

```
mcp__godot__stop_project()
```

- [ ] **Step 6：提交**

```bash
git add CardEffectData.gd CardEffectData.gd.uid .godot/global_script_class_cache.cfg
git commit -m "feat: add CardEffectData resource class"
```

---

## Task 2：原子替换——CardData + EffectResolver + CombatScreen + .tres 文件

> **说明：** 这五类文件必须在同一步骤内完成修改再运行项目。原因：`CardData` 移除 `damage`/`block` 字段后，GDScript 严格类型模式下 `EffectResolver` 和 `CombatScreen` 访问这两个字段会产生解析器报错，无法启动。

**Files:**
- Modify: `e:/MyWork/AIGameTest2/CardData.gd`
- Modify: `e:/MyWork/AIGameTest2/EffectResolver.gd`
- Modify: `e:/MyWork/AIGameTest2/combat/CombatScreen.gd`
- Modify: `e:/MyWork/AIGameTest2/data/cards/strike.tres`
- Modify: `e:/MyWork/AIGameTest2/data/cards/defend.tres`
- Modify: `e:/MyWork/AIGameTest2/data/cards/bash.tres`

- [ ] **Step 1：替换 CardData.gd**

将 `e:/MyWork/AIGameTest2/CardData.gd` 完整替换为：

```gdscript
class_name CardData
extends Resource

@export var card_name: String
@export var cost: int
@export var effects: Array[CardEffectData]
```

- [ ] **Step 2：替换 EffectResolver.gd**

将 `e:/MyWork/AIGameTest2/EffectResolver.gd` 完整替换为：

```gdscript
class_name EffectResolver
extends RefCounted

static func resolve(card: CardData, attacker: Combatant, defender: Combatant) -> void:
	for effect: CardEffectData in card.effects:
		if effect.type == "damage":
			defender.take_damage(effect.value)
		elif effect.type == "block":
			attacker.add_block(effect.value)
```

- [ ] **Step 3：替换 CombatScreen.gd 的 _rebuild_hand()**

将 `e:/MyWork/AIGameTest2/combat/CombatScreen.gd` 中的 `_rebuild_hand()` 函数（第 39–48 行）替换为：

```gdscript
func _rebuild_hand() -> void:
	for btn: Button in _hand_buttons:
		btn.queue_free()
	_hand_buttons.clear()
	for card: CardData in _engine.hand:
		var btn: Button = Button.new()
		var dmg: int = 0
		var blk: int = 0
		for effect: CardEffectData in card.effects:
			if effect.type == "damage":
				dmg = effect.value
			elif effect.type == "block":
				blk = effect.value
		btn.text = "%s\n费用:%d  攻:%d  挡:%d" % [card.card_name, card.cost, dmg, blk]
		btn.pressed.connect(_on_card_pressed.bind(card))
		_hand_container.add_child(btn)
		_hand_buttons.append(btn)
```

改完后完整的 `CombatScreen.gd` 应为：

```gdscript
extends Control

@onready var _lbl_enemy_name: Label    = $VBoxContainer/EnemyPanel/LblEnemyName
@onready var _lbl_enemy_hp: Label      = $VBoxContainer/EnemyPanel/LblEnemyHP
@onready var _lbl_enemy_block: Label   = $VBoxContainer/EnemyPanel/LblEnemyBlock
@onready var _lbl_enemy_intent: Label  = $VBoxContainer/EnemyPanel/LblEnemyIntent
@onready var _lbl_player_hp: Label     = $VBoxContainer/PlayerPanel/LblPlayerHP
@onready var _lbl_player_block: Label  = $VBoxContainer/PlayerPanel/LblPlayerBlock
@onready var _lbl_energy: Label        = $VBoxContainer/PlayerPanel/LblEnergy
@onready var _hand_container: HBoxContainer = $VBoxContainer/HandContainer
@onready var _btn_end_turn: Button     = $VBoxContainer/BtnEndTurn
@onready var _lbl_result: Label        = $LblResult

var _engine: CombatEngine
var _hand_buttons: Array[Button] = []

func _ready() -> void:
	_engine = CombatEngine.new()
	_engine.state_changed.connect(_refresh_ui)
	_engine.combat_ended.connect(_on_combat_ended)
	_btn_end_turn.pressed.connect(_engine.end_turn)
	_engine.setup()

func _on_card_pressed(card: CardData) -> void:
	_engine.play_card(card)

func _refresh_ui() -> void:
	_lbl_enemy_name.text = _engine.enemy.display_name
	_lbl_enemy_hp.text = "生命：%d / %d" % [_engine.enemy.hp, _engine.enemy.max_hp]
	_lbl_enemy_block.text = "格挡：%d" % _engine.enemy.block
	var action: EnemyActionData = _engine.get_current_enemy_action()
	var intent: String = "攻击 %d" % action.value if action.type == "attack" else "格挡 %d" % action.value
	_lbl_enemy_intent.text = "意图：" + intent
	_lbl_player_hp.text = "生命：%d / %d" % [_engine.player.hp, _engine.player.max_hp]
	_lbl_player_block.text = "格挡：%d" % _engine.player.block
	_lbl_energy.text = "能量：%d / 3" % _engine.energy
	_rebuild_hand()

func _rebuild_hand() -> void:
	for btn: Button in _hand_buttons:
		btn.queue_free()
	_hand_buttons.clear()
	for card: CardData in _engine.hand:
		var btn: Button = Button.new()
		var dmg: int = 0
		var blk: int = 0
		for effect: CardEffectData in card.effects:
			if effect.type == "damage":
				dmg = effect.value
			elif effect.type == "block":
				blk = effect.value
		btn.text = "%s\n费用:%d  攻:%d  挡:%d" % [card.card_name, card.cost, dmg, blk]
		btn.pressed.connect(_on_card_pressed.bind(card))
		_hand_container.add_child(btn)
		_hand_buttons.append(btn)

func _on_combat_ended(result: String) -> void:
	_lbl_result.text = "胜利！" if result == "victory" else "游戏结束"
	_lbl_result.visible = true
	_btn_end_turn.disabled = true
	for btn: Button in _hand_buttons:
		btn.disabled = true
```

- [ ] **Step 4：替换 data/cards/strike.tres**

将 `e:/MyWork/AIGameTest2/data/cards/strike.tres` 完整替换为：

```
[gd_resource type="Resource" script_class="CardData" load_steps=4 format=3]

[ext_resource type="Script" uid="uid://13mprie5g221" path="res://CardData.gd" id="1_CardData"]
[ext_resource type="Script" uid="uid://cxv8nrm4tp7dk" path="res://CardEffectData.gd" id="2_CardEffectData"]

[sub_resource type="Resource" id="Effect_damage"]
script = ExtResource("2_CardEffectData")
type = "damage"
value = 6

[resource]
script = ExtResource("1_CardData")
card_name = "打击"
cost = 1
effects = [SubResource("Effect_damage")]
```

- [ ] **Step 5：替换 data/cards/defend.tres**

将 `e:/MyWork/AIGameTest2/data/cards/defend.tres` 完整替换为：

```
[gd_resource type="Resource" script_class="CardData" load_steps=4 format=3]

[ext_resource type="Script" uid="uid://13mprie5g221" path="res://CardData.gd" id="1_CardData"]
[ext_resource type="Script" uid="uid://cxv8nrm4tp7dk" path="res://CardEffectData.gd" id="2_CardEffectData"]

[sub_resource type="Resource" id="Effect_block"]
script = ExtResource("2_CardEffectData")
type = "block"
value = 5

[resource]
script = ExtResource("1_CardData")
card_name = "防御"
cost = 1
effects = [SubResource("Effect_block")]
```

- [ ] **Step 6：替换 data/cards/bash.tres**

将 `e:/MyWork/AIGameTest2/data/cards/bash.tres` 完整替换为：

```
[gd_resource type="Resource" script_class="CardData" load_steps=4 format=3]

[ext_resource type="Script" uid="uid://13mprie5g221" path="res://CardData.gd" id="1_CardData"]
[ext_resource type="Script" uid="uid://cxv8nrm4tp7dk" path="res://CardEffectData.gd" id="2_CardEffectData"]

[sub_resource type="Resource" id="Effect_damage"]
script = ExtResource("2_CardEffectData")
type = "damage"
value = 8

[resource]
script = ExtResource("1_CardData")
card_name = "重击"
cost = 2
effects = [SubResource("Effect_damage")]
```

- [ ] **Step 7：运行项目验证游戏行为**

```
mcp__godot__run_project(projectPath="e:/MyWork/AIGameTest2")
```

等待约 3 秒后调用 `mcp__godot__get_debug_output`，确认：
- 无 `SCRIPT ERROR:` 或 `Parser Error:`（icon.svg 警告可忽略）
- 游戏正常启动进入战斗场景

- [ ] **Step 8：停止项目**

```
mcp__godot__stop_project()
```

- [ ] **Step 9：提交**

```bash
git add CardData.gd EffectResolver.gd combat/CombatScreen.gd data/cards/strike.tres data/cards/defend.tres data/cards/bash.tres
git commit -m "refactor: replace CardData damage/block fields with effects: Array[CardEffectData]"
```

---

## Task 3：验收架构约束并打 v0.4.0 标签

**Files:** 无文件修改

- [ ] **Step 1：确认 CardData.gd 不含旧字段**

使用 Grep 工具在 `e:/MyWork/AIGameTest2/CardData.gd` 中搜索以下关键字，预期均无匹配：
- `damage`
- `block`

- [ ] **Step 2：确认 EffectResolver.gd 遍历 effects**

使用 Grep 工具在 `e:/MyWork/AIGameTest2/EffectResolver.gd` 中搜索 `card.effects`，预期有 1 条匹配。

- [ ] **Step 3：确认三张卡牌 .tres 文件使用新格式**

使用 Read 工具读取以下文件，确认每个文件都包含 `sub_resource` 和 `effects =` 字段，不含 `damage =` 或 `block =` 顶层字段：
- `e:/MyWork/AIGameTest2/data/cards/strike.tres`
- `e:/MyWork/AIGameTest2/data/cards/defend.tres`
- `e:/MyWork/AIGameTest2/data/cards/bash.tres`

- [ ] **Step 4：打 v0.4.0 标签**

```bash
git tag v0.4.0
git log --oneline -5
```

---

## 验收总结

完成所有 Task 后，以下标准应全部满足：

- [ ] 项目无报错启动，直接进入战斗场景
- [ ] 游戏行为与 v0.3.0 完全一致（颚虫 44HP、攻 11/挡 6 交替、起手 5 张）
- [ ] `CardData.gd` 不含 `damage` 或 `block` 字段
- [ ] `EffectResolver.resolve()` 遍历 `card.effects` 而非直接读字段
- [ ] 三张卡牌 .tres 文件均使用 sub_resource 格式，含 `effects =` 字段
- [ ] 全部 v0.3.0 验收标准继续通过
