# v0.19.0 连线地图 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将线性节点序列升级为分支图结构，MapScreen 展示节点按钮与连线，玩家可选择路径。

**Architecture:** NodeData 新增 `connections: Array[NodeData]` 构成有向图；PlayerState 以 `available_nodes`/`completed_nodes`/`current_node` 追踪图导航状态；MapScreen 全部重写，用 `_draw()` 绘制连线、动态按钮响应点击。GameManager 新增 `select_node()` 并修改 `go_to_map()` 推进状态。

**Tech Stack:** Godot 4.4, GDScript 严格类型

---

## 文件变更一览

| 操作 | 文件 | 变更摘要 |
|---|---|---|
| 修改 | `e:/MyWork/AIGameTest2/NodeData.gd` | 新增 connections/col/row 字段 |
| 修改 | `e:/MyWork/AIGameTest2/PlayerState.gd` | 移除线性字段，新增图追踪字段 |
| 修改 | `e:/MyWork/AIGameTest2/GameManager.gd` | 构建分支图、新增 select_node()、修改 go_to_map() 等 |
| 修改 | `e:/MyWork/AIGameTest2/game_over/GameOverScreen.gd` | 改用 completed_nodes.size() |
| 修改 | `e:/MyWork/AIGameTest2/map/MapScreen.tscn` | 简化为 LblTitle + LblHP |
| 修改 | `e:/MyWork/AIGameTest2/map/MapScreen.gd` | 全部重写：动态按钮 + _draw() 连线 |

---

## Task 1：NodeData.gd — 新增图字段

**Files:**
- Modify: `e:/MyWork/AIGameTest2/NodeData.gd`

> NodeData 是后续所有改动的基础类型。本 Task 只新增字段，向后兼容，不影响任何现有代码。

- [ ] **Step 1：更新 NodeData.gd**

将 `e:/MyWork/AIGameTest2/NodeData.gd` 完整内容替换为（缩进使用 Tab）：

```gdscript
class_name NodeData
extends Resource

enum Type { COMBAT, REST }

var type: Type = Type.COMBAT
var enemy_data: EnemyData
var connections: Array[NodeData] = []
var col: int = 0
var row: int = 0
```

- [ ] **Step 2：运行项目确认无报错**

使用 Godot MCP 工具：`mcp__godot__run_project`（projectPath: `e:/MyWork/AIGameTest2`），然后 `mcp__godot__get_debug_output`。

期望：errors 数组中只有 `icon.svg` 相关错误（这是旧有问题），无 GDScript 解析错误。

停止项目：`mcp__godot__stop_project`。

- [ ] **Step 3：提交**

```bash
git -C "e:/MyWork/AIGameTest2" add NodeData.gd
git -C "e:/MyWork/AIGameTest2" commit -m "refactor: add connections/col/row to NodeData for branching map"
```

---

## Task 2：PlayerState.gd + GameManager.gd — 替换线性追踪为图追踪

**Files:**
- Modify: `e:/MyWork/AIGameTest2/PlayerState.gd`
- Modify: `e:/MyWork/AIGameTest2/GameManager.gd`

> PlayerState 移除 `node_sequence`/`current_node:int`，改为图追踪字段。GameManager 同步更新以消除对旧字段的引用。两个文件必须同一 commit，否则中间状态会有运行时错误。

- [ ] **Step 1：更新 PlayerState.gd**

将 `e:/MyWork/AIGameTest2/PlayerState.gd` 完整内容替换为（缩进使用 Tab）：

```gdscript
class_name PlayerState
extends RefCounted

var deck: Array[CardData] = []
var hp: int = 70
var max_hp: int = 70
var map_all_nodes: Array[NodeData] = []
var available_nodes: Array[NodeData] = []
var completed_nodes: Array[NodeData] = []
var current_node: NodeData = null
```

- [ ] **Step 2：更新 GameManager.gd**

将 `e:/MyWork/AIGameTest2/GameManager.gd` 完整内容替换为（缩进使用 Tab）：

```gdscript
extends Node

enum Phase { MENU, MAP, COMBAT, REWARD, REST, WIN, GAME_OVER }

func _ready() -> void:
	var font: FontFile = load("res://data/fonts/NotoSansSC-Regular.otf") as FontFile
	if font:
		var theme: Theme = Theme.new()
		theme.default_font = font
		theme.default_font_size = 16
		get_tree().root.theme = theme

const NORMAL_POOL: Array[String] = [
	"res://data/enemies/jaw_worm.tres",
	"res://data/enemies/fire_lizard.tres",
]
const BOSS_ENEMY: String = "res://data/enemies/boss.tres"

var current_phase: Phase = Phase.MENU
var player_state: PlayerState
var last_rest_heal: int = 0

func start_new_run() -> void:
	player_state = PlayerState.new()
	var strike: CardData = preload("res://data/cards/strike.tres")
	var defend: CardData = preload("res://data/cards/defend.tres")
	var bash: CardData   = preload("res://data/cards/bash.tres")
	var slash: CardData  = preload("res://data/cards/slash.tres")
	for i in 4:
		player_state.deck.append(strike.duplicate())
	for i in 4:
		player_state.deck.append(defend.duplicate())
	player_state.deck.append(bash.duplicate())
	player_state.deck.append(slash.duplicate())

	var a1: NodeData = NodeData.new()
	a1.type = NodeData.Type.COMBAT
	a1.enemy_data = load(NORMAL_POOL[0]) as EnemyData
	a1.col = 0; a1.row = 0

	var a2: NodeData = NodeData.new()
	a2.type = NodeData.Type.COMBAT
	a2.enemy_data = load(NORMAL_POOL[1]) as EnemyData
	a2.col = 0; a2.row = 1

	var b1: NodeData = NodeData.new()
	b1.type = NodeData.Type.REST
	b1.col = 1; b1.row = 0

	var b2: NodeData = NodeData.new()
	b2.type = NodeData.Type.COMBAT
	b2.enemy_data = load(NORMAL_POOL[0]) as EnemyData
	b2.col = 1; b2.row = 1

	var c1: NodeData = NodeData.new()
	c1.type = NodeData.Type.COMBAT
	c1.enemy_data = load(BOSS_ENEMY) as EnemyData
	c1.col = 2; c1.row = 0

	a1.connections.assign([b1, b2])
	a2.connections.assign([b1, b2])
	b1.connections.assign([c1])
	b2.connections.assign([c1])

	player_state.map_all_nodes.assign([a1, a2, b1, b2, c1])
	player_state.available_nodes.assign([a1, a2])

	current_phase = Phase.MAP
	get_tree().change_scene_to_file("res://map/MapScreen.tscn")

func select_node(node: NodeData) -> void:
	player_state.current_node = node
	if node.type == NodeData.Type.REST:
		go_to_rest()
	else:
		go_to_combat()

func go_to_combat() -> void:
	current_phase = Phase.COMBAT
	get_tree().change_scene_to_file("res://combat/CombatScreen.tscn")

func go_to_reward() -> void:
	current_phase = Phase.REWARD
	get_tree().change_scene_to_file("res://reward/RewardScreen.tscn")

func go_to_rest() -> void:
	last_rest_heal = int(player_state.max_hp * 0.3)
	player_state.hp = mini(player_state.hp + last_rest_heal, player_state.max_hp)
	current_phase = Phase.REST
	get_tree().change_scene_to_file("res://rest/RestScreen.tscn")

func go_to_map() -> void:
	if player_state.current_node != null:
		player_state.completed_nodes.append(player_state.current_node)
		player_state.available_nodes.assign(player_state.current_node.connections)
		player_state.current_node = null
	current_phase = Phase.MAP
	get_tree().change_scene_to_file("res://map/MapScreen.tscn")

func go_to_win() -> void:
	current_phase = Phase.WIN
	get_tree().change_scene_to_file("res://win/WinScreen.tscn")

func go_to_game_over() -> void:
	current_phase = Phase.GAME_OVER
	get_tree().change_scene_to_file("res://game_over/GameOverScreen.tscn")

func go_to_menu() -> void:
	player_state = null
	current_phase = Phase.MENU
	get_tree().change_scene_to_file("res://menu/MainMenu.tscn")

func get_current_enemy_data() -> EnemyData:
	return player_state.current_node.enemy_data

func is_final_node() -> bool:
	return player_state.current_node.connections.is_empty()
```

- [ ] **Step 3：运行项目确认无报错**

使用 Godot MCP 工具：`mcp__godot__run_project`（projectPath: `e:/MyWork/AIGameTest2`），然后 `mcp__godot__get_debug_output`。

期望：errors 数组中只有 `icon.svg` 相关错误，无 GDScript 解析错误。主菜单应正常显示。

停止项目：`mcp__godot__stop_project`。

- [ ] **Step 4：提交**

```bash
git -C "e:/MyWork/AIGameTest2" add PlayerState.gd GameManager.gd
git -C "e:/MyWork/AIGameTest2" commit -m "feat: replace linear map tracking with graph-based navigation in PlayerState and GameManager"
```

---

## Task 3：GameOverScreen.gd — 更新进度显示

**Files:**
- Modify: `e:/MyWork/AIGameTest2/game_over/GameOverScreen.gd`

> 旧代码引用了已删除的 `node_sequence` 和 `current_node:int`，必须更新，否则 Game Over 界面会崩溃。

- [ ] **Step 1：更新 GameOverScreen.gd**

将 `e:/MyWork/AIGameTest2/game_over/GameOverScreen.gd` 完整内容替换为（缩进使用 Tab）：

```gdscript
extends Control

@onready var _lbl_node: Label  = $VBoxContainer/LblNode
@onready var _btn_menu: Button = $VBoxContainer/BtnMenu

func _ready() -> void:
	var completed: int = GameManager.player_state.completed_nodes.size()
	var total: int = GameManager.player_state.map_all_nodes.size()
	_lbl_node.text = "已完成 %d / %d 关" % [completed, total]
	_btn_menu.pressed.connect(GameManager.go_to_menu)
```

- [ ] **Step 2：提交**

```bash
git -C "e:/MyWork/AIGameTest2" add game_over/GameOverScreen.gd
git -C "e:/MyWork/AIGameTest2" commit -m "feat: update GameOverScreen to use completed_nodes count"
```

---

## Task 4：MapScreen.tscn + MapScreen.gd — 重写地图 UI

**Files:**
- Modify: `e:/MyWork/AIGameTest2/map/MapScreen.tscn`
- Modify: `e:/MyWork/AIGameTest2/map/MapScreen.gd`

> MapScreen 全部重写。tscn 只保留两个静态 Label；按钮和连线全部由脚本动态生成。两个文件必须同一 commit。

- [ ] **Step 1：更新 MapScreen.tscn**

将 `e:/MyWork/AIGameTest2/map/MapScreen.tscn` 完整内容替换为：

```
[gd_scene load_steps=2 format=3 uid="uid://dmap8x3qnc5vy"]

[ext_resource type="Script" path="res://map/MapScreen.gd" id="1_script"]

[node name="MapScreen" type="Control"]
layout_mode = 3
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
script = ExtResource("1_script")

[node name="LblTitle" type="Label" parent="."]
layout_mode = 1
anchor_left = 0.5
anchor_top = 0.0
anchor_right = 0.5
anchor_bottom = 0.0
offset_left = -100.0
offset_top = 20.0
offset_right = 100.0
offset_bottom = 55.0
text = "选择关卡"
horizontal_alignment = 1

[node name="LblHP" type="Label" parent="."]
layout_mode = 1
anchor_left = 0.5
anchor_top = 0.0
anchor_right = 0.5
anchor_bottom = 0.0
offset_left = -100.0
offset_top = 60.0
offset_right = 100.0
offset_bottom = 90.0
text = "生命：? / ?"
horizontal_alignment = 1
```

- [ ] **Step 2：更新 MapScreen.gd**

将 `e:/MyWork/AIGameTest2/map/MapScreen.gd` 完整内容替换为（缩进使用 Tab）：

```gdscript
extends Control

@onready var _lbl_hp: Label = $LblHP

var _node_positions: Dictionary = {}

func _ready() -> void:
	var state: PlayerState = GameManager.player_state
	_lbl_hp.text = "生命：%d / %d" % [state.hp, state.max_hp]
	_build_map(state)
	queue_redraw()

func _build_map(state: PlayerState) -> void:
	for nd: NodeData in state.map_all_nodes:
		var center: Vector2 = _get_node_pos(nd)
		_node_positions[nd] = center
		var btn := Button.new()
		btn.size = Vector2(110.0, 50.0)
		btn.position = center - Vector2(55.0, 25.0)
		var is_available: bool = state.available_nodes.has(nd)
		var is_completed: bool = state.completed_nodes.has(nd)
		if is_completed:
			btn.text = "✓ " + _get_node_label(nd)
			btn.disabled = true
		elif is_available:
			btn.text = _get_node_label(nd)
			btn.pressed.connect(GameManager.select_node.bind(nd))
		else:
			btn.text = _get_node_label(nd)
			btn.disabled = true
		add_child(btn)

func _draw() -> void:
	var state: PlayerState = GameManager.player_state
	for nd: NodeData in state.map_all_nodes:
		if not _node_positions.has(nd):
			continue
		var from: Vector2 = _node_positions[nd]
		for target: NodeData in nd.connections:
			if not _node_positions.has(target):
				continue
			var to: Vector2 = _node_positions[target]
			var color: Color = Color(0.4, 1.0, 0.4) if state.completed_nodes.has(nd) \
							   else Color(0.5, 0.5, 0.5)
			draw_line(from, to, color, 2.0)

func _get_node_pos(nd: NodeData) -> Vector2:
	if nd.col == 2:
		return Vector2(380.0, 375.0)
	return Vector2(100.0 if nd.col == 0 else 240.0,
				   250.0 if nd.row == 0 else 500.0)

func _get_node_label(nd: NodeData) -> String:
	if nd.connections.is_empty():
		return "Boss 战斗"
	if nd.type == NodeData.Type.REST:
		return "休息站"
	return "战斗"
```

- [ ] **Step 3：运行项目做完整流程验证**

使用 Godot MCP 工具：`mcp__godot__run_project`（projectPath: `e:/MyWork/AIGameTest2`），然后 `mcp__godot__get_debug_output`。

期望：无 GDScript 错误。

然后请用户手动验证以下路径（Godot MCP 无法点击 UI）：
1. 主菜单 → 开始新游戏 → 地图显示 5 个节点，3 列，有灰色连线，第 0 列两个按钮可点击
2. 点击一个战斗节点 → 战斗界面正常加载
3. 胜利 → 领取奖励 → 返回地图：完成节点显示 ✓，出线变绿，第 1 列节点解锁
4. 继续直到 Boss → 胜利 → 胜利界面
5. 重开一局，故意失败 → Game Over 显示"已完成 X / 5 关"

停止项目：`mcp__godot__stop_project`。

- [ ] **Step 4：提交**

```bash
git -C "e:/MyWork/AIGameTest2" add map/MapScreen.tscn map/MapScreen.gd
git -C "e:/MyWork/AIGameTest2" commit -m "feat: rewrite MapScreen for branching map with connection lines"
```

---

## Task 5：收尾 — 打标签 + 开发日志

**Files:**
- Create: `e:/MyWork/Devlog/AIGameTest2_v0.19.0_2026-04-29.md`

- [ ] **Step 1：打标签**

```bash
git -C "e:/MyWork/AIGameTest2" tag v0.19.0
```

- [ ] **Step 2：写开发日志**

新建 `e:/MyWork/Devlog/AIGameTest2_v0.19.0_2026-04-29.md`，内容：

```markdown
# AIGameTest2 v0.19.0 开发日志

**日期：** 2026-04-29
**版本：** v0.19.0
**主题：** 连线地图系统

## 本版本做了什么

将线性节点序列升级为分支图结构，MapScreen 展示节点连线，玩家可选择路径。

### 地图结构（固定）

col=0: 2 个战斗节点 → col=1: 1 休息站 + 1 战斗 → col=2: Boss

### 文件变更

- NodeData.gd：新增 connections/col/row
- PlayerState.gd：移除线性字段，新增图追踪字段
- GameManager.gd：构建分支图，新增 select_node()，修改 go_to_map()
- MapScreen.gd：全部重写，_draw() 绘制连线
- GameOverScreen.gd：改用 completed_nodes 显示进度

## 验收结果

- [ ] 地图显示 5 个节点，2-2-1 布局
- [ ] 节点间有连线（灰/绿）
- [ ] 路径选择和状态推进正常
- [ ] v0.18.0 全部功能正常
```

---

## 验收清单（用户手动验证）

- [ ] 地图显示 5 个节点，布局为 2-2-1 三列
- [ ] 节点间有灰色连线
- [ ] 起始只有第 0 列两个节点可点击，其余置灰
- [ ] 完成战斗/休息后返回地图：已完成节点显示 ✓，出线变绿，下一列节点解锁
- [ ] Boss 战斗胜利后跳转胜利界面
- [ ] 战斗失败跳转 Game Over，显示"已完成 X / 5 关"
- [ ] v0.18.0 全部功能（虚弱、卡牌升级、牌组查看）正常
