# v0.30.0 群攻牌「横扫」实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增一张群体攻击牌「横扫」（费用 2，对所有存活敌人各造成 5 点伤害），并将其加入起始牌组。

**Architecture:** 新增 `target_type = "all"` 分支：CombatEngine 在 `play_card()` 中循环对所有存活敌人执行 `EffectResolver.resolve()`；CombatScreen 点牌后直接出，不进入选目标模式。EffectResolver 本身不需要改动。

**Tech Stack:** Godot 4 GDScript，Resource .tres 数据文件

---

## 文件结构

| 文件 | 操作 |
|------|------|
| `data/cards/whirlwind.tres` | 新建：横扫卡牌数据 |
| `GameManager.gd` | 修改：起始牌组加入横扫 |
| `CombatEngine.gd` | 修改：`play_card()` 支持 `target_type == "all"` |
| `combat/CombatScreen.gd` | 修改：`_on_card_pressed()` 加入 "all" 分支 |
| `menu/MainMenu.tscn` | 修改：版本号更新到 v0.30.0 |

---

### Task 1: 新建 whirlwind.tres

**Files:**
- Create: `data/cards/whirlwind.tres`

- [ ] **Step 1: 创建卡牌数据文件**

将以下内容写入 `data/cards/whirlwind.tres`（与其他卡牌放在同一目录）。
ext_resource 的 uid 与项目中 CardData.gd / CardEffectData.gd 的 uid 保持一致：

```
[gd_resource type="Resource" script_class="CardData" load_steps=4 format=3]

[ext_resource type="Script" uid="uid://13mprie5g221" path="res://CardData.gd" id="1_CardData"]
[ext_resource type="Script" uid="uid://cxv8nrm4tp7dk" path="res://CardEffectData.gd" id="2_CardEffectData"]

[sub_resource type="Resource" id="Effect_damage"]
script = ExtResource("2_CardEffectData")
type = "damage"
value = 5
upgrade_bonus = 2

[resource]
script = ExtResource("1_CardData")
card_name = "横扫"
cost = 2
price = 50
effects = Array[ExtResource("2_CardEffectData")]([SubResource("Effect_damage")])
target_type = "all"
```

说明：
- `load_steps=4`：文件本身 + CardData.gd + CardEffectData.gd + Effect_damage 子资源，共 4 项（与 strike.tres 相同）
- Godot 首次打开项目时会自动补充 `uid=` 行，不影响功能

- [ ] **Step 2: 提交**

```bash
git add data/cards/whirlwind.tres
git commit -m "feat: add whirlwind AoE card data"
```

---

### Task 2: 起始牌组加入横扫（GameManager.gd）

**Files:**
- Modify: `GameManager.gd` lines 18–29

当前代码：
```gdscript
func start_new_run() -> void:
	player_state = PlayerState.new()
	var strike: CardData = preload("res://data/cards/strike.tres")
	var defend: CardData = preload("res://data/cards/defend.tres")
	var bash: CardData   = preload("res://data/cards/bash.tres")
	var slash: CardData  = preload("res://data/cards/slash.tres")
	for i in 4:
		player_state.deck.append(strike.duplicate())
	for i in 4:
		player_state.deck.append(defend.duplicate())
	player_state.deck.append(bash.duplicate())
	player_state.deck.append(slash.duplicate())
```

- [ ] **Step 1: 添加横扫到起始牌组**

将 `start_new_run()` 改为：

```gdscript
func start_new_run() -> void:
	player_state = PlayerState.new()
	var strike: CardData    = preload("res://data/cards/strike.tres")
	var defend: CardData    = preload("res://data/cards/defend.tres")
	var bash: CardData      = preload("res://data/cards/bash.tres")
	var slash: CardData     = preload("res://data/cards/slash.tres")
	var whirlwind: CardData = preload("res://data/cards/whirlwind.tres")
	for i in 4:
		player_state.deck.append(strike.duplicate())
	for i in 4:
		player_state.deck.append(defend.duplicate())
	player_state.deck.append(bash.duplicate())
	player_state.deck.append(slash.duplicate())
	player_state.deck.append(whirlwind.duplicate())
```

- [ ] **Step 2: 提交**

```bash
git add GameManager.gd
git commit -m "feat: add whirlwind to starting deck"
```

---

### Task 3: CombatEngine 支持 target_type == "all"

**Files:**
- Modify: `CombatEngine.gd` lines 53–66 (`play_card` 函数)

当前代码：
```gdscript
func play_card(card_index: int, target_index: int) -> bool:
	var card: CardData = hand[card_index]
	if card.cost > energy:
		return false
	energy -= card.cost
	var target: Combatant = enemies[target_index] if target_index >= 0 else null
	EffectResolver.resolve(card, player, target)
	_apply_engine_effects(card)
	hand.remove_at(card_index)
	_discard_pile.append(card)
	state_changed.emit()
	if _check_end():
		return true
	return true
```

- [ ] **Step 1: 修改 play_card 支持全体攻击**

将 `play_card` 改为：

```gdscript
func play_card(card_index: int, target_index: int) -> bool:
	var card: CardData = hand[card_index]
	if card.cost > energy:
		return false
	energy -= card.cost
	if card.target_type == "all":
		for enemy in enemies:
			if enemy.hp > 0:
				EffectResolver.resolve(card, player, enemy)
	else:
		var target: Combatant = enemies[target_index] if target_index >= 0 else null
		EffectResolver.resolve(card, player, target)
	_apply_engine_effects(card)
	hand.remove_at(card_index)
	_discard_pile.append(card)
	state_changed.emit()
	if _check_end():
		return true
	return true
```

说明：
- `target_type == "all"` 分支：对 `enemies` 中所有 `hp > 0` 的敌人依次调用 `EffectResolver.resolve()`，弱化 / 易伤修正在每个敌人上独立计算
- `else` 分支：原有逻辑（single / none）不变
- `_apply_engine_effects`、手牌移除、`_check_end()` 在两个分支之后统一执行

- [ ] **Step 2: 提交**

```bash
git add CombatEngine.gd
git commit -m "feat: CombatEngine supports target_type all for AoE cards"
```

---

### Task 4: CombatScreen 支持 "all" 分支 + 版本号

**Files:**
- Modify: `combat/CombatScreen.gd` lines 57–67 (`_on_card_pressed`)
- Modify: `menu/MainMenu.tscn` line 35

当前 `_on_card_pressed` 代码：
```gdscript
func _on_card_pressed(card_index: int) -> void:
	if _pending_card_index >= 0:
		_pending_card_index = -1
		_set_targeting_mode(false)
		return
	var card: CardData = _engine.hand[card_index]
	if card.target_type == "none":
		_engine.play_card(card_index, -1)
	else:
		_pending_card_index = card_index
		_set_targeting_mode(true)
```

- [ ] **Step 1: 修改 _on_card_pressed 加入 "all" 分支**

将判断条件从 `card.target_type == "none"` 改为：

```gdscript
func _on_card_pressed(card_index: int) -> void:
	if _pending_card_index >= 0:
		_pending_card_index = -1
		_set_targeting_mode(false)
		return
	var card: CardData = _engine.hand[card_index]
	if card.target_type == "none" or card.target_type == "all":
		_engine.play_card(card_index, -1)
	else:
		_pending_card_index = card_index
		_set_targeting_mode(true)
```

说明：`"all"` 牌与 `"none"` 牌一样直接出牌（`target_index = -1`），不进入选目标模式。CombatEngine 内部负责循环处理所有敌人。

- [ ] **Step 2: 更新版本号**

修改 `menu/MainMenu.tscn` 第 35 行，将版本号从 `v0.29.0` 改为 `v0.30.0`：

```
text = "v0.30.0"
```

- [ ] **Step 3: 提交**

```bash
git add combat/CombatScreen.gd menu/MainMenu.tscn
git commit -m "feat: CombatScreen handles AoE card; bump version to v0.30.0"
```

- [ ] **Step 4: 手动验收**

启动 Godot 运行游戏，开始新局，在有多个敌人的战斗中验证：

1. 手牌中出现「横扫  费用:2  攻:5」
2. 点击横扫立即出牌，**不进入选目标模式**（敌人按钮不亮起）
3. 所有存活敌人各减少 5 HP（按钮文字 HP 数字更新）
4. 己方弱化状态下出横扫，每个敌人只受 `int(5 * 0.75) = 3` 伤害
5. 若某个敌人有易伤状态，该敌人受 `int(5 * 1.5) = 7` 伤害，其余敌人仍受 5 伤害
6. 战斗胜利流程正常（消灭最后一个敌人后出现胜利按钮）
