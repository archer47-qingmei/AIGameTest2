# v0.44.0 受击动画 + 伤害数字飞出 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 敌人被攻击时播放受击动画（红色闪烁 + 缩放抖动），并在敌人位置弹出伤害数字随机向左右飘移淡出。

**Architecture:** `CombatEngine` 新增 `damage_dealt(enemy_index, amount)` 信号，`play_card()` 在 EffectResolver 前后对比 HP 并 emit；`CombatScreen` 监听信号，用 Tween 驱动动画，Label 节点运行时创建。不修改任何 `.tscn`。

**Tech Stack:** Godot 4 GDScript，Tween API。

---

## 文件结构

| 文件 | 动作 | 变更内容 |
|------|------|----------|
| `CombatEngine.gd` | 修改 | 新增 `signal damage_dealt`；`play_card()` HP 快照 + emit |
| `combat/CombatScreen.gd` | 修改 | 连接信号；实现 `_on_damage_dealt()` |
| `menu/MainMenu.tscn` | 修改 | 版本号 v0.43.0 → v0.44.0 |

---

## Task 1: CombatEngine damage_dealt 信号

**Files:**
- Modify: `CombatEngine.gd`

背景：当前 `play_card()` 调用 EffectResolver 后只 emit 通用 `state_changed`，不携带伤害量。本任务新增 `damage_dealt` 信号，并在每次 EffectResolver 执行前后对比敌人 HP，对实际受伤的敌人 emit 信号。

相关现有代码：
```gdscript
# 第 6-7 行
signal state_changed
signal combat_ended(result: String)

# play_card() 第 58-78 行
func play_card(card_index: int, target_index: int) -> bool:
	var card: CardData = hand[card_index]
	if card.cost > energy:
		return false
	energy -= card.cost
	if card.target_type == "all":
		for enemy in enemies:
			if enemy.hp > 0:
				EffectResolver.resolve(card, player, enemy)
	else:
		var target: Combatant = enemies[target_index] if target_index >= 0 else null
		EffectResolver.resolve(card, player, target)
	_apply_engine_effects(card)
	hand.remove_at(card_index)
	...
```

- [ ] **Step 1: 新增 `damage_dealt` 信号**

用 Edit 工具将：
```gdscript
signal state_changed
signal combat_ended(result: String)
```
替换为：
```gdscript
signal state_changed
signal combat_ended(result: String)
signal damage_dealt(enemy_index: int, amount: int)
```

- [ ] **Step 2: 修改 `play_card()` 加入 HP 快照与 emit**

用 Edit 工具将：
```gdscript
	energy -= card.cost
	if card.target_type == "all":
		for enemy in enemies:
			if enemy.hp > 0:
				EffectResolver.resolve(card, player, enemy)
	else:
		var target: Combatant = enemies[target_index] if target_index >= 0 else null
		EffectResolver.resolve(card, player, target)
	_apply_engine_effects(card)
```
替换为：
```gdscript
	energy -= card.cost
	var hp_before: Array[int] = []
	for e: Combatant in enemies:
		hp_before.append(e.hp)
	if card.target_type == "all":
		for enemy in enemies:
			if enemy.hp > 0:
				EffectResolver.resolve(card, player, enemy)
	else:
		var target: Combatant = enemies[target_index] if target_index >= 0 else null
		EffectResolver.resolve(card, player, target)
	for i in enemies.size():
		var dmg: int = hp_before[i] - enemies[i].hp
		if dmg > 0:
			damage_dealt.emit(i, dmg)
	_apply_engine_effects(card)
```

- [ ] **Step 3: 提交**

```bash
git add CombatEngine.gd
git commit -m "feat: emit damage_dealt signal with enemy index and amount"
```

---

## Task 2: CombatScreen 受击动画

**Files:**
- Modify: `combat/CombatScreen.gd`

背景：`_ready()` 目前连接了 `state_changed` 和 `combat_ended` 两个信号（第 31-32 行）。本任务新增 `damage_dealt` 的连接，并实现动画方法。

相关现有代码：
```gdscript
# _ready() 第 31-32 行
	_engine.state_changed.connect(_refresh_ui)
	_engine.combat_ended.connect(_on_combat_ended)
```

- [ ] **Step 1: 连接 `damage_dealt` 信号**

用 Edit 工具将：
```gdscript
	_engine.state_changed.connect(_refresh_ui)
	_engine.combat_ended.connect(_on_combat_ended)
```
替换为：
```gdscript
	_engine.state_changed.connect(_refresh_ui)
	_engine.combat_ended.connect(_on_combat_ended)
	_engine.damage_dealt.connect(_on_damage_dealt)
```

- [ ] **Step 2: 新增 `_on_damage_dealt()` 方法**

用 Edit 工具将文件末尾的：
```gdscript
func _on_proceed() -> void:
	GameManager.end_combat(_engine.player.hp)
```
替换为：
```gdscript
func _on_damage_dealt(enemy_index: int, amount: int) -> void:
	var btn: Button = _enemies_container.get_child(enemy_index) as Button
	if btn == null:
		return
	btn.pivot_offset = btn.size / 2

	var flash_tween: Tween = create_tween()
	flash_tween.tween_property(btn, "modulate", Color(1.0, 0.2, 0.2), 0.05)
	flash_tween.tween_property(btn, "modulate", Color(1.0, 1.0, 1.0), 0.2)

	var shake_tween: Tween = create_tween()
	shake_tween.tween_property(btn, "scale", Vector2(1.15, 0.85), 0.06)
	shake_tween.tween_property(btn, "scale", Vector2(0.9, 1.1), 0.06)
	shake_tween.tween_property(btn, "scale", Vector2(1.05, 0.95), 0.06)
	shake_tween.tween_property(btn, "scale", Vector2(1.0, 1.0), 0.07)

	var lbl: Label = Label.new()
	lbl.text = str(amount)
	lbl.add_theme_font_size_override("font_size", 24)
	lbl.pivot_offset = Vector2(15, 12)
	var btn_center: Vector2 = to_local(btn.get_global_rect().get_center())
	lbl.position = btn_center - Vector2(15, 12)
	lbl.z_index = 10
	add_child(lbl)

	var direction: float = [-1.0, 1.0][randi() % 2]
	var drift_x: float = direction * randf_range(20.0, 60.0)

	var flyout_tween: Tween = create_tween().set_parallel(true)
	flyout_tween.tween_property(lbl, "scale", Vector2(1.5, 1.5), 0.15).set_ease(Tween.EASE_OUT)
	flyout_tween.tween_property(lbl, "position:x", lbl.position.x + drift_x, 0.4)
	flyout_tween.tween_property(lbl, "modulate:a", 0.0, 0.3).set_delay(0.1)
	flyout_tween.finished.connect(lbl.queue_free)

func _on_proceed() -> void:
	GameManager.end_combat(_engine.player.hp)
```

- [ ] **Step 3: 提交**

```bash
git add combat/CombatScreen.gd
git commit -m "feat: add hit flash, shake, and damage number flyout animation"
```

---

## Task 3: 版本号

**Files:**
- Modify: `menu/MainMenu.tscn`

- [ ] **Step 1: 将版本号改为 v0.44.0**

用 Edit 工具将 `menu/MainMenu.tscn` 中的 `text = "v0.43.0"` 替换为 `text = "v0.44.0"`。

- [ ] **Step 2: 提交**

```bash
git add menu/MainMenu.tscn
git commit -m "feat: bump version to v0.44.0"
```

---

## 验收清单

- [ ] 打出攻击牌后，目标敌人 Button 同时红色闪烁 + 缩放抖动
- [ ] 抖动和闪烁结束后 Button 恢复原始 scale 和 modulate（无残留）
- [ ] 伤害数字出现在敌人位置，先放大后随机向左或右飘移淡出
- [ ] 多个敌人同时受伤时，每个敌人独立播放动画
- [ ] 打出非伤害牌（身法/格挡）不触发动画
- [ ] 版本号显示 v0.44.0
