# v0.2.0 架构重构实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 CombatScreen.gd 中的游戏逻辑拆分到 CombatEngine.gd 和 EffectResolver.gd，CombatScreen.gd 只保留 UI 层，游戏行为与 v0.1.0 完全一致。

**Architecture:** 新建 `EffectResolver`（纯静态工具，处理卡牌效果）和 `CombatEngine`（持有游戏状态、管理回合流程、发出信号）。`CombatScreen` 改造为薄 UI 层：持有 Engine 引用，监听 `state_changed` 刷新界面，监听 `combat_ended` 显示结果。

**Tech Stack:** Godot 4.6, GDScript strict 类型模式

---

## 文件变更一览

| 操作 | 文件 | 职责 |
|------|------|------|
| 新建 | `res://EffectResolver.gd` | 纯静态方法：将卡牌效果应用到战斗者 |
| 新建 | `res://CombatEngine.gd` | 游戏状态 + 回合逻辑 + 信号 |
| 改写 | `res://combat/CombatScreen.gd` | 纯 UI：创建引擎、监听信号、转发按钮事件 |
| 不动 | `res://CardData.gd` | 数据类 |
| 不动 | `res://Combatant.gd` | 数据类 + take_damage/add_block |
| 不动 | `res://combat/CombatScreen.tscn` | 场景节点树 |

---

## Task 1：新建 EffectResolver.gd

**Files:**
- Create: `res://EffectResolver.gd`

- [ ] **Step 1：创建文件**

创建 `e:/MyWork/AIGameTest2/EffectResolver.gd`，内容如下：

```gdscript
class_name EffectResolver
extends RefCounted

static func resolve(card: CardData, attacker: Combatant, defender: Combatant) -> void:
	if card.damage > 0:
		defender.take_damage(card.damage)
	if card.block > 0:
		attacker.add_block(card.block)
```

- [ ] **Step 2：验证无解析错误**

用 Godot MCP 运行项目，确认控制台无报错（此时 CombatScreen 还是旧逻辑，游戏应正常运行）：

```
mcp__godot__run_project(projectPath="e:/MyWork/AIGameTest2")
```

预期：游戏正常启动进入战斗场景，控制台无报错。

- [ ] **Step 3：停止项目**

```
mcp__godot__stop_project()
```

- [ ] **Step 4：提交**

```bash
git add EffectResolver.gd
git commit -m "feat: add EffectResolver with static resolve()"
```

---

## Task 2：新建 CombatEngine.gd

**Files:**
- Create: `res://CombatEngine.gd`

- [ ] **Step 1：创建文件**

创建 `e:/MyWork/AIGameTest2/CombatEngine.gd`，内容如下：

```gdscript
class_name CombatEngine
extends RefCounted

signal state_changed
signal combat_ended(result: String)

var player: Combatant
var enemy: Combatant
var hand: Array[CardData] = []
var energy: int = 0
var turn_number: int = 0

var _draw_pile: Array[CardData] = []
var _discard_pile: Array[CardData] = []

func setup() -> void:
	player = Combatant.new()
	player.display_name = "玩家"
	player.hp = 70
	player.max_hp = 70
	player.block = 0

	enemy = Combatant.new()
	enemy.display_name = "颚虫"
	enemy.hp = 44
	enemy.max_hp = 44
	enemy.block = 0

	for i in 4:
		var c: CardData = CardData.new()
		c.card_name = "打击"
		c.cost = 1
		c.damage = 6
		c.block = 0
		_draw_pile.append(c)

	for i in 4:
		var c: CardData = CardData.new()
		c.card_name = "防御"
		c.cost = 1
		c.damage = 0
		c.block = 5
		_draw_pile.append(c)

	var bash: CardData = CardData.new()
	bash.card_name = "重击"
	bash.cost = 2
	bash.damage = 8
	bash.block = 0
	_draw_pile.append(bash)

	_draw_pile.shuffle()
	_start_player_turn()

func play_card(card: CardData) -> void:
	if energy < card.cost:
		return
	energy -= card.cost
	EffectResolver.resolve(card, player, enemy)
	var idx: int = hand.find(card)
	if idx >= 0:
		hand.remove_at(idx)
	_discard_pile.append(card)
	state_changed.emit()
	_check_end()

func end_turn() -> void:
	for card: CardData in hand:
		_discard_pile.append(card)
	hand.clear()
	_do_enemy_turn()
	state_changed.emit()
	if not _check_end():
		_start_player_turn()

func _start_player_turn() -> void:
	turn_number += 1
	energy = 3
	player.block = 0
	_draw_hand()
	state_changed.emit()

func _draw_hand() -> void:
	hand.clear()
	for i in 5:
		if _draw_pile.is_empty():
			if _discard_pile.is_empty():
				break
			for card: CardData in _discard_pile:
				_draw_pile.append(card)
			_discard_pile.clear()
			_draw_pile.shuffle()
		if _draw_pile.is_empty():
			break
		var card: CardData = _draw_pile.pop_back()
		hand.append(card)

func _do_enemy_turn() -> void:
	enemy.block = 0
	if turn_number % 2 == 1:
		player.take_damage(11)
	else:
		enemy.add_block(6)

func _check_end() -> bool:
	if enemy.hp <= 0:
		combat_ended.emit("victory")
		return true
	if player.hp <= 0:
		combat_ended.emit("game_over")
		return true
	return false
```

- [ ] **Step 2：验证无解析错误**

```
mcp__godot__run_project(projectPath="e:/MyWork/AIGameTest2")
```

预期：游戏正常启动（CombatScreen 仍是旧逻辑），控制台无报错。

- [ ] **Step 3：停止项目**

```
mcp__godot__stop_project()
```

- [ ] **Step 4：提交**

```bash
git add CombatEngine.gd
git commit -m "feat: add CombatEngine with turn logic and signals"
```

---

## Task 3：改写 CombatScreen.gd 为纯 UI 层

**Files:**
- Modify: `res://combat/CombatScreen.gd`（完全重写）

- [ ] **Step 1：用以下内容替换 CombatScreen.gd**

将 `e:/MyWork/AIGameTest2/combat/CombatScreen.gd` 完整替换为：

```gdscript
extends Control

@onready var _lbl_enemy_name: Label    = $VBoxContainer/EnemyPanel/LblEnemyName
@onready var _lbl_enemy_hp: Label      = $VBoxContainer/EnemyPanel/LblEnemyHP
@onready var _lbl_enemy_block: Label   = $VBoxContainer/EnemyPanel/LblEnemyBlock
@onready var _lbl_enemy_intent: Label  = $VBoxContainer/EnemyPanel/LblEnemyIntent
@onready var _lbl_player_hp: Label     = $VBoxContainer/PlayerPanel/LblPlayerHP
@onready var _lbl_player_block: Label  = $VBoxContainer/PlayerPanel/LblPlayerBlock
@onready var _lbl_energy: Label        = $VBoxContainer/PlayerPanel/LblEnergy
@onready var _hand_container: HBoxContainer = $VBoxContainer/HandContainer
@onready var _btn_end_turn: Button     = $VBoxContainer/BtnEndTurn
@onready var _lbl_result: Label        = $LblResult

var _engine: CombatEngine
var _hand_buttons: Array[Button] = []

func _ready() -> void:
	_engine = CombatEngine.new()
	_engine.state_changed.connect(_refresh_ui)
	_engine.combat_ended.connect(_on_combat_ended)
	_btn_end_turn.pressed.connect(_engine.end_turn)
	_engine.setup()

func _on_card_pressed(card: CardData) -> void:
	_engine.play_card(card)

func _refresh_ui() -> void:
	_lbl_enemy_name.text = _engine.enemy.display_name
	_lbl_enemy_hp.text = "生命：%d / %d" % [_engine.enemy.hp, _engine.enemy.max_hp]
	_lbl_enemy_block.text = "格挡：%d" % _engine.enemy.block
	var intent: String = "攻击 11" if _engine.turn_number % 2 == 1 else "格挡 6"
	_lbl_enemy_intent.text = "意图：" + intent
	_lbl_player_hp.text = "生命：%d / %d" % [_engine.player.hp, _engine.player.max_hp]
	_lbl_player_block.text = "格挡：%d" % _engine.player.block
	_lbl_energy.text = "能量：%d / 3" % _engine.energy
	_rebuild_hand()

func _rebuild_hand() -> void:
	for btn: Button in _hand_buttons:
		btn.queue_free()
	_hand_buttons.clear()
	for card: CardData in _engine.hand:
		var btn: Button = Button.new()
		btn.text = "%s\n费用:%d  攻:%d  挡:%d" % [card.card_name, card.cost, card.damage, card.block]
		btn.pressed.connect(_on_card_pressed.bind(card))
		_hand_container.add_child(btn)
		_hand_buttons.append(btn)

func _on_combat_ended(result: String) -> void:
	_lbl_result.text = "胜利！" if result == "victory" else "游戏结束"
	_lbl_result.visible = true
	_btn_end_turn.disabled = true
	for btn: Button in _hand_buttons:
		btn.disabled = true
```

- [ ] **Step 2：运行项目，验证游戏行为**

```
mcp__godot__run_project(projectPath="e:/MyWork/AIGameTest2")
```

对照以下验收清单逐项确认：

1. 项目无报错启动，直接进入战斗场景
2. 手牌显示 5 张卡牌按钮（卡名、费用、攻、挡）
3. 能量不足时点击卡牌无效果（打出两张 1 费后点第三张 2 费的重击应无效）
4. 打出攻击牌 → 敌人 HP 减少，标签刷新
5. 打出防御牌 → 玩家格挡增加，标签刷新
6. 点击"结束回合" → 敌人行动（第 1 回合攻 11），玩家 HP 减少，下一回合开始
7. 第 2 回合结束 → 敌人格挡 6，敌人意图标签切换为"格挡 6"
8. 继续战斗至敌人 HP 归零 → 显示"胜利！"，所有按钮禁用
9. 重启后让敌人打死玩家 → 显示"游戏结束"，按钮禁用
10. 打满多回合后，牌库耗尽时弃牌堆自动重新洗入继续抽牌（不卡死）

- [ ] **Step 3：停止项目**

```
mcp__godot__stop_project()
```

- [ ] **Step 4：提交**

```bash
git add combat/CombatScreen.gd
git commit -m "refactor: CombatScreen becomes pure UI layer, wired to CombatEngine"
```

---

## Task 4：打版本标签并验证架构约束

**Files:** 无文件修改

- [ ] **Step 1：确认架构约束**

检查以下三条约束均满足：

- `CombatScreen.gd` 中不存在 `take_damage`、`_draw_pile`、`_discard_pile`、`add_block` 等字样（Grep 验证）
- `CombatEngine.gd` 中不存在 `Label`、`Button`、`queue_free`、`$` 等 UI 相关字样（Grep 验证）
- `EffectResolver.gd` 中只有一个 `static func resolve`，无 Engine/Screen 引用（肉眼确认）

```bash
grep -n "take_damage\|_draw_pile\|_discard_pile\|add_block" e:/MyWork/AIGameTest2/combat/CombatScreen.gd
# 预期：无输出

grep -n "Label\|Button\|queue_free\|\\\$" e:/MyWork/AIGameTest2/CombatEngine.gd
# 预期：无输出
```

- [ ] **Step 2：打 v0.2.0 标签**

```bash
git tag v0.2.0
```

---

## 验收总结

完成所有 Task 后，以下标准应全部满足：

- [ ] 项目无报错启动，直接进入战斗场景
- [ ] 游戏行为与 v0.1.0 完全一致（纯重构，零功能变化）
- [ ] `CombatScreen.gd` 不含任何游戏逻辑
- [ ] `CombatEngine.gd` 不含任何节点引用或 UI 代码
- [ ] `EffectResolver.resolve()` 可独立调用，不依赖 Engine 或 Screen
- [ ] 全部 v0.1.0 验收标准继续通过
