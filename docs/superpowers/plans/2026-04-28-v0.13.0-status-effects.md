# v0.13.0 状态效果（虚弱）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增"虚弱"状态效果——玩家打出"缠绕"卡对敌人施加 2 层虚弱，敌人攻击时伤害降低 25%，每次攻击后虚弱层数 -1，UI 实时显示敌方虚弱层数。

**Architecture:** `Combatant` 新增 `weak` 字段；`EffectResolver` 新增 `apply_damage(source, target, amount)` 静态方法（含虚弱修正）并处理 "weak" 效果类型；`CombatEngine._do_enemy_turn` 使用新方法并递减敌方虚弱，`_start_player_turn` 递减玩家虚弱；`CombatScreen` 显示敌方虚弱层数；新增 `entangle.tres` 卡牌数据文件。

**Tech Stack:** Godot 4.6, GDScript 严格类型模式

---

## 文件变更一览

| 操作 | 文件 | 变更摘要 |
|---|---|---|
| 修改 | `Combatant.gd` | 新增 `weak: int`、`add_weak()` |
| 修改 | `EffectResolver.gd` | 新增 `apply_damage()`、"weak" 效果分支 |
| 修改 | `CombatEngine.gd` | `_do_enemy_turn` 用虚弱修正；`_start_player_turn` 递减玩家虚弱 |
| 修改 | `CardData.gd` | `get_description()` 支持 "weak" 类型显示 |
| 新增 | `data/cards/entangle.tres` | 缠绕卡牌数据 |
| 修改 | `combat/CombatScreen.tscn` | 新增 `LblEnemyWeak` |
| 修改 | `combat/CombatScreen.gd` | 新增 `@onready`；`_refresh_ui` 更新虚弱显示 |

---

## Task 1：更新 Combatant.gd

**Files:**
- Modify: `e:/MyWork/AIGameTest2/Combatant.gd`

> `Combatant` 是纯数据类（RefCounted），新增 `weak` 字段和 `add_weak()` 方法。其他代码不动。

- [ ] **Step 1：修改 Combatant.gd**

将 `e:/MyWork/AIGameTest2/Combatant.gd` 完整内容替换为：

```gdscript
class_name Combatant
extends RefCounted

var display_name: String
var hp: int
var max_hp: int
var block: int
var weak: int = 0

func take_damage(amount: int) -> void:
	if amount <= block:
		block -= amount
	else:
		hp -= amount - block
		block = 0
	if hp < 0:
		hp = 0

func add_block(amount: int) -> void:
	block += amount

func add_weak(n: int) -> void:
	weak += n
```

注意缩进使用 Tab。

- [ ] **Step 2：提交**

```bash
git -C "e:/MyWork/AIGameTest2" add Combatant.gd
git -C "e:/MyWork/AIGameTest2" commit -m "feat: add weak field and add_weak() to Combatant"
```

---

## Task 2：更新 EffectResolver.gd

**Files:**
- Modify: `e:/MyWork/AIGameTest2/EffectResolver.gd`

> 新增 `apply_damage(source, target, amount)` 静态方法，施害方有虚弱时伤害乘以 0.75（取整）。在 `resolve()` 中将 "damage" 分支改为调用此方法，新增 "weak" 分支对防御方施加虚弱。
>
> 注意：`CombatEngine` 的敌人攻击（`_do_enemy_turn`）后续也会调用 `apply_damage()`，Task 3 中处理。

- [ ] **Step 1：修改 EffectResolver.gd**

将 `e:/MyWork/AIGameTest2/EffectResolver.gd` 完整内容替换为：

```gdscript
class_name EffectResolver
extends RefCounted

static func resolve(card: CardData, attacker: Combatant, defender: Combatant) -> void:
	for effect: CardEffectData in card.effects:
		if effect.type == "damage":
			apply_damage(attacker, defender, effect.value)
		elif effect.type == "block":
			attacker.add_block(effect.value)
		elif effect.type == "weak":
			defender.add_weak(effect.value)

static func apply_damage(source: Combatant, target: Combatant, amount: int) -> void:
	var dmg: int = amount
	if source.weak > 0:
		dmg = int(dmg * 0.75)
	target.take_damage(dmg)
```

注意缩进使用 Tab。

- [ ] **Step 2：提交**

```bash
git -C "e:/MyWork/AIGameTest2" add EffectResolver.gd
git -C "e:/MyWork/AIGameTest2" commit -m "feat: add weak effect and apply_damage with weak modifier to EffectResolver"
```

---

## Task 3：更新 CombatEngine.gd

**Files:**
- Modify: `e:/MyWork/AIGameTest2/CombatEngine.gd`

> 两处改动：
> 1. `_do_enemy_turn()`：原 `player.take_damage(action.value)` 改为调用 `EffectResolver.apply_damage(enemy, player, action.value)`，攻击后执行 `enemy.weak = max(0, enemy.weak - 1)`（格挡回合不递减）。
> 2. `_start_player_turn()`：开头加 `player.weak = max(0, player.weak - 1)`（本版玩家不会被施加虚弱，此行预留接口，恒无副作用）。

- [ ] **Step 1：修改 CombatEngine.gd**

将 `e:/MyWork/AIGameTest2/CombatEngine.gd` 完整内容替换为：

```gdscript
class_name CombatEngine
extends RefCounted

signal state_changed
signal combat_ended(result: String)

var player: Combatant
var enemy: Combatant
var hand: Array[CardData] = []
var energy: int = 0
var turn_number: int = 0

var _draw_pile: Array[CardData] = []
var _discard_pile: Array[CardData] = []
var _enemy_data: EnemyData

func setup(initial_deck: Array[CardData], enemy_data: EnemyData, initial_hp: int) -> void:
	_enemy_data = enemy_data
	enemy = Combatant.new()
	enemy.display_name = _enemy_data.display_name
	enemy.hp = _enemy_data.hp
	enemy.max_hp = _enemy_data.hp
	enemy.block = 0

	player = Combatant.new()
	player.display_name = "玩家"
	player.hp = initial_hp
	player.max_hp = 70
	player.block = 0

	for card: CardData in initial_deck:
		_draw_pile.append(card.duplicate())
	_draw_pile.shuffle()
	_start_player_turn()

func get_current_enemy_action() -> EnemyActionData:
	return _enemy_data.actions[(turn_number - 1) % _enemy_data.actions.size()]

func get_draw_pile() -> Array[CardData]:
	return _draw_pile.duplicate()

func get_discard_pile() -> Array[CardData]:
	return _discard_pile.duplicate()

func play_card(card: CardData) -> void:
	if energy < card.cost:
		return
	energy -= card.cost
	EffectResolver.resolve(card, player, enemy)
	_apply_engine_effects(card)
	var idx: int = hand.find(card)
	if idx >= 0:
		hand.remove_at(idx)
	_discard_pile.append(card)
	state_changed.emit()
	_check_end()

func end_turn() -> void:
	for card: CardData in hand:
		_discard_pile.append(card)
	hand.clear()
	_do_enemy_turn()
	state_changed.emit()
	if not _check_end():
		_start_player_turn()

func _start_player_turn() -> void:
	player.weak = max(0, player.weak - 1)
	turn_number += 1
	energy = 3
	player.block = 0
	_draw_hand()
	state_changed.emit()

func _refill_draw_pile_if_needed() -> void:
	if _draw_pile.is_empty() and not _discard_pile.is_empty():
		for card: CardData in _discard_pile:
			_draw_pile.append(card)
		_discard_pile.clear()
		_draw_pile.shuffle()

func _draw_hand() -> void:
	hand.clear()
	for i in 5:
		_refill_draw_pile_if_needed()
		if _draw_pile.is_empty():
			break
		var card: CardData = _draw_pile.pop_back()
		hand.append(card)

func _apply_engine_effects(card: CardData) -> void:
	for effect: CardEffectData in card.effects:
		if effect.type == "draw":
			_draw_cards(effect.value)
		elif effect.type == "energy":
			energy += effect.value

func _draw_cards(n: int) -> void:
	for i: int in n:
		_refill_draw_pile_if_needed()
		if _draw_pile.is_empty():
			break
		var card: CardData = _draw_pile.pop_back()
		hand.append(card)

func _do_enemy_turn() -> void:
	enemy.block = 0
	var action: EnemyActionData = get_current_enemy_action()
	if action.type == "attack":
		EffectResolver.apply_damage(enemy, player, action.value)
		enemy.weak = max(0, enemy.weak - 1)
	else:
		enemy.add_block(action.value)

func _check_end() -> bool:
	if enemy.hp <= 0:
		combat_ended.emit("victory")
		return true
	if player.hp <= 0:
		combat_ended.emit("game_over")
		return true
	return false
```

注意缩进使用 Tab。

- [ ] **Step 2：提交**

```bash
git -C "e:/MyWork/AIGameTest2" add CombatEngine.gd
git -C "e:/MyWork/AIGameTest2" commit -m "feat: apply weak modifier in enemy turn, decrement weak each turn"
```

---

## Task 4：更新 CardData.gd + 新增 entangle.tres

**Files:**
- Modify: `e:/MyWork/AIGameTest2/CardData.gd`
- Create: `e:/MyWork/AIGameTest2/data/cards/entangle.tres`

> `CardData.get_description()` 目前不处理 "weak" 类型，缠绕卡显示描述时需要新增一行。同步新建 `entangle.tres` 卡牌数据文件。`RewardEngine` 会自动扫描 `data/cards/` 目录，文件放进去即自动加入奖励池，无需修改其他代码。

- [ ] **Step 1：修改 CardData.gd**

将 `e:/MyWork/AIGameTest2/CardData.gd` 完整内容替换为：

```gdscript
class_name CardData
extends Resource

@export var card_name: String
@export var cost: int
@export var effects: Array[CardEffectData]

func get_description() -> String:
	var dmg: int = 0
	var blk: int = 0
	var drw: int = 0
	var nrg: int = 0
	var wk: int = 0
	for effect: CardEffectData in effects:
		if effect.type == "damage":   dmg = effect.value
		elif effect.type == "block":  blk = effect.value
		elif effect.type == "draw":   drw = effect.value
		elif effect.type == "energy": nrg = effect.value
		elif effect.type == "weak":   wk = effect.value
	var desc: String = "%s\n费用:%d" % [card_name, cost]
	if dmg > 0: desc += "  攻:%d" % dmg
	if blk > 0: desc += "  挡:%d" % blk
	if drw > 0: desc += "  抽:%d" % drw
	if nrg > 0: desc += "  能:%d" % nrg
	if wk > 0: desc += "  虚弱:%d" % wk
	return desc
```

注意缩进使用 Tab。

- [ ] **Step 2：新建 data/cards/entangle.tres**

新建文件 `e:/MyWork/AIGameTest2/data/cards/entangle.tres`，内容为：

```
[gd_resource type="Resource" script_class="CardData" load_steps=4 format=3]

[ext_resource type="Script" uid="uid://13mprie5g221" path="res://CardData.gd" id="1_CardData"]
[ext_resource type="Script" uid="uid://cxv8nrm4tp7dk" path="res://CardEffectData.gd" id="2_CardEffectData"]

[sub_resource type="Resource" id="Effect_weak"]
script = ExtResource("2_CardEffectData")
type = "weak"
value = 2

[resource]
script = ExtResource("1_CardData")
card_name = "缠绕"
cost = 1
effects = [SubResource("Effect_weak")]
```

- [ ] **Step 3：提交**

```bash
git -C "e:/MyWork/AIGameTest2" add CardData.gd data/cards/entangle.tres
git -C "e:/MyWork/AIGameTest2" commit -m "feat: add entangle card data and weak description to CardData"
```

---

## Task 5：更新 CombatScreen.tscn + CombatScreen.gd

**Files:**
- Modify: `e:/MyWork/AIGameTest2/combat/CombatScreen.tscn`
- Modify: `e:/MyWork/AIGameTest2/combat/CombatScreen.gd`

> 这两个文件必须同步修改并一起提交：`.gd` 中的 `@onready _lbl_enemy_weak` 路径引用 `.tscn` 中的节点，若分开提交中间状态启动时会报 `@onready` 空引用错误。

- [ ] **Step 1：修改 CombatScreen.tscn**

将 `e:/MyWork/AIGameTest2/combat/CombatScreen.tscn` 完整内容替换为：

```
[gd_scene load_steps=2 format=3 uid="uid://combat_screen"]

[ext_resource type="Script" path="res://combat/CombatScreen.gd" id="1_script"]

[node name="CombatScreen" type="Control"]
layout_mode = 3
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
script = ExtResource("1_script")

[node name="VBoxContainer" type="VBoxContainer" parent="."]
layout_mode = 1
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
theme_override_constants/separation = 10

[node name="EnemyPanel" type="VBoxContainer" parent="VBoxContainer"]
layout_mode = 2

[node name="LblEnemyName" type="Label" parent="VBoxContainer/EnemyPanel"]
layout_mode = 2
text = "Enemy"

[node name="LblEnemyHP" type="Label" parent="VBoxContainer/EnemyPanel"]
layout_mode = 2
text = "生命：?"

[node name="LblEnemyBlock" type="Label" parent="VBoxContainer/EnemyPanel"]
layout_mode = 2
text = "格挡：0"

[node name="LblEnemyWeak" type="Label" parent="VBoxContainer/EnemyPanel"]
layout_mode = 2
text = "虚弱：0"

[node name="LblEnemyIntent" type="Label" parent="VBoxContainer/EnemyPanel"]
layout_mode = 2
text = "意图：?"

[node name="PlayerPanel" type="VBoxContainer" parent="VBoxContainer"]
layout_mode = 2

[node name="LblPlayerHP" type="Label" parent="VBoxContainer/PlayerPanel"]
layout_mode = 2
text = "生命：?"

[node name="LblPlayerBlock" type="Label" parent="VBoxContainer/PlayerPanel"]
layout_mode = 2
text = "格挡：0"

[node name="LblEnergy" type="Label" parent="VBoxContainer/PlayerPanel"]
layout_mode = 2
text = "能量：3 / 3"

[node name="HandContainer" type="HBoxContainer" parent="VBoxContainer"]
layout_mode = 2

[node name="BtnEndTurn" type="Button" parent="VBoxContainer"]
layout_mode = 2
text = "结束回合"

[node name="BtnViewDeck" type="Button" parent="VBoxContainer"]
layout_mode = 2
text = "查看牌组"

[node name="LblResult" type="Label" parent="."]
layout_mode = 1
anchors_preset = 8
anchor_left = 0.5
anchor_top = 0.5
anchor_right = 0.5
anchor_bottom = 0.5
visible = false
text = ""

[node name="BtnReturnMenu" type="Button" parent="."]
layout_mode = 1
anchors_preset = 8
anchor_left = 0.5
anchor_top = 0.6
anchor_right = 0.5
anchor_bottom = 0.6
offset_left = -70.0
offset_top = -15.0
offset_right = 70.0
offset_bottom = 15.0
visible = false
text = "返回主菜单"

[node name="BtnGetReward" type="Button" parent="."]
layout_mode = 1
anchors_preset = 8
anchor_left = 0.5
anchor_top = 0.6
anchor_right = 0.5
anchor_bottom = 0.6
offset_left = -60.0
offset_top = -35.0
offset_right = 60.0
offset_bottom = -5.0
visible = false
text = "领取奖励"

[node name="BtnWin" type="Button" parent="."]
layout_mode = 1
anchors_preset = 8
anchor_left = 0.5
anchor_top = 0.6
anchor_right = 0.5
anchor_bottom = 0.6
offset_left = -50.0
offset_top = -55.0
offset_right = 50.0
offset_bottom = -25.0
visible = false
text = "通关！"

[node name="DeckViewPanel" type="Panel" parent="."]
layout_mode = 1
anchor_left = 0.1
anchor_top = 0.05
anchor_right = 0.9
anchor_bottom = 0.95
visible = false

[node name="VBoxContainer" type="VBoxContainer" parent="DeckViewPanel"]
layout_mode = 1
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
theme_override_constants/separation = 5

[node name="TabContainer" type="TabContainer" parent="DeckViewPanel/VBoxContainer"]
layout_mode = 2
size_flags_vertical = 3

[node name="完整牌组" type="ScrollContainer" parent="DeckViewPanel/VBoxContainer/TabContainer"]
layout_mode = 2

[node name="AllCardsList" type="VBoxContainer" parent="DeckViewPanel/VBoxContainer/TabContainer/完整牌组"]
layout_mode = 2

[node name="抽牌堆" type="ScrollContainer" parent="DeckViewPanel/VBoxContainer/TabContainer"]
layout_mode = 2

[node name="DrawList" type="VBoxContainer" parent="DeckViewPanel/VBoxContainer/TabContainer/抽牌堆"]
layout_mode = 2

[node name="弃牌堆" type="ScrollContainer" parent="DeckViewPanel/VBoxContainer/TabContainer"]
layout_mode = 2

[node name="DiscardList" type="VBoxContainer" parent="DeckViewPanel/VBoxContainer/TabContainer/弃牌堆"]
layout_mode = 2

[node name="BtnCloseDeck" type="Button" parent="DeckViewPanel/VBoxContainer"]
layout_mode = 2
text = "关闭"
```

- [ ] **Step 2：修改 CombatScreen.gd**

将 `e:/MyWork/AIGameTest2/combat/CombatScreen.gd` 完整内容替换为：

```gdscript
extends Control

@onready var _lbl_enemy_name: Label    = $VBoxContainer/EnemyPanel/LblEnemyName
@onready var _lbl_enemy_hp: Label      = $VBoxContainer/EnemyPanel/LblEnemyHP
@onready var _lbl_enemy_block: Label   = $VBoxContainer/EnemyPanel/LblEnemyBlock
@onready var _lbl_enemy_weak: Label    = $VBoxContainer/EnemyPanel/LblEnemyWeak
@onready var _lbl_enemy_intent: Label  = $VBoxContainer/EnemyPanel/LblEnemyIntent
@onready var _lbl_player_hp: Label     = $VBoxContainer/PlayerPanel/LblPlayerHP
@onready var _lbl_player_block: Label  = $VBoxContainer/PlayerPanel/LblPlayerBlock
@onready var _lbl_energy: Label        = $VBoxContainer/PlayerPanel/LblEnergy
@onready var _hand_container: HBoxContainer = $VBoxContainer/HandContainer
@onready var _btn_end_turn: Button     = $VBoxContainer/BtnEndTurn
@onready var _lbl_result: Label        = $LblResult
@onready var _btn_return_menu: Button  = $BtnReturnMenu
@onready var _btn_get_reward: Button   = $BtnGetReward
@onready var _btn_win: Button          = $BtnWin
@onready var _btn_view_deck: Button    = $VBoxContainer/BtnViewDeck
@onready var _deck_view_panel: Panel   = $DeckViewPanel
@onready var _btn_close_deck: Button   = $DeckViewPanel/VBoxContainer/BtnCloseDeck
@onready var _all_cards_list: VBoxContainer = $DeckViewPanel/VBoxContainer/TabContainer/完整牌组/AllCardsList
@onready var _draw_list: VBoxContainer      = $DeckViewPanel/VBoxContainer/TabContainer/抽牌堆/DrawList
@onready var _discard_list: VBoxContainer   = $DeckViewPanel/VBoxContainer/TabContainer/弃牌堆/DiscardList

var _engine: CombatEngine
var _hand_buttons: Array[Button] = []

func _ready() -> void:
	_engine = CombatEngine.new()
	_engine.state_changed.connect(_refresh_ui)
	_engine.combat_ended.connect(_on_combat_ended)
	_btn_end_turn.pressed.connect(_engine.end_turn)
	_btn_return_menu.pressed.connect(GameManager.go_to_menu)
	_btn_get_reward.pressed.connect(GameManager.go_to_reward)
	_btn_win.pressed.connect(GameManager.go_to_win)
	_btn_view_deck.pressed.connect(_on_view_deck_pressed)
	_btn_close_deck.pressed.connect(_deck_view_panel.hide)
	_engine.setup(GameManager.player_state.deck, GameManager.get_current_enemy_data(), GameManager.player_state.hp)

func _on_card_pressed(card: CardData) -> void:
	_engine.play_card(card)

func _refresh_ui() -> void:
	_lbl_enemy_name.text = _engine.enemy.display_name
	_lbl_enemy_hp.text = "生命：%d / %d" % [_engine.enemy.hp, _engine.enemy.max_hp]
	_lbl_enemy_block.text = "格挡：%d" % _engine.enemy.block
	_lbl_enemy_weak.text = "虚弱：%d" % _engine.enemy.weak
	var action: EnemyActionData = _engine.get_current_enemy_action()
	var intent: String = "攻击 %d" % action.value if action.type == "attack" else "格挡 %d" % action.value
	_lbl_enemy_intent.text = "意图：" + intent
	_lbl_player_hp.text = "生命：%d / %d" % [_engine.player.hp, _engine.player.max_hp]
	_lbl_player_block.text = "格挡：%d" % _engine.player.block
	_lbl_energy.text = "能量：%d / 3" % _engine.energy
	_rebuild_hand()

func _rebuild_hand() -> void:
	for btn: Button in _hand_buttons:
		btn.queue_free()
	_hand_buttons.clear()
	for card: CardData in _engine.hand:
		var btn: Button = Button.new()
		btn.text = card.get_description()
		btn.pressed.connect(_on_card_pressed.bind(card))
		_hand_container.add_child(btn)
		_hand_buttons.append(btn)

func _on_combat_ended(result: String) -> void:
	_lbl_result.text = "胜利！" if result == "victory" else "游戏结束"
	_lbl_result.visible = true
	_btn_end_turn.disabled = true
	if result == "victory":
		GameManager.player_state.hp = _engine.player.hp
		if GameManager.is_final_node():
			_btn_win.visible = true
		else:
			_btn_get_reward.visible = true
	else:
		_btn_return_menu.visible = true
	for btn: Button in _hand_buttons:
		btn.disabled = true

func _on_view_deck_pressed() -> void:
	var all_cards: Array[CardData] = []
	for card: CardData in _engine.hand:
		all_cards.append(card)
	for card: CardData in _engine.get_draw_pile():
		all_cards.append(card)
	for card: CardData in _engine.get_discard_pile():
		all_cards.append(card)
	_populate_list(_all_cards_list, all_cards)
	_populate_list(_draw_list, _engine.get_draw_pile())
	_populate_list(_discard_list, _engine.get_discard_pile())
	_deck_view_panel.show()

func _populate_list(container: VBoxContainer, cards: Array[CardData]) -> void:
	for child in container.get_children():
		container.remove_child(child)
		child.queue_free()
	for card: CardData in cards:
		var lbl: Label = Label.new()
		lbl.text = card.get_description()
		container.add_child(lbl)
```

注意缩进使用 Tab。

- [ ] **Step 3：提交**

```bash
git -C "e:/MyWork/AIGameTest2" add combat/CombatScreen.tscn combat/CombatScreen.gd
git -C "e:/MyWork/AIGameTest2" commit -m "feat: display enemy weak count in CombatScreen"
```

---

## Task 6：验收 + 打标签

**Files:** 无代码变更

> Godot MCP 工具（`mcp__godot__run_project` 等）在 subagent 环境中不可用，运行时验收由用户手动完成。本 Task 只做收尾提交和打标签。

- [ ] **Step 1：确认 git log 无遗漏**

```bash
git -C "e:/MyWork/AIGameTest2" log --oneline -6
```

期望输出（hash 不同，顺序和内容如下）：
```
xxxxxxx feat: display enemy weak count in CombatScreen
xxxxxxx feat: add entangle card data and weak description to CardData
xxxxxxx feat: apply weak modifier in enemy turn, decrement weak each turn
xxxxxxx feat: add weak effect and apply_damage with weak modifier to EffectResolver
xxxxxxx feat: add weak field and add_weak() to Combatant
xxxxxxx docs: add v0.13.0 status effects design spec
```

- [ ] **Step 2：打标签**

```bash
git -C "e:/MyWork/AIGameTest2" tag v0.13.0
```

---

## 验收清单（用户手动验证）

- [ ] 打出"缠绕"后，敌人面板显示"虚弱：2"
- [ ] 敌人在虚弱状态下攻击，伤害降低 25%（如原 10 → 7）
- [ ] 敌人攻击后虚弱层数 -1（从 2 → 1 → 0）
- [ ] 虚弱归零后伤害恢复正常
- [ ] 敌人格挡回合虚弱不递减
- [ ] 奖励界面可能出现"缠绕"卡，描述显示"虚弱:2"
- [ ] v0.12.0 全部功能正常（查看牌组弹窗等）
