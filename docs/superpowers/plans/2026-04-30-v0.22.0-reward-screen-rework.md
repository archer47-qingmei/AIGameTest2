# v0.22.0 奖励界面重构 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 重构奖励界面为两层结构——战利品列表（含遗物 + 卡牌项）+ 卡牌选择子界面，支持跳过卡牌，遗物点击即领取，Boss 战也经过奖励界面再进胜利界面。

**Architecture:** `GameManager` 新增 `pending_relic: RelicData` 字段，在 `end_combat()` 中根据节点类型赋值，Boss 战改走 `go_to_reward()` 而非直接 `go_to_win()`。`RewardScreen` 完整重构为 LIST / CARD_PICK 两个内部状态，所有 UI 元素在 `_ready()` 中动态创建（清除场景原有 LblTitle 和 CardContainer 子节点）。`RewardEngine` 不变。

**Tech Stack:** Godot 4.6.2 GDScript，Node 动态创建 UI，autoload GameManager

---

## 文件结构

| 操作 | 文件 | 职责 |
|---|---|---|
| 修改 | `GameManager.gd` | 新增 `pending_relic` 字段，重写 `end_combat()`，移除 `_grant_relic()` |
| 修改 | `reward/RewardScreen.gd` | 完整重构为两层状态机 |

---

## Task 1：GameManager — pending_relic + end_combat() 重写

**Files:**
- Modify: `GameManager.gd`

当前 `GameManager.gd` 的关键部分（第 13-14 行、第 62-71 行、第 112-113 行）：

```gdscript
var current_phase: Phase = Phase.MENU
var player_state: PlayerState

func end_combat(final_hp: int) -> void:
	player_state.hp = final_hp
	if is_elite_node():
		_grant_relic(preload("res://data/relics/burning_gem.tres"))
	elif is_final_node():
		_grant_relic(preload("res://data/relics/life_ring.tres"))
	if is_final_node():
		go_to_win()
	else:
		go_to_reward()

func _grant_relic(relic_data: RelicData) -> void:
	player_state.relics.append(relic_data.duplicate())
```

- [ ] **Step 1：将 GameManager.gd 完整替换为以下内容**

```gdscript
extends Node

enum Phase { MENU, MAP, COMBAT, REWARD, REST, WIN, GAME_OVER }

func _ready() -> void:
	var font: FontFile = load("res://data/fonts/NotoSansSC-Regular.otf") as FontFile
	if font:
		var theme: Theme = Theme.new()
		theme.default_font = font
		theme.default_font_size = 16
		get_tree().root.theme = theme

var current_phase: Phase = Phase.MENU
var player_state: PlayerState
var pending_relic: RelicData = null

func start_new_run() -> void:
	player_state = PlayerState.new()
	var strike: CardData = preload("res://data/cards/strike.tres")
	var defend: CardData = preload("res://data/cards/defend.tres")
	var bash: CardData   = preload("res://data/cards/bash.tres")
	var slash: CardData  = preload("res://data/cards/slash.tres")
	for i in 4:
		player_state.deck.append(strike.duplicate())
	for i in 4:
		player_state.deck.append(defend.duplicate())
	player_state.deck.append(bash.duplicate())
	player_state.deck.append(slash.duplicate())

	var a1: NodeData = NodeData.new()
	a1.config = preload("res://data/nodes/node_a1.tres")
	var a2: NodeData = NodeData.new()
	a2.config = preload("res://data/nodes/node_a2.tres")
	var b1: NodeData = NodeData.new()
	b1.config = preload("res://data/nodes/node_b1.tres")
	var b2: NodeData = NodeData.new()
	b2.config = preload("res://data/nodes/node_b2.tres")
	var c1: NodeData = NodeData.new()
	c1.config = preload("res://data/nodes/node_c1.tres")

	a1.connections.assign([b1, b2])
	a2.connections.assign([b1, b2])
	b1.connections.assign([c1])
	b2.connections.assign([c1])

	player_state.map_all_nodes.assign([a1, a2, b1, b2, c1])
	player_state.available_nodes.assign([a1, a2])

	current_phase = Phase.MAP
	get_tree().change_scene_to_file("res://map/MapScreen.tscn")

func select_node(node: NodeData) -> void:
	player_state.current_node = node
	if node.config.type == NodeConfig.Type.REST:
		go_to_rest()
	else:
		go_to_combat()

func go_to_combat() -> void:
	current_phase = Phase.COMBAT
	get_tree().change_scene_to_file("res://combat/CombatScreen.tscn")

func end_combat(final_hp: int) -> void:
	player_state.hp = final_hp
	if is_elite_node():
		pending_relic = preload("res://data/relics/burning_gem.tres")
	elif is_final_node():
		pending_relic = preload("res://data/relics/life_ring.tres")
	else:
		pending_relic = null
	go_to_reward()

func go_to_reward() -> void:
	current_phase = Phase.REWARD
	get_tree().change_scene_to_file("res://reward/RewardScreen.tscn")

func go_to_rest() -> void:
	player_state.apply_rest_heal()
	current_phase = Phase.REST
	get_tree().change_scene_to_file("res://rest/RestScreen.tscn")

func go_to_map() -> void:
	if player_state.current_node != null:
		player_state.completed_nodes.append(player_state.current_node)
		player_state.available_nodes.assign(player_state.current_node.connections)
		player_state.current_node = null
	current_phase = Phase.MAP
	get_tree().change_scene_to_file("res://map/MapScreen.tscn")

func go_to_win() -> void:
	current_phase = Phase.WIN
	get_tree().change_scene_to_file("res://win/WinScreen.tscn")

func go_to_game_over() -> void:
	current_phase = Phase.GAME_OVER
	get_tree().change_scene_to_file("res://game_over/GameOverScreen.tscn")

func go_to_menu() -> void:
	player_state = null
	current_phase = Phase.MENU
	get_tree().change_scene_to_file("res://menu/MainMenu.tscn")

func get_current_enemy_data() -> EnemyData:
	return player_state.current_node.config.enemy_data

func is_final_node() -> bool:
	return player_state.current_node.connections.is_empty()

func is_elite_node() -> bool:
	return player_state.current_node.config.type == NodeConfig.Type.ELITE
```

- [ ] **Step 2：验证**

运行游戏 → 开始新局 → 选 a1 普通战斗 → 胜利 → 确认进入奖励界面（当前 RewardScreen 尚未更新，界面可能空白，但不应报脚本错误）。

- [ ] **Step 3：提交**

```bash
git add GameManager.gd
git commit -m "feat: GameManager adds pending_relic, end_combat always routes through reward"
```

---

## Task 2：RewardScreen 两层状态机重构

**Files:**
- Modify: `reward/RewardScreen.gd`

当前场景 `reward/RewardScreen.tscn` 结构：
```
RewardScreen (Control)
  VBoxContainer
    LblTitle (Label)
    CardContainer (HBoxContainer)
```

`_ready()` 中会用 `child.free()` 清除 VBoxContainer 的所有子节点，动态重建 UI，所以不需要修改 `.tscn` 文件。

- [ ] **Step 1：将 reward/RewardScreen.gd 完整替换为以下内容**

```gdscript
extends Control

enum State { LIST, CARD_PICK }

@onready var _vbox: VBoxContainer = $VBoxContainer

var _state: State = State.LIST
var _btn_relic: Button
var _btn_card: Button
var _card_container: VBoxContainer
var _btn_continue: Button

func _ready() -> void:
	for child in _vbox.get_children():
		child.free()

	_btn_relic = Button.new()
	_vbox.add_child(_btn_relic)
	_btn_relic.pressed.connect(_on_relic_pressed)

	_btn_card = Button.new()
	_btn_card.text = "卡牌"
	_vbox.add_child(_btn_card)
	_btn_card.pressed.connect(_on_card_btn_pressed)

	_card_container = VBoxContainer.new()
	_vbox.add_child(_card_container)
	_card_container.hide()

	_btn_continue = Button.new()
	_btn_continue.text = "继续"
	_vbox.add_child(_btn_continue)
	_btn_continue.pressed.connect(_on_continue_pressed)

	_show_list()

func _show_list() -> void:
	_state = State.LIST
	_card_container.hide()
	_btn_card.show()
	_btn_continue.show()
	if _btn_relic.disabled:
		_btn_relic.show()
	else:
		var relic: RelicData = GameManager.pending_relic
		if relic != null:
			_btn_relic.text = "%s — %s" % [relic.display_name, relic.description]
			_btn_relic.show()
		else:
			_btn_relic.hide()

func _on_relic_pressed() -> void:
	var relic: RelicData = GameManager.pending_relic
	GameManager.player_state.relics.append(relic.duplicate())
	GameManager.pending_relic = null
	_btn_relic.disabled = true
	_btn_relic.text += " ✓"

func _on_card_btn_pressed() -> void:
	_state = State.CARD_PICK
	_btn_relic.hide()
	_btn_card.hide()
	_btn_continue.hide()
	_card_container.show()
	_build_card_options()

func _build_card_options() -> void:
	for child in _card_container.get_children():
		child.free()
	var options: Array[CardData] = RewardEngine.get_options()
	for card: CardData in options:
		var btn: Button = Button.new()
		btn.text = card.get_description()
		btn.pressed.connect(_on_card_selected.bind(card))
		_card_container.add_child(btn)
	var btn_skip: Button = Button.new()
	btn_skip.text = "跳过"
	btn_skip.pressed.connect(_on_skipped)
	_card_container.add_child(btn_skip)

func _on_card_selected(card: CardData) -> void:
	GameManager.player_state.deck.append(card.duplicate())
	_btn_card.text = "卡牌 ✓"
	_btn_card.disabled = true
	_show_list()

func _on_skipped() -> void:
	_btn_card.text = "卡牌（已跳过）"
	_btn_card.disabled = true
	_show_list()

func _on_continue_pressed() -> void:
	if GameManager.is_final_node():
		GameManager.go_to_win()
	else:
		GameManager.go_to_map()
```

- [ ] **Step 2：验证普通战斗奖励界面**

运行游戏 → 开始新局 → 选 a1 普通战斗 → 胜利 → 确认：
- 奖励界面显示「卡牌」按钮 + 「继续」按钮，无遗物按钮
- 点「卡牌」→ 出现 3 张卡牌选项 + 「跳过」按钮
- 选一张卡 → 返回列表，「卡牌 ✓」按钮变灰
- 点「继续」→ 跳转地图界面，无报错

- [ ] **Step 3：验证跳过卡牌**

重新开局 → a1 战斗胜利 → 奖励界面 → 点「卡牌」→ 点「跳过」→ 返回列表，按钮显示「卡牌（已跳过）」→ 点「继续」→ 正常进入地图

- [ ] **Step 4：验证精英战斗奖励界面**

新局 → 选 a1 战斗胜利 → 奖励选卡进入地图 → 选 b2 精英战斗胜利 → 确认：
- 奖励界面显示「燃烧宝石 — 每次战斗开始时，额外获得 1 点能量。」按钮 + 「卡牌」按钮 + 「继续」按钮
- 点遗物按钮 → 按钮变灰显示「✓」，`player_state.relics` 长度变为 1
- 点「卡牌」→ 正常进入卡牌选择
- 点「继续」→ 正常进入地图

- [ ] **Step 5：验证 Boss 战经过奖励界面**

走到 c1 Boss 战胜利 → 确认：
- 进入奖励界面（而非直接胜利界面）
- 显示「生命之环 — ...」按钮 + 「卡牌」按钮 + 「继续」按钮
- 点「继续」→ 跳转胜利界面

- [ ] **Step 6：提交**

```bash
git add reward/RewardScreen.gd
git commit -m "feat: RewardScreen two-level UI with relic option, card pick, and skip"
```

---

## Task 3：集成验证

- [ ] **验收 1：普通战斗 — 只有卡牌项**

开局 → a1 普通战斗胜利 → 奖励界面：
- 无遗物按钮
- 有「卡牌」按钮
- 有「继续」按钮

- [ ] **验收 2：精英战斗 — 遗物可选**

选 a1 胜利选卡 → 选 b2 精英战斗胜利 → 奖励界面：
- 显示「燃烧宝石 — ...」按钮
- 点击领取后按钮变灰（带 ✓）
- 点「卡牌」正常进入卡牌选择
- 领完遗物 + 选卡后点「继续」→ 地图界面
- 下次进入战斗（Boss），战斗界面遗物栏显示「燃烧宝石」，回合 1 能量为 4

- [ ] **验收 3：可以不领遗物直接继续**

精英战胜利 → 奖励界面 → 不点遗物 → 点「继续」→ 正常进入地图
→ 下次战斗遗物栏显示「遗物：无」（`pending_relic` 被下次 `end_combat()` 覆盖为 null）

- [ ] **验收 4：Boss 战经过奖励界面**

走完 a → b → c1 Boss 战胜利 → 奖励界面显示「生命之环」→ 点「继续」→ 胜利界面

- [ ] **验收 5：战斗失败不经过奖励界面**

任意战斗中挂机 → 直接跳 Game Over，不经过奖励界面

- [ ] **最终提交**

```bash
git add .
git commit -m "docs: v0.22.0 integration tests passed"
git tag v0.22.0
```
