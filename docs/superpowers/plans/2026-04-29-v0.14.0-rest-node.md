# v0.14.0 休息节点 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 地图扩展为 4 节点（普通 → 普通 → 休息 → Boss），玩家进入休息节点可回复最大 HP 的 30%。

**Architecture:** `PlayerState` 新增 `max_hp` 字段；`GameManager` 序列生成插入 `"rest"` 字符串并新增 `go_to_rest()`；`MapScreen` 区分节点类型显示标签和路由；新建 `RestScreen` 场景执行回血逻辑。

**Tech Stack:** Godot 4.6, GDScript 严格类型模式

---

## 文件变更一览

| 操作 | 文件 | 变更摘要 |
|---|---|---|
| 修改 | `PlayerState.gd` | 新增 `max_hp: int = 70` |
| 修改 | `GameManager.gd` | Phase 新增 REST；序列插入 "rest"；新增 `go_to_rest()` |
| 修改 | `map/MapScreen.gd` | 节点标签区分类型；点击路由区分战斗/休息 |
| 新增 | `rest/RestScreen.tscn` | 休息场景节点树 |
| 新增 | `rest/RestScreen.gd` | 回血逻辑 + UI 更新 |

---

## Task 1：更新 PlayerState.gd + GameManager.gd

**Files:**
- Modify: `e:/MyWork/AIGameTest2/PlayerState.gd`
- Modify: `e:/MyWork/AIGameTest2/GameManager.gd`

> 这两个文件构成逻辑层基础，必须同步修改：`RestScreen` 依赖 `PlayerState.max_hp`，`GameManager.go_to_rest()` 依赖新增的 `Phase.REST`。一起提交避免中间状态。

- [ ] **Step 1：修改 PlayerState.gd**

将 `e:/MyWork/AIGameTest2/PlayerState.gd` 完整内容替换为：

```gdscript
class_name PlayerState
extends RefCounted

var deck: Array[CardData] = []
var current_node: int = 0
var hp: int = 70
var max_hp: int = 70
var enemy_sequence: Array[String] = []
```

注意缩进使用 Tab。

- [ ] **Step 2：修改 GameManager.gd**

将 `e:/MyWork/AIGameTest2/GameManager.gd` 完整内容替换为：

```gdscript
extends Node

enum Phase { MENU, MAP, COMBAT, REWARD, REST, WIN }

const NORMAL_POOL: Array[String] = [
	"res://data/enemies/jaw_worm.tres",
	"res://data/enemies/fire_lizard.tres",
]
const BOSS_ENEMY: String = "res://data/enemies/boss.tres"

var current_phase: Phase = Phase.MENU
var player_state: PlayerState

func start_new_run() -> void:
	player_state = PlayerState.new()
	var strike: CardData = preload("res://data/cards/strike.tres")
	var defend: CardData = preload("res://data/cards/defend.tres")
	var bash: CardData   = preload("res://data/cards/bash.tres")
	var slash: CardData  = preload("res://data/cards/slash.tres")
	for i in 4:
		player_state.deck.append(strike.duplicate())
	for i in 4:
		player_state.deck.append(defend.duplicate())
	player_state.deck.append(bash.duplicate())
	player_state.deck.append(slash.duplicate())
	var pool: Array[String] = NORMAL_POOL.duplicate()
	pool.shuffle()
	player_state.enemy_sequence.assign(pool)
	player_state.enemy_sequence.append("rest")
	player_state.enemy_sequence.append(BOSS_ENEMY)
	current_phase = Phase.MAP
	get_tree().change_scene_to_file("res://map/MapScreen.tscn")

func go_to_combat() -> void:
	current_phase = Phase.COMBAT
	get_tree().change_scene_to_file("res://combat/CombatScreen.tscn")

func go_to_reward() -> void:
	current_phase = Phase.REWARD
	get_tree().change_scene_to_file("res://reward/RewardScreen.tscn")

func go_to_rest() -> void:
	current_phase = Phase.REST
	get_tree().change_scene_to_file("res://rest/RestScreen.tscn")

func go_to_map() -> void:
	player_state.current_node += 1
	current_phase = Phase.MAP
	get_tree().change_scene_to_file("res://map/MapScreen.tscn")

func go_to_win() -> void:
	current_phase = Phase.WIN
	get_tree().change_scene_to_file("res://win/WinScreen.tscn")

func go_to_menu() -> void:
	player_state = null
	current_phase = Phase.MENU
	get_tree().change_scene_to_file("res://menu/MainMenu.tscn")

func get_current_enemy_data() -> EnemyData:
	return load(player_state.enemy_sequence[player_state.current_node]) as EnemyData

func is_final_node() -> bool:
	return player_state.current_node == player_state.enemy_sequence.size() - 1
```

注意缩进使用 Tab。

- [ ] **Step 3：提交**

```bash
git -C "e:/MyWork/AIGameTest2" add PlayerState.gd GameManager.gd
git -C "e:/MyWork/AIGameTest2" commit -m "feat: add max_hp to PlayerState, add REST phase and go_to_rest() to GameManager"
```

---

## Task 2：更新 map/MapScreen.gd

**Files:**
- Modify: `e:/MyWork/AIGameTest2/map/MapScreen.gd`

> 当前代码将标签生成和按钮路由混在 `_ready()` 的循环中，且硬编码 `go_to_combat`。需要：1) 提取 `_get_node_label()` 方法区分节点类型；2) 路由逻辑判断 `sequence[i] == "rest"` 来选择跳转目标。
>
> 注意：当前只有 `i == current` 的节点有可点击按钮，无需在按钮回调中传递 `i`——点击时 `current_node` 已经是正确值。

- [ ] **Step 1：修改 map/MapScreen.gd**

将 `e:/MyWork/AIGameTest2/map/MapScreen.gd` 完整内容替换为：

```gdscript
extends Control

@onready var _node_container: VBoxContainer = $VBoxContainer/NodeContainer

func _ready() -> void:
	var current: int = GameManager.player_state.current_node
	var sequence: Array[String] = GameManager.player_state.enemy_sequence
	var total: int = sequence.size()
	for i: int in total:
		var btn: Button = Button.new()
		var label: String = _get_node_label(i, sequence, total)
		if i < current:
			btn.text = label + "（已完成）"
			btn.disabled = true
		elif i == current:
			btn.text = label
			if sequence[i] == "rest":
				btn.pressed.connect(GameManager.go_to_rest)
			else:
				btn.pressed.connect(GameManager.go_to_combat)
		else:
			btn.text = label + "（未解锁）"
			btn.disabled = true
		_node_container.add_child(btn)

func _get_node_label(i: int, sequence: Array[String], total: int) -> String:
	if sequence[i] == "rest":
		return "休息站"
	elif i == total - 1:
		return "关卡 %d（Boss）" % (i + 1)
	else:
		return "关卡 %d" % (i + 1)
```

注意缩进使用 Tab。

- [ ] **Step 2：提交**

```bash
git -C "e:/MyWork/AIGameTest2" add map/MapScreen.gd
git -C "e:/MyWork/AIGameTest2" commit -m "feat: distinguish rest/combat nodes in MapScreen labels and routing"
```

---

## Task 3：新建 RestScreen

**Files:**
- Create: `e:/MyWork/AIGameTest2/rest/RestScreen.tscn`
- Create: `e:/MyWork/AIGameTest2/rest/RestScreen.gd`

> `RestScreen.gd` 的 `_ready()` 立即执行回血（读取并修改 `GameManager.player_state.hp`），显示结果，然后等待玩家点击"继续"。回血上限为 `max_hp`，防止溢出。
>
> `.tscn` 和 `.gd` 必须同步创建并一起提交，否则场景启动时找不到脚本。

- [ ] **Step 1：新建 rest/RestScreen.gd**

新建文件 `e:/MyWork/AIGameTest2/rest/RestScreen.gd`，内容为（缩进使用 Tab）：

```gdscript
extends Control

@onready var _lbl_heal: Label      = $VBoxContainer/LblHeal
@onready var _lbl_hp: Label        = $VBoxContainer/LblHP
@onready var _btn_continue: Button = $VBoxContainer/BtnContinue

func _ready() -> void:
	var state: PlayerState = GameManager.player_state
	var heal: int = int(state.max_hp * 0.3)
	state.hp = min(state.hp + heal, state.max_hp)
	_lbl_heal.text = "恢复了 %d 点生命" % heal
	_lbl_hp.text = "当前生命：%d / %d" % [state.hp, state.max_hp]
	_btn_continue.pressed.connect(GameManager.go_to_map)
```

- [ ] **Step 2：新建 rest/RestScreen.tscn**

新建文件 `e:/MyWork/AIGameTest2/rest/RestScreen.tscn`，内容为：

```
[gd_scene load_steps=2 format=3 uid="uid://rest_screen"]

[ext_resource type="Script" path="res://rest/RestScreen.gd" id="1_script"]

[node name="RestScreen" type="Control"]
layout_mode = 3
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
script = ExtResource("1_script")

[node name="VBoxContainer" type="VBoxContainer" parent="."]
layout_mode = 1
anchors_preset = 8
anchor_left = 0.5
anchor_top = 0.5
anchor_right = 0.5
anchor_bottom = 0.5
offset_left = -150.0
offset_top = -60.0
offset_right = 150.0
offset_bottom = 60.0
theme_override_constants/separation = 12

[node name="LblTitle" type="Label" parent="VBoxContainer"]
layout_mode = 2
text = "休息站"
horizontal_alignment = 1

[node name="LblHeal" type="Label" parent="VBoxContainer"]
layout_mode = 2
text = "恢复了 ? 点生命"

[node name="LblHP" type="Label" parent="VBoxContainer"]
layout_mode = 2
text = "当前生命：? / 70"

[node name="BtnContinue" type="Button" parent="VBoxContainer"]
layout_mode = 2
text = "继续"
```

- [ ] **Step 3：提交**

```bash
git -C "e:/MyWork/AIGameTest2" add rest/RestScreen.gd rest/RestScreen.tscn
git -C "e:/MyWork/AIGameTest2" commit -m "feat: add RestScreen with 30% max HP heal"
```

---

## Task 4：验收 + 打标签

**Files:** 无代码变更

> Godot MCP 工具在 subagent 环境中不可用，运行时验收由用户手动完成。本 Task 只做收尾和打标签。

- [ ] **Step 1：确认 git log 无遗漏**

```bash
git -C "e:/MyWork/AIGameTest2" log --oneline -5
```

期望输出（hash 不同，内容如下）：
```
xxxxxxx feat: add RestScreen with 30% max HP heal
xxxxxxx feat: distinguish rest/combat nodes in MapScreen labels and routing
xxxxxxx feat: add max_hp to PlayerState, add REST phase and go_to_rest() to GameManager
xxxxxxx docs: add v0.14.0 rest node implementation plan
xxxxxxx docs: add v0.14.0 rest node design spec
```

- [ ] **Step 2：打标签**

```bash
git -C "e:/MyWork/AIGameTest2" tag v0.14.0
```

---

## 验收清单（用户手动验证）

- [ ] 地图显示 4 个节点，第 3 个标为"休息站"
- [ ] 已完成节点标为"（已完成）"并禁用，未解锁标为"（未解锁）"
- [ ] 点击"休息站"跳转到休息场景
- [ ] 休息场景显示"恢复了 21 点生命"和当前 HP
- [ ] 满血进入时，HP 不超过 max_hp（70）
- [ ] 点击"继续"返回地图，current_node 推进到 Boss 节点
- [ ] Boss 节点可正常点击进入战斗
- [ ] v0.13.0 全部功能正常（虚弱效果、查看牌组等）
