# v0.48.0 拖拽出牌 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将手牌出牌交互从两步点击改为拖拽连线，按下卡牌抬起，拖向目标，虚线连接，松手出牌。

**Architecture:** 新增全屏 `DragLayer`（Control，最顶层），平时 `mouse_filter=IGNORE`，拖拽中切 `STOP` 接管所有输入；HandArea 卡牌按钮监听 `button_down` 触发 `begin_drag()`；DragLayer 管理幽灵牌、虚线绘制（`_draw()`）、目标检测、状态机（IDLE/LIFTED/DRAGGING）；CombatScreen 监听 DragLayer 信号触发 `_engine.play_card()`。旧的 `_pending_card_index` + `_set_targeting_mode()` 整体移除。

**Tech Stack:** Godot 4.4.1 GDScript，严格类型，Control 绝对坐标，`draw_dashed_line()`，Tween 动画。

---

## 文件结构

| 文件 | 动作 | 说明 |
|------|------|------|
| `combat/DragLayer.gd` | 新增 | 拖拽控制器：状态机、幽灵牌、连线、目标检测 |
| `combat/CombatScreen.tscn` | 修改 | 新增 DragLayer 节点（全屏，最后子节点） |
| `combat/CombatScreen.gd` | 修改 | 移除旧两步逻辑，接入 DragLayer 信号 |
| `menu/MainMenu.tscn` | 修改 | 版本号 v0.47.0 → v0.48.0 |

**重要背景（阅读代码时需知）：**
- `CardData.target_type` 是 String：`"single"`（需选目标）、`"all"`（群体）、`"none"`（自身/无目标）
- `CombatEngine.play_card(card_index, target_index)` 中，`target_index = -1` 对应 "none"/"all" 类型
- `CombatScreen._refresh_ui()` 在每次 `state_changed` 信号后调用，它会调用 `_rebuild_hand()` 重建所有手牌按钮
- 敌人面板结构：`Panel > VBoxContainer(LblInfo, LblIntent, StatusRow)` + `Button(BtnOverlay)` —— BtnOverlay 在新系统中始终保持 disabled，不再用于目标选择

---

### Task 1: 创建 DragLayer.gd

**Files:**
- Create: `combat/DragLayer.gd`

- [ ] **Step 1: 创建文件，写入完整脚本**

新建 `E:/MyWork/AIGameTest2/combat/DragLayer.gd`，内容如下：

```gdscript
class_name DragLayer
extends Control

signal card_played(card_index: int, target_engine_index: int)
signal drag_cancelled(card_index: int)
signal target_changed(old_engine_index: int, new_engine_index: int)

enum State { IDLE, LIFTED, DRAGGING }

const DRAG_THRESHOLD := 8.0

var _state: State = State.IDLE
var _card_index: int = -1
var _target_type: String = ""
var _enemy_local_positions: Array[Vector2] = []
var _enemy_engine_indices: Array[int] = []
var _current_slot: int = -1
var _ghost_card: Panel = null
var _origin_local_pos: Vector2 = Vector2.ZERO
var _target_line_ends: Array[Vector2] = []

func begin_drag(card_index: int, card_global_pos: Vector2, card_text: String,
		target_type: String, enemy_global_positions: Array[Vector2],
		enemy_engine_indices: Array[int]) -> void:
	_card_index = card_index
	_target_type = target_type
	_origin_local_pos = to_local(card_global_pos)
	_enemy_engine_indices = enemy_engine_indices.duplicate()
	_enemy_local_positions.clear()
	for gp in enemy_global_positions:
		_enemy_local_positions.append(to_local(gp))
	_current_slot = -1
	_state = State.LIFTED
	mouse_filter = MOUSE_FILTER_STOP
	_create_ghost(card_text, _origin_local_pos)
	if target_type == "all":
		_target_line_ends = _enemy_local_positions.duplicate()
		if not _enemy_local_positions.is_empty():
			_current_slot = 0

func _create_ghost(text: String, local_pos: Vector2) -> void:
	_ghost_card = Panel.new()
	_ghost_card.custom_minimum_size = Vector2(110, 150)
	_ghost_card.size = Vector2(110, 150)
	var style := StyleBoxFlat.new()
	style.bg_color = Color(0.2, 0.2, 0.2)
	style.border_width_left = 2
	style.border_width_right = 2
	style.border_width_top = 2
	style.border_width_bottom = 2
	style.border_color = Color.WHITE
	_ghost_card.add_theme_stylebox_override("panel", style)
	_ghost_card.modulate.a = 0.9
	_ghost_card.mouse_filter = MOUSE_FILTER_IGNORE
	var lbl := Label.new()
	lbl.text = text
	lbl.autowrap_mode = TextServer.AUTOWRAP_WORD_SMART
	lbl.vertical_alignment = VERTICAL_ALIGNMENT_TOP
	lbl.anchor_right = 1.0
	lbl.anchor_bottom = 1.0
	lbl.mouse_filter = MOUSE_FILTER_IGNORE
	_ghost_card.add_child(lbl)
	_ghost_card.position = local_pos
	add_child(_ghost_card)

func _gui_input(event: InputEvent) -> void:
	if _state == State.IDLE:
		return
	if event is InputEventMouseMotion:
		_on_move((event as InputEventMouseMotion).position)
	elif event is InputEventScreenDrag:
		_on_move((event as InputEventScreenDrag).position)
	elif event is InputEventMouseButton:
		var mb := event as InputEventMouseButton
		if mb.button_index == MOUSE_BUTTON_LEFT and not mb.pressed:
			_on_release(mb.position)
	elif event is InputEventScreenTouch:
		var touch := event as InputEventScreenTouch
		if not touch.pressed:
			_on_release(touch.position)

func _on_move(local_pos: Vector2) -> void:
	if _state == State.LIFTED:
		if _origin_local_pos.distance_to(local_pos) > DRAG_THRESHOLD:
			_state = State.DRAGGING
	if _state != State.DRAGGING:
		return
	_ghost_card.position = local_pos - Vector2(55, 75)
	if _target_type == "single":
		var new_slot := _detect_slot(local_pos.x)
		if new_slot != _current_slot:
			var old_engine := _engine_index(_current_slot)
			var new_engine := _engine_index(new_slot)
			_current_slot = new_slot
			_target_line_ends = [_enemy_local_positions[new_slot]] if new_slot >= 0 else []
			target_changed.emit(old_engine, new_engine)
	queue_redraw()

func _on_release(local_pos: Vector2) -> void:
	var viewport_h := get_viewport_rect().size.y
	var above := local_pos.y < viewport_h * 0.65
	var can_play := false
	if _state == State.DRAGGING:
		match _target_type:
			"none", "all":
				can_play = above
			"single":
				can_play = above and _current_slot >= 0
	if can_play:
		_finish_play()
	else:
		_start_cancel()

func _finish_play() -> void:
	var idx := _card_index
	var engine_idx := _engine_index(_current_slot) if _target_type == "single" else -1
	_cleanup()
	card_played.emit(idx, engine_idx)

func _start_cancel() -> void:
	var idx_capture := _card_index
	if _state != State.DRAGGING or _ghost_card == null:
		_cleanup()
		drag_cancelled.emit(idx_capture)
		return
	var tw := create_tween()
	tw.tween_property(_ghost_card, "position", _origin_local_pos, 0.15).set_ease(Tween.EASE_OUT)
	tw.tween_callback(func() -> void:
		_cleanup()
		drag_cancelled.emit(idx_capture)
	)

func _cleanup() -> void:
	if _ghost_card != null:
		_ghost_card.queue_free()
		_ghost_card = null
	_state = State.IDLE
	_card_index = -1
	_target_type = ""
	_enemy_local_positions.clear()
	_enemy_engine_indices.clear()
	_target_line_ends.clear()
	_current_slot = -1
	mouse_filter = MOUSE_FILTER_IGNORE
	queue_redraw()

func _detect_slot(drag_x: float) -> int:
	if _enemy_local_positions.is_empty():
		return -1
	var best := 0
	var best_dist := absf(_enemy_local_positions[0].x - drag_x)
	for i in range(1, _enemy_local_positions.size()):
		var dist := absf(_enemy_local_positions[i].x - drag_x)
		if dist < best_dist:
			best_dist = dist
			best = i
	return best

func _engine_index(slot: int) -> int:
	if slot < 0 or slot >= _enemy_engine_indices.size():
		return -1
	return _enemy_engine_indices[slot]

func _draw() -> void:
	if _state != State.DRAGGING or _ghost_card == null:
		return
	var from := _ghost_card.position + Vector2(55, 0)
	for target_pos: Vector2 in _target_line_ends:
		draw_dashed_line(from, target_pos, Color(1.0, 1.0, 1.0, 0.85), 2.0, 12.0)
```

- [ ] **Step 2: 提交**

```bash
git add combat/DragLayer.gd
git commit -m "feat: add DragLayer drag-to-play controller"
```

---

### Task 2: 在 CombatScreen.tscn 中添加 DragLayer 节点

**Files:**
- Modify: `combat/CombatScreen.tscn`

- [ ] **Step 1: 在 tscn 第 1 行修改 load_steps**

找到第 1 行：
```
[gd_scene load_steps=2 format=3 uid="uid://dhkts607n6vgl"]
```

改为：
```
[gd_scene load_steps=3 format=3 uid="uid://dhkts607n6vgl"]
```

- [ ] **Step 2: 在第 3 行（ext_resource 之后）添加 DragLayer 脚本引用**

找到：
```
[ext_resource type="Script" uid="uid://dxpvteyvm43qd" path="res://combat/CombatScreen.gd" id="1_script"]
```

在这行**之后**插入：
```
[ext_resource type="Script" path="res://combat/DragLayer.gd" id="2_dragscript"]
```

- [ ] **Step 3: 在文件末尾（BtnCloseDeck 节点之后）追加 DragLayer 节点**

在文件末尾（最后一行之后）追加：

```
[node name="DragLayer" type="Control" parent="."]
layout_mode = 1
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
grow_horizontal = 2
grow_vertical = 2
mouse_filter = 2
script = ExtResource("2_dragscript")
```

`mouse_filter = 2` 对应 `MOUSE_FILTER_IGNORE`（Godot 4 枚举值：STOP=0, PASS=1, IGNORE=2）。

- [ ] **Step 4: 提交**

```bash
git add combat/CombatScreen.tscn
git commit -m "feat: add DragLayer node to CombatScreen"
```

---

### Task 3: 更新 CombatScreen.gd

**Files:**
- Modify: `combat/CombatScreen.gd`

此 Task 分 6 步，全部完成后一次性提交。

- [ ] **Step 1: 添加 @onready，移除 _pending_card_index**

找到第 24 行附近的 `@onready var _deck_view_panel` 行，在其后的空行前插入：
```gdscript
@onready var _drag_layer: DragLayer = $DragLayer
```

（完整 @onready 块末尾应如下，供参考）：
```gdscript
@onready var _exhaust_list: VBoxContainer   = $DeckViewPanel/VBoxContainer/TabContainer/消耗区/ExhaustList

@onready var _drag_layer: DragLayer = $DragLayer
```

然后找到并**删除**：
```gdscript
var _pending_card_index: int = -1
```

- [ ] **Step 2: 更新 _ready()，连接 DragLayer 信号**

找到 `_ready()` 中的：
```gdscript
	_hand_area.resized.connect(_layout_hand)
	_refresh_ui()
```

替换为：
```gdscript
	_hand_area.resized.connect(_layout_hand)
	_drag_layer.card_played.connect(_on_drag_card_played)
	_drag_layer.drag_cancelled.connect(_on_drag_cancelled)
	_drag_layer.target_changed.connect(_on_drag_target_changed)
	_refresh_ui()
```

- [ ] **Step 3: 改写 _rebuild_hand()，切换为 button_down**

找到 `_rebuild_hand()` 中的：
```gdscript
		btn.pressed.connect(_on_card_pressed.bind(i))
		_hand_area.add_child(btn)
		_hand_buttons.append(btn)
	_layout_hand()
	if _pending_card_index >= 0:
		_set_targeting_mode(true)
```

替换为：
```gdscript
		btn.button_down.connect(_on_card_button_down.bind(i))
		_hand_area.add_child(btn)
		_hand_buttons.append(btn)
	_layout_hand()
```

- [ ] **Step 4: 更新 _refresh_ui()，移除 BtnOverlay 启用逻辑**

找到 `_refresh_ui()` 中的：
```gdscript
			btn.disabled = (_pending_card_index < 0)
```

替换为：
```gdscript
			btn.disabled = true
```

- [ ] **Step 5: 更新 _on_combat_ended()，移除 _pending_card_index 引用**

找到 `_on_combat_ended()` 的开头：
```gdscript
func _on_combat_ended(result: String) -> void:
	_pending_card_index = -1
	_btn_end_turn.disabled = true
```

替换为：
```gdscript
func _on_combat_ended(result: String) -> void:
	_btn_end_turn.disabled = true
```

- [ ] **Step 6: 删除旧函数，新增拖拽处理函数**

**删除**以下四个函数（整体删除）：

```gdscript
func _on_card_pressed(card_index: int) -> void:
	...

func _unhandled_input(event: InputEvent) -> void:
	...

func _on_enemy_pressed(enemy_index: int) -> void:
	...

func _set_targeting_mode(active: bool) -> void:
	...
```

在 `_rebuild_hand()` 函数**之前**（或 `_on_combat_ended()` 之前任意位置）**新增**以下六个函数：

```gdscript
func _on_card_button_down(card_index: int) -> void:
	var card: CardData = _engine.hand[card_index]
	if _engine.energy < card.cost:
		_shake_card(card_index)
		return
	var btn := _hand_buttons[card_index]
	btn.position.y -= 20
	var enemy_positions: Array[Vector2] = []
	var enemy_indices: Array[int] = []
	for i in _engine.enemies.size():
		if _engine.enemies[i].hp > 0:
			var panel := _enemies_container.get_child(i) as Panel
			enemy_positions.append(panel.get_global_rect().get_center())
			enemy_indices.append(i)
	if card.target_type == "all":
		for idx in enemy_indices:
			(_enemies_container.get_child(idx) as Panel).modulate = Color(1.3, 1.3, 1.0)
	_drag_layer.begin_drag(
		card_index,
		btn.global_position,
		card.get_description(),
		card.target_type,
		enemy_positions,
		enemy_indices
	)

func _shake_card(card_index: int) -> void:
	var btn := _hand_buttons[card_index]
	var orig_x := btn.position.x
	var tw := create_tween()
	tw.tween_property(btn, "position:x", orig_x + 4.0, 0.05)
	tw.tween_property(btn, "position:x", orig_x - 4.0, 0.05)
	tw.tween_property(btn, "position:x", orig_x + 4.0, 0.05)
	tw.tween_property(btn, "position:x", orig_x, 0.05)

func _clear_target_highlights() -> void:
	for i in _enemies_container.get_child_count():
		(_enemies_container.get_child(i) as Panel).modulate = Color.WHITE

func _on_drag_card_played(card_index: int, target_engine_index: int) -> void:
	_clear_target_highlights()
	_engine.play_card(card_index, target_engine_index)

func _on_drag_cancelled(card_index: int) -> void:
	_clear_target_highlights()
	if card_index >= 0 and card_index < _hand_buttons.size():
		var btn := _hand_buttons[card_index]
		if is_instance_valid(btn):
			btn.position.y += 20

func _on_drag_target_changed(old_engine_index: int, new_engine_index: int) -> void:
	if old_engine_index >= 0 and old_engine_index < _enemies_container.get_child_count():
		(_enemies_container.get_child(old_engine_index) as Panel).modulate = Color.WHITE
	if new_engine_index >= 0 and new_engine_index < _enemies_container.get_child_count():
		(_enemies_container.get_child(new_engine_index) as Panel).modulate = Color(1.3, 1.3, 1.0)
```

- [ ] **Step 7: 提交**

```bash
git add combat/CombatScreen.gd
git commit -m "feat: replace two-tap targeting with drag-to-play system"
```

---

### Task 4: 版本号更新与验收

**Files:**
- Modify: `menu/MainMenu.tscn`

- [ ] **Step 1: 更新版本号**

在 `menu/MainMenu.tscn` 第 35 行找到：
```
text = "v0.47.0"
```

改为：
```
text = "v0.48.0"
```

- [ ] **Step 2: 提交版本号**

```bash
git add menu/MainMenu.tscn
git commit -m "feat: bump version to v0.48.0"
```

- [ ] **Step 3: 运行项目验收**

在 Godot 编辑器或通过 Godot MCP 运行项目，进入战斗界面，逐条确认：

- [ ] 点击手牌，卡牌向上移动 20px（目视检查）
- [ ] 向上拖动后，幽灵牌跟随手指/鼠标，白色虚线连接最近的敌人
- [ ] 左右拖动时，虚线切换目标，高亮（偏黄色）跟随切换
- [ ] AoE 牌（群体攻击，如「狂风斩」等 target_type="all"）拖起时，虚线同时指向所有存活敌人，所有敌人高亮
- [ ] 在敌人面板上方区域松手，出牌，效果正确作用于目标
- [ ] 拖回手牌区（底部）松手，幽灵牌飞回原位，卡牌下移归位
- [ ] 能量不足时点击卡牌，卡牌水平震动，不进入拖拽
- [ ] 自身 buff 牌（target_type="none"，如「防御」「充能」）拖起后松手到手牌区以上即打出，无需瞄准敌人
- [ ] 版本号显示 v0.48.0
