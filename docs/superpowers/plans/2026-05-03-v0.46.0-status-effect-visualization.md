# v0.46.0 状态效果可视化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在敌人面板和玩家卡牌上直观显示 weak/vulnerable 剩余回合数，黄色=虚弱，橙色=脆弱。

**Architecture:** 将敌人 Button 替换为 Panel + VBoxContainer(LblInfo/LblIntent/StatusRow) + 全尺寸透明 BtnOverlay 结构；玩家卡牌同理由 Button 改为 Panel + VBoxContainer(LblPlayerName/PlayerStatusRow)；新增 `_build_status_row()` 辅助函数统一构建状态标签行。

**Tech Stack:** Godot 4 GDScript，严格类型，add_theme_color_override 着色，Tween 动画。

---

## 文件结构

| 文件 | 动作 | 变更内容 |
|------|------|----------|
| `combat/CombatScreen.gd` | 修改 | 重构 _build_enemy_panels、_refresh_ui、_set_targeting_mode、_on_combat_ended、_on_damage_dealt；新增 _build_status_row；更新 @onready 引用 |
| `combat/CombatScreen.tscn` | 修改 | PlayerCard Button → PlayerCardPanel Panel + VBoxContainer + PlayerStatusRow |
| `menu/MainMenu.tscn` | 修改 | 版本号 v0.45.0 → v0.46.0 |

---

### Task 1: 重构敌人面板（CombatScreen.gd）

**Files:**
- Modify: `combat/CombatScreen.gd`

- [ ] **Step 1: 替换 _build_enemy_panels()**

将函数从「创建单个 Button」改为「Panel + VBoxContainer + 三个子节点 + BtnOverlay」结构：

```gdscript
func _build_enemy_panels() -> void:
	for child in _enemies_container.get_children():
		child.queue_free()
	for i in _engine.enemies.size():
		var panel := Panel.new()
		panel.custom_minimum_size = Vector2(150, 120)

		var vbox := VBoxContainer.new()
		var lbl_info := Label.new()
		lbl_info.name = "LblInfo"
		lbl_info.autowrap_mode = TextServer.AUTOWRAP_WORD_SMART
		var lbl_intent := Label.new()
		lbl_intent.name = "LblIntent"
		var status_row := HBoxContainer.new()
		status_row.name = "StatusRow"
		status_row.add_theme_constant_override("separation", 6)
		vbox.add_child(lbl_info)
		vbox.add_child(lbl_intent)
		vbox.add_child(status_row)
		panel.add_child(vbox)

		var btn := Button.new()
		btn.name = "BtnOverlay"
		btn.flat = true
		btn.anchors_preset = 15
		btn.pressed.connect(_on_enemy_pressed.bind(i))
		btn.disabled = true
		panel.add_child(btn)

		_enemies_container.add_child(panel)
```

- [ ] **Step 2: 替换 _refresh_ui() 敌人部分**

找到当前 `_refresh_ui()` 中 `for i in _engine.enemies.size():` 循环，替换为通过 get_node 访问子节点，并调用 _build_status_row：

```gdscript
func _refresh_ui() -> void:
	for i in _engine.enemies.size():
		var e: Combatant = _engine.enemies[i]
		var panel: Panel = _enemies_container.get_child(i) as Panel
		var lbl_info: Label = panel.get_node("VBoxContainer/LblInfo") as Label
		var lbl_intent: Label = panel.get_node("VBoxContainer/LblIntent") as Label
		var status_row: HBoxContainer = panel.get_node("VBoxContainer/StatusRow") as HBoxContainer
		var btn: Button = panel.get_node("BtnOverlay") as Button
		if e.hp <= 0:
			lbl_info.text = "%s\n(已死亡)" % e.display_name
			lbl_intent.text = ""
			btn.disabled = true
		else:
			var action: EnemyActionData = _engine.get_enemy_action(i)
			lbl_info.text = "%s\nHP:%d/%d 挡:%d" % [e.display_name, e.hp, e.max_hp, e.block]
			lbl_intent.text = _intent_text(action, e)
			btn.disabled = (_pending_card_index < 0)
		_build_status_row(status_row, e)
	# 以下其余 _refresh_ui 内容保持不变（player HP/block/energy/sword/relics/hand）
```

- [ ] **Step 3: 替换 _set_targeting_mode()**

从直接强转 Button 改为通过 Panel 的 BtnOverlay 子节点：

```gdscript
func _set_targeting_mode(active: bool) -> void:
	_btn_end_turn.disabled = active
	for i in _enemies_container.get_child_count():
		var panel: Panel = _enemies_container.get_child(i) as Panel
		var btn: Button = panel.get_node("BtnOverlay") as Button
		if i < _engine.enemies.size() and _engine.enemies[i].hp > 0:
			btn.disabled = not active
```

- [ ] **Step 4: 替换 _on_combat_ended() 敌人循环**

找到 `_on_combat_ended()` 内的 `for i in _enemies_container.get_child_count():` 循环：

```gdscript
	for i in _enemies_container.get_child_count():
		var panel: Panel = _enemies_container.get_child(i) as Panel
		(panel.get_node("BtnOverlay") as Button).disabled = true
```

- [ ] **Step 5: 替换 _on_damage_dealt() 动画目标**

将 `var btn: Button = ...` 替换为 Panel，动画目标改为 panel：

```gdscript
func _on_damage_dealt(enemy_index: int, amount: int) -> void:
	var panel: Panel = _enemies_container.get_child(enemy_index) as Panel
	if panel == null:
		return
	panel.pivot_offset = panel.size / 2

	var flash_tween: Tween = create_tween()
	flash_tween.tween_property(panel, "modulate", Color(1.0, 0.2, 0.2), 0.05)
	flash_tween.tween_property(panel, "modulate", Color(1.0, 1.0, 1.0), 0.2)

	var shake_tween: Tween = create_tween()
	shake_tween.tween_property(panel, "scale", Vector2(1.15, 0.85), 0.06)
	shake_tween.tween_property(panel, "scale", Vector2(0.9, 1.1), 0.06)
	shake_tween.tween_property(panel, "scale", Vector2(1.05, 0.95), 0.06)
	shake_tween.tween_property(panel, "scale", Vector2(1.0, 1.0), 0.07)

	var lbl: Label = Label.new()
	lbl.text = str(amount)
	lbl.add_theme_font_size_override("font_size", 24)
	lbl.pivot_offset = Vector2(15, 12)
	lbl.z_index = 10
	add_child(lbl)
	lbl.global_position = panel.get_global_rect().get_center() - Vector2(15, 12)

	var direction: float = 1.0 if randi() % 2 == 0 else -1.0
	var drift_x: float = direction * randf_range(20.0, 60.0)

	var flyout_tween: Tween = create_tween().set_parallel(true)
	flyout_tween.tween_property(lbl, "scale", Vector2(1.5, 1.5), 0.15).set_ease(Tween.EASE_OUT)
	flyout_tween.tween_property(lbl, "position:x", lbl.position.x + drift_x, 0.4)
	flyout_tween.tween_property(lbl, "modulate:a", 0.0, 0.3).set_delay(0.1)
	flyout_tween.finished.connect(lbl.queue_free)
```

- [ ] **Step 6: 新增 _build_status_row() 辅助函数**

在 `_on_damage_dealt` 之后添加：

```gdscript
func _build_status_row(row: HBoxContainer, combatant: Combatant) -> void:
	for child in row.get_children():
		child.queue_free()
	if combatant.weak > 0:
		var lbl := Label.new()
		lbl.text = "虚弱×%d" % combatant.weak
		lbl.add_theme_color_override("font_color", Color(1.0, 0.9, 0.2))
		row.add_child(lbl)
	if combatant.vulnerable > 0:
		var lbl := Label.new()
		lbl.text = "脆弱×%d" % combatant.vulnerable
		lbl.add_theme_color_override("font_color", Color(1.0, 0.55, 0.1))
		row.add_child(lbl)
```

- [ ] **Step 7: 运行项目验证敌人面板**

在 Godot 编辑器中运行项目，进入战斗界面，确认：
- 敌人面板正常显示（名字/HP/格挡/意图文字）
- 点击手牌后可正常选择目标（BtnOverlay 响应点击）
- 对敌人施加虚弱卡后，敌人面板出现黄色「虚弱×N」标签
- 回合结束后 N 减 1，归零时标签消失
- 受击动画（flash + shake）仍在 Panel 上正常播放

- [ ] **Step 8: 提交**

```bash
git add combat/CombatScreen.gd
git commit -m "feat: refactor enemy panels to Panel+VBox+StatusRow, add _build_status_row"
```

---

### Task 2: 重构玩家卡牌（tscn + CombatScreen.gd）

**Files:**
- Modify: `combat/CombatScreen.tscn`
- Modify: `combat/CombatScreen.gd`

- [ ] **Step 1: 替换 tscn 中的 PlayerCard Button**

在 `combat/CombatScreen.tscn` 中，找到：

```
[node name="PlayerCard" type="Button" parent="VBoxContainer/PlayerCardRow/CardArea/CardCenter"]
custom_minimum_size = Vector2(150, 120)
layout_mode = 2
disabled = true
text = "剑客"
```

替换为 Panel + VBoxContainer + LblPlayerName + PlayerStatusRow 结构：

```
[node name="PlayerCardPanel" type="Panel" parent="VBoxContainer/PlayerCardRow/CardArea/CardCenter"]
custom_minimum_size = Vector2(150, 120)
layout_mode = 2

[node name="VBoxContainer" type="VBoxContainer" parent="VBoxContainer/PlayerCardRow/CardArea/CardCenter/PlayerCardPanel"]
layout_mode = 1
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
grow_horizontal = 2
grow_vertical = 2
theme_override_constants/separation = 4

[node name="LblPlayerName" type="Label" parent="VBoxContainer/PlayerCardRow/CardArea/CardCenter/PlayerCardPanel/VBoxContainer"]
layout_mode = 2
horizontal_alignment = 1
text = "剑客"

[node name="PlayerStatusRow" type="HBoxContainer" parent="VBoxContainer/PlayerCardRow/CardArea/CardCenter/PlayerCardPanel/VBoxContainer"]
layout_mode = 2
theme_override_constants/separation = 6
```

- [ ] **Step 2: 更新 @onready 引用**

在 `combat/CombatScreen.gd` 中，替换：

```gdscript
# 旧
@onready var _player_card: Button = $VBoxContainer/PlayerCardRow/CardArea/CardCenter/PlayerCard
```

改为：

```gdscript
# 新
@onready var _player_card: Panel = $VBoxContainer/PlayerCardRow/CardArea/CardCenter/PlayerCardPanel
@onready var _player_status_row: HBoxContainer = $VBoxContainer/PlayerCardRow/CardArea/CardCenter/PlayerCardPanel/VBoxContainer/PlayerStatusRow
```

- [ ] **Step 3: 在 _refresh_ui() 中添加玩家状态行**

在 `_refresh_ui()` 末尾（`_rebuild_hand()` 调用之前）添加一行：

```gdscript
	_build_status_row(_player_status_row, _engine.player)
```

注意：`_on_player_damaged()` 无需修改，它对 `_player_card` 做动画，类型已经是 Panel，代码逻辑完全兼容。

- [ ] **Step 4: 运行项目验证玩家卡牌**

在 Godot 编辑器中运行项目，确认：
- 玩家卡牌正常显示（Panel 取代 Button，显示「剑客」文字）
- 玩家受到虚弱效果后，卡牌上出现黄色「虚弱×N」
- 玩家受到脆弱效果后，卡牌上出现橙色「脆弱×N」
- 受击动画（flash + shake + 伤害数字飞出）在玩家卡牌上正常播放
- 全验收清单逐条确认

- [ ] **Step 5: 提交**

```bash
git add combat/CombatScreen.tscn combat/CombatScreen.gd
git commit -m "feat: replace PlayerCard Button with Panel+StatusRow for player status display"
```

---

### Task 3: 版本号更新

**Files:**
- Modify: `menu/MainMenu.tscn`

- [ ] **Step 1: 更新版本号**

在 `menu/MainMenu.tscn` 中找到版本号 Label，将 `v0.45.0` 改为 `v0.46.0`。

通常形如：
```
text = "v0.45.0"
```
改为：
```
text = "v0.46.0"
```

- [ ] **Step 2: 运行项目确认版本号**

启动游戏，进入主菜单，确认版本号显示 `v0.46.0`。

- [ ] **Step 3: 提交**

```bash
git add menu/MainMenu.tscn
git commit -m "feat: bump version to v0.46.0"
```
