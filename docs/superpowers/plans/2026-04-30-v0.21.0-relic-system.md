# v0.21.0 遗物系统 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现遗物系统——精英战和 Boss 战胜利后自动获得遗物，遗物效果在战斗中持续生效。

**Architecture:** 新增 `RelicData`（Resource 数据层）和 `RelicEngine`（静态工具逻辑层），镜像现有的 `CardEffectData` + `EffectResolver` 模式。`CombatEngine` 通过 `setup()` 接收遗物列表，在回合开始时调用 `RelicEngine` 应用效果。`GameManager.end_combat()` 在精英/Boss 节点胜利后自动向 `PlayerState.relics` 追加遗物。

**Tech Stack:** Godot 4.6.2 GDScript，Resource（.tres），RefCounted 静态工具类

---

## 文件结构

| 操作 | 文件 | 职责 |
|---|---|---|
| 新增 | `RelicData.gd` | 遗物蓝图（Resource），含 Trigger enum |
| 新增 | `RelicEngine.gd` | 静态工具，应用遗物效果到 CombatEngine |
| 新增 | `data/relics/burning_gem.tres` | 燃烧宝石数据（COMBAT_START，energy+1） |
| 新增 | `data/relics/life_ring.tres` | 生命之环数据（TURN_START，heal_hp+1） |
| 新增 | `data/enemies/elite_guard.tres` | 精英卫士敌人数据（HP 45，三段行动） |
| 修改 | `NodeConfig.gd` | Type enum 新增 ELITE |
| 修改 | `data/nodes/node_b2.tres` | 类型改为 ELITE，敌人改为 elite_guard |
| 修改 | `PlayerState.gd` | 新增 `relics: Array[RelicData]` |
| 修改 | `CombatEngine.gd` | setup() 新增 relics 参数，_start_player_turn() 调用 RelicEngine |
| 修改 | `GameManager.gd` | 新增 is_elite_node()、_grant_relic()，更新 end_combat() |
| 修改 | `combat/CombatScreen.gd` | setup() 传入遗物，动态创建 LblRelics，刷新遗物显示 |
| 修改 | `map/MapScreen.gd` | _get_node_label() 新增 ELITE 分支 |

---

## Task 1：RelicData 资源类 + 遗物数据文件

**Files:**
- Create: `RelicData.gd`
- Create: `data/relics/burning_gem.tres`
- Create: `data/relics/life_ring.tres`

- [ ] **Step 1：创建 RelicData.gd**

```gdscript
class_name RelicData
extends Resource

enum Trigger { COMBAT_START, TURN_START }

@export var display_name: String = ""
@export var description: String = ""
@export var trigger: Trigger = Trigger.COMBAT_START
@export var effect_type: String = ""
@export var value: int = 0
```

- [ ] **Step 2：创建 data/relics/ 目录**

```bash
mkdir -p "e:/MyWork/AIGameTest2/data/relics"
```

- [ ] **Step 3：创建 data/relics/burning_gem.tres**

```
[gd_resource type="Resource" script_class="RelicData" format=3]

[ext_resource type="Script" path="res://RelicData.gd" id="1_RelicData"]

[resource]
script = ExtResource("1_RelicData")
display_name = "燃烧宝石"
description = "每次战斗开始时，额外获得 1 点能量。"
trigger = 0
effect_type = "energy"
value = 1
```

- [ ] **Step 4：创建 data/relics/life_ring.tres**

```
[gd_resource type="Resource" script_class="RelicData" format=3]

[ext_resource type="Script" path="res://RelicData.gd" id="1_RelicData"]

[resource]
script = ExtResource("1_RelicData")
display_name = "生命之环"
description = "每回合开始时，恢复 1 点生命。"
trigger = 1
effect_type = "heal_hp"
value = 1
```

- [ ] **Step 5：验证**

运行游戏，进入任意战斗界面（此时遗物系统未接入，只验证文件无报错）。Godot 输出中不应出现 RelicData 相关解析错误。

- [ ] **Step 6：提交**

```bash
git add RelicData.gd data/relics/
git commit -m "feat: add RelicData resource and relic .tres files"
```

---

## Task 2：精英节点数据（NodeConfig ELITE + 精英敌人）

**Files:**
- Modify: `NodeConfig.gd:4`
- Create: `data/enemies/elite_guard.tres`
- Modify: `data/nodes/node_b2.tres`

- [ ] **Step 1：NodeConfig.gd 新增 ELITE**

将 [NodeConfig.gd](NodeConfig.gd) 第 4 行：

```gdscript
enum Type { COMBAT, REST }
```

改为：

```gdscript
enum Type { COMBAT, REST, ELITE }
```

完整文件：

```gdscript
class_name NodeConfig
extends Resource

enum Type { COMBAT, REST, ELITE }

@export var type: Type = Type.COMBAT
@export var enemy_data: EnemyData
@export var column: int = 0
@export var row: int = 0
```

- [ ] **Step 2：创建 data/enemies/elite_guard.tres**

精英卫士：HP 45，行动序列 攻击14 → 攻击14 → 格挡8（循环）。

参照 `data/enemies/jaw_worm.tres` 的格式，jaw_worm 的 EnemyData.gd uid 是 `uid://bp7tc2xnj6g4e`，EnemyActionData.gd uid 是 `uid://dkqvfnm3r8w5y`：

```
[gd_resource type="Resource" script_class="EnemyData" load_steps=6 format=3]

[ext_resource type="Script" uid="uid://bp7tc2xnj6g4e" path="res://EnemyData.gd" id="1_EnemyData"]
[ext_resource type="Script" uid="uid://dkqvfnm3r8w5y" path="res://EnemyActionData.gd" id="2_EnemyActionData"]

[sub_resource type="Resource" id="Action_attack1"]
script = ExtResource("2_EnemyActionData")
type = "attack"
value = 14

[sub_resource type="Resource" id="Action_attack2"]
script = ExtResource("2_EnemyActionData")
type = "attack"
value = 14

[sub_resource type="Resource" id="Action_block"]
script = ExtResource("2_EnemyActionData")
type = "block"
value = 8

[resource]
script = ExtResource("1_EnemyData")
display_name = "精英卫士"
hp = 45
actions = Array[ExtResource("2_EnemyActionData")]([SubResource("Action_attack1"), SubResource("Action_attack2"), SubResource("Action_block")])
```

- [ ] **Step 3：更新 data/nodes/node_b2.tres**

将 [data/nodes/node_b2.tres](data/nodes/node_b2.tres) 完整替换为：

```
[gd_resource type="Resource" script_class="NodeConfig" format=3]

[ext_resource type="Resource" path="res://data/enemies/elite_guard.tres" id="1_eliteguard"]
[ext_resource type="Script" path="res://NodeConfig.gd" id="2_NodeConfig"]

[resource]
script = ExtResource("2_NodeConfig")
type = 2
enemy_data = ExtResource("1_eliteguard")
column = 1
row = 1
```

- [ ] **Step 4：验证**

运行游戏 → 开始新局 → 地图界面 b2 节点按钮文字此时仍显示「战斗」（MapScreen 还未更新），但不应出现脚本错误。

- [ ] **Step 5：提交**

```bash
git add NodeConfig.gd data/enemies/elite_guard.tres data/nodes/node_b2.tres
git commit -m "feat: add ELITE node type and elite_guard enemy data"
```

---

## Task 3：RelicEngine 逻辑层

**Files:**
- Create: `RelicEngine.gd`

- [ ] **Step 1：创建 RelicEngine.gd**

```gdscript
class_name RelicEngine
extends RefCounted

static func apply_combat_start(relics: Array[RelicData], engine: CombatEngine) -> void:
	for relic: RelicData in relics:
		if relic.trigger == RelicData.Trigger.COMBAT_START:
			_apply(relic, engine)

static func apply_turn_start(relics: Array[RelicData], engine: CombatEngine) -> void:
	for relic: RelicData in relics:
		if relic.trigger == RelicData.Trigger.TURN_START:
			_apply(relic, engine)

static func _apply(relic: RelicData, engine: CombatEngine) -> void:
	match relic.effect_type:
		"energy":
			engine.energy += relic.value
		"heal_hp":
			engine.player.hp = mini(engine.player.hp + relic.value, engine.player.max_hp)
```

- [ ] **Step 2：验证**

运行游戏，确认 Godot 无解析错误（RelicEngine 此时未被任何代码调用，只验证语法正确）。

- [ ] **Step 3：提交**

```bash
git add RelicEngine.gd
git commit -m "feat: add RelicEngine static utility"
```

---

## Task 4：CombatEngine 遗物集成

**Files:**
- Modify: `CombatEngine.gd`

当前 `CombatEngine.gd` 的 `setup()` 签名（第 17 行）：

```gdscript
func setup(initial_deck: Array[CardData], enemy_data: EnemyData, initial_hp: int, max_hp: int) -> void:
```

当前 `_start_player_turn()`（第 67-73 行）：

```gdscript
func _start_player_turn() -> void:
	player.weak = max(0, player.weak - 1)
	turn_number += 1
	energy = 3
	player.block = 0
	_draw_hand()
	state_changed.emit()
```

- [ ] **Step 1：新增 _relics 字段，更新 setup() 和 _start_player_turn()**

将 [CombatEngine.gd](CombatEngine.gd) 完整替换为：

```gdscript
class_name CombatEngine
extends RefCounted

signal state_changed
signal combat_ended(result: String)

var player: Combatant
var enemy: Combatant
var hand: Array[CardData] = []
var energy: int = 0
var turn_number: int = 0

var _draw_pile: Array[CardData] = []
var _discard_pile: Array[CardData] = []
var _enemy_data: EnemyData
var _relics: Array[RelicData] = []

func setup(initial_deck: Array[CardData], enemy_data: EnemyData, initial_hp: int, max_hp: int, relics: Array[RelicData] = []) -> void:
	_relics = relics
	_enemy_data = enemy_data
	enemy = Combatant.new()
	enemy.display_name = _enemy_data.display_name
	enemy.hp = _enemy_data.hp
	enemy.max_hp = _enemy_data.hp
	enemy.block = 0

	player = Combatant.new()
	player.display_name = "玩家"
	player.hp = initial_hp
	player.max_hp = max_hp
	player.block = 0

	for card: CardData in initial_deck:
		_draw_pile.append(card.duplicate())
	_draw_pile.shuffle()
	_start_player_turn()

func get_current_enemy_action() -> EnemyActionData:
	return _enemy_data.actions[(turn_number - 1) % _enemy_data.actions.size()]

func get_draw_pile() -> Array[CardData]:
	return _draw_pile.duplicate()

func get_discard_pile() -> Array[CardData]:
	return _discard_pile.duplicate()

func play_card(card: CardData) -> void:
	if energy < card.cost:
		return
	energy -= card.cost
	EffectResolver.resolve(card, player, enemy)
	_apply_engine_effects(card)
	var idx: int = hand.find(card)
	if idx >= 0:
		hand.remove_at(idx)
	_discard_pile.append(card)
	state_changed.emit()
	_check_end()

func end_turn() -> void:
	for card: CardData in hand:
		_discard_pile.append(card)
	hand.clear()
	_do_enemy_turn()
	state_changed.emit()
	if not _check_end():
		_start_player_turn()

func _start_player_turn() -> void:
	player.weak = max(0, player.weak - 1)
	turn_number += 1
	energy = 3
	player.block = 0
	_draw_hand()
	if turn_number == 1:
		RelicEngine.apply_combat_start(_relics, self)
	RelicEngine.apply_turn_start(_relics, self)
	state_changed.emit()

func _refill_draw_pile_if_needed() -> void:
	if _draw_pile.is_empty() and not _discard_pile.is_empty():
		for card: CardData in _discard_pile:
			_draw_pile.append(card)
		_discard_pile.clear()
		_draw_pile.shuffle()

func _draw_hand() -> void:
	hand.clear()
	for i in 5:
		_refill_draw_pile_if_needed()
		if _draw_pile.is_empty():
			break
		var card: CardData = _draw_pile.pop_back()
		hand.append(card)

func _apply_engine_effects(card: CardData) -> void:
	for effect: CardEffectData in card.effects:
		if effect.type == "draw":
			_draw_cards(effect.value)
		elif effect.type == "energy":
			energy += effect.value

func _draw_cards(n: int) -> void:
	for i: int in n:
		_refill_draw_pile_if_needed()
		if _draw_pile.is_empty():
			break
		var card: CardData = _draw_pile.pop_back()
		hand.append(card)

func _do_enemy_turn() -> void:
	enemy.block = 0
	enemy.vulnerable = max(0, enemy.vulnerable - 1)
	var action: EnemyActionData = get_current_enemy_action()
	if action.type == "attack":
		EffectResolver.apply_damage(enemy, player, action.value)
		enemy.weak = max(0, enemy.weak - 1)
	else:
		enemy.add_block(action.value)

func _check_end() -> bool:
	if enemy.hp <= 0:
		combat_ended.emit("victory")
		return true
	if player.hp <= 0:
		combat_ended.emit("game_over")
		return true
	return false
```

- [ ] **Step 2：验证**

运行游戏，开始新局，进入战斗，确认：
- 无 relics 时（普通战斗）回合 1 能量仍为 3，行为与之前完全一致
- 无脚本报错

- [ ] **Step 3：提交**

```bash
git add CombatEngine.gd
git commit -m "feat: CombatEngine accepts relics in setup(), hooks RelicEngine on turn start"
```

---

## Task 5：PlayerState + GameManager 遗物流程

**Files:**
- Modify: `PlayerState.gd`
- Modify: `GameManager.gd`

当前 `PlayerState.gd`：

```gdscript
class_name PlayerState
extends RefCounted

var deck: Array[CardData] = []
var hp: int = 70
var max_hp: int = 70
var map_all_nodes: Array[NodeData] = []
var available_nodes: Array[NodeData] = []
var completed_nodes: Array[NodeData] = []
var current_node: NodeData = null
var last_rest_heal: int = 0

func apply_rest_heal() -> int:
	last_rest_heal = int(max_hp * 0.3)
	hp = mini(hp + last_rest_heal, max_hp)
	return last_rest_heal
```

- [ ] **Step 1：PlayerState.gd 新增 relics 字段**

```gdscript
class_name PlayerState
extends RefCounted

var deck: Array[CardData] = []
var hp: int = 70
var max_hp: int = 70
var map_all_nodes: Array[NodeData] = []
var available_nodes: Array[NodeData] = []
var completed_nodes: Array[NodeData] = []
var current_node: NodeData = null
var last_rest_heal: int = 0
var relics: Array[RelicData] = []

func apply_rest_heal() -> int:
	last_rest_heal = int(max_hp * 0.3)
	hp = mini(hp + last_rest_heal, max_hp)
	return last_rest_heal
```

- [ ] **Step 2：GameManager.gd 新增 is_elite_node()、_grant_relic()，更新 end_combat()**

将 [GameManager.gd](GameManager.gd) 完整替换为：

```gdscript
extends Node

enum Phase { MENU, MAP, COMBAT, REWARD, REST, WIN, GAME_OVER }

func _ready() -> void:
	var font: FontFile = load("res://data/fonts/NotoSansSC-Regular.otf") as FontFile
	if font:
		var theme: Theme = Theme.new()
		theme.default_font = font
		theme.default_font_size = 16
		get_tree().root.theme = theme

var current_phase: Phase = Phase.MENU
var player_state: PlayerState

func start_new_run() -> void:
	player_state = PlayerState.new()
	var strike: CardData = preload("res://data/cards/strike.tres")
	var defend: CardData = preload("res://data/cards/defend.tres")
	var bash: CardData   = preload("res://data/cards/bash.tres")
	var slash: CardData  = preload("res://data/cards/slash.tres")
	for i in 4:
		player_state.deck.append(strike.duplicate())
	for i in 4:
		player_state.deck.append(defend.duplicate())
	player_state.deck.append(bash.duplicate())
	player_state.deck.append(slash.duplicate())

	var a1: NodeData = NodeData.new()
	a1.config = preload("res://data/nodes/node_a1.tres")
	var a2: NodeData = NodeData.new()
	a2.config = preload("res://data/nodes/node_a2.tres")
	var b1: NodeData = NodeData.new()
	b1.config = preload("res://data/nodes/node_b1.tres")
	var b2: NodeData = NodeData.new()
	b2.config = preload("res://data/nodes/node_b2.tres")
	var c1: NodeData = NodeData.new()
	c1.config = preload("res://data/nodes/node_c1.tres")

	a1.connections.assign([b1, b2])
	a2.connections.assign([b1, b2])
	b1.connections.assign([c1])
	b2.connections.assign([c1])

	player_state.map_all_nodes.assign([a1, a2, b1, b2, c1])
	player_state.available_nodes.assign([a1, a2])

	current_phase = Phase.MAP
	get_tree().change_scene_to_file("res://map/MapScreen.tscn")

func select_node(node: NodeData) -> void:
	player_state.current_node = node
	if node.config.type == NodeConfig.Type.REST:
		go_to_rest()
	else:
		go_to_combat()

func go_to_combat() -> void:
	current_phase = Phase.COMBAT
	get_tree().change_scene_to_file("res://combat/CombatScreen.tscn")

func end_combat(final_hp: int) -> void:
	player_state.hp = final_hp
	if is_elite_node():
		_grant_relic(preload("res://data/relics/burning_gem.tres"))
	elif is_final_node():
		_grant_relic(preload("res://data/relics/life_ring.tres"))
	if is_final_node():
		go_to_win()
	else:
		go_to_reward()

func _grant_relic(relic_data: RelicData) -> void:
	player_state.relics.append(relic_data.duplicate())

func go_to_reward() -> void:
	current_phase = Phase.REWARD
	get_tree().change_scene_to_file("res://reward/RewardScreen.tscn")

func go_to_rest() -> void:
	player_state.apply_rest_heal()
	current_phase = Phase.REST
	get_tree().change_scene_to_file("res://rest/RestScreen.tscn")

func go_to_map() -> void:
	if player_state.current_node != null:
		player_state.completed_nodes.append(player_state.current_node)
		player_state.available_nodes.assign(player_state.current_node.connections)
		player_state.current_node = null
	current_phase = Phase.MAP
	get_tree().change_scene_to_file("res://map/MapScreen.tscn")

func go_to_win() -> void:
	current_phase = Phase.WIN
	get_tree().change_scene_to_file("res://win/WinScreen.tscn")

func go_to_game_over() -> void:
	current_phase = Phase.GAME_OVER
	get_tree().change_scene_to_file("res://game_over/GameOverScreen.tscn")

func go_to_menu() -> void:
	player_state = null
	current_phase = Phase.MENU
	get_tree().change_scene_to_file("res://menu/MainMenu.tscn")

func get_current_enemy_data() -> EnemyData:
	return player_state.current_node.config.enemy_data

func is_final_node() -> bool:
	return player_state.current_node.connections.is_empty()

func is_elite_node() -> bool:
	return player_state.current_node.config.type == NodeConfig.Type.ELITE
```

- [ ] **Step 3：验证**

运行游戏 → 开始新局 → 选择 a1 或 a2（普通战斗）→ 胜利后进入奖励界面（选卡）→ 确认无报错，流程正常。此时精英节点尚未测试。

- [ ] **Step 4：提交**

```bash
git add PlayerState.gd GameManager.gd
git commit -m "feat: PlayerState adds relics; GameManager grants relic on elite/boss victory"
```

---

## Task 6：CombatScreen UI 遗物显示

**Files:**
- Modify: `combat/CombatScreen.gd`

当前 [combat/CombatScreen.gd](combat/CombatScreen.gd) 的相关部分：

- 第 25 行：`var _engine: CombatEngine`
- 第 38 行：`_engine.setup(GameManager.player_state.deck, GameManager.get_current_enemy_data(), GameManager.player_state.hp, GameManager.player_state.max_hp)`
- 第 55 行：`_lbl_energy.text = "能量：%d / 3" % _engine.energy`

- [ ] **Step 1：更新 combat/CombatScreen.gd**

将 [combat/CombatScreen.gd](combat/CombatScreen.gd) 完整替换为：

```gdscript
extends Control

@onready var _lbl_enemy_name: Label    = $VBoxContainer/EnemyPanel/LblEnemyName
@onready var _lbl_enemy_hp: Label      = $VBoxContainer/EnemyPanel/LblEnemyHP
@onready var _lbl_enemy_block: Label   = $VBoxContainer/EnemyPanel/LblEnemyBlock
@onready var _lbl_enemy_weak: Label       = $VBoxContainer/EnemyPanel/LblEnemyWeak
@onready var _lbl_enemy_vulnerable: Label = $VBoxContainer/EnemyPanel/LblEnemyVulnerable
@onready var _lbl_enemy_intent: Label     = $VBoxContainer/EnemyPanel/LblEnemyIntent
@onready var _lbl_player_hp: Label     = $VBoxContainer/PlayerPanel/LblPlayerHP
@onready var _lbl_player_block: Label  = $VBoxContainer/PlayerPanel/LblPlayerBlock
@onready var _lbl_energy: Label        = $VBoxContainer/PlayerPanel/LblEnergy
@onready var _hand_container: HBoxContainer = $VBoxContainer/HandScroll/HandContainer
@onready var _btn_end_turn: Button     = $VBoxContainer/BtnEndTurn
@onready var _lbl_result: Label        = $LblResult
@onready var _btn_return_menu: Button  = $BtnReturnMenu
@onready var _btn_get_reward: Button   = $BtnGetReward
@onready var _btn_win: Button          = $BtnWin
@onready var _btn_view_deck: Button    = $VBoxContainer/BtnViewDeck
@onready var _deck_view_panel: Panel   = $DeckViewPanel
@onready var _btn_close_deck: Button   = $DeckViewPanel/VBoxContainer/BtnCloseDeck
@onready var _all_cards_list: VBoxContainer = $DeckViewPanel/VBoxContainer/TabContainer/完整牌组/AllCardsList
@onready var _draw_list: VBoxContainer      = $DeckViewPanel/VBoxContainer/TabContainer/抽牌堆/DrawList
@onready var _discard_list: VBoxContainer   = $DeckViewPanel/VBoxContainer/TabContainer/弃牌堆/DiscardList

var _engine: CombatEngine
var _hand_buttons: Array[Button] = []
var _lbl_relics: Label

func _ready() -> void:
	_lbl_relics = Label.new()
	$VBoxContainer/PlayerPanel.add_child(_lbl_relics)

	_engine = CombatEngine.new()
	_engine.state_changed.connect(_refresh_ui)
	_engine.combat_ended.connect(_on_combat_ended)
	_btn_end_turn.pressed.connect(_engine.end_turn)
	_btn_return_menu.pressed.connect(GameManager.go_to_menu)
	_btn_get_reward.pressed.connect(_on_proceed)
	_btn_win.pressed.connect(_on_proceed)
	_btn_view_deck.pressed.connect(_on_view_deck_pressed)
	_btn_close_deck.pressed.connect(_deck_view_panel.hide)
	_engine.setup(GameManager.player_state.deck, GameManager.get_current_enemy_data(),
		GameManager.player_state.hp, GameManager.player_state.max_hp,
		GameManager.player_state.relics)

func _on_card_pressed(card: CardData) -> void:
	_engine.play_card(card)

func _refresh_ui() -> void:
	_lbl_enemy_name.text = _engine.enemy.display_name
	_lbl_enemy_hp.text = "生命：%d / %d" % [_engine.enemy.hp, _engine.enemy.max_hp]
	_lbl_enemy_block.text = "格挡：%d" % _engine.enemy.block
	_lbl_enemy_weak.text = "虚弱：%d" % _engine.enemy.weak
	_lbl_enemy_vulnerable.text = "脆弱：%d" % _engine.enemy.vulnerable
	var action: EnemyActionData = _engine.get_current_enemy_action()
	var attack_value: int = int(action.value * 0.75) if action.type == "attack" and _engine.enemy.weak > 0 else action.value
	var intent: String = "攻击 %d" % attack_value if action.type == "attack" else "格挡 %d" % action.value
	_lbl_enemy_intent.text = "意图：" + intent
	_lbl_player_hp.text = "生命：%d / %d" % [_engine.player.hp, _engine.player.max_hp]
	_lbl_player_block.text = "格挡：%d" % _engine.player.block
	_lbl_energy.text = "能量：%d / 3" % _engine.energy
	var relic_names: PackedStringArray = []
	for r: RelicData in GameManager.player_state.relics:
		relic_names.append(r.display_name)
	_lbl_relics.text = "遗物：" + ("、".join(relic_names) if not relic_names.is_empty() else "无")
	_rebuild_hand()

func _rebuild_hand() -> void:
	for btn: Button in _hand_buttons:
		btn.queue_free()
	_hand_buttons.clear()
	for card: CardData in _engine.hand:
		var btn: Button = Button.new()
		btn.text = card.get_description()
		btn.pressed.connect(_on_card_pressed.bind(card))
		_hand_container.add_child(btn)
		_hand_buttons.append(btn)

func _on_combat_ended(result: String) -> void:
	_btn_end_turn.disabled = true
	for btn: Button in _hand_buttons:
		btn.disabled = true
	if result == "victory":
		_lbl_result.text = "胜利！"
		_lbl_result.visible = true
		if GameManager.is_final_node():
			_btn_win.visible = true
		else:
			_btn_get_reward.visible = true
	else:
		GameManager.go_to_game_over()

func _on_view_deck_pressed() -> void:
	var all_cards: Array[CardData] = []
	for card: CardData in _engine.hand:
		all_cards.append(card)
	for card: CardData in _engine.get_draw_pile():
		all_cards.append(card)
	for card: CardData in _engine.get_discard_pile():
		all_cards.append(card)
	_populate_list(_all_cards_list, all_cards)
	_populate_list(_draw_list, _engine.get_draw_pile())
	_populate_list(_discard_list, _engine.get_discard_pile())
	_deck_view_panel.show()

func _populate_list(container: VBoxContainer, cards: Array[CardData]) -> void:
	for child in container.get_children():
		container.remove_child(child)
		child.queue_free()
	for card: CardData in cards:
		var lbl: Label = Label.new()
		lbl.text = card.get_description()
		container.add_child(lbl)

func _on_proceed() -> void:
	GameManager.end_combat(_engine.player.hp)
```

- [ ] **Step 2：验证**

运行游戏 → 开始新局 → 选 a1（普通战斗）→ 确认遗物栏显示「遗物：无」，能量显示 3/3。无脚本报错。

- [ ] **Step 3：提交**

```bash
git add combat/CombatScreen.gd
git commit -m "feat: CombatScreen displays relics, passes relics to CombatEngine setup"
```

---

## Task 7：MapScreen ELITE 标签

**Files:**
- Modify: `map/MapScreen.gd:262-267`

当前 `_get_node_label()` 方法（约第 262-267 行）：

```gdscript
func _get_node_label(nd: NodeData) -> String:
	if nd.connections.is_empty():
		return "Boss 战斗"
	if nd.config.type == NodeConfig.Type.REST:
		return "休息站"
	return "战斗"
```

- [ ] **Step 1：更新 _get_node_label() 新增 ELITE 分支**

```gdscript
func _get_node_label(nd: NodeData) -> String:
	if nd.connections.is_empty():
		return "Boss 战斗"
	if nd.config.type == NodeConfig.Type.REST:
		return "休息站"
	if nd.config.type == NodeConfig.Type.ELITE:
		return "精英战斗"
	return "战斗"
```

- [ ] **Step 2：验证**

运行游戏 → 开始新局 → 地图界面确认：
- col=0 两个节点显示「战斗」（可点击）
- col=1 上方节点显示「休息站」，下方节点显示「精英战斗」（锁定）
- col=2 Boss 节点显示「Boss 战斗」（锁定）

- [ ] **Step 3：提交**

```bash
git add map/MapScreen.gd
git commit -m "feat: MapScreen shows ELITE label for elite nodes"
```

---

## Task 8：集成验证

验证完整游戏流程，确保所有验收标准通过。

- [ ] **验收 1：精英战斗流程**

运行游戏 → 开始新局 → 选 a1 或 a2（普通战斗）→ 胜利 → 奖励选卡 → 地图出现「精英战斗」可点击 → 进入精英战 → 确认：
- 遗物栏显示「遗物：无」（进入前还没有遗物）
- 敌人名称「精英卫士」，HP 45
- 精英卫士行动循环：攻击14 → 攻击14 → 格挡8
- 胜利后遗物栏将在下次战斗中出现

- [ ] **验收 2：燃烧宝石效果**

精英战胜利 → 奖励选卡（选任意牌）→ 地图 → 进入 Boss 战 → 确认：
- 遗物栏显示「遗物：燃烧宝石」
- 回合 1 能量显示 **4**（而不是 3）
- 回合 2 能量仍为 3（COMBAT_START 只触发一次）

- [ ] **验收 3：Boss 战流程**

Boss 战胜利 → 直接跳转胜利界面（不经过奖励界面）

- [ ] **验收 4：生命之环效果**

本局无法在胜利前使用生命之环（Boss 胜利后游戏结束）。

验证方式：在 `GameManager.end_combat()` 中临时调换两个遗物分配顺序（精英给 life_ring，Boss 给 burning_gem），精英战胜利后进入下一战：
- 遗物栏显示「遗物：生命之环」
- 每回合开始 HP +1（不超过上限）
- 测试完毕后恢复原分配顺序

- [ ] **验收 5：无遗物时行为不变**

开始新局 → a1 普通战斗 → 确认：
- 遗物栏显示「遗物：无」
- 回合 1 能量为 3，行为与 v0.20.0 一致

- [ ] **验收 6：战斗失败**

任意战斗中主动挂机（不操作）→ 确认战斗失败正确跳转 Game Over

- [ ] **最终提交**

```bash
git add .
git commit -m "docs: v0.21.0 integration tests passed"
git tag v0.21.0
```

---

## 验收标准对照

| 验收标准 | 覆盖 Task |
|---|---|
| 地图中 b2 节点显示「精英战斗」 | Task 2, Task 7 |
| 击败精英后，CombatScreen 显示「遗物：燃烧宝石」 | Task 5, Task 6 |
| 击败精英后进入奖励界面（可选卡） | Task 5 |
| 持有燃烧宝石时，战斗第一回合能量为 4 | Task 3, Task 4 |
| 持有生命之环时，每回合开始恢复 1 点生命（不超上限） | Task 3, Task 4 |
| 击败 Boss 后获得「生命之环」，跳转胜利界面 | Task 5 |
| 无遗物时战斗行为与 v0.20.0 一致 | Task 4 |
| 战斗失败正确跳转 Game Over | Task 8 |
