# v0.40.0 四个新遗物 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增四个带代价遗物（窃天、融道、天魔解体、回光返照），扩展 RelicData/RelicEngine 支持四种新效果类型和一个卡牌购买封锁标志。

**Architecture:** 在现有双效果字段体系上新增 4 个 EffectType 枚举值（REMOVE_CARDS=10、FIRST_TURN_ENERGY=11、SECOND_TURN_ENERGY_PENALTY=12、ENERGY_IF_LOW_HP=13）和 `blocks_card_purchase` 标志字段；RelicEngine 新增对应处理分支；ShopScreen 参考 blocks_relic_purchase 模式实现卡牌购买禁用；所有遗物数据通过 `.tres` 文件驱动。

**Tech Stack:** Godot 4 GDScript，Resource `.tres`，现有 RelicEngine / CombatEngine / PlayerState 体系。

---

## 枚举索引速查（本版全量）

```
Trigger:    COMBAT_START=0  TURN_START=1  TURN_END=2  COMBAT_END=3  ON_EQUIP=4

EffectType: ENERGY=0  HEAL_HP=1  BLOCK=2  DRAW=3  SWORD_INTENT=4
            SELF_DAMAGE=5  ENERGY_CAP=6  MAX_HP_PERCENT=7
            FIRST_TURN_DRAW_PENALTY=8  BLOCK_DRAIN=9
            REMOVE_CARDS=10  FIRST_TURN_ENERGY=11
            SECOND_TURN_ENERGY_PENALTY=12  ENERGY_IF_LOW_HP=13
```

---

## 文件结构

| 文件 | 动作 |
|------|------|
| `RelicData.gd` | 修改：新增 `blocks_card_purchase` 字段；EffectType 枚举加 4 个新值 |
| `RelicEngine.gd` | 修改：`_apply_effect` 加 3 个分支；`_apply_equip_effect` 加 REMOVE_CARDS 分支 |
| `shop/ShopScreen.gd` | 修改：`_rebuild_card_list()` 检查 `blocks_card_purchase` |
| `data/relics/qie_tian.tres` | 新建 |
| `data/relics/rong_dao.tres` | 新建 |
| `data/relics/tian_mo_jie_ti.tres` | 新建 |
| `data/relics/hui_guang_fan_zhao.tres` | 新建 |
| `CardPool.gd` | 修改：RELICS 加 4 个新路径 |
| `menu/MainMenu.tscn` | 修改：版本号 v0.39.0 → v0.40.0 |

---

## Task 1: 扩展 RelicData 枚举与字段

**Files:**
- Modify: `RelicData.gd`

- [ ] **Step 1: 替换 RelicData.gd 全部内容**

```gdscript
class_name RelicData
extends Resource

enum Trigger { COMBAT_START, TURN_START, TURN_END, COMBAT_END, ON_EQUIP }
enum EffectType {
	ENERGY, HEAL_HP, BLOCK, DRAW, SWORD_INTENT,
	SELF_DAMAGE, ENERGY_CAP, MAX_HP_PERCENT,
	FIRST_TURN_DRAW_PENALTY, BLOCK_DRAIN,
	REMOVE_CARDS, FIRST_TURN_ENERGY,
	SECOND_TURN_ENERGY_PENALTY, ENERGY_IF_LOW_HP
}

@export var display_name: String = ""
@export var description: String = ""
@export var trigger: Trigger = Trigger.COMBAT_START
@export var effect_type: EffectType = EffectType.ENERGY
@export var value: int = 0
@export var trigger_b: Trigger = Trigger.COMBAT_START
@export var effect_type_b: EffectType = EffectType.ENERGY
@export var value_b: int = 0
@export var has_effect_b: bool = false
@export var blocks_relic_purchase: bool = false
@export var blocks_card_purchase: bool = false
@export var price: int = 0
```

- [ ] **Step 2: 提交**

```bash
git add RelicData.gd
git commit -m "feat: add blocks_card_purchase flag and 4 new EffectType values to RelicData"
```

---

## Task 2: 扩展 RelicEngine 新效果分支

**Files:**
- Modify: `RelicEngine.gd`

- [ ] **Step 1: 替换 RelicEngine.gd 全部内容**

```gdscript
class_name RelicEngine
extends RefCounted

static func apply_combat_start(relics: Array[RelicData], engine: CombatEngine) -> void:
	_apply_for_trigger(relics, RelicData.Trigger.COMBAT_START, engine)

static func apply_turn_start(relics: Array[RelicData], engine: CombatEngine) -> void:
	_apply_for_trigger(relics, RelicData.Trigger.TURN_START, engine)

static func apply_turn_end(relics: Array[RelicData], engine: CombatEngine) -> void:
	_apply_for_trigger(relics, RelicData.Trigger.TURN_END, engine)

static func apply_combat_end(relics: Array[RelicData], engine: CombatEngine) -> void:
	_apply_for_trigger(relics, RelicData.Trigger.COMBAT_END, engine)

static func apply_on_equip(relic: RelicData, player_state: PlayerState) -> void:
	if relic.trigger == RelicData.Trigger.ON_EQUIP:
		_apply_equip_effect(relic.effect_type, relic.value, player_state)
	if relic.has_effect_b and relic.trigger_b == RelicData.Trigger.ON_EQUIP:
		_apply_equip_effect(relic.effect_type_b, relic.value_b, player_state)

static func _apply_for_trigger(relics: Array[RelicData], trigger: RelicData.Trigger, engine: CombatEngine) -> void:
	for relic: RelicData in relics:
		if relic.trigger == trigger:
			_apply_effect(relic.effect_type, relic.value, engine)
		if relic.has_effect_b and relic.trigger_b == trigger:
			_apply_effect(relic.effect_type_b, relic.value_b, engine)

static func _apply_effect(effect_type: RelicData.EffectType, value: int, engine: CombatEngine) -> void:
	match effect_type:
		RelicData.EffectType.ENERGY:
			engine.energy += value
		RelicData.EffectType.HEAL_HP:
			engine.player.hp = mini(engine.player.hp + value, engine.player.max_hp)
		RelicData.EffectType.BLOCK:
			engine.player.add_block(value)
		RelicData.EffectType.DRAW:
			engine.draw_cards(value)
		RelicData.EffectType.SWORD_INTENT:
			engine.player.sword_intent = mini(
				engine.player.sword_intent + value, engine.player.sword_intent_cap
			)
		RelicData.EffectType.SELF_DAMAGE:
			engine.player.hp = max(0, engine.player.hp - value)
		RelicData.EffectType.ENERGY_CAP:
			engine.energy_cap += value
		RelicData.EffectType.FIRST_TURN_DRAW_PENALTY:
			if engine.turn_number == 1:
				for i in value:
					if not engine.hand.is_empty():
						engine.hand.pop_back()
		RelicData.EffectType.BLOCK_DRAIN:
			if engine.player.block > 0:
				engine.player.block -= 1
			else:
				engine.player.hp = max(0, engine.player.hp - 1)
		RelicData.EffectType.FIRST_TURN_ENERGY:
			if engine.turn_number == 1:
				engine.energy += value
		RelicData.EffectType.SECOND_TURN_ENERGY_PENALTY:
			if engine.turn_number == 2:
				engine.energy = max(2, engine.energy - value)
		RelicData.EffectType.ENERGY_IF_LOW_HP:
			if engine.player.hp < int(engine.player.max_hp * 0.3):
				engine.energy += value

static func _apply_equip_effect(effect_type: RelicData.EffectType, value: int, player_state: PlayerState) -> void:
	match effect_type:
		RelicData.EffectType.MAX_HP_PERCENT:
			var reduction: int = max(1, int(player_state.max_hp * value / 100.0))
			player_state.max_hp = max(1, player_state.max_hp - reduction)
			player_state.hp = mini(player_state.hp, player_state.max_hp)
		RelicData.EffectType.REMOVE_CARDS:
			for i in value:
				if player_state.deck.is_empty():
					break
				player_state.deck.remove_at(randi() % player_state.deck.size())
```

- [ ] **Step 2: 提交**

```bash
git add RelicEngine.gd
git commit -m "feat: add REMOVE_CARDS, FIRST_TURN_ENERGY, SECOND_TURN_ENERGY_PENALTY, ENERGY_IF_LOW_HP to RelicEngine"
```

---

## Task 3: ShopScreen 卡牌购买封锁

**Files:**
- Modify: `shop/ShopScreen.gd`

当前 `_rebuild_card_list`（第 21-29 行）：

```gdscript
func _rebuild_card_list() -> void:
	for child in _card_list.get_children():
		child.queue_free()
	for card: CardData in _engine.inventory_cards:
		var btn := Button.new()
		btn.text = "%s  [%d金]" % [card.get_description(), card.price]
		btn.disabled = GameManager.player_state.gold < card.price
		btn.pressed.connect(_on_buy_card.bind(card))
		_card_list.add_child(btn)
```

- [ ] **Step 1: 修改 `_rebuild_card_list`**

用 Edit 工具将上面整段替换为：

```gdscript
func _rebuild_card_list() -> void:
	var purchase_blocked: bool = GameManager.player_state.relics.any(
		func(r: RelicData) -> bool: return r.blocks_card_purchase
	)
	for child in _card_list.get_children():
		child.queue_free()
	for card: CardData in _engine.inventory_cards:
		var btn := Button.new()
		btn.text = "%s  [%d金]" % [card.get_description(), card.price]
		btn.disabled = GameManager.player_state.gold < card.price or purchase_blocked
		btn.pressed.connect(_on_buy_card.bind(card))
		_card_list.add_child(btn)
```

- [ ] **Step 2: 提交**

```bash
git add shop/ShopScreen.gd
git commit -m "feat: disable card purchase in shop when player holds blocks_card_purchase relic"
```

---

## Task 4: 创建 4 个遗物数据文件

**Files:**
- Create: `data/relics/qie_tian.tres`
- Create: `data/relics/rong_dao.tres`
- Create: `data/relics/tian_mo_jie_ti.tres`
- Create: `data/relics/hui_guang_fan_zhao.tres`

枚举索引速查：
```
Trigger:    COMBAT_START=0  TURN_START=1  ON_EQUIP=4
EffectType: ENERGY_CAP=6  MAX_HP_PERCENT=7
            REMOVE_CARDS=10  FIRST_TURN_ENERGY=11
            SECOND_TURN_ENERGY_PENALTY=12  ENERGY_IF_LOW_HP=13
```

- [ ] **Step 1: 创建 qie_tian.tres（窃天）**

效果：COMBAT_START ENERGY_CAP +1，无副效果，blocks_card_purchase=true

```
[gd_resource type="Resource" script_class="RelicData" format=3]

[ext_resource type="Script" path="res://RelicData.gd" id="1_RelicData"]

[resource]
script = ExtResource("1_RelicData")
display_name = "窃天"
description = "真气上限 +1。不能从坊市购买卡牌。"
trigger = 0
effect_type = 6
value = 1
blocks_card_purchase = true
price = 150
```

- [ ] **Step 2: 创建 rong_dao.tres（融道）**

效果：COMBAT_START ENERGY_CAP +1；ON_EQUIP REMOVE_CARDS 2

```
[gd_resource type="Resource" script_class="RelicData" format=3]

[ext_resource type="Script" path="res://RelicData.gd" id="1_RelicData"]

[resource]
script = ExtResource("1_RelicData")
display_name = "融道"
description = "真气上限 +1。装备时随机移除牌组中 2 张牌。"
trigger = 0
effect_type = 6
value = 1
trigger_b = 4
effect_type_b = 10
value_b = 2
has_effect_b = true
price = 150
```

- [ ] **Step 3: 创建 tian_mo_jie_ti.tres（天魔解体）**

效果：TURN_START FIRST_TURN_ENERGY +5；TURN_START SECOND_TURN_ENERGY_PENALTY -1（最低 2）

```
[gd_resource type="Resource" script_class="RelicData" format=3]

[ext_resource type="Script" path="res://RelicData.gd" id="1_RelicData"]

[resource]
script = ExtResource("1_RelicData")
display_name = "天魔解体"
description = "第 1 回合获得 5 点额外真气。第 2 回合真气 -1（最低 2）。"
trigger = 1
effect_type = 11
value = 5
trigger_b = 1
effect_type_b = 12
value_b = 1
has_effect_b = true
price = 150
```

- [ ] **Step 4: 创建 hui_guang_fan_zhao.tres（回光返照）**

效果：TURN_START ENERGY_IF_LOW_HP +3；ON_EQUIP MAX_HP_PERCENT -20

```
[gd_resource type="Resource" script_class="RelicData" format=3]

[ext_resource type="Script" path="res://RelicData.gd" id="1_RelicData"]

[resource]
script = ExtResource("1_RelicData")
display_name = "回光返照"
description = "每回合开始若 HP 低于 30%，获得 3 点真气。最大血量 -20%（立即生效）。"
trigger = 1
effect_type = 13
value = 3
trigger_b = 4
effect_type_b = 7
value_b = 20
has_effect_b = true
price = 150
```

- [ ] **Step 5: 提交**

```bash
git add data/relics/qie_tian.tres data/relics/rong_dao.tres
git add data/relics/tian_mo_jie_ti.tres data/relics/hui_guang_fan_zhao.tres
git commit -m "feat: add 4 new relic data files"
```

---

## Task 5: CardPool 注册 + 版本号

**Files:**
- Modify: `CardPool.gd`
- Modify: `menu/MainMenu.tscn`

- [ ] **Step 1: 将 4 个新遗物加入 RELICS**

打开 `CardPool.gd`，将 `RELICS` 改为：

```gdscript
const RELICS: Array[String] = [
	"res://data/relics/burning_gem.tres",
	"res://data/relics/life_ring.tres",
	"res://data/relics/hua_fan.tres",
	"res://data/relics/ran_hun.tres",
	"res://data/relics/sui_dan.tres",
	"res://data/relics/jie_lei_cui_ti.tres",
	"res://data/relics/tian_dao_sui_pian.tres",
	"res://data/relics/tian_jie_yu_yun.tres",
	"res://data/relics/qie_tian.tres",
	"res://data/relics/rong_dao.tres",
	"res://data/relics/tian_mo_jie_ti.tres",
	"res://data/relics/hui_guang_fan_zhao.tres",
]
```

- [ ] **Step 2: 提交 CardPool**

```bash
git add CardPool.gd
git commit -m "feat: register 4 new relics in CardPool.RELICS"
```

- [ ] **Step 3: 更新版本号**

打开 `menu/MainMenu.tscn`，将 `"v0.39.0"` 改为 `"v0.40.0"`。

- [ ] **Step 4: 提交版本号**

```bash
git add menu/MainMenu.tscn
git commit -m "feat: bump version to v0.40.0"
```

---

## 验收清单

**窃天**
- [ ] 进入商店，卡牌购买按钮全部禁用；遗物购买正常
- [ ] 战斗开始真气显示 4/4

**融道**
- [ ] 购买/获取后，牌组立即随机减少 2 张
- [ ] 战斗开始真气显示 4/4

**天魔解体**
- [ ] 第 1 回合真气显示 8/3（3 基础 + 5 bonus）
- [ ] 第 2 回合真气显示 2/3（3 - 1 = 2，符合最低值）
- [ ] 第 3 回合起真气正常 3/3

**回光返照**
- [ ] 购买后最大血量立即 -20%（如 70 → 56）
- [ ] 血量低于 30% 时，每回合开始真气 +3
- [ ] 血量高于 30% 时，不触发
