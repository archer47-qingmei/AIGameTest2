# v0.3.0 数据文件实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 CombatEngine.gd 中硬编码的卡牌和敌人数据外置为 .tres 资源文件，新增 EnemyActionData 和 EnemyData 资源类，游戏行为与 v0.3.0 完全一致。

**Architecture:** 新建两个 Resource 子类（EnemyActionData、EnemyData）作为数据蓝图，在 data/cards/ 和 data/enemies/ 目录下创建对应 .tres 文件，CombatEngine.setup() 改为 preload() 加载文件并构建运行时 Combatant 实例。CombatScreen._refresh_ui() 意图显示改为调用 engine.get_current_enemy_action()。

**Tech Stack:** Godot 4.6, GDScript strict 类型模式，Godot Resource 系统（.tres）

---

## 文件变更一览

| 操作 | 文件 | 职责 |
|------|------|------|
| 新建 | `res://EnemyActionData.gd` | 单条敌人行动的数据蓝图（type/value） |
| 新建 | `res://EnemyActionData.gd.uid` | Godot 4.4+ 资源 UID 文件 |
| 新建 | `res://EnemyData.gd` | 敌人完整定义蓝图（display_name/hp/actions） |
| 新建 | `res://EnemyData.gd.uid` | Godot 4.4+ 资源 UID 文件 |
| 新建 | `res://data/cards/strike.tres` | 打击牌数据 |
| 新建 | `res://data/cards/defend.tres` | 防御牌数据 |
| 新建 | `res://data/cards/bash.tres` | 重击牌数据 |
| 新建 | `res://data/enemies/jaw_worm.tres` | 颚虫数据（含行动序列） |
| 修改 | `res://CombatEngine.gd` | setup() + _do_enemy_turn() + 新增 get_current_enemy_action() |
| 修改 | `res://combat/CombatScreen.gd` | _refresh_ui() 意图显示改用 get_current_enemy_action() |
| 修改 | `.godot/global_script_class_cache.cfg` | 注册 EnemyActionData 和 EnemyData |
| 不动 | `CardData.gd`、`Combatant.gd`、`EffectResolver.gd`、`CombatScreen.tscn` | — |

---

## Task 1：新建 EnemyActionData.gd 和 EnemyData.gd（含注册）

**Files:**
- Create: `e:/MyWork/AIGameTest2/EnemyActionData.gd`
- Create: `e:/MyWork/AIGameTest2/EnemyActionData.gd.uid`
- Create: `e:/MyWork/AIGameTest2/EnemyData.gd`
- Create: `e:/MyWork/AIGameTest2/EnemyData.gd.uid`
- Modify: `e:/MyWork/AIGameTest2/.godot/global_script_class_cache.cfg`

- [ ] **Step 1：创建 EnemyActionData.gd**

创建 `e:/MyWork/AIGameTest2/EnemyActionData.gd`：

```gdscript
class_name EnemyActionData
extends Resource

@export var type: String
@export var value: int
```

- [ ] **Step 2：创建 EnemyActionData.gd.uid**

创建 `e:/MyWork/AIGameTest2/EnemyActionData.gd.uid`，内容为一行 UID：

```
uid://dkqvfnm3r8w5y
```

- [ ] **Step 3：创建 EnemyData.gd**

创建 `e:/MyWork/AIGameTest2/EnemyData.gd`：

```gdscript
class_name EnemyData
extends Resource

@export var display_name: String
@export var hp: int
@export var actions: Array[EnemyActionData]
```

- [ ] **Step 4：创建 EnemyData.gd.uid**

创建 `e:/MyWork/AIGameTest2/EnemyData.gd.uid`，内容为一行 UID：

```
uid://bp7tc2xnj6g4e
```

- [ ] **Step 5：更新 global_script_class_cache.cfg**

读取 `e:/MyWork/AIGameTest2/.godot/global_script_class_cache.cfg`，在 `list=[` 数组末尾追加两条新记录（保持现有条目不变）：

```
list=[{
"base": &"Resource",
"class": &"CardData",
"icon": "",
"is_abstract": false,
"is_tool": false,
"language": &"GDScript",
"path": "res://CardData.gd"
}, {
"base": &"RefCounted",
"class": &"CombatEngine",
"icon": "",
"is_abstract": false,
"is_tool": false,
"language": &"GDScript",
"path": "res://CombatEngine.gd"
}, {
"base": &"RefCounted",
"class": &"Combatant",
"icon": "",
"is_abstract": false,
"is_tool": false,
"language": &"GDScript",
"path": "res://Combatant.gd"
}, {
"base": &"RefCounted",
"class": &"EffectResolver",
"icon": "",
"is_abstract": false,
"is_tool": false,
"language": &"GDScript",
"path": "res://EffectResolver.gd"
}, {
"base": &"Resource",
"class": &"EnemyActionData",
"icon": "",
"is_abstract": false,
"is_tool": false,
"language": &"GDScript",
"path": "res://EnemyActionData.gd"
}, {
"base": &"Resource",
"class": &"EnemyData",
"icon": "",
"is_abstract": false,
"is_tool": false,
"language": &"GDScript",
"path": "res://EnemyData.gd"
}]
```

- [ ] **Step 6：运行项目验证无报错**

```
mcp__godot__run_project(projectPath="e:/MyWork/AIGameTest2")
```

等待约 3 秒后调用 `mcp__godot__get_debug_output`，确认无 `SCRIPT ERROR:` 或 `Parser Error:`。

- [ ] **Step 7：停止项目**

```
mcp__godot__stop_project()
```

- [ ] **Step 8：提交**

```bash
git add EnemyActionData.gd EnemyActionData.gd.uid EnemyData.gd EnemyData.gd.uid .godot/global_script_class_cache.cfg
git commit -m "feat: add EnemyActionData and EnemyData resource classes"
```

---

## Task 2：创建卡牌 .tres 数据文件

**Files:**
- Create: `e:/MyWork/AIGameTest2/data/cards/strike.tres`
- Create: `e:/MyWork/AIGameTest2/data/cards/defend.tres`
- Create: `e:/MyWork/AIGameTest2/data/cards/bash.tres`

> **背景：** CardData.gd 的 UID 为 `uid://13mprie5g221`（来自 CardData.gd.uid 文件）。`.tres` 文件通过 ext_resource 引用该脚本。

- [ ] **Step 1：创建 data/cards/ 目录并写入 strike.tres**

创建 `e:/MyWork/AIGameTest2/data/cards/strike.tres`：

```
[gd_resource type="Resource" script_class="CardData" load_steps=2 format=3]

[ext_resource type="Script" uid="uid://13mprie5g221" path="res://CardData.gd" id="1_CardData"]

[resource]
script = ExtResource("1_CardData")
card_name = "打击"
cost = 1
damage = 6
block = 0
```

- [ ] **Step 2：写入 defend.tres**

创建 `e:/MyWork/AIGameTest2/data/cards/defend.tres`：

```
[gd_resource type="Resource" script_class="CardData" load_steps=2 format=3]

[ext_resource type="Script" uid="uid://13mprie5g221" path="res://CardData.gd" id="1_CardData"]

[resource]
script = ExtResource("1_CardData")
card_name = "防御"
cost = 1
damage = 0
block = 5
```

- [ ] **Step 3：写入 bash.tres**

创建 `e:/MyWork/AIGameTest2/data/cards/bash.tres`：

```
[gd_resource type="Resource" script_class="CardData" load_steps=2 format=3]

[ext_resource type="Script" uid="uid://13mprie5g221" path="res://CardData.gd" id="1_CardData"]

[resource]
script = ExtResource("1_CardData")
card_name = "重击"
cost = 2
damage = 8
block = 0
```

- [ ] **Step 4：运行项目验证无报错**

此时 CombatEngine 仍用旧逻辑，游戏应正常运行。

```
mcp__godot__run_project(projectPath="e:/MyWork/AIGameTest2")
```

等待 3 秒，调用 `mcp__godot__get_debug_output`，确认无 `ERROR:` 或 `SCRIPT ERROR:`（icon.svg 缺失警告可忽略）。

- [ ] **Step 5：停止项目**

```
mcp__godot__stop_project()
```

- [ ] **Step 6：提交**

```bash
git add data/cards/strike.tres data/cards/defend.tres data/cards/bash.tres
git commit -m "feat: add card .tres data files (strike/defend/bash)"
```

---

## Task 3：创建敌人 .tres 数据文件

**Files:**
- Create: `e:/MyWork/AIGameTest2/data/enemies/jaw_worm.tres`

> **背景：** EnemyData.gd UID = `uid://bp7tc2xnj6g4e`，EnemyActionData.gd UID = `uid://dkqvfnm3r8w5y`（均为 Task 1 创建的 .uid 文件中的值）。jaw_worm.tres 通过 sub_resource 内嵌两条行动记录。

- [ ] **Step 1：创建 data/enemies/ 目录并写入 jaw_worm.tres**

创建 `e:/MyWork/AIGameTest2/data/enemies/jaw_worm.tres`：

```
[gd_resource type="Resource" script_class="EnemyData" load_steps=5 format=3]

[ext_resource type="Script" uid="uid://bp7tc2xnj6g4e" path="res://EnemyData.gd" id="1_EnemyData"]
[ext_resource type="Script" uid="uid://dkqvfnm3r8w5y" path="res://EnemyActionData.gd" id="2_EnemyActionData"]

[sub_resource type="Resource" id="Action_attack"]
script = ExtResource("2_EnemyActionData")
type = "attack"
value = 11

[sub_resource type="Resource" id="Action_block"]
script = ExtResource("2_EnemyActionData")
type = "block"
value = 6

[resource]
script = ExtResource("1_EnemyData")
display_name = "颚虫"
hp = 44
actions = [SubResource("Action_attack"), SubResource("Action_block")]
```

- [ ] **Step 2：运行项目验证无报错**

```
mcp__godot__run_project(projectPath="e:/MyWork/AIGameTest2")
```

等待 3 秒，调用 `mcp__godot__get_debug_output`，确认无报错。

- [ ] **Step 3：停止项目**

```
mcp__godot__stop_project()
```

- [ ] **Step 4：提交**

```bash
git add data/enemies/jaw_worm.tres
git commit -m "feat: add jaw_worm enemy .tres data file"
```

---

## Task 4：改写 CombatEngine.gd

**Files:**
- Modify: `e:/MyWork/AIGameTest2/CombatEngine.gd`

- [ ] **Step 1：用以下内容完整替换 CombatEngine.gd**

将 `e:/MyWork/AIGameTest2/CombatEngine.gd` 替换为：

```gdscript
class_name CombatEngine
extends RefCounted

signal state_changed
signal combat_ended(result: String)

var player: Combatant
var enemy: Combatant
var hand: Array[CardData] = []
var energy: int = 0
var turn_number: int = 0

var _draw_pile: Array[CardData] = []
var _discard_pile: Array[CardData] = []
var _enemy_data: EnemyData

func setup() -> void:
	_enemy_data = preload("res://data/enemies/jaw_worm.tres")
	enemy = Combatant.new()
	enemy.display_name = _enemy_data.display_name
	enemy.hp = _enemy_data.hp
	enemy.max_hp = _enemy_data.hp
	enemy.block = 0

	player = Combatant.new()
	player.display_name = "玩家"
	player.hp = 70
	player.max_hp = 70
	player.block = 0

	var strike: CardData = preload("res://data/cards/strike.tres")
	var defend: CardData = preload("res://data/cards/defend.tres")
	var bash: CardData = preload("res://data/cards/bash.tres")
	for i in 4:
		_draw_pile.append(strike.duplicate())
	for i in 4:
		_draw_pile.append(defend.duplicate())
	_draw_pile.append(bash.duplicate())

	_draw_pile.shuffle()
	_start_player_turn()

func get_current_enemy_action() -> EnemyActionData:
	return _enemy_data.actions[(turn_number - 1) % _enemy_data.actions.size()]

func play_card(card: CardData) -> void:
	if energy < card.cost:
		return
	energy -= card.cost
	EffectResolver.resolve(card, player, enemy)
	var idx: int = hand.find(card)
	if idx >= 0:
		hand.remove_at(idx)
	_discard_pile.append(card)
	state_changed.emit()
	_check_end()

func end_turn() -> void:
	for card: CardData in hand:
		_discard_pile.append(card)
	hand.clear()
	_do_enemy_turn()
	state_changed.emit()
	if not _check_end():
		_start_player_turn()

func _start_player_turn() -> void:
	turn_number += 1
	energy = 3
	player.block = 0
	_draw_hand()
	state_changed.emit()

func _draw_hand() -> void:
	hand.clear()
	for i in 5:
		if _draw_pile.is_empty():
			if _discard_pile.is_empty():
				break
			for card: CardData in _discard_pile:
				_draw_pile.append(card)
			_discard_pile.clear()
			_draw_pile.shuffle()
		if _draw_pile.is_empty():
			break
		var card: CardData = _draw_pile.pop_back()
		hand.append(card)

func _do_enemy_turn() -> void:
	enemy.block = 0
	var action: EnemyActionData = _enemy_data.actions[(turn_number - 1) % _enemy_data.actions.size()]
	if action.type == "attack":
		player.take_damage(action.value)
	else:
		enemy.add_block(action.value)

func _check_end() -> bool:
	if enemy.hp <= 0:
		combat_ended.emit("victory")
		return true
	if player.hp <= 0:
		combat_ended.emit("game_over")
		return true
	return false
```

- [ ] **Step 2：运行项目，验证游戏行为**

```
mcp__godot__run_project(projectPath="e:/MyWork/AIGameTest2")
```

等待 3 秒，调用 `mcp__godot__get_debug_output`，确认：
- 无 `SCRIPT ERROR:` 或 `ERROR:`（icon.svg 警告可忽略）
- 游戏正常启动进入战斗场景

- [ ] **Step 3：停止项目**

```
mcp__godot__stop_project()
```

- [ ] **Step 4：提交**

```bash
git add CombatEngine.gd
git commit -m "refactor: CombatEngine.setup() loads data from .tres files"
```

---

## Task 5：改写 CombatScreen.gd 意图显示

**Files:**
- Modify: `e:/MyWork/AIGameTest2/combat/CombatScreen.gd`（仅改 1 行）

- [ ] **Step 1：替换 _refresh_ui() 中的意图计算行**

将 `e:/MyWork/AIGameTest2/combat/CombatScreen.gd` 第 31 行：

```gdscript
	var intent: String = "攻击 11" if _engine.turn_number % 2 == 1 else "格挡 6"
```

替换为：

```gdscript
	var action: EnemyActionData = _engine.get_current_enemy_action()
	var intent: String = "攻击 %d" % action.value if action.type == "attack" else "格挡 %d" % action.value
```

改完后 `_refresh_ui()` 函数应为：

```gdscript
func _refresh_ui() -> void:
	_lbl_enemy_name.text = _engine.enemy.display_name
	_lbl_enemy_hp.text = "生命：%d / %d" % [_engine.enemy.hp, _engine.enemy.max_hp]
	_lbl_enemy_block.text = "格挡：%d" % _engine.enemy.block
	var action: EnemyActionData = _engine.get_current_enemy_action()
	var intent: String = "攻击 %d" % action.value if action.type == "attack" else "格挡 %d" % action.value
	_lbl_enemy_intent.text = "意图：" + intent
	_lbl_player_hp.text = "生命：%d / %d" % [_engine.player.hp, _engine.player.max_hp]
	_lbl_player_block.text = "格挡：%d" % _engine.player.block
	_lbl_energy.text = "能量：%d / 3" % _engine.energy
	_rebuild_hand()
```

- [ ] **Step 2：运行项目，验证意图标签正确**

```
mcp__godot__run_project(projectPath="e:/MyWork/AIGameTest2")
```

等待 3 秒，调用 `mcp__godot__get_debug_output`，确认无报错，游戏正常启动。

- [ ] **Step 3：停止项目**

```
mcp__godot__stop_project()
```

- [ ] **Step 4：提交**

```bash
git add combat/CombatScreen.gd
git commit -m "refactor: CombatScreen intent display reads from get_current_enemy_action()"
```

---

## Task 6：验收架构约束并打 v0.3.0 标签

**Files:** 无文件修改

- [ ] **Step 1：确认 setup() 中无硬编码数值**

使用 Grep 工具在 `e:/MyWork/AIGameTest2/CombatEngine.gd` 中搜索以下关键字，预期均无匹配：

- `"打击"`、`"防御"`、`"重击"`（卡牌名称）
- `damage = 6`、`damage = 8`、`block = 5`（卡牌数值）
- `hp = 44`（敌人 HP）
- `"颚虫"`（敌人名称）
- `take_damage(11)`、`add_block(6)`（硬编码行动数值）

- [ ] **Step 2：确认数据文件存在**

确认以下文件均存在（使用 Read 工具读取验证）：
- `e:/MyWork/AIGameTest2/data/cards/strike.tres`
- `e:/MyWork/AIGameTest2/data/cards/defend.tres`
- `e:/MyWork/AIGameTest2/data/cards/bash.tres`
- `e:/MyWork/AIGameTest2/data/enemies/jaw_worm.tres`

- [ ] **Step 3：打 v0.3.0 标签**

```bash
git tag v0.3.0
git log --oneline -6
```

---

## 验收总结

完成所有 Task 后，以下标准应全部满足：

- [ ] 项目无报错启动，直接进入战斗场景
- [ ] 游戏行为与 v0.2.0 完全一致（颚虫 44HP、攻 11/挡 6 交替、起手 5 张）
- [ ] `CombatEngine.setup()` 中无任何硬编码卡牌数值或敌人数值
- [ ] `data/cards/` 目录下有 3 个 `.tres` 文件
- [ ] `data/enemies/jaw_worm.tres` 存在且含 2 条行动
- [ ] 全部 v0.2.0 验收标准继续通过
