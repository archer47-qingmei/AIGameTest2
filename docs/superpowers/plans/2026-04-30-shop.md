# 商店系统 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增商店节点（SHOP）、金币奖励系统、ShopEngine + ShopScreen，玩家可在商店花金币购买卡牌和遗物。

**Architecture:** 新增 ShopEngine（RefCounted）处理商店逻辑；ShopScreen（Control）纯显示；金币存在 PlayerState.gold；RewardEngine 扩展生成金币奖励；MapGenerator 在 col 1/2 后处理添加一个 SHOP 节点；所有价格数据外置在 .tres 文件中。

**Tech Stack:** Godot 4 GDScript 严格类型，数据通过 .tres 文件配置，遵循 CLAUDE.md 分层架构原则。

---

## 文件结构

新建：
- `shop/ShopEngine.gd` — RefCounted，商店库存生成与购买逻辑
- `shop/ShopScreen.gd` — Control，商店 UI
- `shop/ShopScreen.tscn` — 商店场景

修改：
- `CardData.gd` — 新增 `price` 字段
- `RelicData.gd` — 新增 `price` 字段
- `data/cards/*.tres` (9 个) — 补 price 值
- `data/relics/*.tres` (2 个) — 补 price 值
- `NodeConfig.gd` — Type 枚举新增 SHOP
- `PlayerState.gd` — 新增 `gold` 字段
- `RewardEngine.gd` — 新增 `get_gold_reward()` 静态方法
- `GameManager.gd` — 新增 `pending_gold`、`collect_gold()`、`go_to_shop()`、Phase.SHOP
- `RewardScreen.gd` — 新增金币按钮
- `MapGenerator.gd` — 新增 `_add_shop_node()` 后处理
- `MapScreen.gd` — `_get_node_label()` 新增 SHOP 分支

---

## Task 1: 数据层 — price 字段 + .tres 文件

**Files:**
- Modify: `CardData.gd`
- Modify: `RelicData.gd`
- Modify: `data/cards/strike.tres`
- Modify: `data/cards/defend.tres`
- Modify: `data/cards/bash.tres`
- Modify: `data/cards/slash.tres`
- Modify: `data/cards/insight.tres`
- Modify: `data/cards/quick_strike.tres`
- Modify: `data/cards/energize.tres`
- Modify: `data/cards/dash.tres`
- Modify: `data/cards/entangle.tres`
- Modify: `data/relics/burning_gem.tres`
- Modify: `data/relics/life_ring.tres`

- [ ] **Step 1: 在 CardData.gd 新增 price 字段**

在 `CardData.gd` 的 `@export var cost: int` 后面加一行：

```gdscript
@export var price: int = 0
```

完整文件变成：
```gdscript
class_name CardData
extends Resource

@export var card_name: String
@export var cost: int
@export var price: int = 0
@export var effects: Array[CardEffectData]
var is_upgraded: bool = false
# ... 其余方法不变
```

- [ ] **Step 2: 在 RelicData.gd 新增 price 字段**

在 `RelicData.gd` 的 `@export var value: int = 0` 后面加一行：

```gdscript
@export var price: int = 0
```

完整文件变成：
```gdscript
class_name RelicData
extends Resource

enum Trigger { COMBAT_START, TURN_START }

@export var display_name: String = ""
@export var description: String = ""
@export var trigger: Trigger = Trigger.COMBAT_START
@export var effect_type: String = ""
@export var value: int = 0
@export var price: int = 0
```

- [ ] **Step 3: 更新 9 个卡牌 .tres 文件，各加 price 行**

在每个卡牌 .tres 的 `[resource]` 段末尾添加对应 price 值：

`data/cards/strike.tres` → 在 `effects = ...` 后加：`price = 30`
`data/cards/defend.tres` → `price = 30`
`data/cards/bash.tres` → `price = 50`
`data/cards/slash.tres` → `price = 30`
`data/cards/insight.tres` → `price = 30`
`data/cards/quick_strike.tres` → `price = 30`
`data/cards/energize.tres` → `price = 50`
`data/cards/dash.tres` → `price = 30`
`data/cards/entangle.tres` → `price = 30`

示例（strike.tres `[resource]` 段完成后的样子）：
```
[resource]
script = ExtResource("1_CardData")
card_name = "打击"
cost = 1
effects = Array[ExtResource("2_CardEffectData")]([SubResource("Effect_damage")])
price = 30
```

- [ ] **Step 4: 更新 2 个遗物 .tres 文件，各加 price 行**

`data/relics/burning_gem.tres` → 在 `value = 1` 后加：`price = 100`
`data/relics/life_ring.tres` → 在 `value = ...` 后加：`price = 150`

- [ ] **Step 5: 语法检查**

```bash
"D:/steam/steamapps/common/Godot Engine/godot.windows.opt.tools.64.exe" --headless --path "E:/MyWork/AIGameTest2" --check-only 2>&1 | tail -10
```

期望：无 ERROR 输出，最后一行类似 `Godot Engine v4.6...`。

- [ ] **Step 6: 提交**

```bash
git add CardData.gd RelicData.gd data/cards/ data/relics/
git commit -m "feat: add price field to CardData and RelicData"
```

---

## Task 2: NodeConfig.Type 新增 SHOP

**Files:**
- Modify: `NodeConfig.gd`

- [ ] **Step 1: 修改 Type 枚举**

将 `NodeConfig.gd` 第 4 行从：
```gdscript
enum Type { COMBAT, REST, ELITE }
```
改为：
```gdscript
enum Type { COMBAT, REST, ELITE, SHOP }
```

- [ ] **Step 2: 语法检查**

```bash
"D:/steam/steamapps/common/Godot Engine/godot.windows.opt.tools.64.exe" --headless --path "E:/MyWork/AIGameTest2" --check-only 2>&1 | tail -5
```

期望：无 ERROR。

- [ ] **Step 3: 提交**

```bash
git add NodeConfig.gd
git commit -m "feat: add SHOP to NodeConfig.Type"
```

---

## Task 3: PlayerState.gold + RewardEngine.get_gold_reward()

**Files:**
- Modify: `PlayerState.gd`
- Modify: `RewardEngine.gd`

- [ ] **Step 1: PlayerState 新增 gold 字段**

在 `PlayerState.gd` 的 `var last_rest_heal: int = 0` 后加：

```gdscript
var gold: int = 0
```

完整 PlayerState.gd：
```gdscript
class_name PlayerState
extends RefCounted

var deck: Array[CardData] = []
var hp: int = 70
var max_hp: int = 70
var map_all_nodes: Array[NodeData] = []
var available_nodes: Array[NodeData] = []
var completed_nodes: Array[NodeData] = []
var current_node: NodeData = null
var last_rest_heal: int = 0
var relics: Array[RelicData] = []
var gold: int = 0

func apply_rest_heal() -> int:
	last_rest_heal = int(max_hp * 0.3)
	hp = mini(hp + last_rest_heal, max_hp)
	return last_rest_heal
```

- [ ] **Step 2: RewardEngine 新增 get_gold_reward()**

在 `RewardEngine.gd` 末尾追加：

```gdscript
static func get_gold_reward(is_elite: bool, is_final: bool) -> int:
	if is_final:
		return randi_range(40, 60)
	if is_elite:
		return randi_range(25, 40)
	return randi_range(15, 25)
```

- [ ] **Step 3: 语法检查**

```bash
"D:/steam/steamapps/common/Godot Engine/godot.windows.opt.tools.64.exe" --headless --path "E:/MyWork/AIGameTest2" --check-only 2>&1 | tail -5
```

期望：无 ERROR。

- [ ] **Step 4: 提交**

```bash
git add PlayerState.gd RewardEngine.gd
git commit -m "feat: add gold to PlayerState, add get_gold_reward to RewardEngine"
```

---

## Task 4: GameManager.pending_gold + collect_gold() + RewardScreen 金币按钮

**Files:**
- Modify: `GameManager.gd`
- Modify: `reward/RewardScreen.gd`

- [ ] **Step 1: GameManager 新增 pending_gold 字段和 collect_gold() 方法**

在 `GameManager.gd` 的 `var pending_relic: RelicData = null` 后加：
```gdscript
var pending_gold: int = 0
```

在 `end_combat()` 方法中，在设置 `pending_relic` 的逻辑之前，加一行生成金币：

完整 `end_combat()` 改为：
```gdscript
func end_combat(final_hp: int) -> void:
	player_state.hp = final_hp
	pending_gold = RewardEngine.get_gold_reward(is_elite_node(), is_final_node())
	if is_elite_node():
		pending_relic = preload("res://data/relics/burning_gem.tres")
	elif is_final_node():
		pending_relic = preload("res://data/relics/life_ring.tres")
	else:
		pending_relic = null
	go_to_reward()
```

在文件末尾新增方法：
```gdscript
func collect_gold(amount: int) -> void:
	player_state.gold += amount
```

- [ ] **Step 2: RewardScreen 新增金币按钮**

`reward/RewardScreen.gd` 完整替换为以下内容（在原有基础上新增 `_btn_gold` 逻辑）：

```gdscript
extends Control

enum State { LIST, CARD_PICK }

@onready var _vbox: VBoxContainer = $VBoxContainer

var _state: State = State.LIST
var _btn_relic: Button
var _btn_gold: Button
var _btn_card: Button
var _card_container: VBoxContainer
var _btn_continue: Button

func _ready() -> void:
	for child in _vbox.get_children():
		_vbox.remove_child(child)
		child.free()

	_btn_relic = Button.new()
	_vbox.add_child(_btn_relic)
	_btn_relic.pressed.connect(_on_relic_pressed)

	_btn_gold = Button.new()
	_vbox.add_child(_btn_gold)
	_btn_gold.pressed.connect(_on_gold_pressed)

	_btn_card = Button.new()
	_btn_card.text = "卡牌"
	_vbox.add_child(_btn_card)
	_btn_card.pressed.connect(_on_card_btn_pressed)

	_card_container = VBoxContainer.new()
	_vbox.add_child(_card_container)
	_card_container.hide()

	_btn_continue = Button.new()
	_btn_continue.text = "继续"
	_vbox.add_child(_btn_continue)
	_btn_continue.pressed.connect(_on_continue_pressed)

	_show_list()

func _show_list() -> void:
	_state = State.LIST
	_card_container.hide()
	_btn_card.show()
	_btn_continue.show()

	if _btn_relic.disabled:
		_btn_relic.show()
	else:
		var relic: RelicData = GameManager.pending_relic
		if relic != null:
			_btn_relic.text = "%s — %s" % [relic.display_name, relic.description]
			_btn_relic.show()
		else:
			_btn_relic.hide()

	if _btn_gold.disabled:
		_btn_gold.show()
	else:
		var amount: int = GameManager.pending_gold
		if amount > 0:
			_btn_gold.text = "拾取 %d 金币" % amount
			_btn_gold.show()
		else:
			_btn_gold.hide()

func _on_relic_pressed() -> void:
	var relic: RelicData = GameManager.pending_relic
	GameManager.player_state.relics.append(relic.duplicate())
	GameManager.pending_relic = null
	_btn_relic.disabled = true
	_btn_relic.text += " ✓"

func _on_gold_pressed() -> void:
	GameManager.collect_gold(GameManager.pending_gold)
	GameManager.pending_gold = 0
	_btn_gold.disabled = true
	_btn_gold.text += " ✓"

func _on_card_btn_pressed() -> void:
	_state = State.CARD_PICK
	_btn_relic.hide()
	_btn_gold.hide()
	_btn_card.hide()
	_btn_continue.hide()
	_card_container.show()
	_build_card_options()

func _build_card_options() -> void:
	for child in _card_container.get_children():
		_card_container.remove_child(child)
		child.free()
	var options: Array[CardData] = RewardEngine.get_options()
	for card: CardData in options:
		var btn: Button = Button.new()
		btn.text = card.get_description()
		btn.pressed.connect(_on_card_selected.bind(card))
		_card_container.add_child(btn)
	var btn_skip: Button = Button.new()
	btn_skip.text = "跳过"
	btn_skip.pressed.connect(_on_skipped)
	_card_container.add_child(btn_skip)

func _on_card_selected(card: CardData) -> void:
	GameManager.player_state.deck.append(card.duplicate())
	_btn_card.text = "卡牌 ✓"
	_btn_card.disabled = true
	_show_list()

func _on_skipped() -> void:
	_btn_card.text = "卡牌（已跳过）"
	_btn_card.disabled = true
	_show_list()

func _on_continue_pressed() -> void:
	if GameManager.is_final_node():
		GameManager.go_to_win()
	else:
		GameManager.go_to_map()
```

- [ ] **Step 3: 语法检查**

```bash
"D:/steam/steamapps/common/Godot Engine/godot.windows.opt.tools.64.exe" --headless --path "E:/MyWork/AIGameTest2" --check-only 2>&1 | tail -5
```

期望：无 ERROR。

- [ ] **Step 4: 提交**

```bash
git add GameManager.gd reward/RewardScreen.gd
git commit -m "feat: add gold reward to combat flow and RewardScreen"
```

---

## Task 5: MapGenerator SHOP 节点 + MapScreen 标签 + GameManager 路由

**Files:**
- Modify: `MapGenerator.gd`
- Modify: `map/MapScreen.gd`
- Modify: `GameManager.gd`

- [ ] **Step 1: MapGenerator 新增 _add_shop_node() 后处理**

在 `MapGenerator.gd` 的 `generate()` 方法中，在 `return all` 之前加一行调用：

```gdscript
static func generate() -> Array[NodeData]:
	var col0: Array[NodeData] = _make_col0()
	var col1: Array[NodeData] = _make_col1()
	var col2: Array[NodeData] = _make_col2()
	var col3: Array[NodeData] = _make_col3()
	for nd in col0:
		nd.connections.assign(col1)
	for nd in col1:
		nd.connections.assign(col2)
	for nd in col2:
		nd.connections.assign(col3)
	var all: Array[NodeData] = []
	all.append_array(col0)
	all.append_array(col1)
	all.append_array(col2)
	all.append_array(col3)
	_add_shop_node(all)
	return all
```

在文件末尾新增方法：

```gdscript
static func _add_shop_node(all_nodes: Array[NodeData]) -> void:
	var candidates: Array[NodeData] = []
	for nd: NodeData in all_nodes:
		if nd.config.column in [1, 2] and nd.config.type != NodeConfig.Type.ELITE:
			candidates.append(nd)
	if candidates.is_empty():
		return
	var chosen: NodeData = candidates[randi() % candidates.size()]
	chosen.config.type = NodeConfig.Type.SHOP
	chosen.config.enemy_data = null
```

- [ ] **Step 2: MapScreen._get_node_label() 新增 SHOP 分支**

将 `map/MapScreen.gd` 的 `_get_node_label()` 方法从：

```gdscript
func _get_node_label(nd: NodeData) -> String:
	if nd.connections.is_empty():
		return "Boss 战斗"
	if nd.config.type == NodeConfig.Type.REST:
		return "休息站"
	if nd.config.type == NodeConfig.Type.ELITE:
		return "精英战斗"
	return "战斗"
```

改为：

```gdscript
func _get_node_label(nd: NodeData) -> String:
	if nd.connections.is_empty():
		return "Boss 战斗"
	match nd.config.type:
		NodeConfig.Type.REST:
			return "休息站"
		NodeConfig.Type.ELITE:
			return "精英战斗"
		NodeConfig.Type.SHOP:
			return "商店"
	return "战斗"
```

- [ ] **Step 3: GameManager 新增 Phase.SHOP 和 go_to_shop()，修改 select_node()**

将 `GameManager.gd` 第 3 行的 Phase 枚举从：
```gdscript
enum Phase { MENU, MAP, COMBAT, REWARD, REST, WIN, GAME_OVER }
```
改为：
```gdscript
enum Phase { MENU, MAP, COMBAT, REWARD, REST, SHOP, WIN, GAME_OVER }
```

将 `select_node()` 从：
```gdscript
func select_node(node: NodeData) -> void:
	player_state.current_node = node
	if node.config.type == NodeConfig.Type.REST:
		go_to_rest()
	else:
		go_to_combat()
```
改为：
```gdscript
func select_node(node: NodeData) -> void:
	player_state.current_node = node
	match node.config.type:
		NodeConfig.Type.REST:
			go_to_rest()
		NodeConfig.Type.SHOP:
			go_to_shop()
		_:
			go_to_combat()
```

在 `go_to_rest()` 方法后新增：
```gdscript
func go_to_shop() -> void:
	current_phase = Phase.SHOP
	get_tree().change_scene_to_file("res://shop/ShopScreen.tscn")
```

- [ ] **Step 4: 语法检查**

```bash
"D:/steam/steamapps/common/Godot Engine/godot.windows.opt.tools.64.exe" --headless --path "E:/MyWork/AIGameTest2" --check-only 2>&1 | tail -5
```

期望：无 ERROR。

- [ ] **Step 5: 提交**

```bash
git add MapGenerator.gd map/MapScreen.gd GameManager.gd
git commit -m "feat: add SHOP node to map generation and routing"
```

---

## Task 6: ShopEngine（新 RefCounted）

**Files:**
- Create: `shop/ShopEngine.gd`

- [ ] **Step 1: 创建 shop/ 目录并新建 ShopEngine.gd**

创建文件 `shop/ShopEngine.gd`，内容如下：

```gdscript
class_name ShopEngine
extends RefCounted

var inventory_cards: Array[CardData] = []
var inventory_relics: Array[RelicData] = []

func generate(card_pool: Array[CardData], relic_pool: Array[RelicData]) -> void:
	var cards := card_pool.duplicate()
	cards.shuffle()
	inventory_cards.assign(cards.slice(0, mini(4, cards.size())))
	var relics := relic_pool.duplicate()
	relics.shuffle()
	inventory_relics.assign(relics.slice(0, mini(2, relics.size())))

func buy_card(card: CardData, player_state: PlayerState) -> bool:
	if player_state.gold < card.price:
		return false
	player_state.gold -= card.price
	player_state.deck.append(card.duplicate())
	inventory_cards.erase(card)
	return true

func buy_relic(relic: RelicData, player_state: PlayerState) -> bool:
	if player_state.gold < relic.price:
		return false
	player_state.gold -= relic.price
	player_state.relics.append(relic.duplicate())
	inventory_relics.erase(relic)
	return true
```

- [ ] **Step 2: 语法检查**

```bash
"D:/steam/steamapps/common/Godot Engine/godot.windows.opt.tools.64.exe" --headless --path "E:/MyWork/AIGameTest2" --check-only 2>&1 | tail -5
```

期望：无 ERROR。

- [ ] **Step 3: 提交**

```bash
git add shop/ShopEngine.gd
git commit -m "feat: add ShopEngine RefCounted class"
```

---

## Task 7: ShopScreen（新 Control + Scene）

**Files:**
- Create: `shop/ShopScreen.gd`
- Create: `shop/ShopScreen.tscn`

- [ ] **Step 1: 创建 shop/ShopScreen.gd**

```gdscript
extends Control

@onready var _lbl_gold: Label = $VBoxContainer/LblGold
@onready var _card_list: VBoxContainer = $VBoxContainer/CardList
@onready var _relic_list: VBoxContainer = $VBoxContainer/RelicList
@onready var _btn_leave: Button = $VBoxContainer/BtnLeave

var _engine: ShopEngine

func _ready() -> void:
	_engine = ShopEngine.new()
	_engine.generate(_card_pool(), _relic_pool())
	_btn_leave.pressed.connect(GameManager.go_to_map)
	_rebuild_ui()

func _card_pool() -> Array[CardData]:
	return [
		preload("res://data/cards/strike.tres"),
		preload("res://data/cards/defend.tres"),
		preload("res://data/cards/bash.tres"),
		preload("res://data/cards/slash.tres"),
		preload("res://data/cards/insight.tres"),
		preload("res://data/cards/quick_strike.tres"),
		preload("res://data/cards/energize.tres"),
		preload("res://data/cards/dash.tres"),
		preload("res://data/cards/entangle.tres"),
	]

func _relic_pool() -> Array[RelicData]:
	return [
		preload("res://data/relics/burning_gem.tres"),
		preload("res://data/relics/life_ring.tres"),
	]

func _rebuild_ui() -> void:
	_lbl_gold.text = "金币：%d" % GameManager.player_state.gold
	_rebuild_card_list()
	_rebuild_relic_list()

func _rebuild_card_list() -> void:
	for child in _card_list.get_children():
		child.queue_free()
	for card: CardData in _engine.inventory_cards:
		var btn := Button.new()
		btn.text = "%s  [%d金]" % [card.get_description(), card.price]
		btn.disabled = GameManager.player_state.gold < card.price
		btn.pressed.connect(_on_buy_card.bind(card))
		_card_list.add_child(btn)

func _rebuild_relic_list() -> void:
	for child in _relic_list.get_children():
		child.queue_free()
	for relic: RelicData in _engine.inventory_relics:
		var btn := Button.new()
		btn.text = "%s — %s  [%d金]" % [relic.display_name, relic.description, relic.price]
		btn.disabled = GameManager.player_state.gold < relic.price
		btn.pressed.connect(_on_buy_relic.bind(relic))
		_relic_list.add_child(btn)

func _on_buy_card(card: CardData) -> void:
	_engine.buy_card(card, GameManager.player_state)
	_rebuild_ui()

func _on_buy_relic(relic: RelicData) -> void:
	_engine.buy_relic(relic, GameManager.player_state)
	_rebuild_ui()
```

- [ ] **Step 2: 创建 shop/ShopScreen.tscn**

创建文件 `shop/ShopScreen.tscn`，内容如下：

```
[gd_scene load_steps=2 format=3]

[ext_resource type="Script" path="res://shop/ShopScreen.gd" id="1_ShopScreen"]

[node name="ShopScreen" type="Control"]
script = ExtResource("1_ShopScreen")
layout_mode = 3
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0

[node name="VBoxContainer" type="VBoxContainer" parent="."]
layout_mode = 1
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0

[node name="LblGold" type="Label" parent="VBoxContainer"]
text = "金币：0"

[node name="LblCards" type="Label" parent="VBoxContainer"]
text = "卡牌"

[node name="CardList" type="VBoxContainer" parent="VBoxContainer"]

[node name="LblRelics" type="Label" parent="VBoxContainer"]
text = "遗物"

[node name="RelicList" type="VBoxContainer" parent="VBoxContainer"]

[node name="BtnLeave" type="Button" parent="VBoxContainer"]
text = "离开商店"
```

- [ ] **Step 3: 语法检查**

```bash
"D:/steam/steamapps/common/Godot Engine/godot.windows.opt.tools.64.exe" --headless --path "E:/MyWork/AIGameTest2" --check-only 2>&1 | tail -5
```

期望：无 ERROR。

- [ ] **Step 4: 完整流程手动验收**

运行游戏验证以下路径：

1. 开始新游戏 → 地图上可以看到"商店"节点
2. 打赢一场战斗 → 奖励界面出现"拾取 X 金币"按钮 → 点击后金币数增加
3. 进入商店节点 → 显示 4 张卡牌和 1~2 个遗物，各有价格
4. 金币不足时按钮显示为禁用
5. 金币足够时购买一张卡牌 → 金币减少，卡牌从列表消失，牌组里多了这张牌
6. 点击"离开商店" → 回到地图，商店节点标记为已完成

- [ ] **Step 5: 提交**

```bash
git add shop/ShopScreen.gd shop/ShopScreen.tscn
git commit -m "feat: add ShopScreen UI and scene"
```
