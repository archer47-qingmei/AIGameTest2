# v0.23.0 地图扩展 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将固定 3 列 5 节点地图替换为随机生成的 4 列 7 节点地图，每局布局不同。

**Architecture:** 新增 `MapGenerator.gd`（RefCounted 静态工具），负责随机生成节点列表和连接关系。`GameManager.start_new_run()` 调用 `MapGenerator.generate()` 替换硬编码拓扑。`MapScreen._get_node_pos()` 更新坐标映射支持第 3 列。

**Tech Stack:** Godot 4.6.2 GDScript，Resource（.tres），RefCounted 静态工具类

---

## 文件结构

| 操作 | 文件 | 职责 |
|---|---|---|
| 新增 | `MapGenerator.gd` | 随机生成 Array[NodeData]，动态创建 NodeConfig，设置连接关系 |
| 修改 | `GameManager.gd` | start_new_run() 调用 MapGenerator，移除硬编码节点 |
| 修改 | `map/MapScreen.gd` | _get_node_pos() 支持第 3 列坐标 |

---

## Task 1：MapGenerator 静态工具类

**Files:**
- Create: `MapGenerator.gd`

### 生成规则

| 列 | 节点数 | 类型 | 敌人 |
|---|---|---|---|
| 第 0 列 | 2 | COMBAT × 2 | jaw_worm 和 fire_lizard，随机分配行 |
| 第 1 列 | 2 | 1 COMBAT + 1 REST，随机分配行 | COMBAT → 随机选 jaw_worm 或 fire_lizard |
| 第 2 列 | 2 | 从 {COMBAT+REST, COMBAT+ELITE, REST+ELITE} 随机选组合，随机分配行 | COMBAT → 随机选，ELITE → elite_guard |
| 第 3 列 | 1 | COMBAT（Boss），row=0 | boss |

连接：第 N 列所有节点 → 第 N+1 列所有节点（用 `nd.connections.assign(next_col)`）。

- [ ] **Step 1：创建 MapGenerator.gd**

```gdscript
class_name MapGenerator
extends RefCounted

static func generate() -> Array[NodeData]:
	var col0: Array[NodeData] = _make_col0()
	var col1: Array[NodeData] = _make_col1()
	var col2: Array[NodeData] = _make_col2()
	var col3: Array[NodeData] = _make_col3()
	for nd in col0:
		nd.connections.assign(col1)
	for nd in col1:
		nd.connections.assign(col2)
	for nd in col2:
		nd.connections.assign(col3)
	var all: Array[NodeData] = []
	all.append_array(col0)
	all.append_array(col1)
	all.append_array(col2)
	all.append_array(col3)
	return all

static func _make_col0() -> Array[NodeData]:
	var enemy_paths: Array[String] = [
		"res://data/enemies/jaw_worm.tres",
		"res://data/enemies/fire_lizard.tres",
	]
	enemy_paths.shuffle()
	var result: Array[NodeData] = []
	for row in 2:
		var nd := NodeData.new()
		nd.config = NodeConfig.new()
		nd.config.type = NodeConfig.Type.COMBAT
		nd.config.enemy_data = load(enemy_paths[row]) as EnemyData
		nd.config.column = 0
		nd.config.row = row
		result.append(nd)
	return result

static func _make_col1() -> Array[NodeData]:
	var types: Array = [NodeConfig.Type.COMBAT, NodeConfig.Type.REST]
	types.shuffle()
	var result: Array[NodeData] = []
	for row in 2:
		var nd := NodeData.new()
		nd.config = NodeConfig.new()
		nd.config.type = types[row]
		nd.config.column = 1
		nd.config.row = row
		if nd.config.type == NodeConfig.Type.COMBAT:
			nd.config.enemy_data = _random_combat_enemy()
		result.append(nd)
	return result

static func _make_col2() -> Array[NodeData]:
	var combos: Array = [
		[NodeConfig.Type.COMBAT, NodeConfig.Type.REST],
		[NodeConfig.Type.COMBAT, NodeConfig.Type.ELITE],
		[NodeConfig.Type.REST,   NodeConfig.Type.ELITE],
	]
	var combo: Array = combos[randi() % combos.size()].duplicate()
	combo.shuffle()
	var result: Array[NodeData] = []
	for row in 2:
		var nd := NodeData.new()
		nd.config = NodeConfig.new()
		nd.config.type = combo[row]
		nd.config.column = 2
		nd.config.row = row
		match nd.config.type:
			NodeConfig.Type.COMBAT:
				nd.config.enemy_data = _random_combat_enemy()
			NodeConfig.Type.ELITE:
				nd.config.enemy_data = load("res://data/enemies/elite_guard.tres") as EnemyData
		result.append(nd)
	return result

static func _make_col3() -> Array[NodeData]:
	var nd := NodeData.new()
	nd.config = NodeConfig.new()
	nd.config.type = NodeConfig.Type.COMBAT
	nd.config.enemy_data = load("res://data/enemies/boss.tres") as EnemyData
	nd.config.column = 3
	nd.config.row = 0
	var result: Array[NodeData] = [nd]
	return result

static func _random_combat_enemy() -> EnemyData:
	var paths: Array[String] = [
		"res://data/enemies/jaw_worm.tres",
		"res://data/enemies/fire_lizard.tres",
	]
	return load(paths[randi() % paths.size()]) as EnemyData
```

- [ ] **Step 2：验证**

运行游戏 → 开始新局（GameManager 尚未调用 MapGenerator，此步只验证文件无解析错误）。Godot 输出中不应出现 MapGenerator 相关报错。

- [ ] **Step 3：提交**

```bash
cd "e:/MyWork/AIGameTest2"
git add MapGenerator.gd
git commit -m "feat: add MapGenerator static utility for random map generation"
```

---

## Task 2：GameManager 调用 MapGenerator

**Files:**
- Modify: `GameManager.gd`

当前 `start_new_run()` 中的节点硬编码部分（第 29–49 行）：

```gdscript
	var a1: NodeData = NodeData.new()
	a1.config = preload("res://data/nodes/node_a1.tres")
	var a2: NodeData = NodeData.new()
	a2.config = preload("res://data/nodes/node_a2.tres")
	var b1: NodeData = NodeData.new()
	b1.config = preload("res://data/nodes/node_b1.tres")
	var b2: NodeData = NodeData.new()
	b2.config = preload("res://data/nodes/node_b2.tres")
	var c1: NodeData = NodeData.new()
	c1.config = preload("res://data/nodes/node_c1.tres")

	a1.connections.assign([b1, b2])
	a2.connections.assign([b1, b2])
	b1.connections.assign([c1])
	b2.connections.assign([c1])

	player_state.map_all_nodes.assign([a1, a2, b1, b2, c1])
	player_state.available_nodes.assign([a1, a2])
```

- [ ] **Step 1：替换硬编码节点为 MapGenerator.generate()**

将上述代码段替换为：

```gdscript
	var nodes: Array[NodeData] = MapGenerator.generate()
	player_state.map_all_nodes.assign(nodes)
	var start_nodes: Array[NodeData] = []
	for nd in nodes:
		if nd.config.column == 0:
			start_nodes.append(nd)
	player_state.available_nodes.assign(start_nodes)
```

完整替换后 `start_new_run()` 应为：

```gdscript
func start_new_run() -> void:
	player_state = PlayerState.new()
	var strike: CardData = preload("res://data/cards/strike.tres")
	var defend: CardData = preload("res://data/cards/defend.tres")
	var bash: CardData   = preload("res://data/cards/bash.tres")
	var slash: CardData  = preload("res://data/cards/slash.tres")
	for i in 4:
		player_state.deck.append(strike.duplicate())
	for i in 4:
		player_state.deck.append(defend.duplicate())
	player_state.deck.append(bash.duplicate())
	player_state.deck.append(slash.duplicate())

	var nodes: Array[NodeData] = MapGenerator.generate()
	player_state.map_all_nodes.assign(nodes)
	var start_nodes: Array[NodeData] = []
	for nd in nodes:
		if nd.config.column == 0:
			start_nodes.append(nd)
	player_state.available_nodes.assign(start_nodes)

	current_phase = Phase.MAP
	get_tree().change_scene_to_file("res://map/MapScreen.tscn")
```

- [ ] **Step 2：验证**

运行游戏 → 开始新局 → 确认能进入地图界面，无脚本报错。此时 MapScreen 尚未更新坐标，第 3 列（Boss）节点位置可能显示异常，但不应崩溃。

- [ ] **Step 3：提交**

```bash
cd "e:/MyWork/AIGameTest2"
git add GameManager.gd
git commit -m "feat: GameManager uses MapGenerator instead of hardcoded map topology"
```

---

## Task 3：MapScreen 坐标映射更新

**Files:**
- Modify: `map/MapScreen.gd`

当前 `_get_node_pos()`（第 50–54 行）：

```gdscript
func _get_node_pos(nd: NodeData) -> Vector2:
	if nd.config.column == 2:
		return Vector2(380.0, 375.0)
	return Vector2(100.0 if nd.config.column == 0 else 240.0,
				   250.0 if nd.config.row == 0 else 500.0)
```

新布局（4 列）：
- 第 0 列：x = 100，第 1 列：x = 210，第 2 列：x = 320
- 第 3 列（Boss，单节点居中）：x = 430，y = 375

- [ ] **Step 1：更新 _get_node_pos()**

将 `_get_node_pos()` 完整替换为：

```gdscript
func _get_node_pos(nd: NodeData) -> Vector2:
	if nd.config.column == 3:
		return Vector2(430.0, 375.0)
	var col_x: float
	match nd.config.column:
		0: col_x = 100.0
		1: col_x = 210.0
		2: col_x = 320.0
		_: col_x = 100.0
	return Vector2(col_x, 250.0 if nd.config.row == 0 else 500.0)
```

- [ ] **Step 2：验证地图布局**

运行游戏 → 开始新局 → 地图界面确认：
- 共 7 个节点按钮，分布在 4 列
- 第 0 列（x≈100）：2 个「战斗」节点（可点击）
- 第 1 列（x≈210）：1 个「战斗」+ 1 个「休息站」（锁定）
- 第 2 列（x≈320）：2 个节点，类型为以下之一：战斗+休息站、战斗+精英战斗、休息站+精英战斗（锁定）
- 第 3 列（x≈430）：1 个「Boss 战斗」（锁定）
- 节点间连线正确，第 0 列节点连到第 1 列两个节点

- [ ] **Step 3：提交**

```bash
cd "e:/MyWork/AIGameTest2"
git add map/MapScreen.gd
git commit -m "feat: MapScreen supports 4-column coordinate mapping"
```

---

## Task 4：集成验证

- [ ] **验收 1：地图随机性**

开局 5 次，观察第 2 列节点类型组合：应在「COMBAT+REST」「COMBAT+ELITE」「REST+ELITE」之间变化（不是每次都相同）。

- [ ] **验收 2：完整游戏流程（普通路线）**

开局 → 第 0 列选战斗 → 胜利 → 奖励 → 地图 → 第 1 列选战斗 → 胜利 → 奖励 → 地图 → 第 2 列选战斗（如有）→ 胜利 → 奖励 → 地图 → Boss 战 → 胜利 → 奖励界面（显示生命之环）→ 继续 → 胜利界面

- [ ] **验收 3：精英路线**

走到含 ELITE 节点的第 2 列 → 进入精英战 → 胜利 → 奖励界面显示「燃烧宝石」

- [ ] **验收 4：休息站流程**

第 1 列选休息站 → 进入休息界面 → 恢复 HP → 返回地图，流程正常

- [ ] **验收 5：战斗失败**

任意战斗挂机 → 正确跳转 Game Over

- [ ] **最终提交**

```bash
cd "e:/MyWork/AIGameTest2"
git add .
git commit -m "docs: v0.23.0 integration tests passed"
git tag v0.23.0
```
