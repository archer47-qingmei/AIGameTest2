# v0.7.0 战斗后奖励选牌实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 战斗胜利后进入奖励场景，从随机 3 张卡牌中选 1 张加入本局 PlayerState，败北直接返回主菜单。

**Architecture:** 新增 PlayerState（RefCounted，持有本局牌库）和 RewardEngine（RefCounted，静态方法生成奖励选项）；GameManager 持有 PlayerState 并负责初始牌库组建；CombatEngine.setup() 接受外部牌库参数；RewardScreen 纯 UI 层。

**Tech Stack:** Godot 4.6, GDScript strict 类型模式，DirAccess 枚举资源文件

---

## 文件变更一览

| 操作 | 文件 | 职责 |
|------|------|------|
| 新建 | `res://PlayerState.gd` | 本局运行时牌库状态 |
| 新建 | `res://PlayerState.gd.uid` | Godot 4.4+ UID sidecar |
| 新建 | `res://RewardEngine.gd` | 奖励选项生成逻辑（静态方法） |
| 新建 | `res://RewardEngine.gd.uid` | Godot 4.4+ UID sidecar |
| 新建 | `res://reward/RewardScreen.gd` | 奖励 UI 脚本 |
| 新建 | `res://reward/RewardScreen.tscn` | 奖励场景 |
| 修改 | `res://GameManager.gd` | 持有 player_state，新增 go_to_reward()，go_to_combat() 初始化牌库 |
| 修改 | `res://CombatEngine.gd` | setup() 接受 Array[CardData] 参数，移除硬编码牌库 |
| 修改 | `res://combat/CombatScreen.tscn` | 新增 BtnGetReward 节点（visible=false） |
| 修改 | `res://combat/CombatScreen.gd` | 传牌库给 setup()，胜利/败北分别显示不同按钮 |

---

## Task 1：PlayerState + RewardEngine（纯数据与逻辑层）

**Files:**
- Create: `e:/MyWork/AIGameTest2/PlayerState.gd`
- Create: `e:/MyWork/AIGameTest2/PlayerState.gd.uid`
- Create: `e:/MyWork/AIGameTest2/RewardEngine.gd`
- Create: `e:/MyWork/AIGameTest2/RewardEngine.gd.uid`

> **背景：** 两个 RefCounted 子类，均无 Node 依赖，符合架构原则。PlayerState 只是数据容器；RewardEngine 只有一个静态方法，用 DirAccess 枚举 `res://data/cards/` 下所有 `.tres` 文件并随机返回 3 张。此 Task 不运行项目，仅创建文件并提交。

- [ ] **Step 1：创建 PlayerState.gd**

创建 `e:/MyWork/AIGameTest2/PlayerState.gd`：

```gdscript
class_name PlayerState
extends RefCounted

var deck: Array[CardData] = []
```

- [ ] **Step 2：创建 PlayerState.gd.uid**

创建 `e:/MyWork/AIGameTest2/PlayerState.gd.uid`：

```
uid://cbn4xp2qt7ema
```

- [ ] **Step 3：创建 RewardEngine.gd**

创建 `e:/MyWork/AIGameTest2/RewardEngine.gd`：

```gdscript
class_name RewardEngine
extends RefCounted

static func get_options() -> Array[CardData]:
	var options: Array[CardData] = []
	var dir := DirAccess.open("res://data/cards/")
	if dir == null:
		return options
	dir.list_dir_begin()
	var file_name := dir.get_next()
	while file_name != "":
		if file_name.ends_with(".tres"):
			var card := load("res://data/cards/" + file_name) as CardData
			if card != null:
				options.append(card)
		file_name = dir.get_next()
	options.shuffle()
	return options.slice(0, mini(3, options.size()))
```

- [ ] **Step 4：创建 RewardEngine.gd.uid**

创建 `e:/MyWork/AIGameTest2/RewardEngine.gd.uid`：

```
uid://df8wkq6ry2pnv
```

- [ ] **Step 5：提交**

```bash
git add PlayerState.gd PlayerState.gd.uid RewardEngine.gd RewardEngine.gd.uid
git commit -m "feat: add PlayerState and RewardEngine"
```

---

## Task 2：GameManager 扩展 + CombatEngine 重构

**Files:**
- Modify: `e:/MyWork/AIGameTest2/GameManager.gd`
- Modify: `e:/MyWork/AIGameTest2/CombatEngine.gd`

> **背景：** GameManager 新增 Phase.REWARD、player_state 字段、go_to_reward()，并在 go_to_combat() 中初始化 PlayerState 和填入初始 10 张牌（硬编码从 CombatEngine 移至此处）。CombatEngine.setup() 签名改为接受 `Array[CardData]` 参数，移除内部的卡牌 preload 逻辑。注意：此 Task 完成后 CombatScreen 还未更新，项目暂时无法运行（setup() 签名已变但调用方未改），将在 Task 3 一并修复。

- [ ] **Step 1：更新 GameManager.gd**

将 `e:/MyWork/AIGameTest2/GameManager.gd` 完整内容替换为：

```gdscript
extends Node

enum Phase { MENU, COMBAT, REWARD }

var current_phase: Phase = Phase.MENU
var player_state: PlayerState

func go_to_combat() -> void:
	player_state = PlayerState.new()
	var strike: CardData = preload("res://data/cards/strike.tres")
	var defend: CardData = preload("res://data/cards/defend.tres")
	var bash: CardData = preload("res://data/cards/bash.tres")
	var slash: CardData = preload("res://data/cards/slash.tres")
	for i in 4:
		player_state.deck.append(strike.duplicate())
	for i in 4:
		player_state.deck.append(defend.duplicate())
	player_state.deck.append(bash.duplicate())
	player_state.deck.append(slash.duplicate())
	current_phase = Phase.COMBAT
	get_tree().change_scene_to_file("res://combat/CombatScreen.tscn")

func go_to_reward() -> void:
	current_phase = Phase.REWARD
	get_tree().change_scene_to_file("res://reward/RewardScreen.tscn")

func go_to_menu() -> void:
	player_state = null
	current_phase = Phase.MENU
	get_tree().change_scene_to_file("res://menu/MainMenu.tscn")
```

- [ ] **Step 2：更新 CombatEngine.gd**

将 `e:/MyWork/AIGameTest2/CombatEngine.gd` 的 `setup()` 方法完整替换为（只改 setup，其余方法不动）：

```gdscript
func setup(initial_deck: Array[CardData]) -> void:
	_enemy_data = preload("res://data/enemies/jaw_worm.tres")
	enemy = Combatant.new()
	enemy.display_name = _enemy_data.display_name
	enemy.hp = _enemy_data.hp
	enemy.max_hp = _enemy_data.hp
	enemy.block = 0

	player = Combatant.new()
	player.display_name = "玩家"
	player.hp = 70
	player.max_hp = 70
	player.block = 0

	for card: CardData in initial_deck:
		_draw_pile.append(card.duplicate())
	_draw_pile.shuffle()
	_start_player_turn()
```

改完后 `CombatEngine.gd` 完整内容应为：

```gdscript
class_name CombatEngine
extends RefCounted

signal state_changed
signal combat_ended(result: String)

var player: Combatant
var enemy: Combatant
var hand: Array[CardData] = []
var energy: int = 0
var turn_number: int = 0

var _draw_pile: Array[CardData] = []
var _discard_pile: Array[CardData] = []
var _enemy_data: EnemyData

func setup(initial_deck: Array[CardData]) -> void:
	_enemy_data = preload("res://data/enemies/jaw_worm.tres")
	enemy = Combatant.new()
	enemy.display_name = _enemy_data.display_name
	enemy.hp = _enemy_data.hp
	enemy.max_hp = _enemy_data.hp
	enemy.block = 0

	player = Combatant.new()
	player.display_name = "玩家"
	player.hp = 70
	player.max_hp = 70
	player.block = 0

	for card: CardData in initial_deck:
		_draw_pile.append(card.duplicate())
	_draw_pile.shuffle()
	_start_player_turn()

func get_current_enemy_action() -> EnemyActionData:
	return _enemy_data.actions[(turn_number - 1) % _enemy_data.actions.size()]

func play_card(card: CardData) -> void:
	if energy < card.cost:
		return
	energy -= card.cost
	EffectResolver.resolve(card, player, enemy)
	var idx: int = hand.find(card)
	if idx >= 0:
		hand.remove_at(idx)
	_discard_pile.append(card)
	state_changed.emit()
	_check_end()

func end_turn() -> void:
	for card: CardData in hand:
		_discard_pile.append(card)
	hand.clear()
	_do_enemy_turn()
	state_changed.emit()
	if not _check_end():
		_start_player_turn()

func _start_player_turn() -> void:
	turn_number += 1
	energy = 3
	player.block = 0
	_draw_hand()
	state_changed.emit()

func _draw_hand() -> void:
	hand.clear()
	for i in 5:
		if _draw_pile.is_empty():
			if _discard_pile.is_empty():
				break
			for card: CardData in _discard_pile:
				_draw_pile.append(card)
			_discard_pile.clear()
			_draw_pile.shuffle()
		if _draw_pile.is_empty():
			break
		var card: CardData = _draw_pile.pop_back()
		hand.append(card)

func _do_enemy_turn() -> void:
	enemy.block = 0
	var action: EnemyActionData = _enemy_data.actions[(turn_number - 1) % _enemy_data.actions.size()]
	if action.type == "attack":
		player.take_damage(action.value)
	else:
		enemy.add_block(action.value)

func _check_end() -> bool:
	if enemy.hp <= 0:
		combat_ended.emit("victory")
		return true
	if player.hp <= 0:
		combat_ended.emit("game_over")
		return true
	return false
```

- [ ] **Step 3：提交**

```bash
git add GameManager.gd CombatEngine.gd
git commit -m "refactor: move deck init to GameManager, CombatEngine.setup() takes external deck"
```

---

## Task 3：CombatScreen 更新 + RewardScreen 新建

**Files:**
- Modify: `e:/MyWork/AIGameTest2/combat/CombatScreen.tscn`
- Modify: `e:/MyWork/AIGameTest2/combat/CombatScreen.gd`
- Create: `e:/MyWork/AIGameTest2/reward/RewardScreen.gd`
- Create: `e:/MyWork/AIGameTest2/reward/RewardScreen.tscn`

> **背景：** CombatScreen 新增 BtnGetReward 节点，_ready() 改为传 PlayerState.deck 给 setup()，_on_combat_ended() 按胜负显示不同按钮。RewardScreen 在 _ready() 调用 RewardEngine.get_options() 动态创建 3 个按钮，选中后加入 PlayerState.deck 并调用 GameManager.go_to_menu()。此 Task 完成后项目可完整运行，最后运行验证。

- [ ] **Step 1：更新 CombatScreen.tscn（新增 BtnGetReward 节点）**

将 `e:/MyWork/AIGameTest2/combat/CombatScreen.tscn` 完整内容替换为：

```
[gd_scene load_steps=2 format=3 uid="uid://combat_screen"]

[ext_resource type="Script" path="res://combat/CombatScreen.gd" id="1_script"]

[node name="CombatScreen" type="Control"]
layout_mode = 3
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
script = ExtResource("1_script")

[node name="VBoxContainer" type="VBoxContainer" parent="."]
layout_mode = 1
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
theme_override_constants/separation = 10

[node name="EnemyPanel" type="VBoxContainer" parent="VBoxContainer"]
layout_mode = 2

[node name="LblEnemyName" type="Label" parent="VBoxContainer/EnemyPanel"]
layout_mode = 2
text = "Enemy"

[node name="LblEnemyHP" type="Label" parent="VBoxContainer/EnemyPanel"]
layout_mode = 2
text = "生命：?"

[node name="LblEnemyBlock" type="Label" parent="VBoxContainer/EnemyPanel"]
layout_mode = 2
text = "格挡：0"

[node name="LblEnemyIntent" type="Label" parent="VBoxContainer/EnemyPanel"]
layout_mode = 2
text = "意图：?"

[node name="PlayerPanel" type="VBoxContainer" parent="VBoxContainer"]
layout_mode = 2

[node name="LblPlayerHP" type="Label" parent="VBoxContainer/PlayerPanel"]
layout_mode = 2
text = "生命：?"

[node name="LblPlayerBlock" type="Label" parent="VBoxContainer/PlayerPanel"]
layout_mode = 2
text = "格挡：0"

[node name="LblEnergy" type="Label" parent="VBoxContainer/PlayerPanel"]
layout_mode = 2
text = "能量：3 / 3"

[node name="HandContainer" type="HBoxContainer" parent="VBoxContainer"]
layout_mode = 2

[node name="BtnEndTurn" type="Button" parent="VBoxContainer"]
layout_mode = 2
text = "结束回合"

[node name="LblResult" type="Label" parent="."]
layout_mode = 1
anchors_preset = 8
anchor_left = 0.5
anchor_top = 0.5
anchor_right = 0.5
anchor_bottom = 0.5
visible = false
text = ""

[node name="BtnReturnMenu" type="Button" parent="."]
layout_mode = 1
anchors_preset = 8
anchor_left = 0.5
anchor_top = 0.6
anchor_right = 0.5
anchor_bottom = 0.6
offset_left = -70.0
offset_top = -15.0
offset_right = 70.0
offset_bottom = 15.0
visible = false
text = "返回主菜单"

[node name="BtnGetReward" type="Button" parent="."]
layout_mode = 1
anchors_preset = 8
anchor_left = 0.5
anchor_top = 0.6
anchor_right = 0.5
anchor_bottom = 0.6
offset_left = -60.0
offset_top = -35.0
offset_right = 60.0
offset_bottom = -5.0
visible = false
text = "领取奖励"
```

- [ ] **Step 2：更新 CombatScreen.gd**

将 `e:/MyWork/AIGameTest2/combat/CombatScreen.gd` 完整内容替换为：

```gdscript
extends Control

@onready var _lbl_enemy_name: Label    = $VBoxContainer/EnemyPanel/LblEnemyName
@onready var _lbl_enemy_hp: Label      = $VBoxContainer/EnemyPanel/LblEnemyHP
@onready var _lbl_enemy_block: Label   = $VBoxContainer/EnemyPanel/LblEnemyBlock
@onready var _lbl_enemy_intent: Label  = $VBoxContainer/EnemyPanel/LblEnemyIntent
@onready var _lbl_player_hp: Label     = $VBoxContainer/PlayerPanel/LblPlayerHP
@onready var _lbl_player_block: Label  = $VBoxContainer/PlayerPanel/LblPlayerBlock
@onready var _lbl_energy: Label        = $VBoxContainer/PlayerPanel/LblEnergy
@onready var _hand_container: HBoxContainer = $VBoxContainer/HandContainer
@onready var _btn_end_turn: Button     = $VBoxContainer/BtnEndTurn
@onready var _lbl_result: Label        = $LblResult
@onready var _btn_return_menu: Button  = $BtnReturnMenu
@onready var _btn_get_reward: Button   = $BtnGetReward

var _engine: CombatEngine
var _hand_buttons: Array[Button] = []

func _ready() -> void:
	_engine = CombatEngine.new()
	_engine.state_changed.connect(_refresh_ui)
	_engine.combat_ended.connect(_on_combat_ended)
	_btn_end_turn.pressed.connect(_engine.end_turn)
	_btn_return_menu.pressed.connect(GameManager.go_to_menu)
	_btn_get_reward.pressed.connect(GameManager.go_to_reward)
	_engine.setup(GameManager.player_state.deck)

func _on_card_pressed(card: CardData) -> void:
	_engine.play_card(card)

func _refresh_ui() -> void:
	_lbl_enemy_name.text = _engine.enemy.display_name
	_lbl_enemy_hp.text = "生命：%d / %d" % [_engine.enemy.hp, _engine.enemy.max_hp]
	_lbl_enemy_block.text = "格挡：%d" % _engine.enemy.block
	var action: EnemyActionData = _engine.get_current_enemy_action()
	var intent: String = "攻击 %d" % action.value if action.type == "attack" else "格挡 %d" % action.value
	_lbl_enemy_intent.text = "意图：" + intent
	_lbl_player_hp.text = "生命：%d / %d" % [_engine.player.hp, _engine.player.max_hp]
	_lbl_player_block.text = "格挡：%d" % _engine.player.block
	_lbl_energy.text = "能量：%d / 3" % _engine.energy
	_rebuild_hand()

func _rebuild_hand() -> void:
	for btn: Button in _hand_buttons:
		btn.queue_free()
	_hand_buttons.clear()
	for card: CardData in _engine.hand:
		var btn: Button = Button.new()
		var dmg: int = 0
		var blk: int = 0
		for effect: CardEffectData in card.effects:
			if effect.type == "damage":
				dmg = effect.value
			elif effect.type == "block":
				blk = effect.value
		btn.text = "%s\n费用:%d  攻:%d  挡:%d" % [card.card_name, card.cost, dmg, blk]
		btn.pressed.connect(_on_card_pressed.bind(card))
		_hand_container.add_child(btn)
		_hand_buttons.append(btn)

func _on_combat_ended(result: String) -> void:
	_lbl_result.text = "胜利！" if result == "victory" else "游戏结束"
	_lbl_result.visible = true
	_btn_end_turn.disabled = true
	if result == "victory":
		_btn_get_reward.visible = true
	else:
		_btn_return_menu.visible = true
	for btn: Button in _hand_buttons:
		btn.disabled = true
```

- [ ] **Step 3：创建 reward/RewardScreen.gd**

创建 `e:/MyWork/AIGameTest2/reward/RewardScreen.gd`（如果 reward/ 目录不存在先创建）：

```gdscript
extends Control

func _ready() -> void:
	var options: Array[CardData] = RewardEngine.get_options()
	for card: CardData in options:
		var btn := Button.new()
		var dmg: int = 0
		var blk: int = 0
		for effect: CardEffectData in card.effects:
			if effect.type == "damage":
				dmg = effect.value
			elif effect.type == "block":
				blk = effect.value
		btn.text = "%s\n费用:%d  攻:%d  挡:%d" % [card.card_name, card.cost, dmg, blk]
		btn.pressed.connect(_on_card_selected.bind(card))
		$CardContainer.add_child(btn)

func _on_card_selected(card: CardData) -> void:
	GameManager.player_state.deck.append(card.duplicate())
	GameManager.go_to_menu()
```

- [ ] **Step 4：创建 reward/RewardScreen.tscn**

创建 `e:/MyWork/AIGameTest2/reward/RewardScreen.tscn`：

```
[gd_scene load_steps=2 format=3 uid="uid://ckp5nv8qxd2la"]

[ext_resource type="Script" path="res://reward/RewardScreen.gd" id="1_script"]

[node name="RewardScreen" type="Control"]
layout_mode = 3
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
script = ExtResource("1_script")

[node name="VBoxContainer" type="VBoxContainer" parent="."]
layout_mode = 1
anchors_preset = 8
anchor_left = 0.5
anchor_top = 0.5
anchor_right = 0.5
anchor_bottom = 0.5
offset_left = -200.0
offset_top = -80.0
offset_right = 200.0
offset_bottom = 80.0
theme_override_constants/separation = 16

[node name="LblTitle" type="Label" parent="VBoxContainer"]
layout_mode = 2
text = "选择一张卡牌加入牌库"
horizontal_alignment = 1

[node name="CardContainer" type="HBoxContainer" parent="VBoxContainer"]
layout_mode = 2
theme_override_constants/separation = 12
```

- [ ] **Step 5：运行项目验证**

使用 `mcp__godot__run_project`（projectPath="e:/MyWork/AIGameTest2"）运行项目。
等待约 3 秒后调用 `mcp__godot__get_debug_output`，确认：
- 无 `SCRIPT ERROR:` 或 `Parser Error:`
- 游戏启动进入主菜单

- [ ] **Step 6：停止项目**

使用 `mcp__godot__stop_project` 停止项目。

- [ ] **Step 7：提交并打标签**

```bash
git add combat/CombatScreen.tscn combat/CombatScreen.gd reward/RewardScreen.gd reward/RewardScreen.tscn
git commit -m "feat: add RewardScreen and wire combat victory to reward flow"
git tag v0.7.0
git log --oneline -5
```

---

## 验收总结

完成所有 Task 后，以下标准应全部满足：

- [ ] 战斗胜利后出现"领取奖励"按钮，点击进入奖励场景
- [ ] 奖励场景显示 3 张随机卡牌选项，包含名称和费用/攻/挡信息
- [ ] 点击任意一张，返回主菜单（该卡已加入 PlayerState）
- [ ] 战斗败北后仍显示"返回主菜单"按钮，直接返回主菜单，无奖励
- [ ] 全部 v0.6.0 验收标准继续通过
