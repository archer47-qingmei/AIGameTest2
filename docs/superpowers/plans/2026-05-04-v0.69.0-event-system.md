# v0.69.0 机缘事件系统 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增机缘（EVENT）节点类型，玩家进入后触发随机叙事事件，通过选项获得奖励或承受代价，本版本实装 12 件事件验证完整系统流程。

**Architecture:** 三层架构——数据层（EventData/EventChoiceData/EventEffectData Resource）、逻辑层（EventPool 路径注册表 + EventEngine 静态执行器）、UI 层（EventScreen 三阶段界面）。地图层在 NodeConfig/MapGenerator/GameManager 追加 EVENT 支持。RestScreen「升级」UI 文案改为「悟道」。

**Tech Stack:** Godot 4.4 GDScript, Resource/.tres 数据文件, Control 场景 UI

---

## File Structure

**新建文件：**
- `EventData.gd` — 事件蓝图 Resource，含 AppearCondition enum
- `EventChoiceData.gd` — 选项蓝图 Resource
- `EventEffectData.gd` — 效果蓝图 Resource，含 EffectType enum（23种）
- `EventPool.gd` — 事件路径注册表 + pick_event 静态方法
- `EventEngine.gd` — 纯静态效果执行器（check_condition / apply_immediate_effects / apply_interactive_effect / describe_effects）
- `event/EventScreen.tscn` — 三阶段事件 UI 场景
- `event/EventScreen.gd` — 事件 UI 脚本
- `data/events/ling_shi_kuang_mai.tres` — 灵石矿脉
- `data/events/po_miao_jie_su.tres` — 破庙借宿
- `data/events/qiu_xian_ku.tres` — 囚仙窟
- `data/events/yu_chan.tres` — 玉蝉
- `data/events/du_shi.tres` — 赌石
- `data/events/wu_dao_shi.tres` — 悟道石
- `data/events/liang_ge_xiu_shi.tres` — 两个修士
- `data/events/lao_xiu_shi_yan_dou.tres` — 老修士的烟斗
- `data/events/ku_jing.tres` — 枯井
- `data/events/si_ceng_xiang_shi.tres` — 似曾相识
- `data/events/ya_bi_ke_zi.tres` — 崖壁刻字
- `data/events/jian_hen.tres` — 剑痕（剑修专属）

**修改文件：**
- `NodeConfig.gd` — 追加 EVENT 到 Type enum
- `MapGenerator.gd` — 新增 `_add_event_nodes()` 静态方法，生成每局 2–3 个 EVENT 节点
- `GameManager.gd` — 追加 Phase.EVENT + `go_to_event()` + select_node EVENT 分支
- `PlayerState.gd` — 追加 `reincarnation_fragments: int = 0` 字段
- `rest/RestScreen.gd` + `rest/RestScreen.tscn` — UI 文案「升级」→「悟道」
- `menu/MainMenu.tscn` — 版本号 v0.68.0 → v0.69.0

---

## EffectType 枚举整数值（供 .tres 文件使用）

```
NOTHING=0  GAIN_GOLD=1  LOSE_GOLD=2  GAIN_HP=3  LOSE_HP=4
HEAL_PERCENT_MAX=5  HEAL_FULL=6  LOSE_HP_PERCENT_CURRENT=7
LOSE_MAX_HP_PERCENT=8  GAIN_RELIC=9  LOSE_RELIC_RANDOM=10
GAIN_CARD=11  GAIN_CURSE_CARD=12  REMOVE_CARD_CHOOSE=13
REMOVE_CARDS_TYPE=14  REMOVE_CARDS_RANDOM=15
UPGRADE_CARD_CHOOSE=16  UPGRADE_CARDS_TYPE=17  COPY_CARD_CHOOSE=18
GAIN_REINCARNATION_FRAGMENT=19  GAIN_MAX_HP_PERCENT=20
GAIN_ENERGY_CAP=21  COMBAT_ELITE_STUB=22
```

---

## Task 1: 数据层 Resource 类

**Files:**
- Create: `EventEffectData.gd`
- Create: `EventChoiceData.gd`
- Create: `EventData.gd`

- [ ] **Step 1: 创建 EventEffectData.gd**

```gdscript
# E:\MyWork\AIGameTest2\EventEffectData.gd
class_name EventEffectData
extends Resource

enum EffectType {
	NOTHING,
	GAIN_GOLD,
	LOSE_GOLD,
	GAIN_HP,
	LOSE_HP,
	HEAL_PERCENT_MAX,
	HEAL_FULL,
	LOSE_HP_PERCENT_CURRENT,
	LOSE_MAX_HP_PERCENT,
	GAIN_RELIC,
	LOSE_RELIC_RANDOM,
	GAIN_CARD,
	GAIN_CURSE_CARD,
	REMOVE_CARD_CHOOSE,
	REMOVE_CARDS_TYPE,
	REMOVE_CARDS_RANDOM,
	UPGRADE_CARD_CHOOSE,
	UPGRADE_CARDS_TYPE,
	COPY_CARD_CHOOSE,
	GAIN_REINCARNATION_FRAGMENT,
	GAIN_MAX_HP_PERCENT,
	GAIN_ENERGY_CAP,
	COMBAT_ELITE_STUB
}

@export var effect_type: EffectType = EffectType.NOTHING
@export var value: int = 0
@export var probability: float = 1.0
@export var card_grade: int = 0
@export var relic_grade: int = 0
@export var card_type_filter: String = ""
```

- [ ] **Step 2: 创建 EventChoiceData.gd**

```gdscript
# E:\MyWork\AIGameTest2\EventChoiceData.gd
class_name EventChoiceData
extends Resource

@export var button_text: String = ""
@export var result_text: String = ""
@export var effects: Array[EventEffectData] = []
@export var cost_gold: int = 0
```

- [ ] **Step 3: 创建 EventData.gd**

```gdscript
# E:\MyWork\AIGameTest2\EventData.gd
class_name EventData
extends Resource

enum AppearCondition {
	NONE,
	AFTER_REALM,
	MIN_DECK_SIZE,
	MIN_RELIC_COUNT,
	MIN_HP_PERCENT,
	MIN_UNUPGRADED_ATTACKS
}

@export var display_name: String = ""
@export var mood: String = ""
@export var flavor_text: String = ""
@export var choices: Array[EventChoiceData] = []
@export var appear_condition: AppearCondition = AppearCondition.NONE
@export var appear_condition_value: int = 0
@export var character_requirement: String = ""
```

- [ ] **Step 4: 在 Godot 编辑器中确认无解析错误**

打开 Godot 编辑器，Script 面板底部 Output 无红色报错。或用 godot-mcp `get_debug_output` 确认三个文件无错误。

- [ ] **Step 5: Commit**

```bash
git add EventEffectData.gd EventChoiceData.gd EventData.gd
git commit -m "feat: add EventEffectData, EventChoiceData, EventData resource classes"
```

---

## Task 2: 逻辑层 EventPool + EventEngine

**Files:**
- Create: `EventPool.gd`
- Create: `EventEngine.gd`

- [ ] **Step 1: 创建 EventPool.gd**

```gdscript
# E:\MyWork\AIGameTest2\EventPool.gd
class_name EventPool
extends RefCounted

const EVENTS: Array[String] = [
	"res://data/events/ling_shi_kuang_mai.tres",
	"res://data/events/po_miao_jie_su.tres",
	"res://data/events/qiu_xian_ku.tres",
	"res://data/events/yu_chan.tres",
	"res://data/events/du_shi.tres",
	"res://data/events/wu_dao_shi.tres",
	"res://data/events/liang_ge_xiu_shi.tres",
	"res://data/events/lao_xiu_shi_yan_dou.tres",
	"res://data/events/ku_jing.tres",
	"res://data/events/si_ceng_xiang_shi.tres",
	"res://data/events/ya_bi_ke_zi.tres",
	"res://data/events/jian_hen.tres",
]

static func pick_event(player_state: PlayerState) -> EventData:
	var candidates: Array[EventData] = []
	for path in EVENTS:
		var event: EventData = load(path) as EventData
		if EventEngine.check_condition(event, player_state):
			candidates.append(event)
	if candidates.is_empty():
		return load(EVENTS[0]) as EventData
	return candidates[randi() % candidates.size()]
```

- [ ] **Step 2: 创建 EventEngine.gd**

```gdscript
# E:\MyWork\AIGameTest2\EventEngine.gd
class_name EventEngine
extends RefCounted

static func check_condition(event: EventData, player_state: PlayerState) -> bool:
	if event.character_requirement != "" and event.character_requirement != player_state.character:
		return false
	match event.appear_condition:
		EventData.AppearCondition.NONE:
			return true
		EventData.AppearCondition.MIN_DECK_SIZE:
			return player_state.deck.size() >= event.appear_condition_value
		EventData.AppearCondition.MIN_RELIC_COUNT:
			return player_state.relics.size() >= event.appear_condition_value
		EventData.AppearCondition.MIN_HP_PERCENT:
			return player_state.hp * 100 >= player_state.max_hp * event.appear_condition_value
		EventData.AppearCondition.MIN_UNUPGRADED_ATTACKS:
			var count: int = 0
			for c: CardData in player_state.deck:
				if c.card_type == "招式" and not c.is_upgraded:
					count += 1
			return count >= event.appear_condition_value
		_:
			return false

static func check_choice_available(choice: EventChoiceData, player_state: PlayerState) -> bool:
	return choice.cost_gold == 0 or player_state.gold >= choice.cost_gold

static func apply_immediate_effects(choice: EventChoiceData, player_state: PlayerState) -> Array[EventEffectData]:
	var interactive: Array[EventEffectData] = []
	for effect: EventEffectData in choice.effects:
		if _is_interactive(effect):
			interactive.append(effect)
		else:
			_apply_effect(effect, player_state)
	return interactive

static func apply_interactive_effect(effect: EventEffectData, selected_card: CardData, player_state: PlayerState) -> void:
	match effect.effect_type:
		EventEffectData.EffectType.UPGRADE_CARD_CHOOSE:
			selected_card.upgrade()
		EventEffectData.EffectType.REMOVE_CARD_CHOOSE:
			player_state.deck.erase(selected_card)
		EventEffectData.EffectType.COPY_CARD_CHOOSE:
			player_state.deck.append(selected_card.duplicate())

static func describe_effects(choice: EventChoiceData) -> String:
	var parts: Array[String] = []
	for effect: EventEffectData in choice.effects:
		var desc: String = _describe_single(effect)
		if not desc.is_empty():
			parts.append(desc)
	return "、".join(parts) if not parts.is_empty() else "无事发生"

static func _is_interactive(effect: EventEffectData) -> bool:
	return effect.effect_type in [
		EventEffectData.EffectType.UPGRADE_CARD_CHOOSE,
		EventEffectData.EffectType.REMOVE_CARD_CHOOSE,
		EventEffectData.EffectType.COPY_CARD_CHOOSE,
	]

static func _apply_effect(effect: EventEffectData, player_state: PlayerState) -> void:
	if effect.probability < 1.0 and randf() > effect.probability:
		return
	match effect.effect_type:
		EventEffectData.EffectType.NOTHING:
			pass
		EventEffectData.EffectType.GAIN_GOLD:
			player_state.gold += effect.value
		EventEffectData.EffectType.LOSE_GOLD:
			player_state.gold = maxi(0, player_state.gold - effect.value)
		EventEffectData.EffectType.GAIN_HP:
			player_state.hp = mini(player_state.hp + effect.value, player_state.max_hp)
		EventEffectData.EffectType.LOSE_HP:
			player_state.hp = maxi(1, player_state.hp - effect.value)
		EventEffectData.EffectType.HEAL_PERCENT_MAX:
			player_state.hp = mini(player_state.hp + int(player_state.max_hp * effect.value / 100.0), player_state.max_hp)
		EventEffectData.EffectType.HEAL_FULL:
			player_state.hp = player_state.max_hp
		EventEffectData.EffectType.LOSE_HP_PERCENT_CURRENT:
			player_state.hp = maxi(1, player_state.hp - int(player_state.hp * effect.value / 100.0))
		EventEffectData.EffectType.LOSE_MAX_HP_PERCENT:
			var loss: int = int(player_state.max_hp * effect.value / 100.0)
			player_state.max_hp = maxi(1, player_state.max_hp - loss)
			player_state.hp = mini(player_state.hp, player_state.max_hp)
		EventEffectData.EffectType.GAIN_RELIC:
			if not CardPool.RELICS.is_empty():
				var idx: int = randi() % CardPool.RELICS.size()
				var relic: RelicData = (load(CardPool.RELICS[idx]) as RelicData).duplicate()
				player_state.relics.append(relic)
		EventEffectData.EffectType.GAIN_CURSE_CARD:
			var curse: CardData = (load("res://data/cards/xin_mo.tres") as CardData).duplicate()
			player_state.deck.append(curse)
		EventEffectData.EffectType.REMOVE_CARDS_TYPE:
			var new_deck: Array[CardData] = []
			for c: CardData in player_state.deck:
				var remove: bool = false
				match effect.card_type_filter:
					"心魔": remove = c.is_curse
					"走火入魔": remove = c.is_zahuorumuo
					_: remove = (c.card_type == effect.card_type_filter)
				if not remove:
					new_deck.append(c)
			player_state.deck = new_deck
		EventEffectData.EffectType.GAIN_REINCARNATION_FRAGMENT:
			player_state.reincarnation_fragments += effect.value
		EventEffectData.EffectType.GAIN_MAX_HP_PERCENT:
			var gain: int = int(player_state.max_hp * effect.value / 100.0)
			player_state.max_hp += gain
		EventEffectData.EffectType.COMBAT_ELITE_STUB:
			pass
		_:
			push_warning("EventEngine: unhandled effect_type %d" % effect.effect_type)

static func _describe_single(effect: EventEffectData) -> String:
	var pct: String = "" if effect.probability >= 1.0 else "（%d%%概率）" % int(effect.probability * 100)
	match effect.effect_type:
		EventEffectData.EffectType.NOTHING: return ""
		EventEffectData.EffectType.GAIN_GOLD: return pct + "获得 %d 灵石" % effect.value
		EventEffectData.EffectType.LOSE_GOLD: return pct + "失去 %d 灵石" % effect.value
		EventEffectData.EffectType.GAIN_HP: return pct + "回复 %d HP" % effect.value
		EventEffectData.EffectType.LOSE_HP: return pct + "受到 %d 伤害" % effect.value
		EventEffectData.EffectType.HEAL_PERCENT_MAX: return pct + "回复最大 HP 的 %d%%" % effect.value
		EventEffectData.EffectType.HEAL_FULL: return pct + "回满 HP"
		EventEffectData.EffectType.LOSE_HP_PERCENT_CURRENT: return pct + "失去当前 HP 的 %d%%" % effect.value
		EventEffectData.EffectType.LOSE_MAX_HP_PERCENT: return pct + "最大 HP 降低 %d%%" % effect.value
		EventEffectData.EffectType.GAIN_RELIC:
			match effect.relic_grade:
				1: return pct + "获得随机法器"
				2: return pct + "获得随机灵器"
				3: return pct + "获得随机仙器"
				_: return pct + "获得随机遗物"
		EventEffectData.EffectType.GAIN_CURSE_CARD: return pct + "牌组加入心魔"
		EventEffectData.EffectType.REMOVE_CARDS_TYPE: return pct + "移除牌组中所有%s" % effect.card_type_filter
		EventEffectData.EffectType.UPGRADE_CARD_CHOOSE: return "选择一张牌悟道"
		EventEffectData.EffectType.REMOVE_CARD_CHOOSE: return "选择一张牌移除"
		EventEffectData.EffectType.COPY_CARD_CHOOSE: return "选择一张牌复制"
		EventEffectData.EffectType.GAIN_REINCARNATION_FRAGMENT: return pct + "获得 %d 个轮回碎片" % effect.value
		EventEffectData.EffectType.GAIN_MAX_HP_PERCENT: return pct + "最大 HP 永久 +%d%%" % effect.value
		_: return ""
```

- [ ] **Step 3: 验证无解析错误**

在 Godot 编辑器 Output 面板确认 EventPool.gd 和 EventEngine.gd 加载无报错。（注：EventPool.EVENTS 中的 .tres 文件还未创建，运行时才会访问，不影响解析。）

- [ ] **Step 4: Commit**

```bash
git add EventPool.gd EventEngine.gd
git commit -m "feat: add EventPool and EventEngine logic layer"
```

---

## Task 3: 地图接入（NodeConfig / MapGenerator / GameManager / PlayerState）

**Files:**
- Modify: `NodeConfig.gd`
- Modify: `MapGenerator.gd`
- Modify: `GameManager.gd`
- Modify: `PlayerState.gd`

### 背景说明

当前 `GameManager.select_node()` 的 match 语句中，没有 EVENT 分支，EVENT 节点会 fall-through 到 `go_to_combat()`。本 task 修复这一问题并添加完整路由。

- [ ] **Step 1: 修改 NodeConfig.gd — 追加 EVENT**

当前第 3 行：
```gdscript
enum Type { COMBAT, REST, ELITE, SHOP, CHEST, BOSS }
```
改为：
```gdscript
enum Type { COMBAT, REST, ELITE, SHOP, CHEST, BOSS, EVENT }
```

- [ ] **Step 2: 修改 PlayerState.gd — 追加 reincarnation_fragments**

在 `var gold: int = 0` 之后追加一行：
```gdscript
var reincarnation_fragments: int = 0
```

- [ ] **Step 3: 修改 GameManager.gd — Phase.EVENT + go_to_event() + select_node 分支**

**3a.** 将 Phase enum 中的 `GAME_OVER` 改为：
```gdscript
enum Phase { MENU, CHAR_SELECT, MAP, COMBAT, REWARD, REST, SHOP, WIN, GAME_OVER, EVENT }
```

**3b.** 在 `select_node()` 方法中，在 `NodeConfig.Type.REST:` 之前插入 EVENT 分支：
```gdscript
func select_node(node: NodeData) -> void:
	player_state.current_node = node
	match node.config.type:
		NodeConfig.Type.EVENT:
			go_to_event()
		NodeConfig.Type.REST:
			go_to_rest()
		NodeConfig.Type.SHOP:
			go_to_shop()
		NodeConfig.Type.CHEST:
			go_to_chest()
		_:
			go_to_combat()
```

**3c.** 在 `go_to_rest()` 之前添加新方法：
```gdscript
func go_to_event() -> void:
	current_phase = Phase.EVENT
	get_tree().change_scene_to_file("res://event/EventScreen.tscn")
```

- [ ] **Step 4: 修改 MapGenerator.gd — 添加 _add_event_nodes()**

**4a.** 在 `_add_shop_nodes()` 方法之后添加新静态方法：
```gdscript
static func _add_event_nodes(all_nodes: Array[NodeData]) -> void:
	var count: int = 0
	for nd: NodeData in all_nodes:
		if nd.config.column >= 1 and nd.config.column <= 3 \
				and nd.config.type == NodeConfig.Type.COMBAT \
				and count < 3:
			if randf() < 0.2:
				nd.config.type = NodeConfig.Type.EVENT
				nd.config.enemy_group = null
				count += 1
```

**4b.** 在 `generate()` 方法末尾、`return all` 之前，在 `_add_shop_nodes(all)` 调用之后追加：
```gdscript
	_add_event_nodes(all)
	return all
```

（原来是 `_add_shop_nodes(all)` 然后 `return all`，现在变成 `_add_shop_nodes(all)` → `_add_event_nodes(all)` → `return all`。）

- [ ] **Step 5: 检查 MapScreen.gd 是否需要处理 EVENT 节点类型**

打开 `E:\MyWork\AIGameTest2\map\MapScreen.gd`，搜索 `NodeConfig.Type`。如果有 match/if 语句处理节点类型但**没有**默认分支，则添加 EVENT 分支（显示文字「机缘」或默认颜色）。如果有默认分支或不限类型，则无需修改。

- [ ] **Step 6: 验证**

启动游戏，开始新局，进入地图界面。确认地图中出现带「机缘」标记的节点（2–3 个）。点击机缘节点后，游戏跳转到空白的 EventScreen（Task 4 之前可能报错找不到场景文件，这是正常的——Task 4 会创建场景）。

- [ ] **Step 7: Commit**

```bash
git add NodeConfig.gd MapGenerator.gd GameManager.gd PlayerState.gd
git commit -m "feat: add EVENT node type to map routing and generation"
```

---

## Task 4: EventScreen UI 场景 + 脚本

**Files:**
- Create: `event/EventScreen.tscn`
- Create: `event/EventScreen.gd`

### 场景结构说明

```
EventScreen (Control, 全屏锚点)
├── VBoxContainer (居中, 400×580)
│   ├── MoodLabel (Label, 居中)
│   ├── EventNameLabel (Label, 居中)
│   ├── FlavorLabel (Label, 自动换行)
│   ├── ChoicesContainer (VBoxContainer)
│   └── ResultView (VBoxContainer, 默认隐藏)
│       ├── ResultLabel (Label, 自动换行)
│       ├── EffectSummaryLabel (Label, 居中)
│       └── ConfirmButton (Button, "确认")
└── CardPickPanel (PanelContainer, 全屏锚点, 默认隐藏)
    └── CenterContainer
        └── InnerBox (VBoxContainer, min_size 360×400)
            ├── PromptLabel (Label)
            ├── CardScrollContainer (ScrollContainer, min_height 200)
            │   └── CardGrid (GridContainer, columns=1)
            └── CardPickConfirmButton (Button, "确认", disabled=true)
```

- [ ] **Step 1: 创建 event/EventScreen.tscn**

```
[gd_scene load_steps=2 format=3 uid="uid://event_screen"]

[ext_resource type="Script" path="res://event/EventScreen.gd" id="1_script"]

[node name="EventScreen" type="Control"]
layout_mode = 3
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
script = ExtResource("1_script")

[node name="VBoxContainer" type="VBoxContainer" parent="."]
layout_mode = 1
anchors_preset = 8
anchor_left = 0.5
anchor_top = 0.5
anchor_right = 0.5
anchor_bottom = 0.5
offset_left = -200.0
offset_top = -290.0
offset_right = 200.0
offset_bottom = 290.0
theme_override_constants/separation = 10

[node name="MoodLabel" type="Label" parent="VBoxContainer"]
layout_mode = 2
horizontal_alignment = 1

[node name="EventNameLabel" type="Label" parent="VBoxContainer"]
layout_mode = 2
horizontal_alignment = 1

[node name="FlavorLabel" type="Label" parent="VBoxContainer"]
layout_mode = 2
autowrap_mode = 3

[node name="ChoicesContainer" type="VBoxContainer" parent="VBoxContainer"]
layout_mode = 2
theme_override_constants/separation = 8

[node name="ResultView" type="VBoxContainer" parent="VBoxContainer"]
layout_mode = 2
visible = false
theme_override_constants/separation = 8

[node name="ResultLabel" type="Label" parent="VBoxContainer/ResultView"]
layout_mode = 2
autowrap_mode = 3

[node name="EffectSummaryLabel" type="Label" parent="VBoxContainer/ResultView"]
layout_mode = 2
horizontal_alignment = 1

[node name="ConfirmButton" type="Button" parent="VBoxContainer/ResultView"]
layout_mode = 2
text = "确认"

[node name="CardPickPanel" type="PanelContainer" parent="."]
layout_mode = 3
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
visible = false

[node name="CenterContainer" type="CenterContainer" parent="CardPickPanel"]
layout_mode = 2

[node name="InnerBox" type="VBoxContainer" parent="CardPickPanel/CenterContainer"]
layout_mode = 2
custom_minimum_size = Vector2(360, 400)
theme_override_constants/separation = 10

[node name="PromptLabel" type="Label" parent="CardPickPanel/CenterContainer/InnerBox"]
layout_mode = 2
text = "请选择一张牌："

[node name="CardScrollContainer" type="ScrollContainer" parent="CardPickPanel/CenterContainer/InnerBox"]
layout_mode = 2
custom_minimum_size = Vector2(0, 200)

[node name="CardGrid" type="GridContainer" parent="CardPickPanel/CenterContainer/InnerBox/CardScrollContainer"]
layout_mode = 2
columns = 1

[node name="CardPickConfirmButton" type="Button" parent="CardPickPanel/CenterContainer/InnerBox"]
layout_mode = 2
text = "确认"
disabled = true
```

- [ ] **Step 2: 创建 event/EventScreen.gd**

```gdscript
# E:\MyWork\AIGameTest2\event\EventScreen.gd
extends Control

@onready var _mood_label: Label             = $VBoxContainer/MoodLabel
@onready var _name_label: Label             = $VBoxContainer/EventNameLabel
@onready var _flavor_label: Label           = $VBoxContainer/FlavorLabel
@onready var _choices_container: VBoxContainer = $VBoxContainer/ChoicesContainer
@onready var _result_view: VBoxContainer    = $VBoxContainer/ResultView
@onready var _result_label: Label           = $VBoxContainer/ResultView/ResultLabel
@onready var _effect_summary: Label         = $VBoxContainer/ResultView/EffectSummaryLabel
@onready var _confirm_btn: Button           = $VBoxContainer/ResultView/ConfirmButton
@onready var _card_pick_panel: PanelContainer = $CardPickPanel
@onready var _prompt_label: Label           = $CardPickPanel/CenterContainer/InnerBox/PromptLabel
@onready var _card_grid: GridContainer      = $CardPickPanel/CenterContainer/InnerBox/CardScrollContainer/CardGrid
@onready var _card_pick_confirm: Button     = $CardPickPanel/CenterContainer/InnerBox/CardPickConfirmButton

var _pending_choice: EventChoiceData = null
var _pending_interactive: Array[EventEffectData] = []
var _selected_card: CardData = null

func _ready() -> void:
	var event: EventData = EventPool.pick_event(GameManager.player_state)
	_populate_event(event)
	_confirm_btn.pressed.connect(_on_confirm)
	_card_pick_confirm.pressed.connect(_on_card_pick_confirm)

func _populate_event(event: EventData) -> void:
	_mood_label.text = "[%s]" % event.mood
	_name_label.text = event.display_name
	_flavor_label.text = event.flavor_text
	for choice: EventChoiceData in event.choices:
		var btn := Button.new()
		var available: bool = EventEngine.check_choice_available(choice, GameManager.player_state)
		btn.disabled = not available
		if choice.cost_gold > 0:
			btn.text = "%s（%d 灵石）" % [choice.button_text, choice.cost_gold]
		else:
			btn.text = choice.button_text
		btn.pressed.connect(_on_choice_selected.bind(choice))
		_choices_container.add_child(btn)

func _on_choice_selected(choice: EventChoiceData) -> void:
	_pending_choice = choice
	_choices_container.hide()
	_result_label.text = choice.result_text
	_effect_summary.text = EventEngine.describe_effects(choice)
	_result_view.show()

func _on_confirm() -> void:
	if _pending_choice == null:
		return
	if _pending_choice.cost_gold > 0:
		GameManager.player_state.gold -= _pending_choice.cost_gold
	var interactive: Array[EventEffectData] = EventEngine.apply_immediate_effects(
		_pending_choice, GameManager.player_state
	)
	_pending_choice = null
	if interactive.is_empty():
		GameManager.go_to_map()
	else:
		_pending_interactive = interactive
		_show_card_pick(_pending_interactive[0])

func _show_card_pick(effect: EventEffectData) -> void:
	_result_view.hide()
	_card_pick_panel.show()
	_selected_card = null
	_card_pick_confirm.disabled = true
	match effect.effect_type:
		EventEffectData.EffectType.UPGRADE_CARD_CHOOSE:
			_prompt_label.text = "请选择一张牌悟道："
			var upgradeable: Array[CardData] = []
			for c: CardData in GameManager.player_state.deck:
				if not c.is_upgraded:
					upgradeable.append(c)
			_populate_card_list(upgradeable)
		EventEffectData.EffectType.REMOVE_CARD_CHOOSE:
			_prompt_label.text = "请选择一张牌移除："
			_populate_card_list(GameManager.player_state.deck)
		EventEffectData.EffectType.COPY_CARD_CHOOSE:
			_prompt_label.text = "请选择一张牌复制："
			_populate_card_list(GameManager.player_state.deck)

func _populate_card_list(cards: Array[CardData]) -> void:
	for child in _card_grid.get_children():
		child.queue_free()
	for card: CardData in cards:
		var btn := Button.new()
		btn.text = card.get_description()
		btn.pressed.connect(_on_card_selected.bind(card))
		_card_grid.add_child(btn)

func _on_card_selected(card: CardData) -> void:
	_selected_card = card
	_card_pick_confirm.disabled = false

func _on_card_pick_confirm() -> void:
	if _selected_card == null or _pending_interactive.is_empty():
		return
	EventEngine.apply_interactive_effect(
		_pending_interactive[0], _selected_card, GameManager.player_state
	)
	_pending_interactive.remove_at(0)
	_selected_card = null
	if _pending_interactive.is_empty():
		GameManager.go_to_map()
	else:
		_show_card_pick(_pending_interactive[0])
```

- [ ] **Step 3: 验证场景加载**

启动游戏，进入地图，点击 EVENT 节点。确认：
1. 事件界面正常显示（暂时会显示空事件或报错找不到 .tres，因为 Task 5-6 才创建数据文件）
2. 无脚本解析错误

如果因为 .tres 文件不存在而崩溃，可以先在 `EventPool.pick_event` 中临时添加保护：
```gdscript
if not ResourceLoader.exists(path):
    continue
```

- [ ] **Step 4: Commit**

```bash
git add event/EventScreen.tscn event/EventScreen.gd
git commit -m "feat: add EventScreen three-phase UI"
```

---

## Task 5: 事件数据文件（前 6 件）

**Files:** `data/events/` 目录下 6 个 .tres 文件

> 说明：所有事件文件遵循相同结构：ext_resource 引用三个脚本，sub_resource 定义效果和选项，[resource] 定义事件主体。每个文件内的 sub_resource id 必须唯一。

- [ ] **Step 1: 创建 data/events/ling_shi_kuang_mai.tres**

```
[gd_resource type="Resource" script_class="EventData" format=3 uid="uid://ev_ling_shi_kuang_mai"]

[ext_resource type="Script" path="res://EventData.gd" id="1_ED"]
[ext_resource type="Script" path="res://EventChoiceData.gd" id="2_ECD"]
[ext_resource type="Script" path="res://EventEffectData.gd" id="3_EFD"]

[sub_resource type="Resource" id="e1"]
script = ExtResource("3_EFD")
effect_type = 1
value = 50
probability = 1.0
card_grade = 0
relic_grade = 0
card_type_filter = ""

[sub_resource type="Resource" id="e2"]
script = ExtResource("3_EFD")
effect_type = 4
value = 8
probability = 1.0
card_grade = 0
relic_grade = 0
card_type_filter = ""

[sub_resource type="Resource" id="c1"]
script = ExtResource("2_ECD")
button_text = "深入开采"
result_text = "你往里走。光越来越亮。然后头顶一声裂响——你被埋在石头里，爬出来的时候手里攥满了灵石。还有血。"
effects = [SubResource("e1"), SubResource("e2")]
cost_gold = 0

[sub_resource type="Resource" id="e3"]
script = ExtResource("3_EFD")
effect_type = 1
value = 20
probability = 1.0
card_grade = 0
relic_grade = 0
card_type_filter = ""

[sub_resource type="Resource" id="c2"]
script = ExtResource("2_ECD")
button_text = "浅尝即止"
result_text = "在入口敲了几块。够用了。深处的蓝光还在闪。"
effects = [SubResource("e3")]
cost_gold = 0

[resource]
script = ExtResource("1_ED")
display_name = "灵石矿脉"
mood = "安静"
flavor_text = "山壁上有一道裂缝，里面透出淡蓝的光。有人在入口刻了一行字：「后辈修士，留一脉给后来人。」字迹潦草——写完就走，没有回头。"
choices = [SubResource("c1"), SubResource("c2")]
appear_condition = 0
appear_condition_value = 0
character_requirement = ""
```

- [ ] **Step 2: 创建 data/events/po_miao_jie_su.tres**

```
[gd_resource type="Resource" script_class="EventData" format=3 uid="uid://ev_po_miao_jie_su"]

[ext_resource type="Script" path="res://EventData.gd" id="1_ED"]
[ext_resource type="Script" path="res://EventChoiceData.gd" id="2_ECD"]
[ext_resource type="Script" path="res://EventEffectData.gd" id="3_EFD"]

[sub_resource type="Resource" id="e1"]
script = ExtResource("3_EFD")
effect_type = 9
value = 0
probability = 1.0
card_grade = 0
relic_grade = 0
card_type_filter = ""

[sub_resource type="Resource" id="c1"]
script = ExtResource("2_ECD")
button_text = "供奉香火"
result_text = "把灵石摆在站立的神像前。神像不动。风吹进来的时候，火堆的余烬亮了一下。地上多了一件东西。"
effects = [SubResource("e1")]
cost_gold = 30

[sub_resource type="Resource" id="c2"]
script = ExtResource("2_ECD")
button_text = "悄悄离开"
result_text = "没有供奉，没有打扰。你在门口坐了一会儿，然后继续上路了。"
effects = []
cost_gold = 0

[resource]
script = ExtResource("1_ED")
display_name = "破庙借宿"
mood = "安静"
flavor_text = "一座破庙。一尊神像倒了，另一尊还在站着——你不认得他是什么神。地上有干草铺和熄灭的篝火。匾只有半边：风……观。"
choices = [SubResource("c1"), SubResource("c2")]
appear_condition = 0
appear_condition_value = 0
character_requirement = ""
```

- [ ] **Step 3: 创建 data/events/qiu_xian_ku.tres**

```
[gd_resource type="Resource" script_class="EventData" format=3 uid="uid://ev_qiu_xian_ku"]

[ext_resource type="Script" path="res://EventData.gd" id="1_ED"]
[ext_resource type="Script" path="res://EventChoiceData.gd" id="2_ECD"]
[ext_resource type="Script" path="res://EventEffectData.gd" id="3_EFD"]

[sub_resource type="Resource" id="e1"]
script = ExtResource("3_EFD")
effect_type = 14
value = 0
probability = 1.0
card_grade = 0
relic_grade = 0
card_type_filter = "心魔"

[sub_resource type="Resource" id="e2"]
script = ExtResource("3_EFD")
effect_type = 4
value = 6
probability = 1.0
card_grade = 0
relic_grade = 0
card_type_filter = ""

[sub_resource type="Resource" id="c1"]
script = ExtResource("2_ECD")
button_text = "加固封印"
result_text = "你把自己的真气注入封印里，描了一遍符。门静了。你也轻了。只是多流了一点血。"
effects = [SubResource("e1"), SubResource("e2")]
cost_gold = 0

[sub_resource type="Resource" id="c2"]
script = ExtResource("2_ECD")
button_text = "绕道"
result_text = "你没碰符咒。你走了。不知道他为什么自愿。"
effects = []
cost_gold = 0

[resource]
script = ExtResource("1_ED")
display_name = "囚仙窟"
mood = "惊悚"
flavor_text = "石窟被旧符咒封着。洞里传出的气息不似妖气。封印石上刻着：「我自愿的。」笔迹很稳。"
choices = [SubResource("c1"), SubResource("c2")]
appear_condition = 0
appear_condition_value = 0
character_requirement = ""
```

- [ ] **Step 4: 创建 data/events/yu_chan.tres**

```
[gd_resource type="Resource" script_class="EventData" format=3 uid="uid://ev_yu_chan"]

[ext_resource type="Script" path="res://EventData.gd" id="1_ED"]
[ext_resource type="Script" path="res://EventChoiceData.gd" id="2_ECD"]
[ext_resource type="Script" path="res://EventEffectData.gd" id="3_EFD"]

[sub_resource type="Resource" id="e1"]
script = ExtResource("3_EFD")
effect_type = 9
value = 0
probability = 1.0
card_grade = 0
relic_grade = 1
card_type_filter = ""

[sub_resource type="Resource" id="e2"]
script = ExtResource("3_EFD")
effect_type = 12
value = 0
probability = 1.0
card_grade = 0
relic_grade = 0
card_type_filter = ""

[sub_resource type="Resource" id="c1"]
script = ExtResource("2_ECD")
button_text = "收入囊中"
result_text = "你摘下玉蝉。翼在你手心振了一下——不是风。是某个人还没放下的东西。"
effects = [SubResource("e1"), SubResource("e2")]
cost_gold = 0

[sub_resource type="Resource" id="c2"]
script = ExtResource("2_ECD")
button_text = "放它离去"
result_text = "玉蝉挂着。你走了。"
effects = []
cost_gold = 0

[resource]
script = ExtResource("1_ED")
display_name = "玉蝉"
mood = "神秘"
flavor_text = "树上挂着一只玉蝉。不是活的——是雕出来的，蝉翼薄到透光。蝉翼上有一道极细的裂痕。有人用指尖描过那道裂痕太多次，玉面都磨凹了。"
choices = [SubResource("c1"), SubResource("c2")]
appear_condition = 0
appear_condition_value = 0
character_requirement = ""
```

- [ ] **Step 5: 创建 data/events/du_shi.tres**

> 「赌石」选项A 包含两个独立概率效果：50%概率获得 80 灵石，50%概率失去 30 灵石，二者独立触发。

```
[gd_resource type="Resource" script_class="EventData" format=3 uid="uid://ev_du_shi"]

[ext_resource type="Script" path="res://EventData.gd" id="1_ED"]
[ext_resource type="Script" path="res://EventChoiceData.gd" id="2_ECD"]
[ext_resource type="Script" path="res://EventEffectData.gd" id="3_EFD"]

[sub_resource type="Resource" id="e1"]
script = ExtResource("3_EFD")
effect_type = 1
value = 80
probability = 0.5
card_grade = 0
relic_grade = 0
card_type_filter = ""

[sub_resource type="Resource" id="e2"]
script = ExtResource("3_EFD")
effect_type = 2
value = 30
probability = 0.5
card_grade = 0
relic_grade = 0
card_type_filter = ""

[sub_resource type="Resource" id="c1"]
script = ExtResource("2_ECD")
button_text = "赌一把"
result_text = "老头看着你——你指了一块。刀切下去，石头自己裂了。结果如何，只有打开了才知道。"
effects = [SubResource("e1"), SubResource("e2")]
cost_gold = 0

[sub_resource type="Resource" id="c2"]
script = ExtResource("2_ECD")
button_text = "不赌"
result_text = "老头看着你。打了个哈欠。"
effects = []
cost_gold = 0

[resource]
script = ExtResource("1_ED")
display_name = "赌石"
mood = "有趣"
flavor_text = "一堆石头堆在溪边。一块温的，一块在轻颤。一个老头在旁边蹲着，伸出两根手指。不说话。指上全是老茧。"
choices = [SubResource("c1"), SubResource("c2")]
appear_condition = 0
appear_condition_value = 0
character_requirement = ""
```

- [ ] **Step 6: 创建 data/events/wu_dao_shi.tres**

```
[gd_resource type="Resource" script_class="EventData" format=3 uid="uid://ev_wu_dao_shi"]

[ext_resource type="Script" path="res://EventData.gd" id="1_ED"]
[ext_resource type="Script" path="res://EventChoiceData.gd" id="2_ECD"]
[ext_resource type="Script" path="res://EventEffectData.gd" id="3_EFD"]

[sub_resource type="Resource" id="e1"]
script = ExtResource("3_EFD")
effect_type = 16
value = 0
probability = 1.0
card_grade = 0
relic_grade = 0
card_type_filter = ""

[sub_resource type="Resource" id="c1"]
script = ExtResource("2_ECD")
button_text = "触碰悟道石"
result_text = "坐下来。闭上眼。石头不教你什么——只是把那些残留递到你手边。"
effects = [SubResource("e1")]
cost_gold = 0

[sub_resource type="Resource" id="c2"]
script = ExtResource("2_ECD")
button_text = "离开"
result_text = "你看了一眼石头。走了。"
effects = []
cost_gold = 0

[resource]
script = ExtResource("1_ED")
display_name = "悟道石"
mood = "安静"
flavor_text = "一块大青石，表面被磨得光滑——不止一个人坐过这里。你手摸上去的时候，石头里还存着前人的灵。很微弱。"
choices = [SubResource("c1"), SubResource("c2")]
appear_condition = 0
appear_condition_value = 0
character_requirement = ""
```

- [ ] **Step 7: 验证前 6 件事件可加载**

启动游戏，进入地图，找到并点击 EVENT 节点，确认能看到事件界面并显示事件名称、色调、文案和选项按钮。选择一个选项，确认进入结果视图，点击确认后返回地图。

- [ ] **Step 8: Commit**

```bash
git add data/events/ling_shi_kuang_mai.tres data/events/po_miao_jie_su.tres
git add data/events/qiu_xian_ku.tres data/events/yu_chan.tres
git add data/events/du_shi.tres data/events/wu_dao_shi.tres
git commit -m "feat: add event data files 1-6 (ling_shi_kuang_mai through wu_dao_shi)"
```

---

## Task 6: 事件数据文件（后 6 件）

**Files:** `data/events/` 目录下另外 6 个 .tres 文件

- [ ] **Step 1: 创建 data/events/liang_ge_xiu_shi.tres**

```
[gd_resource type="Resource" script_class="EventData" format=3 uid="uid://ev_liang_ge_xiu_shi"]

[ext_resource type="Script" path="res://EventData.gd" id="1_ED"]
[ext_resource type="Script" path="res://EventChoiceData.gd" id="2_ECD"]
[ext_resource type="Script" path="res://EventEffectData.gd" id="3_EFD"]

[sub_resource type="Resource" id="e1"]
script = ExtResource("3_EFD")
effect_type = 1
value = 20
probability = 1.0
card_grade = 0
relic_grade = 0
card_type_filter = ""

[sub_resource type="Resource" id="e2"]
script = ExtResource("3_EFD")
effect_type = 3
value = 5
probability = 1.0
card_grade = 0
relic_grade = 0
card_type_filter = ""

[sub_resource type="Resource" id="c1"]
script = ExtResource("2_ECD")
button_text = "加入聊天"
result_text = "你坐下来听了一会儿。打完了——他们很满意，留了些灵石和一股说不清来源的轻松。"
effects = [SubResource("e1"), SubResource("e2")]
cost_gold = 0

[sub_resource type="Resource" id="c2"]
script = ExtResource("2_ECD")
button_text = "独自离开"
result_text = "你走了。身后的争论声越来越远。"
effects = []
cost_gold = 0

[resource]
script = ExtResource("1_ED")
display_name = "两个修士"
mood = "有趣"
flavor_text = "路边有两个散修在吵架。一个人说他的功法最强。另一个人说他的功法更最强。你要不要试试？他们同时转向你。"
choices = [SubResource("c1"), SubResource("c2")]
appear_condition = 0
appear_condition_value = 0
character_requirement = ""
```

- [ ] **Step 2: 创建 data/events/lao_xiu_shi_yan_dou.tres**

```
[gd_resource type="Resource" script_class="EventData" format=3 uid="uid://ev_lao_xiu_shi_yan_dou"]

[ext_resource type="Script" path="res://EventData.gd" id="1_ED"]
[ext_resource type="Script" path="res://EventChoiceData.gd" id="2_ECD"]
[ext_resource type="Script" path="res://EventEffectData.gd" id="3_EFD"]

[sub_resource type="Resource" id="e1"]
script = ExtResource("3_EFD")
effect_type = 6
value = 0
probability = 1.0
card_grade = 0
relic_grade = 0
card_type_filter = ""

[sub_resource type="Resource" id="e2"]
script = ExtResource("3_EFD")
effect_type = 12
value = 0
probability = 1.0
card_grade = 0
relic_grade = 0
card_type_filter = ""

[sub_resource type="Resource" id="c1"]
script = ExtResource("2_ECD")
button_text = "接下"
result_text = "拿起烟斗。深吸一口。什么都没有——没有烟，没有火。只是暖。你递回去。他笑了。\"走吧。\"烟斗灭了。"
effects = [SubResource("e1"), SubResource("e2")]
cost_gold = 0

[sub_resource type="Resource" id="c2"]
script = ExtResource("2_ECD")
button_text = "婉拒"
result_text = "你摇了摇头。他也不坚持——只是把烟斗搁在腿上。\"走吧，小辈。路还长。\""
effects = []
cost_gold = 0

[resource]
script = ExtResource("1_ED")
display_name = "老修士的烟斗"
mood = "感动"
flavor_text = "一块石头上坐着个老头。旁边放着一根灭掉的烟斗。他看你走近，笑了。然后把烟斗递给你。烟斗是暖的。"
choices = [SubResource("c1"), SubResource("c2")]
appear_condition = 0
appear_condition_value = 0
character_requirement = ""
```

- [ ] **Step 3: 创建 data/events/ku_jing.tres**

```
[gd_resource type="Resource" script_class="EventData" format=3 uid="uid://ev_ku_jing"]

[ext_resource type="Script" path="res://EventData.gd" id="1_ED"]
[ext_resource type="Script" path="res://EventChoiceData.gd" id="2_ECD"]
[ext_resource type="Script" path="res://EventEffectData.gd" id="3_EFD"]

[sub_resource type="Resource" id="e1"]
script = ExtResource("3_EFD")
effect_type = 7
value = 40
probability = 1.0
card_grade = 0
relic_grade = 0
card_type_filter = ""

[sub_resource type="Resource" id="e2"]
script = ExtResource("3_EFD")
effect_type = 14
value = 0
probability = 1.0
card_grade = 0
relic_grade = 0
card_type_filter = "心魔"

[sub_resource type="Resource" id="c1"]
script = ExtResource("2_ECD")
button_text = "跳入枯井"
result_text = "你抓着井绳往下放。到了底——那道光不见了。黑暗里有什么东西触碰了你的心魔，把它们带走了一些。爬出来的时候血渗进了手掌。"
effects = [SubResource("e1"), SubResource("e2")]
cost_gold = 0

[sub_resource type="Resource" id="c2"]
script = ExtResource("2_ECD")
button_text = "离开"
result_text = "你往下看了一眼。走了。那道光还在井底闪着。"
effects = []
cost_gold = 0

[resource]
script = ExtResource("1_ED")
display_name = "枯井"
mood = "惊悚"
flavor_text = "路边一口井。井沿上长满青苔——没有人用过很久了。但井里是亮的。有光。那光不是水反射的，是井底自己发的。你往下看的时候，那光闪了一下。像是看到了你。"
choices = [SubResource("c1"), SubResource("c2")]
appear_condition = 0
appear_condition_value = 0
character_requirement = ""
```

- [ ] **Step 4: 创建 data/events/si_ceng_xiang_shi.tres**

```
[gd_resource type="Resource" script_class="EventData" format=3 uid="uid://ev_si_ceng_xiang_shi"]

[ext_resource type="Script" path="res://EventData.gd" id="1_ED"]
[ext_resource type="Script" path="res://EventChoiceData.gd" id="2_ECD"]
[ext_resource type="Script" path="res://EventEffectData.gd" id="3_EFD"]

[sub_resource type="Resource" id="e1"]
script = ExtResource("3_EFD")
effect_type = 19
value = 1
probability = 1.0
card_grade = 0
relic_grade = 0
card_type_filter = ""

[sub_resource type="Resource" id="e2"]
script = ExtResource("3_EFD")
effect_type = 5
value = 10
probability = 1.0
card_grade = 0
relic_grade = 0
card_type_filter = ""

[sub_resource type="Resource" id="c1"]
script = ExtResource("2_ECD")
button_text = "停下来"
result_text = "蹲下去摸那块石头。温度和掌心一样。没有记忆，但你的手有。坐了很久。站起来。"
effects = [SubResource("e1"), SubResource("e2")]
cost_gold = 0

[sub_resource type="Resource" id="c2"]
script = ExtResource("2_ECD")
button_text = "继续赶路"
result_text = "你知道这不是幻觉。但今天不想停。"
effects = []
cost_gold = 0

[resource]
script = ExtResource("1_ED")
display_name = "似曾相识"
mood = "感动"
flavor_text = "一段毫无特征的山道。你踩在一块石头上——缺了一个角。那个角的形状你认识。不是这一世。"
choices = [SubResource("c1"), SubResource("c2")]
appear_condition = 0
appear_condition_value = 0
character_requirement = ""
```

- [ ] **Step 5: 创建 data/events/ya_bi_ke_zi.tres**

```
[gd_resource type="Resource" script_class="EventData" format=3 uid="uid://ev_ya_bi_ke_zi"]

[ext_resource type="Script" path="res://EventData.gd" id="1_ED"]
[ext_resource type="Script" path="res://EventChoiceData.gd" id="2_ECD"]
[ext_resource type="Script" path="res://EventEffectData.gd" id="3_EFD"]

[sub_resource type="Resource" id="e1"]
script = ExtResource("3_EFD")
effect_type = 19
value = 1
probability = 1.0
card_grade = 0
relic_grade = 0
card_type_filter = ""

[sub_resource type="Resource" id="e2"]
script = ExtResource("3_EFD")
effect_type = 9
value = 0
probability = 1.0
card_grade = 0
relic_grade = 0
card_type_filter = ""

[sub_resource type="Resource" id="c1"]
script = ExtResource("2_ECD")
button_text = "临摹刻字"
result_text = "你把那些名字临摹在手心里。字消进皮肤，你感到有什么东西在轻轻触碰你。然后，石缝里掉落下一件旧遗物。"
effects = [SubResource("e1"), SubResource("e2")]
cost_gold = 0

[sub_resource type="Resource" id="c2"]
script = ExtResource("2_ECD")
button_text = "继续前行"
result_text = "光滑的崖壁上刻满了名字。你看了很久。然后走了。"
effects = []
cost_gold = 0

[resource]
script = ExtResource("1_ED")
display_name = "崖壁刻字"
mood = "神秘"
flavor_text = "光滑的崖壁上刻满了名字。不是功法。有一个名字比你早了几千年。笔迹很稳——是手指写的，不是刀子。"
choices = [SubResource("c1"), SubResource("c2")]
appear_condition = 0
appear_condition_value = 0
character_requirement = ""
```

- [ ] **Step 6: 创建 data/events/jian_hen.tres**

> 「剑痕」是剑修专属事件（character_requirement = "sword"），UPGRADE_CARD_CHOOSE 为交互效果。

```
[gd_resource type="Resource" script_class="EventData" format=3 uid="uid://ev_jian_hen"]

[ext_resource type="Script" path="res://EventData.gd" id="1_ED"]
[ext_resource type="Script" path="res://EventChoiceData.gd" id="2_ECD"]
[ext_resource type="Script" path="res://EventEffectData.gd" id="3_EFD"]

[sub_resource type="Resource" id="e1"]
script = ExtResource("3_EFD")
effect_type = 16
value = 0
probability = 1.0
card_grade = 0
relic_grade = 0
card_type_filter = ""

[sub_resource type="Resource" id="c1"]
script = ExtResource("2_ECD")
button_text = "用手指描摹剑痕"
result_text = "你伸出手，指尖顺着那道剑痕走了一遍。剑式不是你的——但那股劲你认得。你闭上眼，把它记下来了。"
effects = [SubResource("e1")]
cost_gold = 0

[sub_resource type="Resource" id="c2"]
script = ExtResource("2_ECD")
button_text = "离开"
result_text = "那道剑不是你的。你走了。"
effects = []
cost_gold = 0

[resource]
script = ExtResource("1_ED")
display_name = "剑痕"
mood = "神秘"
flavor_text = "山壁上有一道很新的剑痕——是今天劈的。这不是你留下的。你拔剑对着那道痕比了一下：角度不同，力道也不一样。不是你。是一个和你用不同握法的人。剑痕旁边有一小块烤焦的红薯皮。红薯皮还是热的。"
choices = [SubResource("c1"), SubResource("c2")]
appear_condition = 0
appear_condition_value = 0
character_requirement = "sword"
```

- [ ] **Step 7: 全量验证**

在游戏中进行以下测试（每次开始新局进行多次验证）：

1. 进入地图，确认 EVENT 节点出现（应该有 2–3 个）
2. 点击 EVENT 节点，确认事件正常显示
3. 选择「供奉香火」（破庙借宿）：确认灵石足够时可选、不足时置灰
4. 选择「触碰悟道石」或「剑痕-描摹剑痕」：确认显示卡牌选择面板，选牌后确认卡牌升级
5. 选择「赌石-赌一把」：多次测试确认灵石数值有随机变化
6. 选择「似曾相识-停下来」：确认 `player_state.reincarnation_fragments` 增加（可在 Godot debugger 查看）
7. 确认所有事件的「离开/不赌/绕道」选项点击后直接返回地图，无报错

- [ ] **Step 8: Commit**

```bash
git add data/events/liang_ge_xiu_shi.tres data/events/lao_xiu_shi_yan_dou.tres
git add data/events/ku_jing.tres data/events/si_ceng_xiang_shi.tres
git add data/events/ya_bi_ke_zi.tres data/events/jian_hen.tres
git commit -m "feat: add event data files 7-12 (liang_ge_xiu_shi through jian_hen)"
```

---

## Task 7: RestScreen「悟道」改名 + 版本号 + 开发日志

**Files:**
- Modify: `rest/RestScreen.tscn`
- Modify: `rest/RestScreen.gd`
- Modify: `menu/MainMenu.tscn`
- Create: `E:\MyWork\Devlog\AIGameTest2_v0.69.0_2026-05-04.md`

- [ ] **Step 1: 修改 rest/RestScreen.tscn**

将以下 3 处文案替换：

| 原文 | 改为 |
|------|------|
| `text = "升级卡牌"` | `text = "悟道"` |
| `text = "选择一张牌升级："` | `text = "选择一张牌悟道："` |
| `text = "确认升级"` | `text = "确认悟道"` |

- [ ] **Step 2: 修改 rest/RestScreen.gd — 不需要逻辑改动**

RestScreen.gd 无需修改。「升级」→「悟道」仅为 UI 文案，底层调用 `card.upgrade()` 不变。

可选：如果 RestScreen.gd 中有硬编码文字（搜索"升级"），将其改为"悟道"。当前版本的 RestScreen.gd 没有硬编码升级文本（所有文本在 .tscn 中），所以跳过此步骤。

- [ ] **Step 3: 修改 menu/MainMenu.tscn — 版本号**

搜索 `v0.68.0`，改为 `v0.69.0`。（通常在一个 Label 节点的 text 属性中）

- [ ] **Step 4: 创建开发日志**

创建 `E:\MyWork\Devlog\AIGameTest2_v0.69.0_2026-05-04.md`，内容参考以下结构（按项目惯例填写）：

```markdown
# AIGameTest2 v0.69.0 开发日志

**日期：** 2026-05-04
**版本：** v0.69.0

## 本版本内容

### 新增：机缘事件系统

- 新增 EVENT 节点类型，地图每局随机生成 2-3 个机缘节点
- 实装数据层三个 Resource 类：EventData / EventChoiceData / EventEffectData
- 实装逻辑层：EventPool（路径注册表）+ EventEngine（静态效果执行器，23 种 EffectType）
- 实装 UI 层：EventScreen 三阶段流程（主界面 → 结果视图 → 卡牌选择面板）
- 实装 12 件事件验证系统流程：
  - 交易类：灵石矿脉、破庙借宿、囚仙窟
  - 冒险类：玉蝉、赌石
  - 变化类：悟道石、两个修士
  - 献祭类：老修士的烟斗、枯井
  - 轮回碎片：似曾相识、崖壁刻字
  - 交错类（剑修专属）：剑痕

### 改动：RestScreen 文案

- 休息站「升级卡牌」→「悟道」（仅 UI 文案，逻辑不变）

## 技术说明

- UPGRADE_CARD_CHOOSE 为交互型效果，在 EventEngine.apply_immediate_effects 中延迟，由 EventScreen.CardPickPanel 处理
- GAIN_RELIC 目前从 CardPool.RELICS 随机选取，暂不区分法器/灵器/仙器（后续版本添加 grade 字段后完善）
- reincarnation_fragments 本版本只做计数，无实际效果
- 交错类其余事件（红薯皮/岩壁手印等）数据文件未加入 EventPool.EVENTS，后续版本添加对应角色后启用
```

- [ ] **Step 5: 最终验证**

启动游戏，确认：
1. 主界面版本号显示 v0.69.0
2. 休息站按钮文案显示「悟道」
3. 随机事件完整流程可跑通（至少验证普通选项 + 交互选项各一次）

- [ ] **Step 6: Commit**

```bash
git add rest/RestScreen.tscn menu/MainMenu.tscn
git add E:/MyWork/Devlog/AIGameTest2_v0.69.0_2026-05-04.md
git commit -m "feat: v0.69.0 — event system complete, RestScreen rename, version bump"
```

---

## 自检清单

**Spec coverage check:**
- [x] EventData / EventChoiceData / EventEffectData — Task 1
- [x] EventPool.pick_event — Task 2
- [x] EventEngine 五个方法 — Task 2
- [x] NodeConfig.Type.EVENT — Task 3
- [x] MapGenerator EVENT 生成（2-3 个/局）— Task 3
- [x] GameManager.go_to_event() + Phase.EVENT + select_node 分支 — Task 3
- [x] PlayerState.reincarnation_fragments — Task 3
- [x] EventScreen 三阶段 UI — Task 4
- [x] 12 件事件 .tres 文件 — Task 5-6
- [x] RestScreen「升级」→「悟道」— Task 7
- [x] 版本号 v0.69.0 — Task 7

**注意事项：**
- EventPool.EVENTS 路径与 Task 5-6 创建的文件路径必须完全对应
- EventScreen.gd 中所有 `$Path` 引用必须与 EventScreen.tscn 节点路径一致
- 玉蝉的 GAIN_RELIC 使用 `relic_grade = 1`，但 EventEngine 当前简化为从所有遗物随机选（因为 RelicData 无 grade 字段）。这是已知简化，在开发日志中说明
