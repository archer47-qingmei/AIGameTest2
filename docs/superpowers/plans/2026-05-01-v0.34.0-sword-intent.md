# v0.34.0 剑意机制 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现剑修核心机制——剑意叠加与消耗；替换剑修起始牌组为 GDD 定义的 13 张；养剑葫遗物在战斗开始真实生效；战斗界面显示剑意计数器。

**Architecture:** 剑意状态（sword_intent / sword_intent_cap / sword_intent_damage_bonus）存放在 Combatant；EffectResolver.resolve() 在伤害计算和 resolve 末尾处理招式加成、终结技消耗；CombatEngine.end_turn() 执行回合结束折半；养剑术通过 CombatEngine._apply_engine_effects() 的新分支持久修改 cap 和 bonus。

**Tech Stack:** Godot 4 GDScript, Resource (.tres), CardEffectData nested sub_resource

---

## File Map

| 文件 | 操作 | 说明 |
|------|------|------|
| `CardData.gd` | 修改 | 新增 card_type / is_finisher / sword_intent_consume_bonus 字段；更新 get_description() |
| `Combatant.gd` | 修改 | 新增 sword_intent / sword_intent_cap / sword_intent_damage_bonus 字段 |
| `data/cards/zhan_tie.tres` | 新建 | 斩铁，招式，1费，打6 |
| `data/cards/po_feng.tres` | 新建 | 破风，招式，2费，打10 |
| `data/cards/ce_bu.tres` | 新建 | 侧步，身法，1费，挡5 |
| `data/cards/lian_xi.tres` | 新建 | 敛息，身法，0费，挡3 |
| `data/cards/chuan_xin.tres` | 新建 | 穿心，招式·终结技，2费，打8，每层+3 |
| `data/cards/yang_jian_shu.tres` | 新建 | 养剑术，功法，0费，剑意上限+5/加成+1 |
| `EffectResolver.gd` | 修改 | resolve() 添加剑意伤害加成和回合末剑意管理 |
| `CombatEngine.gd` | 修改 | end_turn() 剑意折半；_apply_engine_effects() 新增剑意效果 |
| `RelicEngine.gd` | 修改 | SWORD_INTENT 分支改为真实效果 |
| `GameManager.gd` | 修改 | start_new_run() 换用剑修 13 张起始牌组 |
| `combat/CombatScreen.tscn` | 修改 | PlayerPanel 新增 LblSwordIntent 节点 |
| `combat/CombatScreen.gd` | 修改 | @onready + _refresh_ui() 显示剑意 |
| `menu/MainMenu.tscn` | 修改 | 版本号改为 v0.34.0 |

---

### Task 1: CardData.gd + Combatant.gd 新增剑意字段

**Files:**
- Modify: `E:\MyWork\AIGameTest2\CardData.gd`
- Modify: `E:\MyWork\AIGameTest2\Combatant.gd`

- [ ] **Step 1: 修改 CardData.gd — 新增三个导出字段**

在 `@export var target_type: String = "single"` 之后追加：

```gdscript
@export var card_type: String = ""
@export var is_finisher: bool = false
@export var sword_intent_consume_bonus: int = 0
```

- [ ] **Step 2: 修改 CardData.gd — 更新 get_description()**

将 `get_description()` 完整替换为：

```gdscript
func get_description() -> String:
	if is_venom:
		return "%s\n费用:%d  %s" % [card_name, cost, special_text]
	var dmg: int = 0
	var blk: int = 0
	var drw: int = 0
	var nrg: int = 0
	var wk: int = 0
	var vul: int = 0
	var si_cap: int = 0
	var si_bonus: int = 0
	for effect: CardEffectData in effects:
		if effect.type == "damage":               dmg = effect.value
		elif effect.type == "block":              blk = effect.value
		elif effect.type == "draw":               drw = effect.value
		elif effect.type == "energy":             nrg = effect.value
		elif effect.type == "weak":               wk = effect.value
		elif effect.type == "vulnerable":         vul = effect.value
		elif effect.type == "sword_intent_cap":   si_cap = effect.value
		elif effect.type == "sword_intent_bonus": si_bonus = effect.value
	var desc: String = "%s\n费用:%d" % [card_name, cost]
	if dmg > 0:
		desc += "  攻:%d" % dmg
		if sword_intent_consume_bonus > 0:
			desc += "(+剑意×%d)" % sword_intent_consume_bonus
	if blk > 0:  desc += "  挡:%d" % blk
	if drw > 0:  desc += "  抽:%d" % drw
	if nrg > 0:  desc += "  能:%d" % nrg
	if wk > 0:   desc += "  虚弱:%d" % wk
	if vul > 0:  desc += "  脆弱:%d" % vul
	if si_cap > 0:   desc += "  剑意上限+%d" % si_cap
	if si_bonus > 0: desc += "  加成+%d" % si_bonus
	return desc
```

- [ ] **Step 3: 修改 Combatant.gd — 在 vulnerable 之后新增三个字段**

在 `var vulnerable: int = 0` 之后追加：

```gdscript
var sword_intent: int = 0
var sword_intent_cap: int = 10
var sword_intent_damage_bonus: int = 1
```

- [ ] **Step 4: 验证 Combatant.gd 完整 var 区段**

```gdscript
var display_name: String
var hp: int
var max_hp: int
var block: int
var weak: int = 0
var vulnerable: int = 0
var sword_intent: int = 0
var sword_intent_cap: int = 10
var sword_intent_damage_bonus: int = 1
```

- [ ] **Step 5: 提交**

```bash
git add CardData.gd Combatant.gd
git commit -m "feat: add sword intent fields to CardData and Combatant"
```

---

### Task 2: 6 张剑修起始卡牌数据文件

**Files:**
- Create: `E:\MyWork\AIGameTest2\data\cards\zhan_tie.tres`
- Create: `E:\MyWork\AIGameTest2\data\cards\po_feng.tres`
- Create: `E:\MyWork\AIGameTest2\data\cards\ce_bu.tres`
- Create: `E:\MyWork\AIGameTest2\data\cards\lian_xi.tres`
- Create: `E:\MyWork\AIGameTest2\data\cards\chuan_xin.tres`
- Create: `E:\MyWork\AIGameTest2\data\cards\yang_jian_shu.tres`

> 格式参考：项目中所有 .tres 文件头部不含 uid，Godot 在导入时自动生成。CardData.gd 的 Script uid 固定为 `uid://13mprie5g221`，CardEffectData.gd 为 `uid://cxv8nrm4tp7dk`。

- [ ] **Step 1: 创建 data/cards/zhan_tie.tres（斩铁）**

```
[gd_resource type="Resource" script_class="CardData" load_steps=4 format=3]

[ext_resource type="Script" uid="uid://13mprie5g221" path="res://CardData.gd" id="1_CardData"]
[ext_resource type="Script" uid="uid://cxv8nrm4tp7dk" path="res://CardEffectData.gd" id="2_CardEffectData"]

[sub_resource type="Resource" id="Effect_damage"]
script = ExtResource("2_CardEffectData")
type = "damage"
value = 6
upgrade_bonus = 0

[resource]
script = ExtResource("1_CardData")
card_name = "斩铁"
cost = 1
price = 0
effects = Array[ExtResource("2_CardEffectData")]([SubResource("Effect_damage")])
card_type = "招式"
```

- [ ] **Step 2: 创建 data/cards/po_feng.tres（破风）**

```
[gd_resource type="Resource" script_class="CardData" load_steps=4 format=3]

[ext_resource type="Script" uid="uid://13mprie5g221" path="res://CardData.gd" id="1_CardData"]
[ext_resource type="Script" uid="uid://cxv8nrm4tp7dk" path="res://CardEffectData.gd" id="2_CardEffectData"]

[sub_resource type="Resource" id="Effect_damage"]
script = ExtResource("2_CardEffectData")
type = "damage"
value = 10
upgrade_bonus = 0

[resource]
script = ExtResource("1_CardData")
card_name = "破风"
cost = 2
price = 0
effects = Array[ExtResource("2_CardEffectData")]([SubResource("Effect_damage")])
card_type = "招式"
```

- [ ] **Step 3: 创建 data/cards/ce_bu.tres（侧步）**

```
[gd_resource type="Resource" script_class="CardData" load_steps=4 format=3]

[ext_resource type="Script" uid="uid://13mprie5g221" path="res://CardData.gd" id="1_CardData"]
[ext_resource type="Script" uid="uid://cxv8nrm4tp7dk" path="res://CardEffectData.gd" id="2_CardEffectData"]

[sub_resource type="Resource" id="Effect_block"]
script = ExtResource("2_CardEffectData")
type = "block"
value = 5
upgrade_bonus = 0

[resource]
script = ExtResource("1_CardData")
card_name = "侧步"
cost = 1
price = 0
effects = Array[ExtResource("2_CardEffectData")]([SubResource("Effect_block")])
target_type = "none"
card_type = "身法"
```

- [ ] **Step 4: 创建 data/cards/lian_xi.tres（敛息）**

```
[gd_resource type="Resource" script_class="CardData" load_steps=4 format=3]

[ext_resource type="Script" uid="uid://13mprie5g221" path="res://CardData.gd" id="1_CardData"]
[ext_resource type="Script" uid="uid://cxv8nrm4tp7dk" path="res://CardEffectData.gd" id="2_CardEffectData"]

[sub_resource type="Resource" id="Effect_block"]
script = ExtResource("2_CardEffectData")
type = "block"
value = 3
upgrade_bonus = 0

[resource]
script = ExtResource("1_CardData")
card_name = "敛息"
cost = 0
price = 0
effects = Array[ExtResource("2_CardEffectData")]([SubResource("Effect_block")])
target_type = "none"
card_type = "身法"
```

- [ ] **Step 5: 创建 data/cards/chuan_xin.tres（穿心）**

```
[gd_resource type="Resource" script_class="CardData" load_steps=4 format=3]

[ext_resource type="Script" uid="uid://13mprie5g221" path="res://CardData.gd" id="1_CardData"]
[ext_resource type="Script" uid="uid://cxv8nrm4tp7dk" path="res://CardEffectData.gd" id="2_CardEffectData"]

[sub_resource type="Resource" id="Effect_damage"]
script = ExtResource("2_CardEffectData")
type = "damage"
value = 8
upgrade_bonus = 0

[resource]
script = ExtResource("1_CardData")
card_name = "穿心"
cost = 2
price = 0
effects = Array[ExtResource("2_CardEffectData")]([SubResource("Effect_damage")])
card_type = "招式"
is_finisher = true
sword_intent_consume_bonus = 3
```

- [ ] **Step 6: 创建 data/cards/yang_jian_shu.tres（养剑术）**

```
[gd_resource type="Resource" script_class="CardData" load_steps=5 format=3]

[ext_resource type="Script" uid="uid://13mprie5g221" path="res://CardData.gd" id="1_CardData"]
[ext_resource type="Script" uid="uid://cxv8nrm4tp7dk" path="res://CardEffectData.gd" id="2_CardEffectData"]

[sub_resource type="Resource" id="Effect_cap"]
script = ExtResource("2_CardEffectData")
type = "sword_intent_cap"
value = 5
upgrade_bonus = 0

[sub_resource type="Resource" id="Effect_bonus"]
script = ExtResource("2_CardEffectData")
type = "sword_intent_bonus"
value = 1
upgrade_bonus = 0

[resource]
script = ExtResource("1_CardData")
card_name = "养剑术"
cost = 0
price = 0
effects = Array[ExtResource("2_CardEffectData")]([SubResource("Effect_cap"), SubResource("Effect_bonus")])
target_type = "none"
card_type = "功法"
```

- [ ] **Step 7: 提交**

```bash
git add data/cards/zhan_tie.tres data/cards/po_feng.tres data/cards/ce_bu.tres data/cards/lian_xi.tres data/cards/chuan_xin.tres data/cards/yang_jian_shu.tres
git commit -m "feat: add sword cultivator starter card data files"
```

---

### Task 3: EffectResolver.gd 剑意效果逻辑

**Files:**
- Modify: `E:\MyWork\AIGameTest2\EffectResolver.gd`

- [ ] **Step 1: 将 EffectResolver.gd 的 resolve() 完整替换为以下内容**

```gdscript
static func resolve(card: CardData, attacker: Combatant, defender: Combatant) -> void:
	for effect: CardEffectData in card.effects:
		if effect.type == "damage":
			if defender != null:
				var bonus: int = 0
				if card.is_finisher:
					bonus = attacker.sword_intent * card.sword_intent_consume_bonus
				elif card.card_type == "招式":
					bonus = attacker.sword_intent * attacker.sword_intent_damage_bonus
				apply_damage(attacker, defender, effect.value + bonus)
		elif effect.type == "block":
			attacker.add_block(effect.value)
		elif effect.type == "weak":
			if defender != null:
				defender.add_weak(effect.value)
		elif effect.type == "vulnerable":
			if defender != null:
				defender.add_vulnerable(effect.value)
	if card.is_finisher:
		attacker.sword_intent = 0
	elif card.card_type == "招式":
		attacker.sword_intent = mini(attacker.sword_intent + 1, attacker.sword_intent_cap)
```

`apply_damage()` 静态方法不变，仅 resolve() 变化。

- [ ] **Step 2: 验证 apply_damage() 静态方法未被改动**

确认文件末尾 `apply_damage()` 内容与修改前一致：

```gdscript
static func apply_damage(source: Combatant, target: Combatant, amount: int) -> void:
	var dmg: int = amount
	if source.weak > 0:
		dmg = int(dmg * 0.75)
	if target.vulnerable > 0:
		dmg = int(dmg * 1.5)
	target.take_damage(dmg)
```

- [ ] **Step 3: 提交**

```bash
git add EffectResolver.gd
git commit -m "feat: add sword intent damage bonus and consumption to EffectResolver"
```

---

### Task 4: CombatEngine.gd 剑意折半 + 养剑术持久效果

**Files:**
- Modify: `E:\MyWork\AIGameTest2\CombatEngine.gd`

- [ ] **Step 1: 修改 end_turn() — 在 hand.clear() 之后、_do_enemy_turn() 之前追加剑意折半**

找到 `end_turn()` 中以下位置：

```gdscript
	for card: CardData in hand:
		_discard_pile.append(card)
	hand.clear()
	_do_enemy_turn()
```

改为：

```gdscript
	for card: CardData in hand:
		_discard_pile.append(card)
	hand.clear()
	player.sword_intent = player.sword_intent / 2
	_do_enemy_turn()
```

- [ ] **Step 2: 修改 _apply_engine_effects() — 新增两个剑意 elif 分支**

找到 `_apply_engine_effects()` 当前末尾：

```gdscript
func _apply_engine_effects(card: CardData) -> void:
	for effect: CardEffectData in card.effects:
		if effect.type == "draw":
			_draw_cards(effect.value)
		elif effect.type == "energy":
			energy += effect.value
```

改为：

```gdscript
func _apply_engine_effects(card: CardData) -> void:
	for effect: CardEffectData in card.effects:
		if effect.type == "draw":
			_draw_cards(effect.value)
		elif effect.type == "energy":
			energy += effect.value
		elif effect.type == "sword_intent_cap":
			player.sword_intent_cap += effect.value
		elif effect.type == "sword_intent_bonus":
			player.sword_intent_damage_bonus += effect.value
```

- [ ] **Step 3: 提交**

```bash
git add CombatEngine.gd
git commit -m "feat: add sword intent halving on end_turn; handle sword_intent_cap/bonus engine effects"
```

---

### Task 5: RelicEngine.gd SWORD_INTENT 真实效果

**Files:**
- Modify: `E:\MyWork\AIGameTest2\RelicEngine.gd`

- [ ] **Step 1: 将 SWORD_INTENT 分支从 push_warning 改为真实效果**

找到：

```gdscript
		RelicData.EffectType.SWORD_INTENT:
			push_warning("RelicEngine: SWORD_INTENT not yet implemented (v0.34.0)")
```

改为：

```gdscript
		RelicData.EffectType.SWORD_INTENT:
			engine.player.sword_intent = mini(
				engine.player.sword_intent + relic.value,
				engine.player.sword_intent_cap
			)
```

- [ ] **Step 2: 提交**

```bash
git add RelicEngine.gd
git commit -m "feat: implement SWORD_INTENT relic effect in RelicEngine"
```

---

### Task 6: GameManager.gd 替换剑修起始牌组

**Files:**
- Modify: `E:\MyWork\AIGameTest2\GameManager.gd`

- [ ] **Step 1: 在 start_new_run() 中找到旧牌组代码并替换**

找到现有的卡牌 preload 区段（大约如下）：

```gdscript
	var strike: CardData    = preload("res://data/cards/strike.tres")
	var defend: CardData    = preload("res://data/cards/defend.tres")
	var bash: CardData      = preload("res://data/cards/bash.tres")
	var slash: CardData     = preload("res://data/cards/slash.tres")
	var whirlwind: CardData = preload("res://data/cards/whirlwind.tres")
	for i in 4:
		player_state.deck.append(strike.duplicate())
	for i in 4:
		player_state.deck.append(defend.duplicate())
	player_state.deck.append(bash.duplicate())
	player_state.deck.append(slash.duplicate())
	player_state.deck.append(whirlwind.duplicate())
```

整段替换为：

```gdscript
	var zhan_tie: CardData      = preload("res://data/cards/zhan_tie.tres")
	var po_feng: CardData       = preload("res://data/cards/po_feng.tres")
	var ce_bu: CardData         = preload("res://data/cards/ce_bu.tres")
	var lian_xi: CardData       = preload("res://data/cards/lian_xi.tres")
	var chuan_xin: CardData     = preload("res://data/cards/chuan_xin.tres")
	var yang_jian_shu: CardData = preload("res://data/cards/yang_jian_shu.tres")
	for i in 4:
		player_state.deck.append(zhan_tie.duplicate())
	for i in 2:
		player_state.deck.append(po_feng.duplicate())
	for i in 4:
		player_state.deck.append(ce_bu.duplicate())
	player_state.deck.append(lian_xi.duplicate())
	player_state.deck.append(chuan_xin.duplicate())
	player_state.deck.append(yang_jian_shu.duplicate())
```

- [ ] **Step 2: 提交**

```bash
git add GameManager.gd
git commit -m "feat: switch sword cultivator starter deck to GDD-defined 13 cards"
```

---

### Task 7: CombatScreen 剑意计数器 + 版本号

**Files:**
- Modify: `E:\MyWork\AIGameTest2\combat\CombatScreen.tscn`
- Modify: `E:\MyWork\AIGameTest2\combat\CombatScreen.gd`
- Modify: `E:\MyWork\AIGameTest2\menu\MainMenu.tscn`

- [ ] **Step 1: 修改 CombatScreen.tscn — 在 LblPlayerBlock 之后追加 LblSwordIntent**

找到：

```
[node name="LblPlayerBlock" type="Label" parent="VBoxContainer/PlayerPanel"]
layout_mode = 2
text = "格挡：0"
```

在其后追加：

```
[node name="LblSwordIntent" type="Label" parent="VBoxContainer/PlayerPanel"]
layout_mode = 2
text = "剑意：0"
```

同时将文件头部的 `load_steps=2` 改为 `load_steps=2`（无需修改，节点数不影响 load_steps）。

- [ ] **Step 2: 修改 CombatScreen.gd — 新增 @onready**

在现有 `@onready var _lbl_energy` 行之后追加：

```gdscript
@onready var _lbl_sword_intent: Label = $VBoxContainer/PlayerPanel/LblSwordIntent
```

- [ ] **Step 3: 修改 CombatScreen.gd — 在 _refresh_ui() 中追加剑意刷新**

在 `_lbl_energy.text = ...` 行之后追加：

```gdscript
	_lbl_sword_intent.text = "剑意：%d / %d" % [_engine.player.sword_intent, _engine.player.sword_intent_cap]
```

- [ ] **Step 4: 修改 menu/MainMenu.tscn — 版本号改为 v0.34.0**

找到：

```
text = "v0.33.0"
```

改为：

```
text = "v0.34.0"
```

- [ ] **Step 5: 运行游戏验证完整流程**

运行项目（F5），依次验证：

1. 主菜单显示 "v0.34.0"
2. 选剑修开始游戏，进入战斗
3. 战斗开始，玩家面板显示"剑意：3 / 10"（养剑葫触发）
4. 打出斩铁（1费）：造成 6 + 3×1 = 9 伤害；剑意变为 4；显示"剑意：4 / 10"
5. 打出穿心（2费）：造成 8 + 4×3 = 20 伤害；剑意归零；显示"剑意：0 / 10"
6. 点结束回合：剑意 0 折半仍为 0；下回合开始再次显示"剑意：3 / 10"（养剑葫每场战斗开始只触发一次）

   **注意第 6 步**：养剑葫 trigger = COMBAT_START（在 `turn_number == 1` 时触发），第 2 回合不再触发，剑意从回合结束折半后的值开始。

7. 打出养剑术（0费）：显示"剑意：X / 15"（上限变为 15）
8. 手牌中斩铁显示 "斩铁\n费用:1  攻:6"，穿心显示 "穿心\n费用:2  攻:8(+剑意×3)"，养剑术显示 "养剑术\n费用:0  剑意上限+5  加成+1"

- [ ] **Step 6: 提交**

```bash
git add combat/CombatScreen.tscn combat/CombatScreen.gd menu/MainMenu.tscn
git commit -m "feat: add sword intent counter to CombatScreen; bump version to v0.34.0"
```
