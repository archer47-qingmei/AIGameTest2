# v0.71.0 遗物补完 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实装设计文档中缺失的 13 件遗物，覆盖 ON_EQUIP 类、商店类、UI 类、战斗触发类，扩展 RelicEngine/CombatEngine/ShopEngine/RewardEngine 支持新机制。

**Architecture:**
- Task 1 扩展 RelicData/PlayerState 数据层（新 EffectType 17-29）
- Tasks 2-4 扩展 RelicEngine/Combatant/EffectResolver/CombatEngine/ShopEngine/RewardEngine/GameManager/CombatScreen/RewardScreen/ShopScreen 逻辑层
- Tasks 5-7 创建 13 件遗物 `.tres` 数据文件
- Task 8 注册进 CardPool 并升版本号

**Tech Stack:** Godot 4.4 GDScript, `.tres` Resource files, GDScript static class pattern

---

## File Map

| 文件 | 操作 | 说明 |
|------|------|------|
| `RelicData.gd` | 修改 | 新增 EffectType 17-29 |
| `PlayerState.gd` | 修改 | 新增 max_deck_size: int = 20 |
| `RelicEngine.gd` | 修改 | ON_EQUIP 新效果 + apply_on_event_enter/apply_on_attack_played/apply_on_block_success |
| `Combatant.gd` | 修改 | 新增 first_attack_bonus: int = 0 |
| `EffectResolver.gd` | 修改 | _dmg_bonus 加入 first_attack_bonus |
| `CombatEngine.gd` | 修改 | took_damage/combat_gold/_block_gold_count + play_card/\_enemy_attack hooks |
| `GameManager.gd` | 修改 | go_to_event() 调用 apply_on_event_enter |
| `combat/CombatScreen.gd` | 修改 | _on_proceed() 结算 combat_gold |
| `RewardEngine.gd` | 修改 | get_options() 增加 count 参数 |
| `reward/RewardScreen.gd` | 修改 | _build_card_options 读取 EXTRA_REWARD_CARD |
| `shop/ShopEngine.gd` | 修改 | generate(player_state) + 商店遗物效果 |
| `shop/ShopScreen.gd` | 修改 | 传入 player_state |
| `data/relics/*.tres` (×13) | 新建 | 13 件遗物数据文件 |
| `CardPool.gd` | 修改 | RELICS 追加 13 条路径 |
| `menu/MainMenu.tscn` | 修改 | 版本号 v0.70.0 → v0.71.0 |

---

## Task 1: RelicData 新 EffectType + PlayerState.max_deck_size

**Files:**
- Modify: `RelicData.gd`
- Modify: `PlayerState.gd`

- [ ] **Step 1: 修改 RelicData.gd 枚举**

将 `RelicData.gd` 中 `EffectType` 枚举替换为以下完整版本：

```gdscript
enum EffectType {
	ENERGY, HEAL_HP, BLOCK, DRAW, SWORD_INTENT,
	SELF_DAMAGE, ENERGY_CAP, MAX_HP_PERCENT,
	FIRST_TURN_DRAW_PENALTY, BLOCK_DRAIN,
	REMOVE_CARDS, FIRST_TURN_ENERGY,
	SECOND_TURN_ENERGY_PENALTY, ENERGY_IF_LOW_HP,
	DRAW_IF_LOW_HP,
	HEAL_ONCE_IF_HALF_HP,
	PREVENT_DEATH_ONCE,
	BUFF_BASE_ATTACKS,      # 17: ON_EQUIP: 未悟道招式每条damage效果 += value
	REDUCE_SKILL_COST,      # 18: ON_EQUIP: 功法牌 cost = maxi(1, cost - 1)
	COPY_RANDOM_CARD,       # 19: ON_EQUIP: 随机复制一张牌加入牌组
	MAX_DECK_SIZE,          # 20: ON_EQUIP: player_state.max_deck_size += value
	GOLD_ON_EVENT,          # 21: apply_on_event_enter: gold += value
	BLOCK_ON_ATTACK_PLAYED, # 22: apply_on_attack_played: add_block(value)
	FIRST_ATTACK_BONUS,     # 23: COMBAT_START: player.first_attack_bonus += value
	GOLD_IF_NO_DAMAGE,      # 24: COMBAT_END: if not took_damage → combat_gold += value
	GOLD_ON_BLOCK,          # 25: apply_on_block_success: combat_gold += 1, cap 15
	EXTRA_REWARD_CARD,      # 26: RewardScreen: reward card count += value
	SHOP_DISCOUNT,          # 27: ShopEngine.generate: 所有价格 × 0.85
	SHOP_SECOND_HALF_PRICE, # 28: ShopEngine: 第二件起半价
	EXTRA_SHOP_RELIC,       # 29: ShopEngine.generate: 额外提供 1 件遗物（used 后失效）
}
```

Trigger 枚举无需改动（现有 0-4 已够用，新效果通过 EffectType 区分）。

- [ ] **Step 2: 修改 PlayerState.gd 新增字段**

在 `PlayerState.gd` 的字段列表末尾，`energy_cap: int = 3` 之后添加：

```gdscript
var max_deck_size: int = 20
```

- [ ] **Step 3: 提交**

```bash
git add RelicData.gd PlayerState.gd
git commit -m "feat: add RelicData EffectType 17-29 and PlayerState.max_deck_size"
```

---

## Task 2: RelicEngine — ON_EQUIP 新效果 + 三个新触发方法

**Files:**
- Modify: `RelicEngine.gd`

- [ ] **Step 1: 扩展 _apply_equip_effect**

在 `_apply_equip_effect` 函数中，现有 `REMOVE_CARDS` case 之后添加以下 case（保持 `match` 语法）：

```gdscript
		RelicData.EffectType.BUFF_BASE_ATTACKS:
			for card: CardData in player_state.deck:
				if card.card_type == "招式" and not card.is_upgraded:
					for effect: CardEffectData in card.effects:
						if effect.type == "damage":
							effect.value += value
		RelicData.EffectType.REDUCE_SKILL_COST:
			for card: CardData in player_state.deck:
				if card.card_type == "功法":
					card.cost = maxi(1, card.cost - 1)
		RelicData.EffectType.COPY_RANDOM_CARD:
			if not player_state.deck.is_empty():
				var src: CardData = player_state.deck[randi() % player_state.deck.size()]
				player_state.deck.append(src.duplicate())
		RelicData.EffectType.MAX_DECK_SIZE:
			player_state.max_deck_size += value
		RelicData.EffectType.GOLD_ON_EVENT:
			player_state.gold += value
```

- [ ] **Step 2: 扩展 _apply_effect 处理 FIRST_ATTACK_BONUS 和 GOLD_IF_NO_DAMAGE**

在 `_apply_effect` 函数中，现有 `PREVENT_DEATH_ONCE: pass` 之后添加：

```gdscript
		RelicData.EffectType.FIRST_ATTACK_BONUS:
			engine.player.first_attack_bonus += value
		RelicData.EffectType.GOLD_IF_NO_DAMAGE:
			if not engine.took_damage:
				engine.combat_gold += value
```

- [ ] **Step 3: 新增三个公开方法**

在文件末尾（`_apply_equip_effect` 之后）添加：

```gdscript
static func apply_on_event_enter(relics: Array[RelicData], player_state: PlayerState) -> void:
	for relic: RelicData in relics:
		_apply_equip_effect(relic.effect_type, relic.value, player_state)
		if relic.has_effect_b:
			_apply_equip_effect(relic.effect_type_b, relic.value_b, player_state)

static func apply_on_attack_played(relics: Array[RelicData], engine: CombatEngine) -> void:
	for relic: RelicData in relics:
		_apply_on_attack_effect(relic.effect_type, relic.value, engine)
		if relic.has_effect_b:
			_apply_on_attack_effect(relic.effect_type_b, relic.value_b, engine)

static func apply_on_block_success(relics: Array[RelicData], engine: CombatEngine) -> void:
	for relic: RelicData in relics:
		_apply_on_block_effect(relic.effect_type, relic.value, engine)
		if relic.has_effect_b:
			_apply_on_block_effect(relic.effect_type_b, relic.value_b, engine)

static func _apply_on_attack_effect(effect_type: RelicData.EffectType, value: int, engine: CombatEngine) -> void:
	match effect_type:
		RelicData.EffectType.BLOCK_ON_ATTACK_PLAYED:
			engine.player.add_block(value)

static func _apply_on_block_effect(effect_type: RelicData.EffectType, value: int, engine: CombatEngine) -> void:
	match effect_type:
		RelicData.EffectType.GOLD_ON_BLOCK:
			if engine._block_gold_count < 15:
				engine.combat_gold += 1
				engine._block_gold_count += 1
```

注意：`apply_on_event_enter` 复用了 `_apply_equip_effect`，因为 GOLD_ON_EVENT 在那里处理。新方法遍历所有遗物并按 EffectType 分派，不依赖 trigger 字段。

- [ ] **Step 4: 提交**

```bash
git add RelicEngine.gd
git commit -m "feat: RelicEngine ON_EQUIP new effects and apply_on_event/attack/block_success methods"
```

---

## Task 3: Combatant + EffectResolver + CombatEngine 战斗钩子

**Files:**
- Modify: `Combatant.gd`
- Modify: `EffectResolver.gd`
- Modify: `CombatEngine.gd`

- [ ] **Step 1: Combatant.gd 新增 first_attack_bonus 字段**

在 `Combatant.gd` 的字段列表末尾（`var strength: int = 0` 之后）添加：

```gdscript
var first_attack_bonus: int = 0
```

- [ ] **Step 2: EffectResolver._dmg_bonus 加入 first_attack_bonus**

将 `EffectResolver.gd` 中的 `_dmg_bonus` 函数替换为：

```gdscript
static func _dmg_bonus(card: CardData, attacker: Combatant) -> int:
	if card.is_finisher:
		return attacker.sword_intent * card.sword_intent_consume_bonus
	if card.card_type == "招式":
		return attacker.sword_intent * attacker.sword_intent_damage_bonus + attacker.first_attack_bonus
	return 0
```

- [ ] **Step 3: CombatEngine.gd 新增战斗追踪字段**

在 `CombatEngine.gd` 的变量声明区（`var turn_number: int = 0` 之后）添加：

```gdscript
var took_damage: bool = false
var combat_gold: int = 0
var _block_gold_count: int = 0
```

- [ ] **Step 4: CombatEngine._enemy_attack 设置 took_damage 并触发 apply_on_block_success**

将 `_enemy_attack` 函数替换为：

```gdscript
func _enemy_attack(attacker: Combatant, value: int, hits: int = 1) -> void:
	var hp_before: int = player.hp
	for _h in hits:
		EffectResolver.apply_damage(attacker, player, value)
	var dmg: int = hp_before - player.hp
	if dmg > 0:
		took_damage = true
		player_damaged.emit(dmg)
	elif value > 0:
		RelicEngine.apply_on_block_success(_relics, self)
	attacker.weak = max(0, attacker.weak - 1)
```

- [ ] **Step 5: CombatEngine.play_card 触发 apply_on_attack_played 并清除 first_attack_bonus**

在 `play_card` 函数中，找到 `_apply_engine_effects(card)` 这一行。在其**之后**添加：

```gdscript
	if card.card_type == "招式":
		RelicEngine.apply_on_attack_played(_relics, self)
		player.first_attack_bonus = 0
```

完整的 play_card 中相关段落变为（在 `_apply_engine_effects(card)` 和 `var si_gained` 之间）：

```gdscript
	_apply_engine_effects(card)
	if card.card_type == "招式":
		RelicEngine.apply_on_attack_played(_relics, self)
		player.first_attack_bonus = 0
	var si_gained := player.sword_intent - si_before
```

- [ ] **Step 6: 提交**

```bash
git add Combatant.gd EffectResolver.gd CombatEngine.gd
git commit -m "feat: first_attack_bonus field, took_damage/combat_gold tracking, attack/block hooks in CombatEngine"
```

---

## Task 4: 系统集成 — GameManager / CombatScreen / RewardEngine / RewardScreen / ShopEngine / ShopScreen

**Files:**
- Modify: `GameManager.gd`
- Modify: `combat/CombatScreen.gd`
- Modify: `RewardEngine.gd`
- Modify: `reward/RewardScreen.gd`
- Modify: `shop/ShopEngine.gd`
- Modify: `shop/ShopScreen.gd`

- [ ] **Step 1: GameManager.go_to_event() 调用 apply_on_event_enter**

将 `GameManager.gd` 中的 `go_to_event()` 替换为：

```gdscript
func go_to_event() -> void:
	current_phase = Phase.EVENT
	RelicEngine.apply_on_event_enter(player_state.relics, player_state)
	get_tree().change_scene_to_file("res://event/EventScreen.tscn")
```

- [ ] **Step 2: CombatScreen._on_proceed() 结算 combat_gold**

将 `combat/CombatScreen.gd` 中的 `_on_proceed()` 替换为：

```gdscript
func _on_proceed() -> void:
	if _engine.combat_gold > 0:
		GameManager.player_state.gold += _engine.combat_gold
	GameManager.end_combat(_engine.player.hp)
```

- [ ] **Step 3: RewardEngine.get_options() 增加 count 参数**

将 `RewardEngine.gd` 中的 `get_options` 函数替换为：

```gdscript
static func get_options(character: String, count: int = 3) -> Array[CardData]:
	var pool_paths: Array[String] = []
	match character:
		"sword":
			pool_paths.assign(CardPool.SWORD_REWARD_CARDS)
		_:
			push_warning("RewardEngine.get_options: unknown character '%s', fallback to CARDS" % character)
			pool_paths.assign(CardPool.CARDS)
	var pool: Array[CardData] = []
	for path: String in pool_paths:
		var card := load(path) as CardData
		if card != null:
			pool.append(card)
	pool.shuffle()
	return pool.slice(0, mini(count, pool.size()))
```

- [ ] **Step 4: RewardScreen._build_card_options 读取 EXTRA_REWARD_CARD**

在 `reward/RewardScreen.gd` 中，将 `_build_card_options()` 函数替换为：

```gdscript
func _build_card_options() -> void:
	for child in _card_container.get_children():
		_card_container.remove_child(child)
		child.free()
	var options: Array[CardData] = RewardEngine.get_options(
		GameManager.player_state.character,
		_reward_card_count()
	)
	for card: CardData in options:
		var btn: Button = Button.new()
		btn.text = card.get_description()
		btn.pressed.connect(_on_card_selected.bind(card))
		_card_container.add_child(btn)
	var btn_skip: Button = Button.new()
	btn_skip.text = "跳过"
	btn_skip.pressed.connect(_on_skipped)
	_card_container.add_child(btn_skip)
```

并在 `_on_skipped()` 之后添加新函数：

```gdscript
func _reward_card_count() -> int:
	var count: int = 3
	for r: RelicData in GameManager.player_state.relics:
		if r.effect_type == RelicData.EffectType.EXTRA_REWARD_CARD or \
		   (r.has_effect_b and r.effect_type_b == RelicData.EffectType.EXTRA_REWARD_CARD):
			count += r.value
	return count
```

- [ ] **Step 5: ShopEngine.gd 重写 generate() + 新增购买后打折逻辑**

将 `shop/ShopEngine.gd` 完整替换为：

```gdscript
class_name ShopEngine
extends RefCounted

var inventory_cards: Array[CardData] = []
var inventory_relics: Array[RelicData] = []
var purchase_count: int = 0

func generate(player_state: PlayerState = null) -> void:
	var cards: Array[CardData] = []
	for path: String in CardPool.SWORD_REWARD_CARDS:
		var card := load(path) as CardData
		if card != null:
			cards.append(card)
	cards.shuffle()
	inventory_cards.clear()
	for c: CardData in cards.slice(0, mini(4, cards.size())):
		inventory_cards.append(c.duplicate())

	var all_relics: Array[RelicData] = []
	for path: String in CardPool.RELICS:
		var relic := load(path) as RelicData
		if relic != null:
			all_relics.append(relic)
	all_relics.shuffle()

	var relic_count: int = 2
	if player_state != null:
		for r: RelicData in player_state.relics:
			if (r.effect_type == RelicData.EffectType.EXTRA_SHOP_RELIC and not r.used) or \
			   (r.has_effect_b and r.effect_type_b == RelicData.EffectType.EXTRA_SHOP_RELIC and not r.used):
				relic_count += 1
				r.used = true
				break

	inventory_relics.clear()
	for r: RelicData in all_relics.slice(0, mini(relic_count, all_relics.size())):
		inventory_relics.append(r.duplicate())

	if player_state != null:
		_apply_shop_discount(player_state)

func buy_card(card: CardData, player_state: PlayerState) -> bool:
	if player_state.gold < card.price:
		return false
	player_state.gold -= card.price
	player_state.deck.append(card.duplicate())
	inventory_cards.erase(card)
	_maybe_apply_ledger_discount(player_state)
	return true

func buy_relic(relic: RelicData, player_state: PlayerState) -> bool:
	if player_state.gold < relic.price:
		return false
	player_state.gold -= relic.price
	var equipped: RelicData = relic.duplicate()
	player_state.relics.append(equipped)
	RelicEngine.apply_on_equip(equipped, player_state)
	inventory_relics.erase(relic)
	_maybe_apply_ledger_discount(player_state)
	return true

func _apply_shop_discount(player_state: PlayerState) -> void:
	var has_discount: bool = false
	for r: RelicData in player_state.relics:
		if r.effect_type == RelicData.EffectType.SHOP_DISCOUNT or \
		   (r.has_effect_b and r.effect_type_b == RelicData.EffectType.SHOP_DISCOUNT):
			has_discount = true
			break
	if not has_discount:
		return
	for c: CardData in inventory_cards:
		c.price = int(c.price * 0.85)
	for r: RelicData in inventory_relics:
		r.price = int(r.price * 0.85)

func _maybe_apply_ledger_discount(player_state: PlayerState) -> void:
	if purchase_count > 0:
		return
	purchase_count += 1
	var has_ledger: bool = false
	for r: RelicData in player_state.relics:
		if r.effect_type == RelicData.EffectType.SHOP_SECOND_HALF_PRICE or \
		   (r.has_effect_b and r.effect_type_b == RelicData.EffectType.SHOP_SECOND_HALF_PRICE):
			has_ledger = true
			break
	if not has_ledger:
		return
	for c: CardData in inventory_cards:
		c.price = maxi(1, c.price / 2)
	for r: RelicData in inventory_relics:
		r.price = maxi(1, r.price / 2)
```

- [ ] **Step 6: ShopScreen._ready() 传入 player_state**

将 `shop/ShopScreen.gd` 中的 `_ready()` 替换为：

```gdscript
func _ready() -> void:
	_engine = ShopEngine.new()
	_engine.generate(GameManager.player_state)
	_btn_leave.pressed.connect(GameManager.go_to_map)
	_rebuild_ui()
```

- [ ] **Step 7: 提交**

```bash
git add GameManager.gd combat/CombatScreen.gd RewardEngine.gd reward/RewardScreen.gd shop/ShopEngine.gd shop/ShopScreen.gd
git commit -m "feat: wire combat_gold, event_enter gold, extra reward card, shop discount/ledger/extra relic"
```

---

## Task 5: .tres 数据文件 — ON_EQUIP 类遗物（5 件）

**Files:**
- Create: `data/relics/jie_huo_he_xin.tres`
- Create: `data/relics/ling_gen_can_zhi.tres`
- Create: `data/relics/tong_xin_suo.tres`
- Create: `data/relics/jiu_ri_bi_ji.tres`
- Create: `data/relics/xu_kong_ping.tres`

- [ ] **Step 1: 创建 jie_huo_he_xin.tres（劫火核心，仙器，ON_EQUIP BUFF_BASE_ATTACKS）**

```
[gd_resource type="Resource" script_class="RelicData" format=3 uid="uid://rl_jie_huo_he_xin"]

[ext_resource type="Script" path="res://RelicData.gd" id="1_RD"]

[resource]
script = ExtResource("1_RD")
display_name = "劫火核心"
description = "一小团暗红色火，没有燃料。烧了太久，烧出了自己的意志。它不烧东西——只烧你体内不需要的那部分修为。不是帮你。是喜欢烧。\n\n你所有未悟道的招式牌 +3 伤害"
trigger = 4
effect_type = 17
value = 3
price = 250
```

- [ ] **Step 2: 创建 ling_gen_can_zhi.tres（灵根残枝，法器，ON_EQUIP REDUCE_SKILL_COST）**

```
[gd_resource type="Resource" script_class="RelicData" format=3 uid="uid://rl_ling_gen_can_zhi"]

[ext_resource type="Script" path="res://RelicData.gd" id="1_RD"]

[resource]
script = ExtResource("1_RD")
display_name = "灵根残枝"
description = "一截枯了的灵脉。已经死了。但你摸它的时候，内部的灵气经络还在走——一圈一圈。像死掉的树还在打年轮。\n\n你所有功法牌的真气消耗 -1（最低 1）"
trigger = 4
effect_type = 18
value = 1
price = 75
```

- [ ] **Step 3: 创建 tong_xin_suo.tres（同心锁，仙器，ON_EQUIP COPY_RANDOM_CARD）**

```
[gd_resource type="Resource" script_class="RelicData" format=3 uid="uid://rl_tong_xin_suo"]

[ext_resource type="Script" path="res://RelicData.gd" id="1_RD"]

[resource]
script = ExtResource("1_RD")
display_name = "同心锁"
description = "两块可以分开的灵石，永远在寻找对方。你把其中一块放在牌组末端，它在往前爬——不知道要去哪里，但一直在爬。\n\n装备时，随机复制牌组中 1 张牌加入牌组"
trigger = 4
effect_type = 19
value = 1
price = 250
```

- [ ] **Step 4: 创建 jiu_ri_bi_ji.tres（旧日笔迹，灵器，ON_EQUIP MAX_DECK_SIZE）**

```
[gd_resource type="Resource" script_class="RelicData" format=3 uid="uid://rl_jiu_ri_bi_ji"]

[ext_resource type="Script" path="res://RelicData.gd" id="1_RD"]

[resource]
script = ExtResource("1_RD")
display_name = "旧日笔迹"
description = "一本薄册。前几页全是基础错误——一个人从零开始自学五行功法的笔记。后几页另起一种笔迹，把前面每一行错的都改过来了。两种笔迹不是同一个人。改错的那个人没有名字。\n\n牌组上限 +2"
trigger = 4
effect_type = 20
value = 2
price = 150
```

- [ ] **Step 5: 创建 xu_kong_ping.tres（虚空瓶，灵器，GOLD_ON_EVENT）**

`trigger = 0` 是占位值（apply_on_event_enter 按 EffectType 分派，不检查 trigger）。

```
[gd_resource type="Resource" script_class="RelicData" format=3 uid="uid://rl_xu_kong_ping"]

[ext_resource type="Script" path="res://RelicData.gd" id="1_RD"]

[resource]
script = ExtResource("1_RD")
display_name = "虚空瓶"
description = "一只小瓶，瓶口是封死的。瓶里装着的不是丹药——是什么都没有。但你盯着它看的时候，隐约觉得瓶壁那边有人在说话。隔着很厚的空。听不清。\n\n每次进入机缘事件，获得 20 灵石"
trigger = 0
effect_type = 21
value = 20
price = 150
```

- [ ] **Step 6: 提交**

```bash
git add data/relics/jie_huo_he_xin.tres data/relics/ling_gen_can_zhi.tres data/relics/tong_xin_suo.tres data/relics/jiu_ri_bi_ji.tres data/relics/xu_kong_ping.tres
git commit -m "feat: add ON_EQUIP relics 劫火核心/灵根残枝/同心锁/旧日笔迹/虚空瓶"
```

---

## Task 6: .tres 数据文件 — 商店类 + UI 类遗物（4 件）

**Files:**
- Create: `data/relics/wu_xu_xiang_lu.tres`
- Create: `data/relics/fang_shi_zhang_bu.tres`
- Create: `data/relics/si_xian_mu_jie.tres`
- Create: `data/relics/lun_hui_guan_jing.tres`

- [ ] **Step 1: 创建 wu_xu_xiang_lu.tres（雾墟香炉，法器，SHOP_DISCOUNT）**

ShopEngine.generate() 按 EffectType=27 识别，trigger=0 是占位值。

```
[gd_resource type="Resource" script_class="RelicData" format=3 uid="uid://rl_wu_xu_xiang_lu"]

[ext_resource type="Script" path="res://RelicData.gd" id="1_RD"]

[resource]
script = ExtResource("1_RD")
display_name = "雾墟香炉"
description = "一只铜制小手炉，没有盖。炉膛里有一缕烟，不散。烟雾偶尔聚成一张脸——然后又散开。它不是你认识的人。但你每一次看，它都在看你。\n\n坊市物品价格 -15%"
trigger = 0
effect_type = 27
value = 15
price = 75
```

- [ ] **Step 2: 创建 fang_shi_zhang_bu.tres（坊市账簿，灵器，SHOP_SECOND_HALF_PRICE）**

ShopEngine._maybe_apply_ledger_discount() 按 EffectType=28 识别。

```
[gd_resource type="Resource" script_class="RelicData" format=3 uid="uid://rl_fang_shi_zhang_bu"]

[ext_resource type="Script" path="res://RelicData.gd" id="1_RD"]

[resource]
script = ExtResource("1_RD")
display_name = "坊市账簿"
description = "一本旧账本。全是手写的交易记录：一件灵器换了一个馒头。三十颗灵石换了一枚扣子。一个吻换了一份丹药。账目全平。结算方式不按灵石——按「值不值」。\n\n在坊市购买第一件物品后，第二件半价"
trigger = 0
effect_type = 28
value = 0
price = 150
```

- [ ] **Step 3: 创建 si_xian_mu_jie.tres（丝线母结，仙器，EXTRA_SHOP_RELIC）**

ShopEngine.generate() 按 EffectType=29 + used 标志识别。

```
[gd_resource type="Resource" script_class="RelicData" format=3 uid="uid://rl_si_xian_mu_jie"]

[ext_resource type="Script" path="res://RelicData.gd" id="1_RD"]

[resource]
script = ExtResource("1_RD")
display_name = "丝线母结"
description = "一截丝线的末梢，断口处自己收束成了一个结。它一直在微颤——极低极慢的颤。像是丝线的另一端，有人在拉。\n\n坊市额外提供 1 件遗物（整局仅一次）"
trigger = 0
effect_type = 29
value = 1
price = 250
```

- [ ] **Step 4: 创建 lun_hui_guan_jing.tres（轮回观镜，仙器，EXTRA_REWARD_CARD）**

RewardScreen._reward_card_count() 按 EffectType=26 识别。

```
[gd_resource type="Resource" script_class="RelicData" format=3 uid="uid://rl_lun_hui_guan_jing"]

[ext_resource type="Script" path="res://RelicData.gd" id="1_RD"]

[resource]
script = ExtResource("1_RD")
display_name = "轮回观镜"
description = "一块手板大、从山体上自然剥落的矿石。晶面上映的不是当前——是你上一次。你没死的那次。你差点赢的那次。你走错了一招的那次。一遍一遍。\n\n你在选择卡牌奖励时多 1 个选项"
trigger = 0
effect_type = 26
value = 1
price = 250
```

- [ ] **Step 5: 提交**

```bash
git add data/relics/wu_xu_xiang_lu.tres data/relics/fang_shi_zhang_bu.tres data/relics/si_xian_mu_jie.tres data/relics/lun_hui_guan_jing.tres
git commit -m "feat: add shop/reward relics 雾墟香炉/坊市账簿/丝线母结/轮回观镜"
```

---

## Task 7: .tres 数据文件 — 战斗触发类遗物（4 件）

**Files:**
- Create: `data/relics/hun_si_chan_rao.tres`
- Create: `data/relics/gui_xu_zhi_gu.tres`
- Create: `data/relics/ling_shi_chong.tres`
- Create: `data/relics/wu_ming_zhi_gu.tres`

- [ ] **Step 1: 创建 hun_si_chan_rao.tres（魂丝缠绕，灵器，BLOCK_ON_ATTACK_PLAYED）**

apply_on_attack_played() 按 EffectType=22 识别，trigger=0 是占位值。

```
[gd_resource type="Resource" script_class="RelicData" format=3 uid="uid://rl_hun_si_chan_rao"]

[ext_resource type="Script" path="res://RelicData.gd" id="1_RD"]

[resource]
script = ExtResource("1_RD")
display_name = "魂丝缠绕"
description = "一截半透明的丝线，极细极韧。不是你要它——是它缠上你的。顺着你的经脉往上爬。不冷。只是轻轻的。像有人在很远的地方，用手指点着你的脉搏在默念什么。\n\n每当你打出招式牌，获得 1 点格挡"
trigger = 0
effect_type = 22
value = 1
price = 150
```

- [ ] **Step 2: 创建 gui_xu_zhi_gu.tres（归墟指骨，法器，FIRST_ATTACK_BONUS）**

trigger = 0 (COMBAT_START)，在每场战斗开始时由 apply_combat_start → _apply_effect 将 first_attack_bonus += 3 设置到 Combatant 上，在第一张招式打出后由 CombatEngine 清零。

```
[gd_resource type="Resource" script_class="RelicData" format=3 uid="uid://rl_gui_xu_zhi_gu"]

[ext_resource type="Script" path="res://RelicData.gd" id="1_RD"]

[resource]
script = ExtResource("1_RD")
display_name = "归墟指骨"
description = "一小截骨头。不是人的。它死了很久，但灵力还在骨头里走。像是在跑——已经不知道目的地了，但还在跑。\n\n每场战斗，你的第一张招式牌伤害 +3"
trigger = 0
effect_type = 23
value = 3
price = 75
```

- [ ] **Step 3: 创建 ling_shi_chong.tres（灵石虫，法器，GOLD_IF_NO_DAMAGE）**

trigger = 3 (COMBAT_END)，战斗结束时检查 engine.took_damage == false，则 engine.combat_gold += 8。

```
[gd_resource type="Resource" script_class="RelicData" format=3 uid="uid://rl_ling_shi_chong"]

[ext_resource type="Script" path="res://RelicData.gd" id="1_RD"]

[resource]
script = ExtResource("1_RD")
display_name = "灵石虫"
description = "一只拇指大的虫子，后半截身体已经变成了灵石。从石缝里挖出来的时候它在睡觉。醒了，没走。蹲在你肩上。偶尔啃你灵石上沾的杂质。不咬人。\n\n战斗结束时，若你本场战斗没有受到伤害，获得 8 灵石"
trigger = 3
effect_type = 24
value = 8
price = 75
```

- [ ] **Step 4: 创建 wu_ming_zhi_gu.tres（无名之骨，仙器，GOLD_ON_BLOCK）**

apply_on_block_success() 按 EffectType=25 识别，每次完全格挡一次攻击时 combat_gold += 1，上限 15 次（_block_gold_count）。

```
[gd_resource type="Resource" script_class="RelicData" format=3 uid="uid://rl_wu_ming_zhi_gu"]

[ext_resource type="Script" path="res://RelicData.gd" id="1_RD"]

[resource]
script = ExtResource("1_RD")
display_name = "无名之骨"
description = "一块旧骨头，表面有密密麻麻的刻痕。不是字——是计数。横一道，竖一道。不知道谁刻的。刻了很久。骨头从虚空里漏了出来。\n\n每当你成功格挡一次伤害，获得 1 灵石（一场最多 15）"
trigger = 0
effect_type = 25
value = 1
price = 250
```

- [ ] **Step 5: 提交**

```bash
git add data/relics/hun_si_chan_rao.tres data/relics/gui_xu_zhi_gu.tres data/relics/ling_shi_chong.tres data/relics/wu_ming_zhi_gu.tres
git commit -m "feat: add combat-trigger relics 魂丝缠绕/归墟指骨/灵石虫/无名之骨"
```

---

## Task 8: CardPool 注册 + 版本号

**Files:**
- Modify: `CardPool.gd`
- Modify: `menu/MainMenu.tscn`

- [ ] **Step 1: CardPool.RELICS 追加 13 条路径**

将 `CardPool.gd` 中的 `RELICS` 常量替换为以下完整版本：

```gdscript
const RELICS: Array[String] = [
	"res://data/relics/burning_gem.tres",
	"res://data/relics/can_xiu_hun_jing.tres",
	"res://data/relics/tian_di_lie_hen.tres",
	"res://data/relics/shuo_zhi_hui_yin.tres",
	"res://data/relics/dan_xin_jing.tres",
	"res://data/relics/wu_ming_hu_fu.tres",
	"res://data/relics/sui_jing_pian.tres",
	"res://data/relics/bu_san_zhi_nian.tres",
	"res://data/relics/jie_huo_he_xin.tres",
	"res://data/relics/ling_gen_can_zhi.tres",
	"res://data/relics/tong_xin_suo.tres",
	"res://data/relics/jiu_ri_bi_ji.tres",
	"res://data/relics/xu_kong_ping.tres",
	"res://data/relics/wu_xu_xiang_lu.tres",
	"res://data/relics/fang_shi_zhang_bu.tres",
	"res://data/relics/si_xian_mu_jie.tres",
	"res://data/relics/lun_hui_guan_jing.tres",
	"res://data/relics/hun_si_chan_rao.tres",
	"res://data/relics/gui_xu_zhi_gu.tres",
	"res://data/relics/ling_shi_chong.tres",
	"res://data/relics/wu_ming_zhi_gu.tres",
]
```

- [ ] **Step 2: 版本号 v0.70.0 → v0.71.0**

在 `menu/MainMenu.tscn` 中，搜索 `v0.70.0` 并替换为 `v0.71.0`。

- [ ] **Step 3: 提交**

```bash
git add CardPool.gd menu/MainMenu.tscn
git commit -m "feat: v0.71.0 register 13 new relics in CardPool, bump version"
```

---

## 自检清单

- [ ] **Spec 覆盖率检查**
  - 13 件遗物全部创建：✓（Tasks 5-7）
  - ON_EQUIP 效果：BUFF_BASE_ATTACKS/REDUCE_SKILL_COST/COPY_RANDOM_CARD/MAX_DECK_SIZE ✓（Task 2）
  - GameManager.go_to_event() 触发 GOLD_ON_EVENT ✓（Task 4）
  - CombatEngine.play_card() 触发 apply_on_attack_played ✓（Task 3）
  - CombatEngine._enemy_attack() 触发 apply_on_block_success ✓（Task 3）
  - FIRST_ATTACK_BONUS 在 COMBAT_START 设置，在 play_card 清零 ✓（Tasks 2,3）
  - GOLD_IF_NO_DAMAGE 在 COMBAT_END 检查 took_damage ✓（Tasks 2,3）
  - combat_gold 在 CombatScreen._on_proceed() 转给 player_state ✓（Task 4）
  - ShopEngine.generate(player_state) 处理 SHOP_DISCOUNT/EXTRA_SHOP_RELIC ✓（Task 4）
  - SHOP_SECOND_HALF_PRICE 在首次购买后打折剩余库存 ✓（Task 4）
  - EXTRA_REWARD_CARD 在 RewardScreen 增加选项数 ✓（Task 4）
  - CardPool.RELICS 追加 13 条路径 ✓（Task 8）
  - 版本号更新 ✓（Task 8）

- [ ] **Placeholder 扫描**：计划中无 TBD / TODO / "类似 Task N" 等占位文本

- [ ] **类型一致性**：
  - EffectType 枚举值 17-29 在 RelicData.gd 和 .tres 文件中数值一致
  - `engine.took_damage` / `engine.combat_gold` / `engine._block_gold_count` 在 RelicEngine 和 CombatEngine 中字段名一致
  - `player.first_attack_bonus` 在 Combatant/EffectResolver/CombatEngine/RelicEngine 中字段名一致
