# v0.72.0 多境界地图 + 金丹境 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实装多境界地图架构（筑基→金丹）、金丹境 11 只怪物、以及诅咒卡系统扩展（业火触发 + 暴怒/恐惧/悲伤三张新诅咒）

**Architecture:** 在 PlayerState 新增 current_realm 字段，MapGenerator 按 realm 选不同敌人池；GameManager 新增 advance_realm() 跨境界；RewardScreen Boss 结算后检查 is_last_realm 决定是否进入下一境界还是直接胜利。诅咒卡通过 CardData 4 个新 bool 字段驱动，引擎在 play_card/end_turn/_start_player_turn 中分别检查。金丹境怪物引擎扩展通过 EnemyData/Combatant 新字段 + CombatEngine match 新分支实现。

**Tech Stack:** GDScript 4.4, Godot Resource (.tres), 无外部测试框架（手动游戏验证）

---

## 文件清单

### 修改
- `PlayerState.gd` — 新增 `current_realm: int = 0`
- `MapGenerator.gd` — generate(realm) 参数 + 金丹境常量 + 内部函数传 realm
- `GameManager.gd` — advance_realm() + is_last_realm() + _setup_map(realm)
- `reward/RewardScreen.gd` — _on_continue_pressed 判断 is_last_realm
- `CardData.gd` — 新增 is_ye_huo/is_bao_nu/is_kong_ju/is_bei_shang
- `CombatEngine.gd` — 诅咒卡触发逻辑 + 10 个新 action type + 引擎新字段处理 + preloads
- `combat/CombatScreen.gd` — _rebuild_hand 灰化恐惧封锁的身法牌
- `EnemyData.gd` — 5 个新字段
- `Combatant.gd` — 2 个新字段

### 新建
- `data/cards/bao_nu.tres`, `kong_ju.tres`, `bei_shang.tres`
- `data/cards/ye_huo.tres` — 更新加 is_ye_huo=true
- `data/enemies/` — 12 个 .tres（11 怪物，twin_sword 分攻守两个）
- `data/enemy_groups/` — 11 个 .tres

---

## Task 1：多境界架构

**Files:**
- Modify: `PlayerState.gd`
- Modify: `MapGenerator.gd`
- Modify: `GameManager.gd`
- Modify: `reward/RewardScreen.gd`

- [ ] **Step 1: PlayerState 新增 current_realm**

在 `PlayerState.gd` 的 `max_deck_size` 行后添加：

```gdscript
var current_realm: int = 0  # 0=筑基, 1=金丹
```

完整修改（在 `max_deck_size: int = 20` 后插入）：

```
var current_realm: int = 0
```

- [ ] **Step 2: MapGenerator 新增金丹境常量 + generate(realm) 签名**

将 `MapGenerator.gd` 的 `BOSS_REWARD_RELIC_PATH` 行之后、`ROW_BASE_X` 行之前，插入以下常量：

```gdscript
const JD_EARLY_GROUP_PATHS: Array[String] = [
	"res://data/enemy_groups/single_jd_fragment.tres",
	"res://data/enemy_groups/single_tide_demon.tres",
	"res://data/enemy_groups/single_thunder_beast.tres",
]
const JD_MID_GROUP_PATHS: Array[String] = [
	"res://data/enemy_groups/single_charm_snake.tres",
	"res://data/enemy_groups/single_guardian_lion.tres",
	"res://data/enemy_groups/single_fire_spirit.tres",
	"res://data/enemy_groups/single_twin_vulture.tres",
]
const JD_ELITE_GROUP_PATHS: Array[String] = [
	"res://data/enemy_groups/single_jd_calamity.tres",
	"res://data/enemy_groups/single_seven_emotions.tres",
	"res://data/enemy_groups/twin_sword_cultivators.tres",
]
const JD_BOSS_GROUP_PATH: String = "res://data/enemy_groups/single_49_tribulation.tres"
const JD_ELITE_REWARD_RELIC_PATH: String = "res://data/relics/burning_gem.tres"
const JD_BOSS_REWARD_RELIC_PATH: String = "res://data/relics/iron_armor.tres"
```

- [ ] **Step 3: MapGenerator.generate() 改为接受 realm 参数**

将 `static func generate() -> Array[NodeData]:` 改为：

```gdscript
static func generate(realm: int = 0) -> Array[NodeData]:
	var node_grid: Dictionary = {}

	var boss_group_path: String = JD_BOSS_GROUP_PATH if realm == 1 else BOSS_GROUP_PATH
	var boss_reward_path: String = JD_BOSS_REWARD_RELIC_PATH if realm == 1 else BOSS_REWARD_RELIC_PATH

	var boss := NodeData.new()
	boss.config = NodeConfig.new()
	boss.config.column = TOTAL_COLUMNS - 1
	boss.config.row = 1
	boss.config.type = NodeConfig.Type.BOSS
	boss.config.enemy_group = load(boss_group_path) as EnemyGroupData
	boss.config.reward_relic = load(boss_reward_path) as RelicData
	boss.config.map_position = BOSS_MAP_POSITION
	node_grid[Vector2i(TOTAL_COLUMNS - 1, 1)] = boss

	for _p in NUM_PATHS:
		var row: int = randi() % NUM_ROWS
		var prev_nd: NodeData = null
		for col in TOTAL_COLUMNS - 1:
			var key := Vector2i(col, row)
			if not node_grid.has(key):
				node_grid[key] = _make_node(col, row)
			var nd: NodeData = node_grid[key]
			if prev_nd != null and not prev_nd.connections.has(nd):
				prev_nd.connections.append(nd)
			prev_nd = nd
			row = clampi(row + (randi() % 3) - 1, 0, NUM_ROWS - 1)
		if prev_nd != null and not prev_nd.connections.has(boss):
			prev_nd.connections.append(boss)

	var all: Array[NodeData] = []
	for nd: NodeData in node_grid.values():
		all.append(nd)
	_assign_types(all, realm)
	_add_shop_nodes(all)
	_add_event_nodes(all)
	return all
```

- [ ] **Step 4: _assign_types 和 _random_combat_group 改为接受 realm**

将 `_assign_types(all: Array[NodeData])` 改为：

```gdscript
static func _assign_types(all: Array[NodeData], realm: int) -> void:
	var elite_paths: Array[String] = JD_ELITE_GROUP_PATHS if realm == 1 else ELITE_GROUP_PATHS
	var elite_relic_path: String = JD_ELITE_REWARD_RELIC_PATH if realm == 1 else ELITE_REWARD_RELIC_PATH

	var by_col: Dictionary = {}
	for nd: NodeData in all:
		var col: int = nd.config.column
		if col == TOTAL_COLUMNS - 1:
			continue
		if not by_col.has(col):
			by_col[col] = []
		by_col[col].append(nd)

	for col in range(0, TOTAL_COLUMNS - 1):
		var nodes: Array = by_col.get(col, [])
		if nodes.is_empty():
			continue
		for nd: NodeData in nodes:
			nd.config.enemy_group = _random_combat_group(col, realm)
		match col:
			REST_COL:
				if nodes.size() >= 2:
					nodes.shuffle()
					nodes[0].config.type = NodeConfig.Type.REST
					nodes[0].config.enemy_group = null
			CHEST_REST_COL:
				nodes.shuffle()
				nodes[0].config.type = NodeConfig.Type.CHEST
				nodes[0].config.enemy_group = null
				if nodes.size() >= 2:
					nodes[1].config.type = NodeConfig.Type.REST
					nodes[1].config.enemy_group = null
			ELITE_START_COL, ELITE_START_COL + 1, ELITE_END_COL:
				nodes.shuffle()
				nodes[0].config.type = NodeConfig.Type.ELITE
				nodes[0].config.enemy_group = load(elite_paths[randi() % elite_paths.size()]) as EnemyGroupData
				nodes[0].config.reward_relic = load(elite_relic_path) as RelicData

	var early_elite_col: int = randi() % (EARLY_ELITE_MAX_COL + 1)
	var early_nodes: Array = by_col.get(early_elite_col, [])
	if not early_nodes.is_empty():
		early_nodes.shuffle()
		early_nodes[0].config.type = NodeConfig.Type.ELITE
		early_nodes[0].config.enemy_group = load(elite_paths[randi() % elite_paths.size()]) as EnemyGroupData
		early_nodes[0].config.reward_relic = load(elite_relic_path) as RelicData
```

将 `_random_combat_group(col: int)` 改为：

```gdscript
static func _random_combat_group(col: int, realm: int) -> EnemyGroupData:
	var early_paths: Array[String] = JD_EARLY_GROUP_PATHS if realm == 1 else EARLY_GROUP_PATHS
	var mid_paths: Array[String] = JD_MID_GROUP_PATHS if realm == 1 else MID_GROUP_PATHS
	var paths: Array[String] = early_paths if col <= 3 else mid_paths
	return load(paths[randi() % paths.size()]) as EnemyGroupData
```

- [ ] **Step 5: GameManager 新增 advance_realm() 和 is_last_realm()**

在 `GameManager.gd` 的 `_setup_map()` 方法内，将 `MapGenerator.generate()` 改为：

```gdscript
func _setup_map() -> void:
	var nodes: Array[NodeData] = MapGenerator.generate(player_state.current_realm)
	player_state.map_all_nodes.assign(nodes)
	var start_nodes: Array[NodeData] = []
	for nd in nodes:
		if nd.config.column == 0:
			start_nodes.append(nd)
	player_state.available_nodes.assign(start_nodes)
	current_phase = Phase.MAP
	get_tree().change_scene_to_file("res://map/MapScreen.tscn")
```

在 `is_final_node()` 方法后面插入两个新方法：

```gdscript
func is_last_realm() -> bool:
	return player_state.current_realm >= 1  # v0.72.0: 只有筑基+金丹两境界

func advance_realm() -> void:
	player_state.current_realm += 1
	player_state.map_all_nodes.clear()
	player_state.available_nodes.clear()
	player_state.completed_nodes.clear()
	player_state.current_node = null
	pending_card_reward = true
	_setup_map()
```

- [ ] **Step 6: RewardScreen._on_continue_pressed 加境界推进逻辑**

将 `reward/RewardScreen.gd` 中 `_on_continue_pressed()` 整体替换为：

```gdscript
func _on_continue_pressed() -> void:
	if GameManager.is_final_node():
		if GameManager.is_last_realm():
			GameManager.go_to_win()
		else:
			GameManager.advance_realm()
	else:
		GameManager.go_to_map()
```

- [ ] **Step 7: 验证**

启动 Godot，开始新游戏，打通筑基境 Boss（全部10列），在奖励界面点击"继续"。

预期：地图界面重新出现，节点全部是金丹境怪物（jd_fragment/tide_demon/thunder_beast 等名字，若数据文件已创建）；若 Task 5 还没完成，跳过此验证或先创建占位数据文件。

- [ ] **Step 8: Commit**

```bash
git add PlayerState.gd MapGenerator.gd GameManager.gd reward/RewardScreen.gd
git commit -m "feat: multi-realm map architecture (筑基→金丹)"
```

---

## Task 2：诅咒卡数据

**Files:**
- Modify: `CardData.gd`
- Modify: `data/cards/ye_huo.tres`
- Create: `data/cards/bao_nu.tres`
- Create: `data/cards/kong_ju.tres`
- Create: `data/cards/bei_shang.tres`

- [ ] **Step 1: CardData.gd 新增 4 个 bool 字段**

在 `CardData.gd` 的 `@export var is_zahuorumuo: bool = false` 行后插入：

```gdscript
@export var is_ye_huo: bool = false
@export var is_bao_nu: bool = false
@export var is_kong_ju: bool = false
@export var is_bei_shang: bool = false
```

- [ ] **Step 2: 更新 ye_huo.tres 加 is_ye_huo=true**

用 Write 工具（不用 PowerShell）覆盖 `data/cards/ye_huo.tres`：

```
[gd_resource type="Resource" script_class="CardData" format=3]

[ext_resource type="Script" path="res://CardData.gd" id="1_CardData"]

[resource]
script = ExtResource("1_CardData")
card_name = "业火"
cost = 0
is_curse = true
is_ye_huo = true
special_text = "无法通过休息站移除。每次战斗开始时受到 2 点伤害。"
target_type = "none"
```

- [ ] **Step 3: 新建 bao_nu.tres**

用 Write 工具创建 `data/cards/bao_nu.tres`：

```
[gd_resource type="Resource" script_class="CardData" format=3]

[ext_resource type="Script" path="res://CardData.gd" id="1_CardData"]

[resource]
script = ExtResource("1_CardData")
card_name = "暴怒"
cost = 0
is_curse = true
is_bao_nu = true
special_text = "无法打出。每次你打出一张牌，受到 2 点伤害（每张暴怒叠加）"
target_type = "none"
```

- [ ] **Step 4: 新建 kong_ju.tres**

用 Write 工具创建 `data/cards/kong_ju.tres`：

```
[gd_resource type="Resource" script_class="CardData" format=3]

[ext_resource type="Script" path="res://CardData.gd" id="1_CardData"]

[resource]
script = ExtResource("1_CardData")
card_name = "恐惧"
cost = 0
is_curse = true
is_kong_ju = true
special_text = "无法打出。持有时，无法使用身法牌"
target_type = "none"
```

- [ ] **Step 5: 新建 bei_shang.tres**

用 Write 工具创建 `data/cards/bei_shang.tres`：

```
[gd_resource type="Resource" script_class="CardData" format=3]

[ext_resource type="Script" path="res://CardData.gd" id="1_CardData"]

[resource]
script = ExtResource("1_CardData")
card_name = "悲伤"
cost = 0
is_curse = true
is_bei_shang = true
special_text = "无法打出。回合结束：弃置 1 张手牌（每张悲伤叠加）"
target_type = "none"
```

- [ ] **Step 6: Commit**

```bash
git add CardData.gd data/cards/ye_huo.tres data/cards/bao_nu.tres data/cards/kong_ju.tres data/cards/bei_shang.tres
git commit -m "feat: curse card data fields and new tres files (暴怒/恐惧/悲伤/业火)"
```

---

## Task 3：诅咒卡引擎逻辑

**Files:**
- Modify: `CombatEngine.gd`
- Modify: `combat/CombatScreen.gd`

- [ ] **Step 1: CombatEngine 顶部新增三个 preload 常量**

在 `CombatEngine.gd` 的 `const ZAHUORUMUO_CARD` 行后插入：

```gdscript
const BAO_NU_CARD: CardData = preload("res://data/cards/bao_nu.tres")
const KONG_JU_CARD: CardData = preload("res://data/cards/kong_ju.tres")
const BEI_SHANG_CARD: CardData = preload("res://data/cards/bei_shang.tres")
```

- [ ] **Step 2: play_card() 开头添加恐惧封锁检查**

在 `play_card()` 中，`if card.is_curse or card.is_zahuorumuo:` 块之后、`if card.cost > energy:` 行之前插入：

```gdscript
if card.card_type == "身法" and hand.any(func(c: CardData) -> bool: return c.is_kong_ju):
    return false
```

- [ ] **Step 3: play_card() 中添加暴怒伤害**

在 `play_card()` 中，`hand.remove_at(card_index)` 行之前（即 `if card.card_type == "功法":` 之前）插入：

```gdscript
var bao_nu_count: int = 0
for c in hand:
    if c.is_bao_nu:
        bao_nu_count += 1
if bao_nu_count > 0:
    player.hp = max(0, player.hp - bao_nu_count * 2)
    player_damaged.emit(bao_nu_count * 2)
```

- [ ] **Step 4: _start_player_turn() 中添加业火触发**

在 `_start_player_turn()` 中，`if turn_number == 1:` 块（`RelicEngine.apply_combat_start` 调用处）之前插入：

```gdscript
if turn_number == 1:
    var ye_huo_count: int = 0
    for c in _draw_pile:
        if c.is_ye_huo:
            ye_huo_count += 1
    for c in _discard_pile:
        if c.is_ye_huo:
            ye_huo_count += 1
    for c in hand:
        if c.is_ye_huo:
            ye_huo_count += 1
    if ye_huo_count > 0:
        player.hp = max(0, player.hp - ye_huo_count * 2)
        player_damaged.emit(ye_huo_count * 2)
```

注意：这段 `if turn_number == 1:` 是新增的，与下方现有的 `if turn_number == 1: RelicEngine.apply_combat_start(...)` 是两个独立的 if 块。

- [ ] **Step 5: _start_player_turn() 末尾添加 next_block_halved 处理**

在 `_start_player_turn()` 末尾（`state_changed.emit()` 之前）插入：

```gdscript
if player.next_block_halved:
    player.block = player.block / 2
    player.next_block_halved = false
```

- [ ] **Step 6: end_turn() 中添加悲伤弃牌**

在 `end_turn()` 中，走火入魔处理完成之后（`for _z in zao_count:` 块结束后）、`for card: CardData in hand: _discard_pile.append(card)` 之前插入：

```gdscript
var bei_shang_count: int = 0
for c in hand:
    if c.is_bei_shang:
        bei_shang_count += 1
for _b in bei_shang_count:
    var non_curse_indices: Array[int] = []
    for idx in hand.size():
        var c: CardData = hand[idx]
        if not c.is_curse and not c.is_zahuorumuo and not c.is_venom \
                and not c.is_ye_huo and not c.is_bao_nu and not c.is_kong_ju and not c.is_bei_shang:
            non_curse_indices.append(idx)
    if non_curse_indices.is_empty():
        break
    var victim_idx: int = non_curse_indices[randi() % non_curse_indices.size()]
    _discard_pile.append(hand[victim_idx])
    hand.remove_at(victim_idx)
```

- [ ] **Step 7: 新增 can_play_card() 公开方法**

在 `CombatEngine.gd` 的 `draw_cards(n)` 方法之前插入：

```gdscript
func can_play_card(card: CardData) -> bool:
    if card.card_type == "身法" and hand.any(func(c: CardData) -> bool: return c.is_kong_ju):
        return false
    return true
```

- [ ] **Step 8: CombatScreen._rebuild_hand() 添加恐惧灰化**

在 `CombatScreen.gd` 的 `_rebuild_hand()` 中，`btn.button_down.connect(...)` 行之后插入：

```gdscript
if not _engine.can_play_card(card):
    btn.modulate = Color(0.6, 0.6, 0.6)
```

并在 `_on_card_button_down()` 中，`if _engine.energy < card.cost:` 检查之前插入：

```gdscript
if not _engine.can_play_card(card):
    return
```

- [ ] **Step 9: 手动验证**

启动游戏，在 Debug 模式用以下步骤验证：
1. 在牌组里加一张业火，开始战斗 → 第一回合开始时应受到 2 点伤害（不过格挡）
2. 在牌组里加两张暴怒，打出一张普通牌 → 受到 4 点伤害
3. 在牌组里加一张恐惧，尝试打出身法牌 → 身法牌显示灰色，无法打出
4. 在牌组里加一张悲伤，按"结束回合" → 应随机弃掉一张非诅咒手牌

- [ ] **Step 10: Commit**

```bash
git add CombatEngine.gd combat/CombatScreen.gd
git commit -m "feat: curse card engine logic (业火/暴怒/恐惧/悲伤)"
```

---

## Task 4：怪物引擎扩展

**Files:**
- Modify: `EnemyData.gd`
- Modify: `Combatant.gd`
- Modify: `CombatEngine.gd`

- [ ] **Step 1: EnemyData.gd 新增 5 个字段**

在 `EnemyData.gd` 末尾（`periodic_action` 行后）追加：

```gdscript
@export var passive_block_per_turn: int = 0
@export var emotion_card_damage_bonus: bool = false
@export var death_kills_others: bool = false
@export var skip_wave_gap_threshold: float = 0.0
@export var gains_strength_from_aoe: bool = false
```

- [ ] **Step 2: Combatant.gd 新增 2 个字段**

在 `Combatant.gd` 末尾（`first_attack_bonus` 行后）追加：

```gdscript
var draw_reduction: int = 0
var next_block_halved: bool = false
```

- [ ] **Step 3: CombatEngine._do_enemy_turn() 加 passive_block_per_turn 逻辑**

在 `_do_enemy_turn()` 中，`enemies[i].block = 0` 行之后插入：

```gdscript
if data.passive_block_per_turn > 0:
    enemies[i].add_block(data.passive_block_per_turn)
```

- [ ] **Step 4: CombatEngine._do_enemy_turn() match 块新增 10 个 action types**

在 match 块的 `"attack_zahuorumuo":` 分支之后、`_:` 默认分支之前插入：

```gdscript
"attack_self_damage":
    _enemy_attack(enemies[i], action.value)
    enemies[i].hp = max(0, enemies[i].hp - action.count)
"draw_penalty":
    player.draw_reduction += action.count
"attack_half_next_block":
    _enemy_attack(enemies[i], action.value)
    player.next_block_halved = true
"block_all_enemies":
    for j in enemies.size():
        if enemies[j].hp > 0:
            enemies[j].add_block(action.value)
"aoe_all":
    _enemy_attack(enemies[i], action.value)
    for j in enemies.size():
        if enemies[j].hp > 0:
            enemies[j].hp = max(0, enemies[j].hp - action.value)
    if data.gains_strength_from_aoe:
        enemies[i].strength += 2
"attack_bao_nu":
    _enemy_attack(enemies[i], action.value)
    for _k in action.count:
        _discard_pile.append(BAO_NU_CARD.duplicate())
"attack_kong_ju":
    _enemy_attack(enemies[i], action.value)
    for _k in action.count:
        _discard_pile.append(KONG_JU_CARD.duplicate())
"attack_bei_shang":
    _enemy_attack(enemies[i], action.value)
    for _k in action.count:
        _discard_pile.append(BEI_SHANG_CARD.duplicate())
"attack_venom":
    _enemy_attack(enemies[i], action.value)
    _add_venoms_to_draw(action.count)
"wave_gap":
    pass
```

同时将 ATTACK_TYPES_FOR_CHARGE 常量更新（在文件顶部），加入新攻击类型：

```gdscript
const ATTACK_TYPES_FOR_CHARGE: Array[String] = [
	"attack", "attack_weak", "attack_vulnerable", "multi_attack", "attack_curse", "vampiric_attack",
	"attack_self_damage", "attack_half_next_block", "attack_bao_nu", "attack_kong_ju", "attack_bei_shang", "attack_venom"
]
```

- [ ] **Step 5: CombatEngine._resolve_enemy_actions() 加 skip_wave_gap 逻辑**

将 `_resolve_enemy_actions()` 中的 random/non-random action 选择段：

```gdscript
if data.random_actions:
    action = _weighted_random_action(action_list)
else:
    action = action_list[(turn_number - 1) % action_list.size()]
```

改为：

```gdscript
if data.random_actions:
    action = _weighted_random_action(action_list)
else:
    var action_idx: int = (turn_number - 1) % action_list.size()
    action = action_list[action_idx]
    if action.type == "wave_gap" and data.skip_wave_gap_threshold > 0.0 \
            and float(enemies[i].hp) / float(data.hp) < data.skip_wave_gap_threshold:
        action = action_list[(action_idx + 1) % action_list.size()]
```

- [ ] **Step 6: CombatEngine._check_end() 加 death_kills_others 逻辑**

将 `_check_end()` 开头替换为：

```gdscript
func _check_end() -> bool:
    for i in enemies.size():
        if enemies[i].hp <= 0 and _enemy_data_list[i].death_kills_others:
            for j in enemies.size():
                if j != i:
                    enemies[j].hp = 0
    var any_alive: bool = enemies.any(func(e: Combatant) -> bool: return e.hp > 0)
    ...  # 其余不变
```

- [ ] **Step 7: CombatEngine.draw_cards() 加 draw_reduction 逻辑**

将 `draw_cards(n: int)` 改为：

```gdscript
func draw_cards(n: int) -> void:
    var actual: int = max(0, n - player.draw_reduction)
    player.draw_reduction = 0
    _draw_cards(actual)
```

- [ ] **Step 8: CombatScreen._intent_text() 新增 action type 显示文本**

在 `_intent_text()` 的 `"attack_zahuorumuo":` 分支后、`_:` 默认分支前插入：

```gdscript
"attack_self_damage":
    return "意图：爆裂 %d" % val
"draw_penalty":
    return "意图：缠绕 -%d抽" % action.count
"attack_half_next_block":
    return "意图：震地 %d" % val
"block_all_enemies":
    return "意图：护盾 +%d格挡" % action.value
"aoe_all":
    return "意图：天劫 %d" % val
"attack_bao_nu":
    return "意图：怒容 %d" % val
"attack_kong_ju":
    return "意图：惧容 %d" % val
"attack_bei_shang":
    return "意图：悲容 %d" % val
"attack_venom":
    return "意图：蛇毒 %d" % val
"wave_gap":
    return "意图：蓄势"
```

- [ ] **Step 9: Commit**

```bash
git add EnemyData.gd Combatant.gd CombatEngine.gd combat/CombatScreen.gd
git commit -m "feat: enemy engine expansion (passive_block, death_kills_others, 10 new action types)"
```

---

## Task 5：金丹境普通怪数据（7只）

**Files:** 创建 `data/enemies/` 和 `data/enemy_groups/` 下各 7 个文件

所有文件必须用 Write 工具创建，禁止用 PowerShell Set-Content。

- [ ] **Step 1: 金丹碎片 EnemyData**

创建 `data/enemies/jd_fragment.tres`：

```
[gd_resource type="Resource" script_class="EnemyData" load_steps=9 format=3]

[ext_resource type="Script" path="res://EnemyData.gd" id="1_EnemyData"]
[ext_resource type="Script" path="res://EnemyActionData.gd" id="2_EnemyActionData"]

[sub_resource type="Resource" id="Action_charge1"]
script = ExtResource("2_EnemyActionData")
type = "charge"

[sub_resource type="Resource" id="Action_charge2"]
script = ExtResource("2_EnemyActionData")
type = "charge"

[sub_resource type="Resource" id="Action_burst"]
script = ExtResource("2_EnemyActionData")
type = "attack_self_damage"
value = 9
count = 5

[sub_resource type="Resource" id="Action_atk1"]
script = ExtResource("2_EnemyActionData")
type = "attack"
value = 7

[sub_resource type="Resource" id="Action_atk2"]
script = ExtResource("2_EnemyActionData")
type = "attack"
value = 7

[sub_resource type="Resource" id="Action_atk3"]
script = ExtResource("2_EnemyActionData")
type = "attack"
value = 7

[resource]
script = ExtResource("1_EnemyData")
display_name = "金丹碎片"
hp = 69
actions = Array[ExtResource("2_EnemyActionData")]([SubResource("Action_charge1"), SubResource("Action_charge2"), SubResource("Action_burst"), SubResource("Action_atk1"), SubResource("Action_atk2"), SubResource("Action_atk3")])
```

- [ ] **Step 2: 金丹碎片 EnemyGroup**

创建 `data/enemy_groups/single_jd_fragment.tres`：

```
[gd_resource type="Resource" script_class="EnemyGroupData" load_steps=4 format=3]

[ext_resource type="Script" path="res://EnemyGroupData.gd" id="1_GroupData"]
[ext_resource type="Script" path="res://EnemyData.gd" id="2_EnemyData"]
[ext_resource type="Resource" path="res://data/enemies/jd_fragment.tres" id="3_enemy"]

[resource]
script = ExtResource("1_GroupData")
enemies = Array[ExtResource("2_EnemyData")]([ExtResource("3_enemy")])
```

- [ ] **Step 3: 潮汐妖 EnemyData + Group**

创建 `data/enemies/tide_demon.tres`：

```
[gd_resource type="Resource" script_class="EnemyData" load_steps=5 format=3]

[ext_resource type="Script" path="res://EnemyData.gd" id="1_EnemyData"]
[ext_resource type="Script" path="res://EnemyActionData.gd" id="2_EnemyActionData"]

[sub_resource type="Resource" id="Action_atk"]
script = ExtResource("2_EnemyActionData")
type = "attack"
value = 12

[sub_resource type="Resource" id="Action_blk"]
script = ExtResource("2_EnemyActionData")
type = "block"
value = 12

[resource]
script = ExtResource("1_EnemyData")
display_name = "潮汐妖"
hp = 65
actions = Array[ExtResource("2_EnemyActionData")]([SubResource("Action_atk"), SubResource("Action_blk")])
```

创建 `data/enemy_groups/single_tide_demon.tres`：

```
[gd_resource type="Resource" script_class="EnemyGroupData" load_steps=4 format=3]

[ext_resource type="Script" path="res://EnemyGroupData.gd" id="1_GroupData"]
[ext_resource type="Script" path="res://EnemyData.gd" id="2_EnemyData"]
[ext_resource type="Resource" path="res://data/enemies/tide_demon.tres" id="3_enemy"]

[resource]
script = ExtResource("1_GroupData")
enemies = Array[ExtResource("2_EnemyData")]([ExtResource("3_enemy")])
```

- [ ] **Step 4: 雷劫兽 EnemyData + Group**

创建 `data/enemies/thunder_beast.tres`：

```
[gd_resource type="Resource" script_class="EnemyData" load_steps=7 format=3]

[ext_resource type="Script" path="res://EnemyData.gd" id="1_EnemyData"]
[ext_resource type="Script" path="res://EnemyActionData.gd" id="2_EnemyActionData"]

[sub_resource type="Resource" id="Action_atk10"]
script = ExtResource("2_EnemyActionData")
type = "attack"
value = 10
weight = 55

[sub_resource type="Resource" id="Action_multi"]
script = ExtResource("2_EnemyActionData")
type = "multi_attack"
value = 4
count = 2
weight = 25

[sub_resource type="Resource" id="Action_atk16"]
script = ExtResource("2_EnemyActionData")
type = "attack"
value = 16
weight = 15

[sub_resource type="Resource" id="Action_idle"]
script = ExtResource("2_EnemyActionData")
type = "no_action"
weight = 5

[resource]
script = ExtResource("1_EnemyData")
display_name = "雷劫兽"
hp = 73
random_actions = true
actions = Array[ExtResource("2_EnemyActionData")]([SubResource("Action_atk10"), SubResource("Action_multi"), SubResource("Action_atk16"), SubResource("Action_idle")])
```

创建 `data/enemy_groups/single_thunder_beast.tres`：

```
[gd_resource type="Resource" script_class="EnemyGroupData" load_steps=4 format=3]

[ext_resource type="Script" path="res://EnemyGroupData.gd" id="1_GroupData"]
[ext_resource type="Script" path="res://EnemyData.gd" id="2_EnemyData"]
[ext_resource type="Resource" path="res://data/enemies/thunder_beast.tres" id="3_enemy"]

[resource]
script = ExtResource("1_GroupData")
enemies = Array[ExtResource("2_EnemyData")]([ExtResource("3_enemy")])
```

- [ ] **Step 5: 魅蛇妖 EnemyData + Group**

创建 `data/enemies/charm_snake.tres`：

```
[gd_resource type="Resource" script_class="EnemyData" load_steps=7 format=3]

[ext_resource type="Script" path="res://EnemyData.gd" id="1_EnemyData"]
[ext_resource type="Script" path="res://EnemyActionData.gd" id="2_EnemyActionData"]

[sub_resource type="Resource" id="Action_zao"]
script = ExtResource("2_EnemyActionData")
type = "discard_zahuorumuo"
value = 0
count = 1

[sub_resource type="Resource" id="Action_bite"]
script = ExtResource("2_EnemyActionData")
type = "attack"
value = 7

[sub_resource type="Resource" id="Action_wrap"]
script = ExtResource("2_EnemyActionData")
type = "draw_penalty"
count = 1

[sub_resource type="Resource" id="Action_venom"]
script = ExtResource("2_EnemyActionData")
type = "attack_venom"
value = 9
count = 1

[resource]
script = ExtResource("1_EnemyData")
display_name = "魅蛇妖"
hp = 63
actions = Array[ExtResource("2_EnemyActionData")]([SubResource("Action_zao"), SubResource("Action_bite"), SubResource("Action_wrap"), SubResource("Action_venom")])
```

创建 `data/enemy_groups/single_charm_snake.tres`：

```
[gd_resource type="Resource" script_class="EnemyGroupData" load_steps=4 format=3]

[ext_resource type="Script" path="res://EnemyGroupData.gd" id="1_GroupData"]
[ext_resource type="Script" path="res://EnemyData.gd" id="2_EnemyData"]
[ext_resource type="Resource" path="res://data/enemies/charm_snake.tres" id="3_enemy"]

[resource]
script = ExtResource("1_GroupData")
enemies = Array[ExtResource("2_EnemyData")]([ExtResource("3_enemy")])
```

- [ ] **Step 6: 护山灵兽 EnemyData + Group**

创建 `data/enemies/guardian_lion.tres`：

```
[gd_resource type="Resource" script_class="EnemyData" load_steps=5 format=3]

[ext_resource type="Script" path="res://EnemyData.gd" id="1_EnemyData"]
[ext_resource type="Script" path="res://EnemyActionData.gd" id="2_EnemyActionData"]

[sub_resource type="Resource" id="Action_blk"]
script = ExtResource("2_EnemyActionData")
type = "block"
value = 14

[sub_resource type="Resource" id="Action_shake"]
script = ExtResource("2_EnemyActionData")
type = "attack_half_next_block"
value = 8

[resource]
script = ExtResource("1_EnemyData")
display_name = "护山灵兽"
hp = 83
actions = Array[ExtResource("2_EnemyActionData")]([SubResource("Action_blk"), SubResource("Action_shake")])
```

创建 `data/enemy_groups/single_guardian_lion.tres`：

```
[gd_resource type="Resource" script_class="EnemyGroupData" load_steps=4 format=3]

[ext_resource type="Script" path="res://EnemyGroupData.gd" id="1_GroupData"]
[ext_resource type="Script" path="res://EnemyData.gd" id="2_EnemyData"]
[ext_resource type="Resource" path="res://data/enemies/guardian_lion.tres" id="3_enemy"]

[resource]
script = ExtResource("1_GroupData")
enemies = Array[ExtResource("2_EnemyData")]([ExtResource("3_enemy")])
```

- [ ] **Step 7: 火灵 EnemyData + Group**

创建 `data/enemies/fire_spirit.tres`：

```
[gd_resource type="Resource" script_class="EnemyData" load_steps=9 format=3]

[ext_resource type="Script" path="res://EnemyData.gd" id="1_EnemyData"]
[ext_resource type="Script" path="res://EnemyActionData.gd" id="2_EnemyActionData"]

[sub_resource type="Resource" id="Action_charge1"]
script = ExtResource("2_EnemyActionData")
type = "charge"

[sub_resource type="Resource" id="Action_charge2"]
script = ExtResource("2_EnemyActionData")
type = "charge"

[sub_resource type="Resource" id="Action_burst"]
script = ExtResource("2_EnemyActionData")
type = "attack"
value = 11

[sub_resource type="Resource" id="Action_atk1"]
script = ExtResource("2_EnemyActionData")
type = "attack"
value = 7

[sub_resource type="Resource" id="Action_atk2"]
script = ExtResource("2_EnemyActionData")
type = "attack"
value = 7

[sub_resource type="Resource" id="Action_atk3"]
script = ExtResource("2_EnemyActionData")
type = "attack"
value = 7

[resource]
script = ExtResource("1_EnemyData")
display_name = "火灵"
hp = 67
actions = Array[ExtResource("2_EnemyActionData")]([SubResource("Action_charge1"), SubResource("Action_charge2"), SubResource("Action_burst"), SubResource("Action_atk1"), SubResource("Action_atk2"), SubResource("Action_atk3")])
```

创建 `data/enemy_groups/single_fire_spirit.tres`：

```
[gd_resource type="Resource" script_class="EnemyGroupData" load_steps=4 format=3]

[ext_resource type="Script" path="res://EnemyGroupData.gd" id="1_GroupData"]
[ext_resource type="Script" path="res://EnemyData.gd" id="2_EnemyData"]
[ext_resource type="Resource" path="res://data/enemies/fire_spirit.tres" id="3_enemy"]

[resource]
script = ExtResource("1_GroupData")
enemies = Array[ExtResource("2_EnemyData")]([ExtResource("3_enemy")])
```

- [ ] **Step 8: 双头鹫 EnemyData + Group**

创建 `data/enemies/twin_vulture.tres`：

```
[gd_resource type="Resource" script_class="EnemyData" load_steps=6 format=3]

[ext_resource type="Script" path="res://EnemyData.gd" id="1_EnemyData"]
[ext_resource type="Script" path="res://EnemyActionData.gd" id="2_EnemyActionData"]

[sub_resource type="Resource" id="Action_peck"]
script = ExtResource("2_EnemyActionData")
type = "multi_attack"
value = 6
count = 2
weight = 40

[sub_resource type="Resource" id="Action_dive"]
script = ExtResource("2_EnemyActionData")
type = "attack"
value = 13
weight = 35

[sub_resource type="Resource" id="Action_soar"]
script = ExtResource("2_EnemyActionData")
type = "block"
value = 9
weight = 25

[resource]
script = ExtResource("1_EnemyData")
display_name = "双头鹫"
hp = 76
random_actions = true
actions = Array[ExtResource("2_EnemyActionData")]([SubResource("Action_peck"), SubResource("Action_dive"), SubResource("Action_soar")])
```

创建 `data/enemy_groups/single_twin_vulture.tres`：

```
[gd_resource type="Resource" script_class="EnemyGroupData" load_steps=4 format=3]

[ext_resource type="Script" path="res://EnemyGroupData.gd" id="1_GroupData"]
[ext_resource type="Script" path="res://EnemyData.gd" id="2_EnemyData"]
[ext_resource type="Resource" path="res://data/enemies/twin_vulture.tres" id="3_enemy"]

[resource]
script = ExtResource("1_GroupData")
enemies = Array[ExtResource("2_EnemyData")]([ExtResource("3_enemy")])
```

- [ ] **Step 9: Commit（普通怪）**

```bash
git add data/enemies/jd_fragment.tres data/enemies/tide_demon.tres data/enemies/thunder_beast.tres
git add data/enemies/charm_snake.tres data/enemies/guardian_lion.tres data/enemies/fire_spirit.tres data/enemies/twin_vulture.tres
git add data/enemy_groups/single_jd_fragment.tres data/enemy_groups/single_tide_demon.tres data/enemy_groups/single_thunder_beast.tres
git add data/enemy_groups/single_charm_snake.tres data/enemy_groups/single_guardian_lion.tres data/enemy_groups/single_fire_spirit.tres data/enemy_groups/single_twin_vulture.tres
git commit -m "feat: 金丹境普通怪数据 (金丹碎片/潮汐妖/雷劫兽/魅蛇妖/护山灵兽/火灵/双头鹫)"
```

---

## Task 6：金丹境精英怪 + Boss 数据

**Files:** 创建精英 4 个 EnemyData + Boss 1 个 + 对应 Group 4 个

- [ ] **Step 1: 金丹劫修 EnemyData + Group**

创建 `data/enemies/jd_calamity.tres`：

```
[gd_resource type="Resource" script_class="EnemyData" load_steps=8 format=3]

[ext_resource type="Script" path="res://EnemyData.gd" id="1_EnemyData"]
[ext_resource type="Script" path="res://EnemyActionData.gd" id="2_EnemyActionData"]

[sub_resource type="Resource" id="Action_atk10"]
script = ExtResource("2_EnemyActionData")
type = "attack"
value = 10

[sub_resource type="Resource" id="Action_blk"]
script = ExtResource("2_EnemyActionData")
type = "block"
value = 10

[sub_resource type="Resource" id="Action_atk12"]
script = ExtResource("2_EnemyActionData")
type = "attack"
value = 12

[sub_resource type="Resource" id="Action_pre"]
script = ExtResource("2_EnemyActionData")
type = "pre_charge"

[sub_resource type="Resource" id="Action_aoe"]
script = ExtResource("2_EnemyActionData")
type = "aoe_all"
value = 18

[resource]
script = ExtResource("1_EnemyData")
display_name = "金丹劫修"
hp = 175
gains_strength_from_aoe = true
actions = Array[ExtResource("2_EnemyActionData")]([SubResource("Action_atk10"), SubResource("Action_blk"), SubResource("Action_atk12"), SubResource("Action_pre"), SubResource("Action_aoe")])
```

创建 `data/enemy_groups/single_jd_calamity.tres`：

```
[gd_resource type="Resource" script_class="EnemyGroupData" load_steps=4 format=3]

[ext_resource type="Script" path="res://EnemyGroupData.gd" id="1_GroupData"]
[ext_resource type="Script" path="res://EnemyData.gd" id="2_EnemyData"]
[ext_resource type="Resource" path="res://data/enemies/jd_calamity.tres" id="3_enemy"]

[resource]
script = ExtResource("1_GroupData")
enemies = Array[ExtResource("2_EnemyData")]([ExtResource("3_enemy")])
```

- [ ] **Step 2: 七情魔 EnemyData + Group**

创建 `data/enemies/seven_emotions.tres`：

```
[gd_resource type="Resource" script_class="EnemyData" load_steps=7 format=3]

[ext_resource type="Script" path="res://EnemyData.gd" id="1_EnemyData"]
[ext_resource type="Script" path="res://EnemyActionData.gd" id="2_EnemyActionData"]

[sub_resource type="Resource" id="Action_bao"]
script = ExtResource("2_EnemyActionData")
type = "attack_bao_nu"
value = 8
count = 1

[sub_resource type="Resource" id="Action_kong"]
script = ExtResource("2_EnemyActionData")
type = "attack_kong_ju"
value = 6
count = 1

[sub_resource type="Resource" id="Action_bei"]
script = ExtResource("2_EnemyActionData")
type = "attack_bei_shang"
value = 5
count = 1

[sub_resource type="Resource" id="Action_wild"]
script = ExtResource("2_EnemyActionData")
type = "attack"
value = 12

[resource]
script = ExtResource("1_EnemyData")
display_name = "七情魔"
hp = 160
emotion_card_damage_bonus = true
actions = Array[ExtResource("2_EnemyActionData")]([SubResource("Action_bao"), SubResource("Action_kong"), SubResource("Action_bei"), SubResource("Action_wild")])
```

创建 `data/enemy_groups/single_seven_emotions.tres`：

```
[gd_resource type="Resource" script_class="EnemyGroupData" load_steps=4 format=3]

[ext_resource type="Script" path="res://EnemyGroupData.gd" id="1_GroupData"]
[ext_resource type="Script" path="res://EnemyData.gd" id="2_EnemyData"]
[ext_resource type="Resource" path="res://data/enemies/seven_emotions.tres" id="3_enemy"]

[resource]
script = ExtResource("1_GroupData")
enemies = Array[ExtResource("2_EnemyData")]([ExtResource("3_enemy")])
```

- [ ] **Step 3: 双生子剑修（攻剑 + 守剑）EnemyData**

创建 `data/enemies/twin_sword_attack.tres`：

```
[gd_resource type="Resource" script_class="EnemyData" load_steps=6 format=3]

[ext_resource type="Script" path="res://EnemyData.gd" id="1_EnemyData"]
[ext_resource type="Script" path="res://EnemyActionData.gd" id="2_EnemyActionData"]

[sub_resource type="Resource" id="Action_a1"]
script = ExtResource("2_EnemyActionData")
type = "attack"
value = 11

[sub_resource type="Resource" id="Action_a2"]
script = ExtResource("2_EnemyActionData")
type = "attack"
value = 11

[sub_resource type="Resource" id="Action_a3"]
script = ExtResource("2_EnemyActionData")
type = "attack"
value = 11

[resource]
script = ExtResource("1_EnemyData")
display_name = "攻剑"
hp = 90
death_kills_others = true
actions = Array[ExtResource("2_EnemyActionData")]([SubResource("Action_a1"), SubResource("Action_a2"), SubResource("Action_a3")])
```

创建 `data/enemies/twin_sword_defense.tres`：

```
[gd_resource type="Resource" script_class="EnemyData" load_steps=6 format=3]

[ext_resource type="Script" path="res://EnemyData.gd" id="1_EnemyData"]
[ext_resource type="Script" path="res://EnemyActionData.gd" id="2_EnemyActionData"]

[sub_resource type="Resource" id="Action_d1"]
script = ExtResource("2_EnemyActionData")
type = "block_all_enemies"
value = 8

[sub_resource type="Resource" id="Action_d2"]
script = ExtResource("2_EnemyActionData")
type = "block_all_enemies"
value = 8

[sub_resource type="Resource" id="Action_d3"]
script = ExtResource("2_EnemyActionData")
type = "block_all_enemies"
value = 8

[resource]
script = ExtResource("1_EnemyData")
display_name = "守剑"
hp = 90
death_kills_others = true
actions = Array[ExtResource("2_EnemyActionData")]([SubResource("Action_d1"), SubResource("Action_d2"), SubResource("Action_d3")])
```

- [ ] **Step 4: 双生子剑修 EnemyGroup**

创建 `data/enemy_groups/twin_sword_cultivators.tres`：

```
[gd_resource type="Resource" script_class="EnemyGroupData" load_steps=5 format=3]

[ext_resource type="Script" path="res://EnemyGroupData.gd" id="1_GroupData"]
[ext_resource type="Script" path="res://EnemyData.gd" id="2_EnemyData"]
[ext_resource type="Resource" path="res://data/enemies/twin_sword_attack.tres" id="3_attack"]
[ext_resource type="Resource" path="res://data/enemies/twin_sword_defense.tres" id="4_defense"]

[resource]
script = ExtResource("1_GroupData")
enemies = Array[ExtResource("2_EnemyData")]([ExtResource("3_attack"), ExtResource("4_defense")])
```

- [ ] **Step 5: 四九天劫 Boss EnemyData**

创建 `data/enemies/tribulation_49.tres`（40 个动作，用 5 个可复用 sub-resource）：

```
[gd_resource type="Resource" script_class="EnemyData" load_steps=8 format=3]

[ext_resource type="Script" path="res://EnemyData.gd" id="1_EnemyData"]
[ext_resource type="Script" path="res://EnemyActionData.gd" id="2_EnemyActionData"]

[sub_resource type="Resource" id="Action_atk4"]
script = ExtResource("2_EnemyActionData")
type = "attack"
value = 4

[sub_resource type="Resource" id="Action_atk6"]
script = ExtResource("2_EnemyActionData")
type = "attack"
value = 6

[sub_resource type="Resource" id="Action_atk8"]
script = ExtResource("2_EnemyActionData")
type = "attack"
value = 8

[sub_resource type="Resource" id="Action_atk10"]
script = ExtResource("2_EnemyActionData")
type = "attack"
value = 10

[sub_resource type="Resource" id="Action_gap"]
script = ExtResource("2_EnemyActionData")
type = "wave_gap"

[resource]
script = ExtResource("1_EnemyData")
display_name = "四九天劫"
hp = 345
passive_block_per_turn = 5
skip_wave_gap_threshold = 0.5
actions = Array[ExtResource("2_EnemyActionData")]([SubResource("Action_atk4"), SubResource("Action_atk4"), SubResource("Action_atk4"), SubResource("Action_atk4"), SubResource("Action_atk4"), SubResource("Action_atk4"), SubResource("Action_atk4"), SubResource("Action_atk4"), SubResource("Action_atk4"), SubResource("Action_gap"), SubResource("Action_atk6"), SubResource("Action_atk6"), SubResource("Action_atk6"), SubResource("Action_atk6"), SubResource("Action_atk6"), SubResource("Action_atk6"), SubResource("Action_atk6"), SubResource("Action_atk6"), SubResource("Action_atk6"), SubResource("Action_gap"), SubResource("Action_atk8"), SubResource("Action_atk8"), SubResource("Action_atk8"), SubResource("Action_atk8"), SubResource("Action_atk8"), SubResource("Action_atk8"), SubResource("Action_atk8"), SubResource("Action_atk8"), SubResource("Action_atk8"), SubResource("Action_gap"), SubResource("Action_atk10"), SubResource("Action_atk10"), SubResource("Action_atk10"), SubResource("Action_atk10"), SubResource("Action_atk10"), SubResource("Action_atk10"), SubResource("Action_atk10"), SubResource("Action_atk10"), SubResource("Action_atk10"), SubResource("Action_gap")])
```

- [ ] **Step 6: 四九天劫 EnemyGroup**

创建 `data/enemy_groups/single_49_tribulation.tres`：

```
[gd_resource type="Resource" script_class="EnemyGroupData" load_steps=4 format=3]

[ext_resource type="Script" path="res://EnemyGroupData.gd" id="1_GroupData"]
[ext_resource type="Script" path="res://EnemyData.gd" id="2_EnemyData"]
[ext_resource type="Resource" path="res://data/enemies/tribulation_49.tres" id="3_enemy"]

[resource]
script = ExtResource("1_GroupData")
enemies = Array[ExtResource("2_EnemyData")]([ExtResource("3_enemy")])
```

- [ ] **Step 7: 手动验证**

启动 Godot，使用 TestSelectScreen（如果有测试关卡选择），或新建一局进入金丹境，分别挑战：
- 金丹劫修：第5回合应看到 aoe_all 意图（天劫 18），受击时若有 gains_strength_from_aoe 则其力量+2
- 七情魔：攻击时弃牌堆出现暴怒/恐惧/悲伤
- 双生子：杀死一只，另一只同时死亡
- 四九天劫：HP > 50% 时第10回合显示"蓄势"意图；HP < 50% 时第10回合直接跳到第11回合攻击；每回合自动获得 5 格挡

- [ ] **Step 8: Commit（精英 + Boss）**

```bash
git add data/enemies/jd_calamity.tres data/enemies/seven_emotions.tres
git add data/enemies/twin_sword_attack.tres data/enemies/twin_sword_defense.tres data/enemies/tribulation_49.tres
git add data/enemy_groups/single_jd_calamity.tres data/enemy_groups/single_seven_emotions.tres
git add data/enemy_groups/twin_sword_cultivators.tres data/enemy_groups/single_49_tribulation.tres
git commit -m "feat: 金丹境精英怪 + Boss 数据 (金丹劫修/七情魔/双生子剑修/四九天劫)"
```

---

## Task 7：版本号 + 开发日志

**Files:**
- Modify: `menu/MainMenu.gd` 或 `menu/MainMenu.tscn` 中版本号标签
- Create: `E:\MyWork\Devlog\AIGameTest2_v0.72.0_2026-05-05.md`

- [ ] **Step 1: 更新主菜单版本号**

找到主菜单场景中显示版本号的 Label。通常在 `menu/MainMenu.gd` 或场景文件中有 `v0.71.0` 字样：

```bash
grep -r "v0.71.0" menu/
```

找到后将 `v0.71.0` 改为 `v0.72.0`。

- [ ] **Step 2: 写开发日志**

创建 `E:\MyWork\Devlog\AIGameTest2_v0.72.0_2026-05-05.md`，内容：

```markdown
# AIGameTest2 v0.72.0 开发日志

**日期：** 2026-05-05
**版本目标：** 多境界地图架构 + 金丹境 11 只怪物 + 诅咒卡扩展

---

## 本版本做了什么

### 多境界地图架构

- **PlayerState** 新增 `current_realm: int = 0`（0=筑基, 1=金丹）
- **MapGenerator.generate(realm)** 按 realm 选择对应敌人池常量；金丹境新增 JD_EARLY/MID/ELITE/BOSS_GROUP_PATHS
- **GameManager** 新增 `advance_realm()`（重置地图状态、current_realm+1、重新生成地图）和 `is_last_realm()`
- **RewardScreen** Boss 结算后：`is_final_node()` 时检查 `is_last_realm()`，筑基打完 → 推进境界，金丹打完 → 胜利

### 诅咒卡扩展

- **CardData** 新增 4 个字段：`is_ye_huo / is_bao_nu / is_kong_ju / is_bei_shang`
- **业火**：`_start_player_turn()` 第一回合扫描全牌库，每张扣 2 HP（不过格挡）；`ye_huo.tres` 更新加 `is_ye_huo = true`
- **暴怒**：`play_card()` 时统计手中暴怒数量，每张扣 2 HP（不过格挡）
- **恐惧**：`play_card()` 拦截身法牌（`can_play_card()` 新方法）；CombatScreen 灰化无法出的身法牌
- **悲伤**：`end_turn()` 走火入魔处理后，每张悲伤随机弃 1 张非诅咒手牌到弃牌堆
- 新建 `bao_nu.tres / kong_ju.tres / bei_shang.tres`

### 金丹境怪物引擎扩展

**EnemyData 新增字段：** `passive_block_per_turn / emotion_card_damage_bonus / death_kills_others / skip_wave_gap_threshold / gains_strength_from_aoe`

**Combatant 新增字段：** `draw_reduction / next_block_halved`

**CombatEngine 新增 10 个 action types：**
`attack_self_damage / draw_penalty / attack_half_next_block / block_all_enemies / aoe_all / attack_bao_nu / attack_kong_ju / attack_bei_shang / attack_venom / wave_gap`

**其他引擎改动：**
- `draw_cards(n)` 扣减 `draw_reduction` 后归零
- `_start_player_turn()` 末尾处理 `next_block_halved`（格挡减半后清除）
- `_do_enemy_turn()` block=0 后若 `passive_block_per_turn>0` 补充被动格挡
- `_check_end()` 检查 `death_kills_others`，一人死亡则另一人同时死亡
- `_resolve_enemy_actions()` 若 wave_gap 且 HP < skip_wave_gap_threshold 则跳过

### 金丹境怪物数据（11只）

| 怪物 | 类型 | HP | 特殊机制 |
|------|------|-----|---------|
| 金丹碎片 | 普通 | 69 | charge×2 蓄力后 attack_self_damage 绽放 |
| 潮汐妖 | 普通 | 65 | 攻击/格挡交替循环 |
| 雷劫兽 | 普通 | 73 | 随机 attack/multi_attack/重击/无动作 |
| 魅蛇妖 | 普通 | 63 | 走火入魔+蛇毒+减抽牌 |
| 护山灵兽 | 普通 | 83 | 格挡+震地（玩家下回合格挡减半）|
| 火灵 | 普通 | 67 | charge×2 蓄力后攻击22 |
| 双头鹫 | 普通 | 76 | 随机双啄/俯冲/升空 |
| 金丹劫修 | 精英 | 175 | aoe_all 18 + gains_strength_from_aoe |
| 七情魔 | 精英 | 160 | 投放暴怒/恐惧/悲伤 |
| 双生子剑修 | 精英 | 90×2 | death_kills_others 一死俱亡 |
| 四九天劫 | Boss | 345 | passive_block_per_turn=5, wave_gap, skip_wave_gap_threshold=0.5 |

---

## 遗留事项（下版本）

- `emotion_card_damage_bonus` 字段已加入但引擎未实装加成（七情魔持有此标记，攻击伤害暂不受诅咒牌数量影响）
- 元婴/化神境怪物：realm=2/3 预留，下版本填充
- `is_last_realm()` hardcode 为 `current_realm >= 1`，扩展时改为 `>= MAX_REALM`
```

- [ ] **Step 3: Commit**

```bash
git add menu/
git commit -m "feat: bump version to v0.72.0"
```

开发日志不提交到 git（存放在 Devlog 目录外）。

---

## 自我检查

**Spec 覆盖：**
- ✅ Part 1 多境界架构：PlayerState.current_realm, MapGenerator.generate(realm), GameManager.advance_realm/is_last_realm, RewardScreen 触发
- ✅ Part 2 诅咒卡：4 个 CardData 字段 + 4 个 .tres 文件
- ✅ Part 3 诅咒卡引擎：业火/暴怒/恐惧/悲伤触发逻辑 + can_play_card + CombatScreen 灰化
- ✅ Part 4 金丹境引擎扩展：EnemyData 5 字段 + Combatant 2 字段 + 10 新 action types + 6 逻辑块
- ✅ Part 5 金丹境怪物数据：7 普通 + 3 精英 + 1 Boss = 11 只，对应 11 个 EnemyGroup
- ✅ 版本号 + 开发日志

**注意事项：**
- 所有 .tres 文件必须用 Write/Bash 工具写入，禁止 PowerShell Set-Content（BOM 问题）
- 金丹碎片 value=9（is_charging 翻倍后 = 18）
- `passive_block_per_turn` 实现为：block=0 之后补充格挡（非之前）
- `death_kills_others` 在 `_check_end()` 中、`any_alive` 计算之前触发
- 双生子双方都需设 `death_kills_others = true`
