# v0.42.0 休息站二选一 + 升级预览 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将休息站改为二选一（休息回血 OR 升级一张卡牌），升级时支持点击卡牌预览升级后属性，数值有提升的部分用绿色显示。

**Architecture:** `GameManager.go_to_rest()` 移除自动回血调用；`RestScreen` 重写为两阶段 UI（选择 → 结果）；`CardData` 新增 `get_upgrade_preview_bbcode()` 方法生成含 BBCode 颜色标签的升级预览文本；同步补充主要卡牌的 `upgrade_bonus` 数值。

**Tech Stack:** Godot 4 GDScript，RichTextLabel BBCode（`[color=green]...[/color]`），`.tres` Resource 文件。

---

## 文件结构

| 文件 | 动作 | 变更内容 |
|------|------|----------|
| `CardData.gd` | 修改 | 新增 `get_upgrade_preview_bbcode() -> String` |
| `GameManager.gd` | 修改 | `go_to_rest()` 删除 `apply_rest_heal()` 调用 |
| `rest/RestScreen.tscn` | 重写 | 新增选择面板、预览面板，原节点重新布局 |
| `rest/RestScreen.gd` | 重写 | 两阶段逻辑 + 升级预览交互 |
| `data/cards/*.tres` | 修改（20 个文件） | 为当前 upgrade_bonus=0 的卡牌补充数值 |
| `menu/MainMenu.tscn` | 修改 | 版本号 v0.41.0 → v0.42.0 |

---

## Task 1：CardData.get_upgrade_preview_bbcode()

**Files:**
- Modify: `CardData.gd`

背景：`CardData.get_description()` 返回普通文本。本方法在不修改卡牌数据的情况下，模拟升级后数值，对有 `upgrade_bonus > 0` 的字段用绿色 BBCode 包裹。供 RestScreen 的 `RichTextLabel` 显示。

- [ ] **Step 1: 在 `CardData.gd` 末尾添加辅助方法 `_green()` 和主方法 `get_upgrade_preview_bbcode()`**

用 Edit 工具在 `CardData.gd` 最后一行（`return desc` 的 `get_description()` 结尾）后追加以下代码：

```gdscript
func get_upgrade_preview_bbcode() -> String:
	if is_upgraded:
		return get_description()
	if is_venom:
		return "%s+\n费用:%d  %s" % [card_name, cost, special_text]
	var dmg: int = 0;        var dmg_bonus: int = 0; var dmg_times: int = 1
	var blk: int = 0;        var blk_bonus: int = 0
	var drw: int = 0;        var drw_bonus: int = 0
	var nrg: int = 0;        var nrg_bonus: int = 0
	var wk: int = 0;         var wk_bonus: int = 0
	var vul: int = 0;        var vul_bonus: int = 0
	var si_cap: int = 0;     var si_cap_bonus: int = 0
	var si_bns: int = 0;     var si_bns_bonus: int = 0
	var dpt: int = 0;        var dpt_bonus: int = 0
	var si_retain: bool = false
	var si_blk: int = 0;     var si_blk_bonus: int = 0
	var nts: int = 0;        var nts_bonus: int = 0
	var ntd: int = 0;        var ntd_bonus: int = 0
	var fb: int = 0;         var fb_bonus: int = 0
	var fsb: int = 0;        var fsb_bonus: int = 0
	var sins: int = 0;       var sins_bonus: int = 0
	for effect: CardEffectData in effects:
		match effect.type:
			"damage":            dmg = effect.value;  dmg_bonus = effect.upgrade_bonus;  dmg_times = effect.times
			"block":             blk = effect.value;  blk_bonus = effect.upgrade_bonus
			"draw":              drw = effect.value;  drw_bonus = effect.upgrade_bonus
			"energy":            nrg = effect.value;  nrg_bonus = effect.upgrade_bonus
			"weak":              wk  = effect.value;  wk_bonus  = effect.upgrade_bonus
			"vulnerable":        vul = effect.value;  vul_bonus = effect.upgrade_bonus
			"sword_intent_cap":  si_cap  = effect.value; si_cap_bonus  = effect.upgrade_bonus
			"sword_intent_bonus":si_bns  = effect.value; si_bns_bonus  = effect.upgrade_bonus
			"draw_per_turn":     dpt     = effect.value; dpt_bonus     = effect.upgrade_bonus
			"sword_intent_retain": si_retain = true
			"sword_intent_block_bonus": si_blk = effect.value; si_blk_bonus = effect.upgrade_bonus
			"next_turn_si":      nts  = effect.value; nts_bonus  = effect.upgrade_bonus
			"next_turn_draw":    ntd  = effect.value; ntd_bonus  = effect.upgrade_bonus
			"finisher_block":    fb   = effect.value; fb_bonus   = effect.upgrade_bonus
			"first_si_block":    fsb  = effect.value; fsb_bonus  = effect.upgrade_bonus
			"sword_intent_if_no_style": sins = effect.value; sins_bonus = effect.upgrade_bonus
	var desc: String = "%s+\n费用:%d" % [card_name, cost]
	if dmg > 0:
		if dmg_times > 1:
			desc += "  攻:%s×%d" % [_green(dmg, dmg_bonus), dmg_times]
		else:
			desc += "  攻:%s" % _green(dmg, dmg_bonus)
		if sword_intent_consume_bonus > 0:
			desc += "(+剑意×%d)" % sword_intent_consume_bonus
	if blk  > 0: desc += "  挡:%s"         % _green(blk,  blk_bonus)
	if drw  > 0: desc += "  抽:%s"         % _green(drw,  drw_bonus)
	if nrg  > 0: desc += "  能:%s"         % _green(nrg,  nrg_bonus)
	if wk   > 0: desc += "  虚弱:%s"       % _green(wk,   wk_bonus)
	if vul  > 0: desc += "  脆弱:%s"       % _green(vul,  vul_bonus)
	if si_cap  > 0: desc += "  剑意上限+%s"  % _green(si_cap,  si_cap_bonus)
	if si_bns  > 0: desc += "  剑意加成+%s"  % _green(si_bns,  si_bns_bonus)
	if dpt     > 0: desc += "  多抽+%s张/回" % _green(dpt,     dpt_bonus)
	if si_retain:   desc += "  剑意永续"
	if si_blk  > 0: desc += "  剑意挡加成+%s"  % _green(si_blk, si_blk_bonus)
	if nts     > 0: desc += "  下回合剑意+%s"   % _green(nts,    nts_bonus)
	if ntd     > 0: desc += "  下回合抽+%s"     % _green(ntd,    ntd_bonus)
	if fb      > 0: desc += "  终结技触发挡+%s" % _green(fb,     fb_bonus)
	if fsb     > 0: desc += "  首剑意+挡%s"     % _green(fsb,    fsb_bonus)
	if sins    > 0: desc += "  无招式时+剑意%s" % _green(sins,   sins_bonus)
	if retain_si_if_target_attacks: desc += "  攻击意图→剑意不消耗"
	var any_bonus: bool = effects.any(func(e: CardEffectData) -> bool: return e.upgrade_bonus > 0)
	if not any_bonus:
		desc += "\n（属性不变）"
	return desc

static func _green(val: int, bonus: int) -> String:
	if bonus > 0:
		return "[color=green]%d[/color]" % (val + bonus)
	return "%d" % val
```

- [ ] **Step 2: 提交**

```bash
git add CardData.gd
git commit -m "feat: add get_upgrade_preview_bbcode to CardData"
```

---

## Task 2：移除 GameManager 自动回血

**Files:**
- Modify: `GameManager.gd`（第 78-81 行）

- [ ] **Step 1: 删除 `go_to_rest()` 中的 `apply_rest_heal()` 调用**

用 Edit 工具将：

```gdscript
func go_to_rest() -> void:
	player_state.apply_rest_heal()
	current_phase = Phase.REST
	get_tree().change_scene_to_file("res://rest/RestScreen.tscn")
```

替换为：

```gdscript
func go_to_rest() -> void:
	current_phase = Phase.REST
	get_tree().change_scene_to_file("res://rest/RestScreen.tscn")
```

- [ ] **Step 2: 提交**

```bash
git add GameManager.gd
git commit -m "feat: move rest heal trigger from GameManager to RestScreen"
```

---

## Task 3：重建 RestScreen.tscn

**Files:**
- Overwrite: `rest/RestScreen.tscn`

节点结构说明（各面板同级，按需显示/隐藏）：
- `PanelChoice`（VBoxContainer）：两个选择按钮，初始可见
- `LblHeal`、`LblHP`：选休息后显示，初始隐藏
- `LblUpgradePrompt`、`ScrollContainer/CardList`：选升级后显示，初始隐藏
- `PanelPreview`（Panel）：点卡牌后显示预览，初始隐藏
- `BtnContinue`：做出选择完成后显示，初始隐藏

- [ ] **Step 1: 用 Write 工具整体替换 `rest/RestScreen.tscn`**

```
[gd_scene load_steps=2 format=3 uid="uid://rest_screen"]

[ext_resource type="Script" path="res://rest/RestScreen.gd" id="1_script"]

[node name="RestScreen" type="Control"]
layout_mode = 3
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
script = ExtResource("1_script")

[node name="VBoxContainer" type="VBoxContainer" parent="."]
layout_mode = 1
anchors_preset = 8
anchor_left = 0.5
anchor_top = 0.5
anchor_right = 0.5
anchor_bottom = 0.5
offset_left = -200.0
offset_top = -270.0
offset_right = 200.0
offset_bottom = 270.0
theme_override_constants/separation = 12

[node name="LblTitle" type="Label" parent="VBoxContainer"]
layout_mode = 2
text = "休息站"
horizontal_alignment = 1

[node name="PanelChoice" type="VBoxContainer" parent="VBoxContainer"]
layout_mode = 2
theme_override_constants/separation = 10

[node name="BtnRest" type="Button" parent="VBoxContainer/PanelChoice"]
layout_mode = 2
text = "休息（+? 生命）"

[node name="BtnUpgrade" type="Button" parent="VBoxContainer/PanelChoice"]
layout_mode = 2
text = "升级卡牌"

[node name="LblHeal" type="Label" parent="VBoxContainer"]
layout_mode = 2
text = "恢复了 ? 点生命"
visible = false

[node name="LblHP" type="Label" parent="VBoxContainer"]
layout_mode = 2
text = "当前生命：? / ?"
visible = false

[node name="LblUpgradePrompt" type="Label" parent="VBoxContainer"]
layout_mode = 2
text = "选择一张牌升级："
visible = false

[node name="ScrollContainer" type="ScrollContainer" parent="VBoxContainer"]
layout_mode = 2
custom_minimum_size = Vector2(0, 150)
visible = false

[node name="CardList" type="VBoxContainer" parent="VBoxContainer/ScrollContainer"]
layout_mode = 2

[node name="PanelPreview" type="Panel" parent="VBoxContainer"]
layout_mode = 2
visible = false

[node name="PreviewContent" type="VBoxContainer" parent="VBoxContainer/PanelPreview"]
layout_mode = 1
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
offset_left = 8.0
offset_top = 8.0
offset_right = -8.0
offset_bottom = -8.0
theme_override_constants/separation = 8

[node name="LblCurrent" type="Label" parent="VBoxContainer/PanelPreview/PreviewContent"]
layout_mode = 2
text = ""

[node name="RtlUpgraded" type="RichTextLabel" parent="VBoxContainer/PanelPreview/PreviewContent"]
layout_mode = 2
bbcode_enabled = true
fit_content = true
text = ""

[node name="HBoxContainer" type="HBoxContainer" parent="VBoxContainer/PanelPreview/PreviewContent"]
layout_mode = 2

[node name="BtnConfirmUpgrade" type="Button" parent="VBoxContainer/PanelPreview/PreviewContent/HBoxContainer"]
layout_mode = 2
text = "确认升级"

[node name="BtnCancelPreview" type="Button" parent="VBoxContainer/PanelPreview/PreviewContent/HBoxContainer"]
layout_mode = 2
text = "返回"

[node name="BtnContinue" type="Button" parent="VBoxContainer"]
layout_mode = 2
text = "继续"
visible = false
```

- [ ] **Step 2: 提交**

```bash
git add rest/RestScreen.tscn
git commit -m "feat: rebuild RestScreen.tscn with choice and preview panels"
```

---

## Task 4：重写 RestScreen.gd

**Files:**
- Overwrite: `rest/RestScreen.gd`

- [ ] **Step 1: 用 Write 工具整体替换 `rest/RestScreen.gd`**

```gdscript
extends Control

@onready var _panel_choice: VBoxContainer        = $VBoxContainer/PanelChoice
@onready var _btn_rest: Button                   = $VBoxContainer/PanelChoice/BtnRest
@onready var _btn_upgrade: Button                = $VBoxContainer/PanelChoice/BtnUpgrade
@onready var _lbl_heal: Label                    = $VBoxContainer/LblHeal
@onready var _lbl_hp: Label                      = $VBoxContainer/LblHP
@onready var _lbl_upgrade_prompt: Label          = $VBoxContainer/LblUpgradePrompt
@onready var _scroll: ScrollContainer            = $VBoxContainer/ScrollContainer
@onready var _card_list: VBoxContainer           = $VBoxContainer/ScrollContainer/CardList
@onready var _panel_preview: Panel               = $VBoxContainer/PanelPreview
@onready var _lbl_current: Label                 = $VBoxContainer/PanelPreview/PreviewContent/LblCurrent
@onready var _rtl_upgraded: RichTextLabel        = $VBoxContainer/PanelPreview/PreviewContent/RtlUpgraded
@onready var _btn_confirm_upgrade: Button        = $VBoxContainer/PanelPreview/PreviewContent/HBoxContainer/BtnConfirmUpgrade
@onready var _btn_cancel_preview: Button         = $VBoxContainer/PanelPreview/PreviewContent/HBoxContainer/BtnCancelPreview
@onready var _btn_continue: Button               = $VBoxContainer/BtnContinue

var _upgrade_buttons: Array[Button] = []
var _pending_card: CardData = null

func _ready() -> void:
	var state: PlayerState = GameManager.player_state
	var heal_amount: int = int(state.max_hp * PlayerState.REST_HEAL_RATIO)
	_btn_rest.text = "休息（+%d 生命）" % heal_amount
	_btn_rest.pressed.connect(_on_rest_chosen)
	_btn_upgrade.pressed.connect(_on_upgrade_chosen)
	_btn_confirm_upgrade.pressed.connect(_on_confirm_upgrade)
	_btn_cancel_preview.pressed.connect(_on_cancel_preview)
	_btn_continue.pressed.connect(GameManager.go_to_map)

func _on_rest_chosen() -> void:
	var state: PlayerState = GameManager.player_state
	var healed: int = state.apply_rest_heal()
	_panel_choice.hide()
	_lbl_heal.text = "恢复了 %d 点生命" % healed
	_lbl_hp.text = "当前生命：%d / %d" % [state.hp, state.max_hp]
	_lbl_heal.show()
	_lbl_hp.show()
	_btn_continue.show()

func _on_upgrade_chosen() -> void:
	_panel_choice.hide()
	_lbl_upgrade_prompt.show()
	_scroll.show()
	_populate_upgrade_list()

func _populate_upgrade_list() -> void:
	for card: CardData in GameManager.player_state.deck:
		var btn: Button = Button.new()
		btn.text = card.get_description()
		btn.disabled = card.is_upgraded
		btn.pressed.connect(_on_card_clicked.bind(card))
		_card_list.add_child(btn)
		_upgrade_buttons.append(btn)

func _on_card_clicked(card: CardData) -> void:
	_pending_card = card
	_lbl_current.text = card.get_description()
	_rtl_upgraded.text = card.get_upgrade_preview_bbcode()
	_lbl_upgrade_prompt.hide()
	_scroll.hide()
	_panel_preview.show()

func _on_confirm_upgrade() -> void:
	if _pending_card == null:
		return
	_pending_card.upgrade()
	_pending_card = null
	_panel_preview.hide()
	for btn: Button in _upgrade_buttons:
		btn.disabled = true
	_btn_continue.show()

func _on_cancel_preview() -> void:
	_pending_card = null
	_panel_preview.hide()
	_lbl_upgrade_prompt.show()
	_scroll.show()
```

- [ ] **Step 2: 提交**

```bash
git add rest/RestScreen.gd
git commit -m "feat: rewrite RestScreen with two-phase choice and upgrade preview"
```

---

## Task 5：补充卡牌 upgrade_bonus 数值

**Files:**
- Modify: 以下 20 个 `.tres` 文件（当前 upgrade_bonus 全为 0）

背景：所有初始卡和大部分普通卡 `upgrade_bonus = 0`，升级后只有名字加 "+" 而无数值变化，体验差。本 Task 为它们补充合理升级加成。

**各卡修改值（只列出需要改的 upgrade_bonus 字段）：**

| 卡牌文件 | effect type | 当前值 | upgrade_bonus |
|----------|-------------|--------|---------------|
| zhan_tie.tres | damage | 6 | **3** |
| po_feng.tres | damage | 10 | **3** |
| ce_bu.tres | block | 5 | **3** |
| lian_xi.tres | block | 3 | **2** |
| chuan_xin.tres | damage | 8 | **3** |
| yang_jian_shu.tres | sword_intent_cap | 5 | **1** |
| yang_jian_shu.tres | sword_intent_bonus | 1 | **1** |
| heng_jian.tres | block | 10 | **3** |
| jian_bu.tres | block | 6 | **3** |
| jian_qi_ying_ti.tres | next_turn_si | 2 | **1** |
| jian_qiao.tres | block | 7 | **3** |
| jian_qiao.tres | sword_intent_if_no_style | 1 | **1** |
| jing_hong.tres | damage | 7 | **3** |
| lie_kong.tres | damage | 10 | **3** |
| lue_ying.tres | damage | 4 | **2** |
| lue_ying.tres | draw | 1 | **1** |
| po_jun.tres | damage | 3 | **2** |
| ren_jian_he_yi.tres | finisher_block | 3 | **2** |
| sui_xing.tres | damage | 5 | **3** |
| wan_jian_gui_zong.tres | sword_intent_cap | 5 | **1** |
| wan_jian_gui_zong.tres | draw_per_turn | 1 | **1** |
| wu_hen.tres | damage | 6 | **3** |
| xin_jian.tres | sword_intent_block_bonus | 1 | **1** |
| yi_sui_xin_fa.tres | first_si_block | 2 | **1** |

不修改（无可升级数值）：
- `jian_yu.tres`（sword_intent_retain 是布尔标志）
- `venom.tres`（is_venom=true，无 effects）

- [ ] **Step 1: 逐一用 Edit 工具修改每个 .tres 文件**

对每个文件，找到对应 `type = "xxx"` 的 sub_resource 块，将 `upgrade_bonus = 0` 改为对应值。

例如 `data/cards/zhan_tie.tres`，将：
```
type = "damage"
value = 6
upgrade_bonus = 0
```
改为：
```
type = "damage"
value = 6
upgrade_bonus = 3
```

按表格逐一处理完所有 24 条修改。

- [ ] **Step 2: 提交**

```bash
git add data/cards/zhan_tie.tres data/cards/po_feng.tres data/cards/ce_bu.tres \
        data/cards/lian_xi.tres data/cards/chuan_xin.tres data/cards/yang_jian_shu.tres \
        data/cards/heng_jian.tres data/cards/jian_bu.tres data/cards/jian_qi_ying_ti.tres \
        data/cards/jian_qiao.tres data/cards/jing_hong.tres data/cards/lie_kong.tres \
        data/cards/lue_ying.tres data/cards/po_jun.tres data/cards/ren_jian_he_yi.tres \
        data/cards/sui_xing.tres data/cards/wan_jian_gui_zong.tres data/cards/wu_hen.tres \
        data/cards/xin_jian.tres data/cards/yi_sui_xin_fa.tres
git commit -m "feat: add upgrade_bonus to starter and common cards"
```

---

## Task 6：版本号

**Files:**
- Modify: `menu/MainMenu.tscn`

- [ ] **Step 1: 将版本号 v0.41.0 改为 v0.42.0**

用 Edit 工具将 `menu/MainMenu.tscn` 中的 `text = "v0.41.0"` 替换为 `text = "v0.42.0"`。

- [ ] **Step 2: 提交**

```bash
git add menu/MainMenu.tscn
git commit -m "feat: bump version to v0.42.0"
```

---

## 验收清单

- [ ] 到达休息站只显示两个选项：休息 / 升级卡牌
- [ ] 选"休息" → 回血生效，显示回血量和当前生命，出现「继续」
- [ ] 选"升级卡牌" → 显示卡牌列表，不触发回血
- [ ] 点击一张卡牌 → 弹出预览面板，LblCurrent 显示当前描述，RtlUpgraded 显示升级后属性
- [ ] 有 upgrade_bonus > 0 的数值显示绿色
- [ ] 无 upgrade_bonus 的卡（如毒液）显示"（属性不变）"
- [ ] 点「确认升级」→ 升级生效，列表全部禁用，出现「继续」
- [ ] 点「返回」→ 关闭预览，回到卡牌列表
- [ ] 斩铁/破风/侧步等初始卡升级后攻/挡数值有提升
- [ ] 版本号显示 v0.42.0
